
## rules/worktree-location.md
**Type**: Rule (standing convention)
**Portable**: Yes
**Reason**: Workspace-neutral convention — the `.worktrees/` placement rule applies to any repo managed by any agent or human, with no dependency on repo-specific tooling or org structure.
**Trigger**: Agent is about to create or move a git worktree.
**Steps/contract**: (1) Ensure `./.worktrees/` exists at repo root. (2) Create new worktrees under `./.worktrees/<branch-or-task-name>`. (3) Leave existing worktrees in place unless user explicitly requests a move. (4) If the repo documents its own worktree convention, honour that over this default.
**Strip**: Concrete path examples (`~/vesta-cx/vesta`, `~/mia-cx/textile`) — they are illustrative only and carry no transferable information.
**Structure/format**: Short rule + rationale bullets + 4-step workflow. Clean and well-scoped; could be trimmed to rule + steps only.
**Notes**: Low complexity, high portability. No conflicts expected with other rules. A strong candidate for direct promotion as-is (with example paths stripped).
## rules/package-manager.md

**Type**: Rule (always-apply, single-command preference)
**Portable**: Yes
**Reason**: The `pnpm add` → `pnpm i` distinction is a universal pnpm workspace hazard — the `.pnpm-store` pollution problem applies in any repo using pnpm workspaces, not just this one.
**Trigger**: Any task that installs npm/pnpm dependencies; fires on `pnpm add` usage.
**Steps/contract**: Single-rule contract — always use `pnpm i <pkg>` (prod) or `pnpm i -D <pkg>` (dev); never `pnpm add`.
**Strip**: Nothing to strip; no repo-specific paths, names, or org references present.
**Structure/format**: Frontmatter rule with short rationale section. Compact and clear; easily adopted as-is.
**Notes**: The "why" rationale (`.pnpm-store` in repo root → git perf degradation) is genuinely useful to carry forward so agents understand the motivation, not just the rule.
## rules/skill-promotion.md

**Type**: Rule / governance policy
**Portable**: Yes
**Reason**: The five-criteria promotion gate and anti-patterns are domain-agnostic meta-governance that applies to any skill library, regardless of project or tech stack. The underlying reasoning problem (premature extraction vs. earned reuse) is universal.
**Trigger**: When an agent or user is deciding whether to extract a repeated procedure into a new skill file.
**Steps/contract**: (1) Check all five promotion criteria (repeats across tasks; recognizable I/O; value not tied to one task; reduces repeated reasoning; followable without original context). (2) If any criterion fails, do not promote. (3) Check anti-patterns (unstable idea, narrow task, better as spec, too vague, would duplicate existing skill). (4) On promotion, update the skill registry immediately.
**Strip**: The "Why this rule exists" rationale section is explanatory backstory — useful for humans reading the file, but not needed in an operational SOP prompt.
**Structure/format**: Enumerated criteria (promote-when / do-not-create) plus a registry discipline one-liner. Clean and directive; translates directly into a checklist block.
**Notes**: Registry-sync discipline ("update immediately, do not let it fall out of sync") is a strong portable guardrail worth preserving verbatim. The rule deliberately uses "all five hold" as a conjunction gate — this should be kept as-is rather than softened to a majority vote.
## rules/rule-adherence.md

**Type**: Meta-rule / foundational rule (not a workflow SOP)
**Portable**: Yes
**Reason**: Directly addresses a known LLM failure mode — models silently dropping rules when context is crowded or a task feels "quick". This concern is universal across any agent setup, not specific to this repo.
**Trigger**: `alwaysApply: true` — loads in every session unconditionally; should remain always-on in any target system.
**Steps/contract**: Single imperative — every rule currently in context is mandatory; follow each exactly even under time or context pressure; when a rule is unclear, ask rather than assuming it doesn't apply.
**Strip**: The repo-specific example paths (`apps/erato/src/routes/`, `erato-rules/`, `11-pnpm-install-command-preference.mdc`) must be replaced with generic placeholders or dropped; they leak internal project structure.
**Structure/format**: Short prose rule with a "Why this rule exists" rationale block and concrete Good/Bad examples — well-suited for direct reuse as a SKILL.md or rules file entry.
**Notes**: This is a meta-rule about rule adherence itself, making it the foundation every other rule depends on. The "Why this rule exists" section is unusually valuable — it teaches the agent *why* the instruction matters, which strengthens compliance. High-priority candidate for inclusion as a standing rule in any portable agent system.

## skills/agent-optimize-rules/SKILL.md

**Type**: Skill (meta-skill / rules-maintenance workflow)
**Portable**: Yes
**Reason**: The three refactoring patterns (Vague→Specific, Negative→Positive, Imprecise→Precise) and the tribal-knowledge filter are universal heuristics applicable to any agent rule/skill library. No dependency on org structure, specific tools, or repo topology.
**Trigger**: Agent or user is auditing, refactoring, or writing rules, skills, or subagent prompts; or when any prompt file feels bloated, vague, or over-specified.
**Steps/contract**: (1) Read the rule/skill. (2) Ask "Is this tribal?" — if discoverable from code/tests/external docs, delete or move it. (3) Ask "Is this focused?" — if over length limit, extract to external docs. (4) Ask "Does it explain why?" — if a constraint or gotcha is missing, add it. (5) Apply the three refactoring patterns in sequence. (6) Commit.
**Strip**: The type-specific section referencing `.cursor/rules/*.mdc` and `.cursor/skills/*/SKILL.md` paths — these are repo-layout conventions for this specific workspace. Replace with generic paths or remove entirely when promoting. The `Subagents (.cursor/agents/*.md)` section similarly embeds a workspace-specific path.
**Structure/format**: Quick checklist → type-specific checks → three named refactoring patterns with before/after examples → example cleanups → 6-step workflow. Well-structured and pedagogically strong. The before/after examples are the highest-value content and should be preserved verbatim.
**Notes**: The negative→positive pattern rationale ("negative instructions make models fixate on the forbidden action") is a genuinely important insight and should be kept with its explanation intact. The "justify subagent existence" check (could this be a rule or skill?) is a strong portable governance heuristic. This skill is itself a strong candidate for direct promotion with only the path references stripped; it applies to any skill library including the one being assembled by this audit.
## rules/write-back-gate.md
**Type**: Rule / decision gate
**Portable**: Yes
**Reason**: The three-question gate (stable? reusable? clear destination?) is a universal LLM failure-mode fix — agents in any system over-persist ephemeral reasoning into permanent storage. The routing table and "do not write back" list are domain-agnostic.
**Trigger**: Before persisting any artifact — note, fact, rule, doc — to a permanent file during or after a task.
**Steps/contract**: (1) Answer all three questions: Will this still matter later? Can it be reused later? Does it have a clear destination file? (2) If any answer is not clearly yes, do not write back. (3) If writing back, route by content type: stable project context → facts layer; repeatable procedures → skills layer; repo-wide guidance → top-level rules file; task-local notes → change summary only. (4) On adding/removing fact or skill files, update the corresponding index or registry.
**Strip**: Routing labels list (`facts_update`, `skill_promotion`, etc.) — the "hints, not a filing system" caveat acknowledges these are illustrative; a target system can define its own labels or omit them entirely.
**Structure/format**: Prose rule + three-question gate + write-when criteria list + destination routing table + do-not-write list + rationale block. Well-structured; the routing table is the most portable element and should be kept verbatim (with destination paths genericised).
**Notes**: Complements `rules/skill-promotion.md` (which gates skill extraction specifically) and `rules/rule-adherence.md` (which ensures rules aren't silently dropped). Together these three form a coherent meta-governance triad. The "Facts are stable context, not a conversation archive" closing line is worth preserving as-is — it encodes the key mental model in a single sentence.

## skills/audit-deps/SKILL.md

**Type**: Skill (multi-step workflow SOP)
**Portable**: Yes
**Reason**: The full lifecycle — detect package manager, run vulnerability audit, categorize outdated packages by semver bump risk, apply safe updates, and create phased issues for major bumps — is universally applicable across any JS/TS project using pnpm/npm/yarn/bun, with light adaptation paths noted for Python/Go/Rust.
**Trigger**: User wants to audit dependencies, check for CVEs, update packages, or mentions "outdated", "vulnerabilities", "dependency upgrade".
**Steps/contract**: (1) Detect lockfile and select appropriate package manager commands. (2) Run vulnerability audit; classify findings as Critical/High/Moderate/Low. (3) Run outdated check; categorize each package as Patch/Minor/Major with risk tier. (4) Present a structured summary table (vulnerabilities + outdated + recommended actions). (5) Execute safe patch/minor updates immediately; commit with conventional message. (6) For each major bump, create a separate GitHub issue with worktree setup + phased upgrade steps + acceptance criteria checklist. (7) Handle transitive vulnerabilities via `pnpm.overrides` as a last resort, with mandatory inline comment + upstream issue link.
**Strip**: Nothing — no repo-specific paths, org names, or internal references present. The example package names (lodash, svelte, vite, typescript) are illustrative only and carry no leaked context; they can be retained as concrete examples.
**Structure/format**: Workflow with numbered phases, decision tables (lockfile→command matrix; bump-category→risk→action), markdown summary template, shell code blocks, commit message format, and `gh issue create` template with heredoc body. Comprehensive and immediately actionable. No trimming needed.
**Notes**: The Rules block at the end ("Never blindly update all majors at once", "Major upgrades get worktrees", "Document overrides") is a strong portable guardrail set worth preserving verbatim. The `pnpm.overrides` transitive-vulnerability escape hatch with the "document + link upstream issue + remove once patched" lifecycle is a best-practice detail that differentiates this skill from simpler audit tools. High-confidence promotion candidate with no modifications required.

## skills/audit-security/SKILL.md

**Type**: Skill (multi-step workflow SOP)
**Portable**: Yes
**Reason**: The four-phase pattern — automated grep/scan first, attack-surface mapping second, spawn a specialist subagent third, merge and present fourth — is a reusable orchestration contract for any security audit, regardless of tech stack or org. The grep patterns (secrets, CORS, auth, CSP, debug flags) are broadly applicable; only entry-point discovery patterns are framework-specific.
**Trigger**: User asks for a whole-codebase security audit, mentions "security audit", "pen test", "hardening", or "vulnerability scan" — explicitly scoped to full-repo review rather than a single PR diff.
**Steps/contract**: (1) Run automated scans (secrets regex, `.env` in git, CORS/auth/CSP/debug grep, `pnpm audit`). (2) Map attack surface (entry points, user-input handlers, DB/storage interactions, external fetch calls). (3) Spawn Security Reviewer subagent with automated-scan output, attack-surface map, and explicit full-audit scope override. (4) Merge automated output + agent assessment into a tiered action-item report (Must fix / Should fix / Consider). (5) Optionally create GitHub issues for CRITICAL/HIGH findings.
**Strip**: (a) SvelteKit-specific entry-point patterns (`+server.ts`, `event.request`) — replace with generic equivalents or make them examples. (b) Cloudflare-specific storage patterns (`D1|KV|R2|Durable`) — move to examples or footnote. (c) `~/.agents/agents/review-and-qa/security-reviewer.md` subagent path — repo-specific; document the interface contract, not the local path. (d) `pnpm audit` — should mention npm/yarn fallbacks. (e) References to the `audit-deps` skill by local name — describe the delegation pattern generically.
**Structure/format**: Excellent structure: numbered phases with embedded bash blocks, a rules section with concrete "no security theater" / "evidence-based" guardrails, and a trigger list. The output template (Automated Scans + Security Reviewer Assessment + tiered Action Items) is immediately reusable. Guardrails ("automated scans first, agent second", "don't fix — report") are concise and strong.
**Notes**: The "no security theater" rule is the highest-value element — it enforces that every recommendation names the concrete threat it mitigates. Worth preserving verbatim. The clean separation of scanning from remediation (this skill produces the assessment; fixes happen in separate issues/PRs) is a strong architectural principle. Framework-specific bash patterns should be factored into an optional "examples" appendix so the core workflow remains stack-agnostic. Strong candidate for promotion once local path references and SvelteKit/Cloudflare specifics are replaced with generic equivalents.

## skills/next-slice/SKILL.md

**Type**: Skill (multi-step procedural workflow)
**Portable**: Mostly — the 7-step flow (identify state → clean worktree → fast-forward main → find next slice → read plan → create worktree → report) is universally reusable. The `.plans/<number>-<slug>.md` naming convention and GitHub issue lookup are soft dependencies that require matching conventions in the target repo.
**Reason**: Any team using git worktrees + numbered plan files + GitHub PRs can adopt this verbatim. The orchestration logic (cleanup, pull, create, handoff) is pure git/gh workflow with no org-specific tooling.
**Trigger**: User says "next slice", "move on", "next plan", "advance", or wants to start the next issue after a PR merge.
**Steps/contract**: (1) Identify repo root, current worktree, feature branch, and confirm PR is merged. (2) Remove the old worktree and delete its branch from repo root. (3) `git pull --ff-only origin main`. (4) Scan `.plans/` for lowest-numbered plan with an open GitHub issue. (5) Read the plan file to extract issue number, worktree name, branch name, and acceptance criteria. (6) `git worktree add .worktrees/<name> -b <branch> main`. (7) Print a structured handoff summary; begin implementation only if user requested it.
**Strip**: No repo-specific paths, names, or org references are present — skill is already clean.
**Structure/format**: Frontmatter description + narrative intro + 7-step workflow with inline bash snippets + Rules section. Well-structured; sections are appropriately scoped and readable.
**Notes**: The `-d` vs `-D` guard (only force-delete if PR is confirmed merged) is a valuable safety rule worth preserving verbatim. The "don't auto-start implementation" rule is a good default-safe behaviour that prevents runaway execution. High portability; the only adoption cost is aligning plan file naming to `<number>-<slug>.md` and using GitHub issues — both of which are documented expectations, not hidden assumptions.

## skills/gc/SKILL.md
**Type**: Skill (interactive workflow)
**Portable**: Yes — with minor stripping
**Reason**: The core SOP (inspect state → group by logical concern → execute commits → push) is entirely portable across repos and orgs. The concern-to-conventional-type mapping table, commit message format, and checklist are self-contained and have no repo-specific dependencies.
**Trigger**: User wants to organize uncommitted changes into logical commits, split a large changeset, or create a series of conventional commits from the working tree.
**Steps/contract**: (1) `git status` / `git diff` to inspect state; (2) `gh issue list` to check for issue refs; (3) group changes by single concern (config, formatting, behavior, tests, docs, misc); (4) execute each `git add` + `git commit` via Bash without asking approval; (5) `git push`; (6) confirm with summary table.
**Strip**: `Co-Authored-By` trailer instruction (references specific Anthropic model names as examples — ephemeral); the flavourful body examples are illustrative and could be trimmed to one example per type, or replaced with a single representative sample.
**Structure/format**: Well-structured — concern mapping table, commit format spec, body examples by type, full example block, and a pre-finish checklist. Clear and actionable throughout.
**Notes**: Unusually high-quality execution discipline — explicitly forbids asking for approval and requires Bash tool execution rather than printing commands. The "flavourful body" convention is opinionated but portable; the org using this SOP may want to tone it down or keep it as culture. Issue-auto-close refs (`Fixes #N`) are GitHub-specific but the instruction already scopes them correctly.

## skills/pr-file/SKILL.md

**Type**: Skill (PR filing workflow)
**Portable**: Yes
**Reason**: The entire workflow — repo convention discovery, branch/base detection, context gathering from commits and plan files, body templating, title formatting, and `gh pr create` invocation — is generic GitHub workflow with no org-specific identifiers. The repo-wins override logic for PR templates and CONTRIBUTING.md makes it self-adapting to any target repo.
**Trigger**: User says "file a PR", "open a PR", "create a PR", or wants to submit a feature branch for review.
**Steps/contract**: (1) Discover repo PR template and CONTRIBUTING.md conventions; repo rules override skill defaults. (2) Determine feature branch and base branch via git/gh. (3) Gather context: commits, linked issue numbers, plan files, diff stats. (4) Build PR body using repo template if found, else use structured default (Closes #N → summary → What's in this PR → Tests → Acceptance criteria). (5) Choose conventional-commit-style title (type(scope): description, ≤72 chars). (6) File with `gh pr create --body-file -` and report the resulting URL.
**Strip**: Nothing — no org names, repo paths, or env-specific content present. The default template and all rules are fully generic.
**Structure/format**: Six-step numbered workflow with embedded bash snippets, a default PR body template in a fenced code block, a title-format section, and a compact Rules list. Well-structured and immediately executable; no restructuring needed.
**Notes**: Strong candidate. The repo-wins override pattern (check for `.github/PULL_REQUEST_TEMPLATE.md` and `CONTRIBUTING.md` before applying defaults) is a best-practice design worth preserving verbatim. The explicit "no Co-Authored-By trailer" and "no confirmation prompt" rules are clean guardrails worth carrying. Could be cross-linked with `pr-merge` and `pr-review` skills as a natural trilogy.

## skills/deploy-verify/SKILL.md
**Type**: Skill (post-deployment smoke-test workflow)
**Portable**: Yes — with minor de-platforming
**Reason**: The core protocol (enumerate routes, Playwright HTTP + screenshot check, console-error capture, structured result table, CI integration pattern) is framework-agnostic and applies to any web deployment, not just SvelteKit/Cloudflare Pages.
**Trigger**: User says "verify deploy", "smoke test", "check production", or wants to confirm a deployment is healthy after a release.
**Steps/contract**: (1) Resolve production URL (from user, wrangler, or gh CLI). (2) Enumerate routes to check (ask user or infer from project structure). (3) Execute Playwright script — HTTP status, page title, console errors, full-page screenshot per route. (4) Present results in a markdown table with verdict. (5) Optional deeper checks on request: response times, meta tags, asset integrity, API health, screenshot diff.
**Strip**: SvelteKit-specific `find src/routes` route enumeration command; Cloudflare/Wrangler-specific deployment-URL discovery commands and CI snippet (`cloudflare/wrangler-action@v3`, `wrangler pages deployment list`). Replace with generic "fetch deployment URL from your CD output" guidance.
**Structure/format**: Frontmatter → narrative intro → 5-step numbered workflow with inline code blocks → Rules section with 5 bullets. Well-structured and easy to follow; CI integration section is a bonus appendix. Verbosity is appropriate — the Playwright script is the actual deliverable so its presence in the skill is justified.
**Notes**: High-value SOP — the Playwright script is complete and self-contained, making this immediately runnable without scaffolding. The Rules section ("never modify", "generous timeouts", "console errors are warnings not failures") encodes non-obvious gotchas worth preserving. Only the CD-platform details need parameterisation for full portability.

## skills/prd-create/SKILL.md

**Type**: Skill (interactive multi-step workflow)
**Portable**: Yes
**Reason**: The five-step PRD workflow — user interview, codebase exploration, relentless clarification, module design, then issue creation — is entirely repo-agnostic. The "deep module" heuristic and the PRD template sections (Problem Statement, Solution, User Stories, Implementation Decisions, Testing Decisions, Out of Scope, Worktree Setup, Further Notes) carry no org-specific assumptions.
**Trigger**: User wants to write a PRD, create a product requirements document, or plan a new feature.
**Steps/contract**: (1) Elicit a long, detailed problem description and solution ideas from the user. (2) Explore the codebase to verify assertions and understand current state. (3) Interview the user relentlessly — walk down each branch of the decision tree, resolve dependencies one-by-one. (4) Sketch major modules to build/modify; prefer deep modules (rich functionality, simple testable interface); confirm with user which modules need tests. (5) Write the PRD using the template and submit as a GitHub issue with best-fit metadata (labels, assignee, milestone, project, issue type).
**Strip**: The `gh api` discovery commands are portable; keep them. The worktree setup block (`git worktree add .worktrees/<feature-name>`) duplicates the worktree-location rule and can be referenced rather than repeated. No org-specific paths, names, or repo references present — nothing else to strip.
**Structure/format**: Short preamble + numbered 5-step workflow + inline `gh` shell commands for metadata discovery + `<prd-template>` XML block with section headers. Well-structured; template sections are the durable artifact. The "may skip steps" opener is a useful affordance to preserve.
**Notes**: Strong candidate for direct promotion. The "deep module" definition embedded in step 4 is a high-value portable heuristic and should be retained verbatim. The PRD template is the most reusable part — it maps cleanly to universal feature-planning needs. The GitHub metadata discovery pattern (labels, milestones, projects, assignees, issue types) is also portable and worth keeping as a self-contained sub-procedure.

## skills/pr-merge/SKILL.md
**Type**: Skill (PR merge workflow with flavour comment protocol)
**Portable**: Yes
**Reason**: The entire workflow uses only `gh` CLI commands available in any GitHub repo — no org-specific tooling, paths, or identifiers. The merge-readiness gate (CI + approvals + mergeability), flavour-comment-before-merge ordering, and conventional commit subject format are universally applicable conventions.
**Trigger**: A PR is ready to merge and the user wants a clean, memorable merge with narrative context visible in GitHub's timeline.
**Steps/contract**: (1) Identify PR number or `gh pr list` to find it. (2) `gh pr view` for full context; `gh pr diff` for changeset. (3) Check linked issues via body/commit refs; `gh issue view` for each. (4) Verify merge-readiness: `gh pr checks`, reviews field, `gh pr view --json mergeable` — stop and report blockers if not green. (5) Post flavour comment via `gh pr comment` summarising impact with personality. (6) `gh pr merge --merge --subject "<type(scope): description (#N)>"` — no body in the commit. (7) Confirm to user with a closing quip.
**Strip**: The example flavour comments (Kansas quote, auth milk metaphor, deps war, hydra refactor) — illustrative; keep one representative sample per type or replace with a single `{feat example}` placeholder. The specific "Another one bites the dust 🎤" confirmation quip is opinionated culture — adoptable as-is or omittable.
**Structure/format**: Narrative intro + 6-step numbered workflow with inline `gh` bash blocks + merge commit subject format section + pre-finish checklist. Clean and executable throughout. Checklist mirrors the six steps, which is a useful redundancy for human review. No restructuring needed.
**Notes**: Natural trilogy with `pr-file` and `pr-review` — cross-link all three. The "post comment BEFORE merge" ordering is the key discipline worth calling out explicitly: it ensures the narrative is in GitHub's timeline at the exact merge event. The merge-style detection (`allow_merge_commit`/`allow_squash_merge`/`allow_rebase_merge` via `gh api`) is a best-practice portability detail worth preserving. "Do not ask for approval — merge immediately after posting the comment" is a strong no-confirmation-prompt guardrail consistent with gc and pr-file. High-confidence promotion candidate; no modifications required beyond optionally trimming example flavour lines.

## skills/pr-resolve-discussions/SKILL.md

**Type**: Skill (multi-step triage + implementation workflow)
**Portable**: Yes
**Reason**: The full protocol — gather PR context via GraphQL, classify each thread into five categories, trace blast radius, design fixes at the right abstraction level, reply with commit SHAs, resolve threads, summarize in a table — is pure GitHub workflow with no org-specific identifiers or repo-specific tooling. The classification table, investigation heuristics, and "Garbage Elimination Principles" are domain-agnostic engineering norms.
**Trigger**: PR has unresolved review threads; user says "resolve discussions", "fix review comments", "address PR feedback", or a PR is blocked on unresolved threads.
**Steps/contract**: (1) Fetch unresolved, non-outdated threads via `gh api graphql` (full query provided). (2) Classify each thread: Real bug / Real design smell / Valid concern already handled / False positive / Preference. (3) For real findings: trace blast radius, find root cause (not just symptom), check for same class of bug elsewhere, choose fix level (types > fail-loud > runtime guard > patch). (4) Implement fixes directly — no issues, no permission requests; update all call sites, fix tests that asserted buggy behaviour, add tests if none exist. (5) Reply to each thread with commit SHA and one-line rationale; resolve the thread. (6) Organise commits by concern, push, and present a summary table (thread / file / classification / action / commit).
**Strip**: Nothing required — no org names, repo paths, or internal identifiers present. Philosophy and Garbage Elimination sections are strong portable reasoning content worth keeping verbatim.
**Structure/format**: Philosophy section → 6-step numbered workflow with embedded GraphQL + bash snippets, a classification table with actions, an implementation-standards list, reply format templates, and a 7-principle "Garbage Elimination" closing block. Well-structured; the classification table and GEP block are the highest-value portable elements.
**Notes**: The "err on the side of real" heuristic (investigate if 60% sure it's a false positive) is an unusually strong directive that combats a common LLM failure mode (reflexively dismissing findings). The Garbage Elimination Principles — especially "lies in the type system are bugs" and "any is contagious" — are broadly applicable engineering principles that extend this skill's value beyond PR review into general code-quality enforcement. Pairs naturally with `pr-review` (which produces the threads this skill resolves) and `pr-merge` (which follows resolution). High-confidence promotion candidate with no modifications needed.

## skills/prd-create/SKILL.md

**Type**: Skill (interactive workflow — user interview → codebase exploration → module design → GitHub issue)
**Portable**: Yes, with minor trimming
**Reason**: The five-step PRD workflow (interview → explore → interview → module sketch → write & file) is a reusable product-thinking process with no hard dependencies on a specific repo, org, or toolchain. The GitHub issue filing and `gh` CLI usage are standard enough to transfer to any GitHub-hosted project.
**Trigger**: User wants to write a PRD, create a product requirements document, or plan a new feature.
**Steps/contract**: (1) Collect a long, detailed problem description from the user. (2) Explore the repo to verify assertions and understand current state. (3) Interview the user relentlessly, walking each branch of the design tree and resolving decision dependencies one-by-one. (4) Sketch major modules to build or modify; prefer deep modules (rich functionality behind a simple, stable, testable interface) over shallow ones; confirm with user. (5) Write the PRD using the embedded template and file it as a GitHub issue — discover and apply repo metadata (labels, milestones, projects, assignees, issue types) before creating.
**Strip**: The `## Worktree Setup` section inside the PRD template (`.worktrees/<feature-name>` convention is org-specific and duplicates the `worktree-location` rule). No other repo-specific content to remove.
**Structure/format**: Frontmatter + numbered prose steps + embedded bash discovery block + `<prd-template>` XML block. The template is detailed and portable as-is. The deep-module definition embedded in step 4 is a valuable heuristic worth preserving.
**Notes**: High-value candidate. The Socratic interview loop and deep-module heuristic are the distinctive, transferable elements. The PRD template itself (Problem Statement → Solution → User Stories → Implementation Decisions → Testing Decisions → Out of Scope → Further Notes) is a solid, reusable structure. The GitHub metadata discovery block is a best-practice snippet worth keeping.

## skills/pr-merge/SKILL.md

**Type**: Skill (interactive workflow, tool-heavy)
**Portable**: Yes — with one caveat (see Strip)
**Reason**: The end-to-end PR merge workflow (fetch context → read diff → check readiness → post comment → merge → confirm) is a universal GitHub PR pattern. Nothing in the steps references org-specific repos, CI systems, or custom tooling beyond the `gh` CLI.
**Trigger**: User says "merge the PR", "merge #N", or a PR is ready to land and needs a clean commit + human-readable summary.
**Steps/contract**: (1) Identify PR number (user-supplied or `gh pr list`). (2) Fetch full PR metadata via `gh pr view --json`. (3) Read full diff via `gh pr diff`. (4) Check linked issues for close references. (5) Verify merge readiness: CI green, approvals present, no conflicts — stop and report if not. (6) Post a flavourful summary comment via `gh pr comment` before merging. (7) Merge via `gh pr merge --merge --subject "<conventional-commit-subject> (#N)"` — no commit body. (8) Confirm to user.
**Strip**: The "flavour" personality framing is persona-specific (casual tone, puns, lyrics). Teams wanting a professional/neutral tone should strip or replace the flavour section with a plain-language summary requirement. The comment examples are illustrative only.
**Structure/format**: Numbered workflow (8 steps) + commit-subject format spec + pre-merge checklist. Well-scoped, each step is executable. The checklist is a strong close-out pattern worth preserving verbatim.
**Notes**: The hard stop in step 5 (do not merge a non-green PR) is the highest-value portable guardrail. The separation of narrative (PR comment) from commit metadata (subject-only, no body) is a clean convention worth carrying forward. Flavour framing is the only element that may need toning down per team culture.

## skills/pr-review/SKILL.md

**Type**: Orchestration skill (multi-agent PR review coordinator)
**Portable**: Yes
**Reason**: The three-tier parallel review pattern (correctness + security + verification), decision table for which agents to spawn, collation format, and inline GitHub comment posting protocol are all repo-agnostic. The only non-portable surface is the subagent file paths (`~/.agents/agents/review-and-qa/`), which are pointer references rather than embedded logic.
**Trigger**: User asks to review a PR, check a PR, or wants feedback before merging.
**Steps/contract**: (1) Fetch PR context via `gh pr view` + `gh pr diff`. (2) Classify the PR (security-sensitive? has acceptance criteria?) using a decision table → select agents. (3) Spawn selected agents in parallel: PR Reviewer (bugs/correctness), Security Reviewer (threat model/OWASP), Verifier (acceptance criteria check). (4) Collate into a structured summary with Code Review / Security Assessment / Verification / Overall Verdict sections. (5) Post collated findings as a GitHub inline review via `gh api …/reviews` with per-finding inline comments anchored to diff lines; fall back to chat if no PR exists.
**Strip**: Subagent file paths (`review-and-qa/pr-reviewer.md` etc.) — replace with role descriptions. Hardcoded `~/.agents/` prefix — not portable across workspaces.
**Structure/format**: Well-structured — subagent table, workflow numbered steps, decision table, collation template with markdown, inline comment posting code snippet, rules list, follow-up skills section. Slightly verbose but every section adds signal. Could trim the inline-comment edge-case notes modestly.
**Notes**: The decision table (Standard / Security-sensitive / Has criteria / Both) is the highest-value extractable element — it's an opinionated, battle-tested policy for when to escalate review depth. The inline GitHub review API pattern (batch inline comments via `gh api …/reviews` rather than `gh pr review --request-changes`) solves a real GitHub limitation around authors reviewing their own PRs; that nuance is worth preserving verbatim. Strong candidate for promotion with subagent paths abstracted to role labels.

## skills/pr-resolve-discussions/SKILL.md

**Type**: Skill (multi-step triage + implementation workflow)
**Portable**: Yes
**Reason**: The full protocol — fetch unresolved threads via GraphQL, classify into five categories, trace blast radius, design fixes at the correct abstraction level, reply with commit SHAs, resolve threads, and summarise in a table — is pure GitHub workflow with zero org-specific identifiers or repo-specific tooling. The classification table, investigation heuristics, and "Garbage Elimination Principles" are universal engineering norms.
**Trigger**: PR has unresolved review threads; user says "resolve discussions", "fix review comments", "address PR feedback", or a PR is blocked on unresolved threads and needs to reach merge-ready state.
**Steps/contract**: (1) Fetch unresolved, non-outdated threads via `gh api graphql` (full query provided). (2) Classify each thread: Real bug / Real design smell / Valid concern already handled / False positive / Preference. (3) For real findings: trace blast radius upstream and downstream, identify root cause not just symptom, search codebase for the same class of bug, choose fix level (types > fail-loud > runtime guard > patch-only as last resort). (4) Implement fixes directly — no issue creation, no permission requests; update all call sites, fix tests that asserted buggy behaviour, add tests where none exist, run existing tests to confirm no regressions. (5) Reply to each thread with commit SHA + one-sentence rationale; resolve the thread via GraphQL mutation. (6) Organise commits by concern (not one per thread), push, and present a summary table (thread / file / classification / action / commit).
**Strip**: Nothing — no org names, repo paths, or internal identifiers are present. The Philosophy section and Garbage Elimination Principles block are domain-agnostic and should be preserved verbatim.
**Structure/format**: Philosophy section (2 short paragraphs) → 6-step numbered workflow with embedded GraphQL + bash snippets → 5-row classification table with Action column → implementation-standards prose list → reply-format templates (fixed vs false-positive) → 7-principle "Garbage Elimination" closing block. Well-structured; classification table and GEP block are the highest-value portable elements.
**Notes**: The "err on the side of real" heuristic (treat a finding as real if only 60 % sure it is a false positive) is an unusually strong directive that combats a common LLM failure mode of reflexively dismissing reviewer concerns. The Garbage Elimination Principles — especially "lies in the type system are bugs", "`any` is contagious", and "implicit contracts become wrong contracts" — extend this skill's value well beyond PR resolution into general code-quality enforcement. Pairs naturally with `pr-review` (which produces the threads this skill resolves) and `pr-merge` (which follows). High-confidence promotion candidate; no modifications required.

## skills/setup-changesets/SKILL.md

**Type**: Skill (tool-setup workflow — install, configure, automate)
**Portable**: Yes
**Reason**: The changeset lifecycle (PR-time intent capture → versioning PR → release) is a universal OSS/product versioning pattern. Nothing in the skill is tied to this repo's domain, team structure, or org secrets beyond placeholder values.
**Trigger**: User wants to add changesets, set up versioning/changelog automation, or mentions "changesets" in any repo.
**Steps/contract**: (1) Detect project shape (monorepo vs single-package, Cloudflare presence, existing `.changeset/`). (2) `pnpm add -D @changesets/cli @changesets/changelog-github`. (3) `pnpm changeset init`. (4) Write `.changeset/config.json` with `privatePackages.version/tag: true` for apps. (5) Ensure `package.json` has `name`, `private`, `version` fields. (6) Create `.github/workflows/release.yml` using `changesets/action@v1`; optionally add Cloudflare deploy step gated on `hasChangesets == 'false'`. (7) Optionally add `pnpm changeset status --since=origin/main` to CI. (8) Commit with conventional commit message.
**Strip**: The note referencing the `setup-ci` skill's `.npmrc` `store-dir` convention — it is a cross-skill internal reference; extract the underlying requirement (set `store-dir=node_modules/.pnpm-store` in `.npmrc`) and state it directly.
**Structure/format**: Well-structured — frontmatter, rationale section, numbered workflow with bash blocks and YAML snippets, worktree usage appendix. Detailed and actionable; can be adopted near-verbatim.
**Notes**: High-value candidate. The non-interactive changeset authoring pattern (heredoc to `.changeset/<name>.md`) is particularly portable for agent workflows. The monorepo branch (`fixed`/`linked` arrays) is a useful addendum that expands the skill's coverage without cluttering the primary path. Recommend keeping the Cloudflare deploy step as an opt-in callout rather than a first-class step.

## skills/prd-to-issues/SKILL.md

**Type**: Skill (interactive multi-step workflow)
**Portable**: Yes
**Reason**: The tracer-bullet vertical-slice decomposition methodology and HITL/AFK classification are repo-agnostic techniques applicable to any GitHub-hosted project. The GitHub CLI commands use placeholder `OWNER/REPO` variables throughout, making them structurally portable.
**Trigger**: User wants to break a PRD into independently-grabbable GitHub issues / implementation tickets.
**Steps/contract**: (1) Fetch PRD from GitHub issue. (2) Optionally explore codebase. (3) Discover repo metadata (labels, milestones, projects, assignees, issue types). (4) Draft vertical slices with HITL/AFK classification and dependency graph. (5) Quiz user on granularity, dependencies, and metadata assignments; iterate to approval. (6) Create issues in dependency order with full metadata, attach as sub-issues of parent PRD, and set GraphQL blocking relationships. (7) Print confirmation table and verify sub-issue checklist and blocking graph on GitHub.
**Strip**: Nothing repo-specific — all commands use `OWNER/REPO` placeholders. Concrete example issue numbers in the confirmation table (`#3`, `#4`, etc.) are illustrative; the pattern is what transfers.
**Structure/format**: Numbered 7-step process with bash code blocks for all CLI operations, a GraphQL batching pattern with shell associative arrays, an `<issue-template>` XML block for issue body structure, and a final summary table. Well-structured and detailed; directly adoptable as a skill.
**Notes**: High-value SOP with strong technical depth. The HITL/AFK slice classification is a distinctive pattern worth preserving verbatim. The sub-issues API (`POST .../sub_issues`) and GraphQL `addBlockedBy` mutation are specific to GitHub and are legitimate portability constraints (not bloat) — keep them. The `<issue-template>` block (worktree setup, acceptance criteria, blocked-by, user stories addressed) is a reusable artifact template that could be extracted as a standalone deliverable format.

## skills/prd-to-plan/SKILL.md

**Type**: Skill (interactive planning workflow — PRD → phased plan files)
**Portable**: Yes
**Reason**: The full workflow (PRD in context → codebase exploration → identify durable architectural decisions → draft vertical slices → quiz user → write plan files) is repo-agnostic. The tracer-bullet slicing discipline, the prohibition on premature implementation detail, and the durable-decision identification step are universal planning heuristics applicable to any software project regardless of stack or org.
**Trigger**: User wants to break down a PRD, create an implementation plan, plan phases from a PRD, or mentions "tracer bullets".
**Steps/contract**: (1) Confirm PRD is in context (ask user to paste if not). (2) Explore codebase for architecture, patterns, and integration layers. (3) Identify durable decisions that won't change across phases (route structures, schema shape, key models, auth approach, 3rd-party boundaries). (4) Draft vertical slices — each a thin end-to-end path through every layer (schema + API + UI + tests); each slice demoable on its own; prefer many thin over few thick. (5) Present breakdown as numbered list (title + user stories covered) and quiz user on granularity; iterate until approved. (6) Write `.plans/<number>-<slug>.md` per slice and a `.plans/README.md` root linking all slices with dependency table and architectural decisions; add `.plans/` to `.gitignore`.
**Strip**: The `## Worktree setup` subsection inside both plan templates (`.worktrees/<short-name>` convention duplicates the `worktree-location` rule and embeds workspace-specific scaffolding commands that should be a referenced rule, not inline content). No other repo-specific paths, names, or org references are present.
**Structure/format**: Frontmatter description + numbered 6-step prose workflow + `<vertical-slice-rules>` constraint block + root-plan template in fenced XML block + slice-plan template in fenced XML block. Well-structured and immediately executable. Templates are the primary durable artifact; they are clean and portable as-is (post worktree-section removal). The constraint block is concise and highly portable.
**Notes**: The `<vertical-slice-rules>` block is the highest-value portable element — particularly "do NOT include specific file names or function names" (prevents plans from rotting as code evolves) and "DO include durable decisions: route paths, schema shapes, data model names". This encodes a non-obvious but critical planning hygiene principle worth preserving verbatim. The user-quiz step (present before writing files; iterate until approved) is a strong guardrail against wasted plan-writing that should be retained. Natural dependency with `prd-create` (upstream), `prd-to-issues` (parallel), and `next-slice` (downstream consumer of the plan files this skill produces). High-confidence promotion candidate; only modification needed is extracting the worktree setup block to a cross-reference.

## skills/release/SKILL.md
**Type**: Skill (dual-path release workflow SOP — changesets or manual from conventional commits)
**Portable**: Yes
**Reason**: Both paths cover the full release lifecycle (detect strategy → version bump → changelog → commit/tag/push → GitHub Release) using only standard tools (`git`, `gh`, `pnpm`/`npm`, `changeset`). The semver bump inference table, CHANGELOG grouping format, and `gh release create` invocation are universal to any GitHub-hosted JS/TS project regardless of org or stack.
**Trigger**: User says "release", "cut a release", "publish", "changelog", or "release notes".
**Steps/contract**: Auto-detect: check for `.changeset/config.json` → branch to changesets or manual path. **Changesets path**: (1) `pnpm changeset status` — bail if nothing pending; (2) `pnpm changeset version` to bump + write CHANGELOG + delete consumed changesets; (3) show user new version + CHANGELOG entries, let them edit; (4) `git commit -m "chore: version packages"` + `git push --follow-tags`; (5) extract CHANGELOG section via `awk` + `gh release create --verify-tag`. **Manual path**: (1) find last tag or first commit as baseline; (2) `git log <last>..HEAD --oneline --no-merges`; (3) parse commits → determine semver bump (any `feat`→minor; any `fix` only→patch; `BREAKING CHANGE`/`!`→major; chore-only→patch or skip) → confirm with user; (4) generate grouped CHANGELOG entry (Features / Bug Fixes / Other Changes) with PR/issue links; (5) prepend to `CHANGELOG.md`, show user for review; (6) `npm version X.Y.Z --no-git-tag-version`, commit, `git tag -a`, push + `--follow-tags`; (7) `gh release create --notes-file --verify-tag`. Checklist closes both paths.
**Strip**: (a) `pnpm` hardcoded in the manual path's version step (`npm version`) — inconsistent with changesets path; replace with package-manager-agnostic guidance or add pnpm equivalent (`pnpm version`). (b) Worktree escape hatch (`git worktree list | grep 'main\|bare'`) — the grep pattern is workspace-specific; replace with generic instruction to run from the main checkout. (c) `pnpm changeset status/version` commands are correct for pnpm workspaces but should note the plain `changeset` CLI fallback for npm/yarn repos.
**Structure/format**: Strategy-detection gate → two fully labeled flow sections (## Changesets Release / ## Manual Release) each with numbered steps + bash code blocks + user-confirmation checkpoints → Worktree Note → 6-item close-out Checklist. Clean and complete; sections are well-scoped and independently followable. The semver bump table in the manual path is the highest-value portable element and should be preserved verbatim.
**Notes**: The auto-detect design (changeset config presence → branch) makes this self-adapting to any repo without user input — a strong architectural decision. The interactive human-in-the-loop pauses (show new version, offer CHANGELOG edit) are valuable workflow discipline that prevent silent automation errors. The `awk` command for extracting a single version's CHANGELOG section is elegant and worth preserving verbatim. Minor inconsistency between paths: changesets path implicitly uses pnpm while manual path calls `npm version` — should be unified or made package-manager-aware. Strong candidate for direct promotion once the worktree grep pattern and manual-path `npm` reference are generalised.

## agents/engineering/debugger.md
**Type**: Agent role / workflow SOP (root-cause analysis specialist)
**Portable**: Yes
**Reason**: The diagnose-only discipline, evidence-chain requirement, and one-hypothesis-at-a-time methodology are universal debugging practices independent of any stack, org, or toolchain. The structured Root Cause Report format is immediately adoptable in any engineering context.
**Trigger**: Something is broken and the cause is unknown; agent is asked to diagnose a bug, failure, or production incident.
**Steps/contract**: (1) Triage — identify symptom precisely. (2) Reproduce — confirm or flag env differences. (3) Gather evidence — logs, stack traces, git history, config. (4) Form 2–5 ranked hypotheses with supporting evidence. (5) Investigate one hypothesis at a time; record result; continue. (6) Bisect if regression and hypotheses stall. (7) Write Root Cause Report (symptom, repro steps, root cause, evidence chain, introducing change, ruled-out hypotheses, recommended fix, severity/blast radius, follow-ups). (8) Hand off — do not implement the fix.
**Strip**: Interface routing labels ("Receives from: Engineering Orchestrator", "Hands off to: Implementor") — these are org-specific agent-graph wiring that carry no portable value. Model/model_alt frontmatter fields are runtime config, not SOP content.
**Structure/format**: Role preamble + 6 hard rules + 8-step ordered workflow + fully-templated report format + guidelines section. Exceptionally well-structured; the report template is the most portable artifact and should be preserved verbatim. Hard rules double as a checklist.
**Notes**: One of the strongest candidates in this repo. The "diagnose only, never fix" hard boundary prevents scope creep and is worth preserving explicitly. The Root Cause Report template covers severity and blast radius — an often-missing dimension in generic debugging guides. The guidelines section ("think like a detective, not a developer"; "first error, not last") carries genuine epistemic value beyond mere procedure.

## skills/setup-ci/SKILL.md

**Type**: Skill (imperative workflow — scaffold CI/CD files)
**Portable**: Partial
**Reason**: The 6-step workflow (detect project shape → configure pnpm store-dir → generate CI workflow → generate deploy workflow → remind about secrets → commit) is universally sound for any pnpm + GitHub Actions project. However, the deploy step hard-codes Cloudflare Pages + Wrangler, making that half non-portable. The CI half (lint/typecheck/test/build) is fully portable.
**Trigger**: User wants to add CI, set up GitHub Actions, configure deployment pipelines, or automate lint/test/build/deploy on PRs and merges.
**Steps/contract**: (1) Detect project shape — inspect `package.json` scripts, `wrangler.toml`, framework markers, Node version files, `.npmrc`. (2) Set `store-dir=node_modules/.pnpm-store` in `.npmrc` so pnpm store lives inside `node_modules`. (3) Generate `.github/workflows/ci.yml` — PR-triggered, lint/typecheck/test/build, pnpm cache on `node_modules` keyed to `pnpm-lock.yaml` hash, concurrency group with cancel-in-progress. (4) Generate `.github/workflows/deploy.yml` — push-to-main-triggered, build + Cloudflare Pages via `wrangler-action@v3`, deploy concurrency group without cancel-in-progress. (5) Remind user to set `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` secrets. (6) Stage and commit with conventional commit message.
**Strip**: Cloudflare Pages / Wrangler deploy workflow (steps 4–5 and the deploy.yml template) — these are host-specific. SvelteKit build-output paths (`.svelte-kit/cloudflare`). Caching rationale table (useful inline documentation, but noisy in a minimal SOP).
**Structure/format**: Well-structured — frontmatter description → concept explanation (pnpm caching) → numbered checklist workflow → full YAML templates → caching deep-dive notes. Templates and the caching explanation are the highest-value sections. Slightly long but each section is self-contained.
**Notes**: The pnpm `node_modules` caching insight (cache `node_modules` with the store nested inside, not the store alone — 3-5x faster on cache hit) is a genuinely transferable best practice worth preserving verbatim. The CI workflow is the portable core; the deploy workflow should be marked as a Cloudflare-specific extension pattern. Strong candidate for splitting into two skills: `setup-ci-github-actions` (portable) and `setup-ci-cloudflare-deploy` (host-specific extension).

## agents/engineering/developer.md

**Type**: Subagent definition / workflow SOP (spec-first, approval-gated plan-then-implement)
**Portable**: Yes
**Reason**: The four hard rules (spec first, wait for approval, no scope creep, self-verify) and the 10-step workflow are entirely domain-agnostic. The Spec Format and Verification Report Format are standalone templates applicable to any implementation task in any codebase or stack.
**Trigger**: Any focused, single-scope implementation task where one agent can handle the full job without delegation; user requests a feature, fix, or change and the scope is well-bounded.
**Steps/contract**: (1) Ask 1–4 clarifying questions if requirements are ambiguous; skip if clear. (2) Grep/Read to understand existing code patterns before touching anything. (3) Write spec using the embedded template (Goal, Tasks, Acceptance Criteria, Non-goals, Assumptions, Verification Plan, Rollback Plan). (4) STOP — present the spec and say "Please review and approve the plan above." Write zero code. (5) Wait for explicit user approval before proceeding. (6) Implement each task in order, matching existing project patterns. (7) Test visually in browser if working on a web UI with a dev server. (8) Note any out-of-scope discoveries as follow-ups — do not implement them. (9) Run every command in the Verification Plan. (10) Produce inline Verification Report (✅/⚠️/❌ per acceptance criterion with evidence, Commands Run, Risk Notes, Follow-ups).
**Strip**: The `## Interfaces` section (Engineering Orchestrator / Review & QA Orchestrator / Debugger) — org-specific agent topology with no standalone value; the workflow is self-contained without it. The `model` and `model_alt` frontmatter fields — agent-infrastructure metadata irrelevant outside this specific harness.
**Structure/format**: Frontmatter + Interfaces + Hard Rules (4 numbered) + Workflow (10 numbered steps) + Spec Format fenced template + Verification Report Format fenced template + Guidelines. Clean and immediately executable. The two fenced templates are the highest-value portable artifacts and should be preserved verbatim.
**Notes**: The STOP/Wait discipline (steps 4–5) is the most distinctive and valuable element — an explicit human-in-the-loop approval gate that prevents premature implementation, encoding a non-obvious LLM failure mode fix. The no-scope-creep rule ("discover more work → update spec → re-confirm") is a strong guardrail worth preserving verbatim. The Verification Report ✅/⚠️/❌ schema with mandatory evidence strings is a reusable close-out template that pairs naturally with the `self-validation` skill. High-confidence promotion candidate; only Interfaces section and model fields need stripping.

## AGENTS.md

**Type**: Persistent-context memory file (two-section blob: learned user preferences + workspace facts)
**Portable**: Partial — the "Learned User Preferences" subsection contains 6 portable rules; the "Learned Workspace Facts" subsection has zero portability
**Reason**: User preferences are domain-agnostic LLM prompt-writing and agent-discipline heuristics applicable across any project or toolchain. Workspace facts are pure ephemeral context (specific filesystem paths, project names, file locations) tied to one machine and one user's setup — they carry no transferable signal.
**Trigger**: Always-on context injection (AGENTS.md / CLAUDE.md format); not a workflow SOP and has no trigger condition. Individual preferences could be promoted as always-apply rules.
**Steps/contract**: No workflow contract — the file is a flat list. Extractable rules from the preferences section: (1) Use positive framing in LLM prompts (not "don't do X"). (2) Keep prompts focused on the current task — omit backstory, prior-phase references, and historical context. (3) Reasoning sections explain *why* a reframe is happening, not what constraints exist. (4) Prefer bottom-up discovery with example lists over canonical/exhaustive lists — let categories emerge from data. (5) When a wrong file was edited in error, revert that edit and re-apply to the correct file; leave both files clean. (6) Historical/archive files must be left intact during bulk renames — only update files where the reference is actively used.
**Strip**: The entire "Learned Workspace Facts" section (paths like `/Users/mia/.agents`, `.worktrees/role-to-sop/`, `continual-learning.json`, specific tool and file references). Also strip the framing wrapper ("Learned User Preferences" heading, bullet-list presentation) in favour of individual structured rule entries.
**Structure/format**: Two markdown H2 sections, each with plain bullet lists. No frontmatter, no numbered steps, no rationale blocks. Raw memory-dump format — needs reshaping before any content can be promoted to a reusable rule or skill.
**Notes**: The 6 preferences are genuinely high-value portable rules worth individual promotion — particularly (1) positive framing (the "don't think about pink elephants" rationale is the kind of *why* that strengthens agent compliance) and (4) bottom-up discovery (canonical lists as constraints vs. examples as hints encodes a subtle but important prompting principle). They are best promoted as separate entries in a standing `rules/prompt-writing.md` or similar file rather than kept as a monolithic AGENTS.md blob. The AGENTS.md format itself (continual-learning memory blob auto-injected into sessions) is an interesting meta-pattern but not a candidate SOP — the pattern is the file format, not the content.

## agents/engineering/implementor.md

**Type**: Agent role definition (scoped worker / executor)
**Portable**: Yes
**Reason**: The single-task executor pattern — no scope creep, no refactoring, no delegation, commit and report back — is universally applicable as a SOP for any worker agent operating under an orchestrator, regardless of repo or org context.
**Trigger**: Agent is assigned a bounded implementation task by an orchestrator or shepherd and must execute without expanding scope.
**Steps/contract**: (1) Read the task description (objective, scope, definition of done, verification plan). (2) Research the codebase with Grep, Glob, and Read to understand existing patterns. (3) Implement minimally, following existing patterns. (4) Run the task's verification commands; if unable to run them, state why explicitly. (5) Commit with a clear message. (6) Report back in 1–3 sentences: what was done, verification outcome, any risks or follow-ups.
**Strip**: Orchestrator names ("Engineering Orchestrator", "PR Shepherd", "Ralph") — these are org-specific role labels; replace with generic "dispatcher/orchestrator" in the portable version. Model fields (`claude-sonnet-4-6:medium`, `gpt-5.4-mini:medium`) — runtime config, not SOP content.
**Structure/format**: Frontmatter + Interfaces + Hard Rules + Execution steps + Completion block. Well-structured; Hard Rules and Execution map cleanly to a portable skill. Completion block is a concise, enforceable close-out protocol.
**Notes**: Hard Rules section is the most portable asset — the trio (no scope creep, no refactors, no delegation) is a tight, reusable constraint set for any implementor-class agent. The completion summary requirement pairs naturally with the `self-validation` skill. Strong candidate for promotion; minimal stripping needed.

## agents/engineering/researcher.md
**Type**: Agent role / workflow SOP (pre-planning investigation specialist)
**Portable**: Yes
**Reason**: The frame→scope→investigate→cross-reference→synthesize workflow is a universal research methodology applicable to any codebase, library, API, or problem domain. The Research Brief format, evidence-citation requirement, and "no planning, no implementing" discipline are stack-agnostic and carry no org or toolchain dependencies.
**Trigger**: Investigation is needed before planning or implementation; a question about a codebase, library, or problem space requires structured evidence gathering before a decision is made.
**Steps/contract**: (1) Frame — restate the research question in one sentence; ask 1–3 clarifying questions if vague. (2) Scope — list 2–5 specific sub-questions to investigate; share the list before diving in. (3) Investigate — use Grep, Glob, Read, and web tools; start broad (file structure, entry points) then go deep on what matters; follow unexpected findings and note detours; call dead ends explicitly and move on. (4) Cross-reference — look for contradictions between code, docs, and comments. (5) Synthesize — write the Research Brief (Key Findings → How It Works Today → Options & Trade-offs table → Risks & Concerns → Open Questions → Recommendations → Sources). (6) Report — highlight the 1–3 most important findings the caller needs immediately.
**Strip**: Interface routing labels ("Receives from: Engineering Orchestrator", "Hands off to: Engineering Orchestrator / Developer") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields are runtime config, not SOP content.
**Structure/format**: Role preamble (one-sentence purpose + scope boundary) + 5 hard rules + 6-step ordered workflow + fully templated Research Brief format (with embedded Options & Trade-offs table, Risks & Concerns, Open Questions, Recommendations, Sources sections) + guidelines section. Exceptionally well-structured; the Research Brief template is the primary portable artifact and should be preserved verbatim.
**Notes**: Strong promotion candidate. The "no planning, no implementing" hard boundary is the defining discipline — preserving it explicitly prevents scope creep from investigation into execution. The "evidence or nothing" rule (every claim must cite a file path, code snippet, or doc URL, or be labelled as an assumption) is a high-value epistemic guardrail that distinguishes this from generic research workflows. The Options & Trade-offs table enforces separation of observation from interpretation, which is a non-obvious structural virtue. The guidelines section ("think like a detective, not an advocate; follow the evidence even if it contradicts the initial hypothesis") mirrors the debugger.md ethos — together they form a natural epistemic pair: debugger diagnoses known failures, researcher investigates unknowns. The "scope your investigation" hard rule (surface and ask before ballooning scope) is a practical anti-runaway guardrail worth preserving verbatim.

## agents/engineering/refactorer.md
**Type**: Agent role / workflow SOP (safe incremental refactoring specialist)
**Portable**: Yes
**Reason**: The 8-step workflow, 6 hard rules, named refactoring pattern catalogue, and two structured report templates are entirely stack-agnostic. No org-specific tooling, paths, or identifiers present beyond the interface routing labels.
**Trigger**: Code needs restructuring, migration, or cleanup without changing behavior; user asks to refactor, reorganize, or clean up existing code.
**Steps/contract**: (1) Assess — map blast radius: what depends on this code, what it depends on, what tests cover it. (2) Baseline — run test suite; if already failing, stop and tell the user. (3) Plan — write refactoring plan with each atomic step, the pattern applied, risk level, and rollback strategy. (4) STOP — present plan and explicitly say "Please review and approve"; do not proceed. (5) Wait — make zero code changes until the user explicitly approves. (6) Execute incrementally — smallest possible change → test suite → commit if green, revert and diagnose if red; never push forward on a failing suite. (7) Verify — run full test suite one final time after all steps complete. (8) Report — summarize what changed, which patterns were applied, and any follow-ups discovered.
**Strip**: Interface routing section ("Receives from: Engineering Orchestrator", "Post-completion: Verifier, Review & QA Orchestrator") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields — runtime configuration, not SOP content.
**Structure/format**: Role preamble + 6 hard rules + 8-step ordered workflow + Refactoring Plan Format (markdown template with Goal / Baseline / Steps / Risk Points / Rollback Strategy / Non-goals / Follow-ups sections) + Verification Report Format (Baseline vs Final / Changes Applied / Behavior Confirmation / Risk Notes / Follow-ups) + named Refactoring Patterns reference table + Guidelines section. Exceptionally well-structured and immediately actionable; both templates are complete and reusable as-is.
**Notes**: Strong promotion candidate. The "plan first → hard stop → explicit approval → execute only after go" discipline is the highest-value portable pattern — it prevents the common failure mode of agents refactoring without a shared understanding of scope. The named pattern catalogue (strangler fig, parallel implementation, feature flag gate, incremental migration) with when-to-use guidance encodes battle-tested engineering judgment beyond simple procedure. The "no speculative generalization" hard rule (refactor toward current needs, not imagined future ones) is a non-obvious but critical heuristic worth preserving verbatim. The "if a refactor uncovers a bug, document it but do NOT fix it in the same change" guideline prevents dangerous scope creep and is also worth keeping. Hard rules double as a pre-execution checklist. Pairs naturally with the debugger agent (root-cause analysis before refactoring) and pr-review/pr-resolve-discussions (structural findings often surface in review). Only modification needed is stripping the interface routing section.

## agents/engineering/orchestrator.md

**Type**: Agent role / orchestration SOP (triage-route-orchestrate coordinator)
**Portable**: Yes — with stripping
**Reason**: The route-vs-orchestrate triage decision, approval gates, specialist routing table structure, wave execution protocol, and both plan format templates are domain-agnostic engineering coordination patterns. The core discipline (assess → plan → stop for approval → delegate → verify → report) applies to any multi-specialist agent team regardless of stack, org, or domain. The "never edit code" constraint and "one concern per specialist" rule encode a universally useful orchestrator/implementor separation.
**Trigger**: Engineering work arrives at a central coordinator; scope is ambiguous or may span multiple specialists; agent must classify work as simple (one specialist can handle it solo) or complex (requires wave-based phased execution across multiple specialists).
**Steps/contract**: (1) Assess — classify as Route (single specialist, solo execution) or Orchestrate (multi-task, phased waves). Rule of thumb: "more than one independent task?" → Orchestrate, else → Route. (2) **Route path**: (a) Write Routing Plan (specialist, inputs, expected output, decision points, one-line risk statement); (b) STOP — present plan and wait for explicit approval; (c) spawn specialist via delegation tool; (d) evaluate result — if failed or surfaced new info, change inputs or change specialist (do not retry with same inputs); (e) summarise delivery. (3) **Orchestrate path**: (a) ask 1–4 clarifying questions if requirements are unclear, skip if obvious; (b) explore codebase to understand relevant areas; (c) write Wave Plan (Goal, Tasks with definition-of-done, Acceptance Criteria, Non-goals, Assumptions, Verification Plan, Rollback Plan); (d) STOP — present plan and wait for explicit approval; (e) delegate Wave 1 specialists; (f) wait for Wave 1 completion; (g) spawn Verifier and wait for results; (h) iterate on failures — fix spec and re-delegate; (i) repeat waves; (j) final Verifier pass; (k) report results. (4) Apply common workflow patterns as routing heuristics: New Feature → Researcher → wave orchestration → [Implementors] → test runner → Technical Writer; Bug Fix → Debugger → Developer; Refactor → Researcher → Refactorer; Investigation → Researcher solo; Test Fixing → iterative test agent solo; Simple/Focused Task → Developer solo.
**Strip**: (a) The `## Interfaces` routing labels (VP Engineering, Review & QA Orchestrator) — org-specific agent-graph wiring with no portable value. (b) Named specialist roles (`Ralph`, `Implementor`, `UI Designer`, etc.) — team-specific names; keep the table structure and When/Do-NOT-Use columns, but note that role names are illustrative. (c) `model` and `model_alt` frontmatter fields — runtime infrastructure metadata. (d) `Ralph` as a named test-running agent — the concept (iterative exit-condition-is-tests-pass loop) is portable but the name is not.
**Structure/format**: Frontmatter → Interfaces → Hard Rules (6 numbered) → Team routing table (Specialist / Use When / Do NOT Use When) → Workflow section (assess → Route workflow → Orchestrate workflow, both as numbered sub-steps) → Common Workflow Patterns (named archetypes with specialist chains) → Plan Formats (two fenced templates: Routing Plan and Wave Plan). Exceptionally well-structured. The two plan format templates are the primary durable artifacts and should be preserved verbatim. The routing table structure (When / Do NOT) is a reusable design pattern even if role names change.
**Notes**: The "never edit code yourself" hard rule is the most distinctive and transferable element — it enforces a clean orchestrator/implementor boundary and prevents scope creep at the coordination layer. The "change inputs or change specialist, do not retry blindly" rule is a high-value failure-handling heuristic that combats a common LLM failure mode. The Wave Plan template closely mirrors the developer.md Spec Format; together they form a natural planning-execution handoff contract. The Common Workflow Patterns section encodes opinionated but portable sequencing heuristics for recurring task archetypes (Feature / Bug / Refactor / Investigation) — these are immediately transferable as a routing decision guide. Pairs naturally with `developer.md` (which executes the tasks this agent plans) and `debugger.md` (which feeds root-cause reports into the Bug Fix pattern). Recommend promoting as a coordinator role template with role names genericised to descriptions. High-confidence candidate; only Interfaces section, named agents, and model fields need stripping.

## agents/engineering/ui-designer.md

**Type**: Agent role definition with embedded SOPs
**Portable**: Partially — the structured SOPs inside are highly portable; the agent scaffolding (name, model, Interfaces routing) is repo-specific
**Reason**: The file bundles two separable concerns: (1) a strong, standalone SOP for "design-system-first UI work" with a concrete discovery checklist and hard accessibility rules, and (2) an agent wrapper (name/model/routing) that is specific to the dotagents orchestration topology. The SOP content is stack-agnostic and applies to any frontend codebase.
**Trigger**: Any task that produces frontend UI code; fires when an agent is about to write components, layouts, or styles.
**Steps/contract**: (1) Discover design tokens and component primitives in the codebase before writing any code. (2) Enforce WCAG AA: 4.5:1 text contrast, visible `:focus-visible`, semantic HTML over ARIA, keyboard operability, no color-alone meaning. (3) Use project spacing scale, color tokens, and existing component primitives — never introduce a conflicting design system. (4) Implement all interactive states: default, hover, active, focus, disabled, loading, error. (5) Honour `prefers-reduced-motion`; never use `transition: all`. (6) Run pre-completion checklist before delivering.
**Strip**: `name`/`model`/`model_alt` frontmatter fields; "Interfaces" routing block (Engineering Orchestrator, VP Design, Review & QA Orchestrator, Verifier) — these are dotagents topology-specific and carry no transferable value.
**Structure/format**: Well-structured with clear Hard Rules / Aesthetic Guidelines / Workflow / Pre-Completion Checklist sections. Checklist is concise and directly actionable. Slight redundancy between the Workflow steps and the Hard Rules sections — could be consolidated when promoting to a skill.
**Notes**: The WCAG AA ruleset and the "discover before you build" discipline are the two highest-value portable elements. The pre-completion checklist is a ready-made self-validation gate. Strong candidate for a `web-frontend-design` skill enhancement or a standalone `ui-accessibility` SOP; strip the agent wrapper and promote the SOPs directly.

## agents/engineering/technical-writer.md
**Type**: Agent role / workflow SOP (documentation specialist)
**Portable**: Yes
**Reason**: The read-before-write discipline, five hard rules, seven-step workflow, and six document-type templates (API Reference, Migration Guide, ADR, README, Changelog, Runbook) are universally applicable writing practices with no dependency on any org, stack, or toolchain. Every template maps to a standard documentation pattern used across the industry.
**Trigger**: Documentation needs writing or updating — API docs, migration guides, READMEs, ADRs, changelogs, or runbooks.
**Steps/contract**: (1) Clarify scope: what document, who reads it, what is the deliverable — ask 1–3 questions if ambiguous. (2) Survey existing docs (glob `*.md`, `docs/`, `CHANGELOG*`, `ADR*`, `README*`); note tone, heading depth, example usage, admonition style. (3) Read the relevant code paths — handlers/routes/types for API docs; diff/changelog for migration guides; implementation + related issues for ADRs. (4) Write a section outline and wait for approval before proceeding. (5) Draft the full document using the appropriate template. (6) Self-review against four checks: can reader accomplish goal with only this doc? undefined terms? code examples match codebase? unnecessary repetition? (7) Deliver with explicit gap flags ("X exists in code but has no error-handling docs — flagging for follow-up").
**Strip**: Interface routing block ("Receives from: Engineering Orchestrator", "Informed by: Developer or Implementor") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields are runtime config, not SOP content.
**Structure/format**: Role preamble + 5 hard rules + 7-step ordered workflow + 6 fully-templated document formats + guidelines section. Exceptionally well-structured; the six document templates are the highest-value portable artifact and should be preserved verbatim. The guidelines section (scannable over literary, examples are load-bearing, link don't repeat, present tense active voice, know the four doc types) encodes non-obvious craft principles worth carrying.
**Notes**: Strong candidate. The "Audience first — name the reader" rule is one of the most frequently violated writing principles and is worth preserving verbatim with its examples (README→contributors, API ref→integrators, runbook→on-call). The "no hallucinated details" hard rule with explicit placeholder language ("Behavior when X is undocumented" / "TODO: confirm with maintainer") is a high-value portable guardrail for any documentation agent. The Diátaxis-aligned four-doc-type closing guideline (Reference / Tutorial / How-to / Explanation) elevates this from a style guide to a structural framework. The "outline first, wait" gate in step 4 is a strong scope-control discipline. Direct promotion candidate with only the interface routing section stripped.

## agents/engineering/devops.md
**Type**: Agent role definition / multi-step workflow SOP (CI/CD, deployment, infrastructure-as-code)
**Portable**: Yes
**Reason**: The six hard rules, 8-step workflow, Plan Format template, and Validation Report template are entirely stack-agnostic. The technology preferences section names specific tools (GitHub Actions, Terraform, Helm, Prometheus) but frames them explicitly as adaptable preferences with "evaluate what the project already uses" caveats — the policy structure is portable even where specific tool names are not.
**Trigger**: Agent is asked to build or audit a CI/CD pipeline, create a deployment plan, configure infrastructure, write IaC, or diagnose a deployment incident.
**Steps/contract**: (1) Assess existing infra, pipeline configs, Dockerfiles — read before proposing anything. (2) Diagnose root cause (if fixing) or define scope (if building). (3) Write deployment/infra plan using the embedded Plan Format template. (4) STOP if plan touches production — present plan and wait for explicit approval. (5) Implement pipeline configs, Dockerfiles, IaC definitions, or monitoring config. (6) Validate via dry-run (`terraform plan`, `docker build`, YAML lint). (7) Document inline; update runbooks if they exist. (8) Report: what changed, what to watch post-deploy; use Validation Report template.
**Strip**: Interface routing section ("Receives from: Engineering Orchestrator", "Coordinates with: Head of QA") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields — runtime config, not SOP content.
**Structure/format**: Role preamble + 6 hard rules + 8-step ordered workflow + Plan Format template (Objective / Current State / Proposed Changes / Deployment Strategy / Rollback Plan / Risks / Monitoring+Verification) + Validation Report template (Changes Applied / Dry-Run Results / Pre-Deploy Checklist / Risks & Watch Items) + Guidelines (default opinions + tech preferences + quality bar). Exceptionally well-structured; both templates are self-contained deliverable formats ready for direct reuse.
**Notes**: The six hard rules are the highest-value portable element — especially "rollback plan required before any deployment plan is considered complete" and "no manual steps in production (flag as tech debt if unavoidable)". The prod-approval gate ("present the plan, say 'Ready to apply — please confirm', do NOT execute without explicit approval") is a strong safety guardrail worth preserving verbatim. The "12-factor app" and "immutable deploys" opinions in the Guidelines section are broadly applicable defaults that encode non-obvious discipline (build once, deploy everywhere; dev/prod parity). "What Good Looks Like" quality bar ("deploys are boring — triggered by merge, fully automated, observable, and reversible; there is exactly one way to deploy, and it's the pipeline") is a compact, reusable definition of DevOps maturity worth keeping as-is. Strong candidate for direct promotion with only the interface routing block stripped.

## agents/engineering/ralph.md
**Type**: Agent role / workflow SOP (autonomous iterative TDD loop coordinator)
**Portable**: Yes
**Reason**: The core pattern — interactive planning, then fresh sub-agents per iteration with disk-based state transfer and test/lint backpressure as the exit condition — is a universal agentic orchestration design. No org-specific tooling, repo paths, or tech-stack assumptions are embedded. The IMPLEMENTATION_PLAN.md template acts as a portable inter-agent handoff protocol usable in any agentic system.
**Trigger**: Task has a clear "definition of done" expressible as passing tests/lints; user wants hands-off autonomous iteration without managing context window accumulation manually.
**Steps/contract**: Phase 1 — (1) Understand task from initial message. (2) Discuss with user: what to build, how to test, what acceptance criteria are. (3) Write `IMPLEMENTATION_PLAN.md` to disk with Goal, Spec, Test Commands, Acceptance Criteria, Current Status, and Findings sections. (4) Say "Implementation plan written. Approve?" and wait for explicit user approval before proceeding. Phase 2 — loop until tests pass: (1) Spawn fresh Implementor sub-agent with path to plan + iteration focus + prior test output; sub-agent reads plan, does work, updates Findings and Current Status on disk. (2) Run test commands via fresh sub-agent or Bash; capture full output. (3) ALL PASS → update status to COMPLETE, report to user. FAIL → update Findings, increment iteration count, feed failure output into next iteration prompt. Hard stops: after 3+ consecutive failures on the same issue, stop and surface to user; after 10 total iterations, stop and report findings regardless.
**Strip**: Interface routing labels ("Receives from: Engineering Orchestrator", "Delegates to: Implementor") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields — runtime config, not SOP content. The `while :; do cat PROMPT.md | claude; done` illustrative shell line — references a specific CLI binary by name; replace with a generic pseudocode equivalent if retaining the analogy.
**Structure/format**: Frontmatter + role preamble + Interfaces block + 5 hard rules + Phase 1 (4-step workflow + IMPLEMENTATION_PLAN.md template) + Phase 2 (loop pseudocode + 3-step per-iteration breakdown + 5 loop rules) + Guidelines section. Well-organized; plan template and hard rules are the highest-value portable artifacts.
**Notes**: The "fresh context every iteration, disk as the shared brain, backpressure as the quality signal" triad is a genuinely novel orchestration philosophy worth preserving verbatim — it encodes the entire mental model in three phrases. The IMPLEMENTATION_PLAN.md template is the core portable artifact: it serves as both working memory and stateless handoff protocol between iterations. The "after 3+ failures on the same issue, stop and ask" loop rule is a strong runaway-guard worth keeping as-is. The "spec-driven gap analysis before each iteration" directive (compare codebase against spec, not just fix the last test failure) is a non-obvious, high-value instruction that prevents whack-a-mole iteration cycles. Strong promotion candidate; only modifications needed are stripping the interface routing block and the CLI-specific shell analogy.

## agents/review-and-qa/security-reviewer.md

**Type**: Agent role / workflow SOP (security code-review specialist)
**Portable**: Yes
**Reason**: The six hard rules (never modify, distinguish theoretical from exploitable, evidence-based only, scope to the change, no security theater, escalate secrets immediately), the 6-step workflow (context → trust boundaries → threat model → OWASP evaluation → dependency audit → report), and the Security Assessment output format are entirely stack- and org-agnostic. The OWASP Top 10 checklist and severity definitions apply universally.
**Trigger**: Security audit requested; changes touch auth, data handling, or external interfaces; a PR is flagged for security review.
**Steps/contract**: (1) Gather context — diff, auth model, data flow, dependency lockfiles. (2) Map trust boundaries (external input → app, app → DB, app → external services, client → server, privileged → unprivileged). (3) Threat-model each trust boundary touched (unauthenticated attacker, authenticated-but-unauthorized user, malformed input, secrets/PII flow). (4) Evaluate against OWASP Top 10 and common vulnerability classes (injection, broken auth, broken access control, sensitive data exposure, misconfiguration, XSS, insecure deserialization, SSRF, mass assignment, rate-limiting). (5) Audit dependencies via lockfiles and available audit tooling (`npm audit`, `pip audit`, `cargo audit`, `govulncheck`). (6) Produce Security Assessment (Scope → Threat Model → Findings per severity → Dependency Audit → Verdict).
**Strip**: Interface routing labels ("Receives from: Review & QA Orchestrator", "Hands off to: PR Reviewer / Implementor / Head of QA") — these are agent-graph wiring specific to this repo's orchestration topology. Model/model_alt frontmatter fields are runtime config, not SOP content.
**Structure/format**: Role preamble + 6 hard rules + 6-step ordered workflow + templated Security Assessment output format + severity definitions + guidelines section. Exceptionally well-structured. The output template (Scope / Threat Model / Findings / Dependency Audit / Verdict) and the severity matrix (CRITICAL/HIGH/MEDIUM/LOW with merge-gate implications) are the most portable artifacts and should be preserved verbatim.
**Notes**: Strong promotion candidate. The "distinguish theoretical from exploitable" hard rule is a high-value directive that prevents alert fatigue — one of the most common failure modes in security review tooling. The "no security theater" rule (every recommendation must name a concrete threat it mitigates) is equally strong and worth keeping verbatim. The closing guideline on structural garbage ("a `catch {}` on an auth check silently passes unauthenticated users") is an unusually precise insight: it reframes code-quality issues in security-sensitive paths as the vulnerability class they enable, not just smells — worth preserving in full. Complements `skills/audit-security` (which orchestrates the full-repo scan and spawns this agent) and `skills/audit-deps` (dependency audit sub-procedure). Together these three form a coherent security-review triad. No stack-specific or org-specific content to strip beyond the interface routing labels.

## agents/review-and-qa/pr-reviewer.md
**Type**: Agent role / review SOP (PR code review specialist)
**Portable**: Yes
**Reason**: The review focus areas, noise-suppression discipline (high-confidence only, zero comments if nothing found), severity tiers, output format, and "structural rot" taxonomy are universally applicable to any PR review workflow regardless of stack, org, or toolchain. No org-specific identifiers, paths, or tooling references are present.
**Trigger**: A PR needs code review focused on bugs, security, correctness, and API contracts — especially when the reviewer wants actionable signal rather than style noise.
**Steps/contract**: (1) Gather context via Grep, Read, Glob, and `gh` CLI before forming any opinion. (2) Analyse changed code only — never comment on unmodified context lines. (3) Check upstream Security Reviewer findings before reviewing security-sensitive changes. (4) Classify findings into focus areas: Potential Bugs, Security Concerns, Functional Correctness, API Contract Violations, Database/Data Errors, Structural Rot. (5) Write review: Summary (1–2 sentences) + Verdict (✅/⚠️/❌) + Issues list (severity + file:line + description + suggested fix). (6) If issues found, delegate fixes to Implementor; delegate post-fix checks to Verifier. (7) Post zero comments if no high-confidence issues exist.
**Strip**: Interface routing labels ("Receives from: Review & QA Orchestrator", "Upstream dependency: Security Reviewer", "Hands off to: Implementor/Verifier") — these are agent-graph wiring internal to this repo's orchestration topology, not portable SOP content. Model/model_alt frontmatter fields are runtime config, not review logic.
**Structure/format**: Role preamble → Objectives (3 items) → Interfaces block → Comment Guidelines (5 bullets) → Review Focus Areas (6 categories, with Structural Rot expanded into 5 sub-patterns with rationale) → Areas to Avoid (5 bullets) → Output Format (Summary + Verdict + Issues template) → Delegation rules → Summary recap. Well-structured and immediately executable. The Structural Rot section is the highest-value element; each sub-pattern includes a concrete "why it's dangerous" explanation, not just a label.
**Notes**: The "Structural Rot / Garbage" taxonomy is the standout portable contribution — the five patterns (type lies, swallowed errors, dead code, implicit contracts, copy-paste duplication) with per-pattern danger explanations are a transferable code-quality framework. The "explain why it's dangerous, not just that it exists; prefer the architectural fix over the surface patch" directive is a strong anti-surface-patching principle worth preserving verbatim. The "PRIORITIZE LESS NOISE over completeness" rule is a high-value default that combats over-commenting. Pairs naturally with `pr-review` (orchestrator that spawns this agent), `pr-resolve-discussions` (implements the fixes this review identifies), and `pr-merge` (downstream). High-confidence promotion candidate; only modification needed is stripping the interface routing block.

## agents/review-and-qa/verifier.md
**Type**: Agent role / workflow SOP (evidence-driven acceptance-criteria verifier)
**Portable**: Yes
**Reason**: The evidence-gate discipline ("no evidence, no verification"), the AC-checklist-as-single-source-of-truth rule, the five-category output format (VERIFIED / DEVIATION / MISSING / Risk Notes / Follow-ups), and the structured Fix Request contract are universal QA practices that apply across any tech stack, org, or project type.
**Trigger**: An implementation wave is complete and work needs to be verified against a spec's Acceptance Criteria; or a PR/task needs a formal sign-off before merge/handoff.
**Steps/contract**: (0) Preflight — read spec; confirm AC are specific and testable; flag ambiguities as Spec Issues before proceeding. (1) Map work → criteria — for each AC, identify corresponding tasks, commits, and test commands; unresolvable mappings default to ❌ MISSING. (2) Execute verification — run Verification Plan commands exactly via Bash; if unable, state why and substitute static review labelled with confidence. (3) Edge-case checks — risk-based selection: APIs (compat/validation/error shapes), UI (empty/loading/error/a11y), data models (migrations/nullability/serialization), concurrency (races/retries/idempotency), perf (O(n²) risk/caching). (4) Garbage audit — watch for type lies, swallowed errors, dead code, implicit contracts, and placeholder code merged to main; report in Risk Notes only (non-blocking). Output: Verification Summary (verdict + confidence) → AC Checklist (per-criterion ✅/⚠️/❌) → Evidence Index → Tests/Commands Run → Risk Notes → optional Follow-ups. Fix Requests use a six-field template; implementor redirected to orchestrator if they propose changing AC.
**Strip**: Interface routing labels ("Receives from: Engineering Orchestrator / PR Shepherd / Refactorer", "Fix requests to: Implementor") — org-specific agent-graph wiring; replace with generic role descriptions or omit. Model/model_alt frontmatter fields — runtime config, not SOP content.
**Structure/format**: Role preamble (2 sentences) + 5 hard rules + Tools section (1 sentence) + Process (5 numbered steps with subsections) + fully-templated Output Format + Fix Request template + Completion block. Exceptionally well-structured; the AC Checklist format and the Garbage Audit step are the highest-value portable elements and should be preserved verbatim.
**Notes**: One of the strongest candidates in this repo. The "no partial approvals" hard rule is a critical guardrail against common LLM over-eagerness to approve partial work. The Garbage Audit step is unusually valuable: it enforces codebase health even when AC are satisfied, and the explicit rationale ("codebases are context for future AI-assisted changes — garbage in the code produces garbage in future completions") is worth preserving verbatim. The five-category output format (especially the DEVIATION and MISSING distinction) encodes real QA nuance. The Fix Request template cleanly closes the verification → fix → re-verify loop. Natural peer to `agents/review-and-qa/pr-reviewer.md` (correctness focus) and downstream of the Engineering Orchestrator spec contract. High-confidence promotion candidate; only modification needed is replacing interface routing labels with generic role references.

## agents/review-and-qa/pr-shepherd.md
**Type**: Agent role / orchestration SOP (PR merge-readiness coordinator)
**Portable**: Yes — with minor stripping
**Reason**: The ASSESS→ACT→WAIT polling loop, the five ACT sub-procedures (fix code / re-request review / rebase / re-trigger CI / reply to non-code comments), and the exit-condition contract (green + no unresolved threads + mergeable, or max iterations) are pure `gh` CLI patterns with no org-specific tooling. The 10-iteration cap and `sleep 60` cadence are concrete, immediately adoptable defaults.
**Trigger**: A PR needs to cross the finish line — CI is failing, review comments are open, or the branch is behind trunk. Use when a PR needs active coordination rather than passive waiting.
**Steps/contract**: (1) ASSESS — `gh pr view` for mergeability, `gh api .../comments` for review threads, `gh pr checks` for CI, `gh pr view --comments` for general discussion. (2) ACT — (A) group actionable review comments by file/concern, spawn an Implementor per group, then reply to each comment with commit SHA; (B) re-request review or post a ping after code changes land; (C) `gh pr update-branch` or delegate manual rebase to Implementor if behind/conflicted; (D) `gh run rerun --failed` only for demonstrably transient failures, logging the reasoning; (E) reply to non-code discussion threads directly. (3) WAIT — `sleep 60`, then return to step 1. Repeat up to 10 iterations. Exit on SUCCESS (mergeable + green + no unresolved threads) or MAX ITERATIONS (report blockers). Hard rule: never yield for any other reason while work remains.
**Strip**: Specialist routing table (Implementor / Verifier with specific agent names) — replace with role-contract descriptions (a code-execution agent; a correctness-check agent). Interface labels ("Receives from: Review & QA Orchestrator", "Delegates to: Implementor, Verifier") — org-specific agent-graph wiring. Model/model_alt frontmatter — runtime config, not SOP.
**Structure/format**: Role preamble + Specialists table + Interfaces section + 6 Hard Rules + Main Loop pseudocode + 3-step workflow with `gh` bash blocks + Exit Conditions block. Well-structured; the Hard Rules double as a decision-ready checklist and are the highest-value portable element. The pseudocode loop is concise and clear; keep it verbatim.
**Notes**: The "NEVER merge the PR" hard rule is the single most important guardrail — it enforces the human-in-the-loop boundary and should be preserved verbatim in any target system. The "only re-trigger CI if you have strong reason to believe the failure is transient" rule, combined with the logging requirement, is a strong discipline against wasted CI minutes. The "don't over-fix" rule (scope limited to review comments and CI failures — no refactors) prevents a common agent failure mode. This completes a natural quad with `pr-review` (thread generation), `pr-resolve-discussions` (deep fix protocol), and `pr-merge` (final merge step) — cross-link all four. Strong candidate for promotion once specialist paths and interface labels are replaced with role descriptions.

## agents/customer-service/rep.md
**Type**: Agent role / workflow SOP (Tier 1 customer-service representative)
**Portable**: Yes
**Reason**: The acknowledge-classify-resolve-escalate loop, the six hard rules, and the two structured output formats (customer-facing response + Escalation Summary) are universal Tier 1 CS patterns with no dependency on any org's product, ticketing system, or toolchain. The escalation triggers cover the complete standard set of Tier 1 limits.
**Trigger**: Agent is playing a front-line customer-service role; handling inbound customer inquiries; or any workflow needing a triage + escalation gate before a specialist takes over.
**Steps/contract**: (1) Acknowledge and collect — greet, empathise, gather account ID, issue description, steps already tried, error messages. (2) Classify — FAQ / known playbook / account access / order status / basic troubleshooting / anything else. (3) Resolve and confirm — provide answer or walk through fix; ask "Does this resolve your issue?" (4) Escalate when: basic troubleshooting failed, policy exception or non-standard refund requested, billing dispute, angry/threatening customer or manager request, issue not in playbook, security/privacy/legal concern — hand off with structured Escalation Summary.
**Strip**: Model/model_alt frontmatter fields (runtime config, not SOP). Interface routing labels ("Receives from: CS Orchestrator", "Escalates to: CS Specialist") are org-specific agent-graph wiring; replace with the role descriptions if referencing at all.
**Structure/format**: Role preamble + 6 hard rules + 4-step workflow with inline classification table + two output format templates (customer-facing response structure + Escalation Summary fields) + guidelines section. Clean and well-scoped; templates are immediately usable without modification.
**Notes**: The Escalation Summary template is the highest-value portable artifact — it captures all the structured handoff fields a Tier 2 agent needs (account ID, issue, category, steps tried, sentiment, escalation reason, verbatim quote). The "one thing at a time" guideline and the 2-minute search limit before escalating are strong non-obvious guardrails that prevent both over-dumping and stalling. The "never blame the customer" framing rule ("this is a common one" vs. "you did it wrong") is a portable soft-skills directive worth preserving verbatim. Strong candidate for direct promotion; no modifications required beyond stripping frontmatter and interface routing labels.

## agents/customer-service/orchestrator.md
**Type**: Agent role / orchestration SOP (routing + escalation coordinator)
**Portable**: Yes — with minor stripping
**Reason**: The three-tier triage logic, escalation signal matrix, analyst-engagement triggers, and resolution-tracking protocol are universal customer service orchestration patterns. Nothing in the workflow is bound to a specific product, platform, or org; the tier/model cost-efficiency framing generalises to any tiered agent system where lower tiers are cheaper.
**Trigger**: An agent is acting as the routing/monitoring layer for a customer service pipeline — new ticket arrives, or a ticket needs escalation review.
**Steps/contract**: (1) Intake — extract customer tier, issue type, sentiment, and contact history from the ticket. (2) Apply triage rules — route to Tier 1 (default), Tier 2 (repeat contacts, billing disputes, security/privacy, already-angry), or Tier 3 (VIP/Enterprise, cancellation intent, legal/PR risk, Tier 2 exhausted). (3) Monitor active tickets for eight escalation signals (sentiment shift, out-of-playbook, policy exception needed, two-failed-attempts, manager request, retention risk, cross-dept coordination); escalate one tier on signal; require handoff summary before moving ticket. (4) Engage CS Analyst on pattern-level triggers only (50-ticket batch, 2x volume spike, 3+ same-root-cause, resolution-time drift, post-Tier-3 audit). (5) Track every ticket through Open / Pending Customer / Resolved / Escalated / Closed states; flag SLA breaches before processing new intakes.
**Strip**: Model-name references in the cost-efficiency guideline (haiku/sonnet/opus) — these are runtime config, not portable SOP content. Specific dollar-threshold placeholder for billing disputes — a target system should inject its own threshold.
**Structure/format**: Role preamble (hard rules) → 4-section numbered workflow (Intake & Triage, Monitor & Escalate, Analyst Engagement, Resolution Tracking) → 4 structured output format templates (Routing Decision, Escalation Directive, Analysis Request, Status Report) → Guidelines section. Exceptionally clean — the escalation signal matrix (table), analyst-engagement conditions (bulleted list), and output templates are the highest-value portable elements.
**Notes**: The six hard rules are individually strong and worth preserving verbatim — particularly "escalate on signals, not hunches" and "engage the Analyst on patterns, not individual tickets". The cost-efficiency guideline ("route as low as defensible") is a broadly applicable tiered-agent principle that should be kept with model names replaced by generic tier labels. The "handoff-quality determines escalation success" guideline encodes a frequently-missed operational dependency. The Analyst engagement section cleanly separates reactive single-ticket handling from strategic pattern detection — a valuable architectural boundary. Strong candidate for direct promotion with model names and billing-threshold placeholder stripped.

## agents/review-and-qa/orchestrator.md
**Type**: Agent role / orchestration SOP (routing layer for review & QA work)
**Portable**: Yes — with minor stripping
**Reason**: The four-scenario classification logic, security-first sequencing rule, auto-escalation heuristic, and structured output format are generic GitHub PR/code-review patterns with no org-specific identifiers. The only non-portable surface is the four specialist file paths, which are workspace-specific pointers rather than embedded logic.
**Trigger**: Any incoming review or QA request — "review this PR", "verify this meets requirements", "security review", "help land this PR" — or ambiguous requests that need routing before work can begin.
**Steps/contract**: (1) Classify the request into one of four scenarios: Standard PR Review / PR Shepherding / Post-Implementation Verification / Security Review; ask one clarifying question if ambiguous. (2) Detect security sensitivity independently of what the user asked — auto-add Security Reviewer if changes touch auth, tokens, encryption, user input, permissions, data storage, or external APIs. (3) Build routing plan: select specialists and order (Security Reviewer always runs first when included; PR Shepherd never combines with other specialists). (4) Delegate to each specialist with full context: PR/branch/commit ref, focus area, and upstream findings for downstream steps. (5) Summarize routing to the user — what was dispatched and in what order.
**Strip**: Specialist file paths (`review-and-qa/pr-reviewer.md`, `review-and-qa/pr-shepherd.md`, etc.) — replace with role descriptions or abstract interface labels. Model/model_alt frontmatter fields are runtime configuration, not SOP content.
**Structure/format**: Hard rules block (5 rules) → specialist table (4 rows with file and when-to-use columns) → 5-step workflow → structured output format template (Routing Assessment + Delegation Plan + Summary) → guidelines section. Well-organized; the output template and security-first sequencing rule are the most portable artifacts. No restructuring needed.
**Notes**: The hard rule "sequence security before quality" — with the explicit rationale that security findings may change the scope of subsequent reviews — is a strong portable principle worth preserving verbatim. The auto-detect security sensitivity logic (step 2 fires regardless of the user's explicit ask) is a valuable behavioral default that prevents missed escalations and should be kept as a first-class step, not a footnote. The PR Shepherd distinction ("ongoing coordination role, not a one-shot review — do not combine with other specialists in a sequence") encodes a subtle but important workflow boundary. The structural-rot guideline (flag type lies, swallowed errors, `any` casts, dead code for AI-context hygiene) echoes the Garbage Elimination Principles from `pr-resolve-discussions` — these two skills share a philosophical foundation and should be cross-linked on promotion. Strong candidate for promotion with specialist file paths replaced by role labels.

## agents/customer-service/specialist.md
**Type**: Agent role / tiered workflow SOP (Tier 2 customer service specialist)
**Portable**: Yes — with minor stripping
**Reason**: The investigation workflow (review escalation → classify issue → investigate by category → form hypothesis → resolve or escalate → communicate → document) is a universal tiered support pattern applicable to any software product or service. The Hard Rules (evidence before action, one-case-one-resolution, always produce case notes, escalate on red flags, separate internal/external notes) are domain-agnostic best practices. The two output templates (Case Resolution and Case Handoff) are clean, reusable structured formats with no org-specific identifiers.
**Trigger**: A support case has been escalated from Tier 1 and requires deeper investigation — technical, account, or process issues where root-cause analysis, account history review, or multi-step troubleshooting is needed.
**Steps/contract**: (1) Review the escalation — read Tier 1 notes and customer history; never re-ask already-collected information. (2) Classify the issue: Technical / Account / Process. (3) Investigate by category with category-specific evidence-gathering (logs + repro for Technical; billing history + audit logs for Account; step-by-step workflow trace for Process). (4) Form a ranked hypothesis; test the most likely first; record results. (5) Resolve (apply fix, standard credit, workaround) or escalate (Case Handoff to Tier 3) based on authority limits and red-flag signals. (6) Communicate to the customer — clear, empathetic, no jargon, no internal references. (7) Close the case with a full Case Resolution or Case Handoff document; no case leaves without written documentation.
**Strip**: Interface routing labels ("Receives from: CS Rep", "Escalates to: CS Escalation Manager", "Flags to: CS Analyst") — these are org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields — runtime config, not SOP content. Tier-branded agent names ("CS Orchestrator", "CS Analyst") — replace with generic role descriptions ("orchestrator", "pattern-tracking channel") if the interface section is retained at all.
**Structure/format**: Frontmatter → interfaces block → 6 hard rules → 7-step ordered workflow with per-category investigation substeps → dual-template output section (Case Resolution / Case Handoff, each as a markdown fenced block with labelled subsections) → 8-bullet guidelines section. Excellent structure; the two output templates are the most portable artifacts and should be preserved verbatim. Hard rules double as an enforcement checklist.
**Notes**: Strong candidate. The "evidence before action" and "one case, one resolution" rules guard against the two most common support anti-patterns (remedy before diagnosis; scope creep into unrelated account issues). The dual-template design cleanly separates resolved cases from escalations — a good structural principle reusable in any support or incident-response context. The Guidelines section contains high-value operational wisdom worth preserving verbatim: "empathy without progress makes things worse"; "proportional remedies — over-compensating trains customers to escalate for perks"; "never 'we'll get back to you' — say who, by when, via what channel." The red-flag escalation checklist (legal threats, compliance/regulatory data, VIP flag, churn signals — explicit cancellation language, long tenure, multiple recent escalations) is a portable trigger list valuable in any support or account-management context. Closest structural analog in this repo is `agents/engineering/debugger.md` — both impose a hard scope boundary ("diagnose only, never fix" vs. "one case, one resolution") to prevent runaway action. High-confidence promotion candidate; modifications required are stripping interface routing labels and renaming org-specific agent names only.

## agents/customer-service/analyst.md

**Type**: Agent role / analytical workflow SOP (support pattern analyst)
**Portable**: Yes
**Reason**: The quantification discipline, correlation-vs-causation labelling, time-context requirement, and evidence-chain enforcement are universal analytical practices applicable to any support or data-analysis context, with zero dependency on a specific org, product, or toolchain. The Support Insights Report template and the "epidemiologist" mental model framing are immediately reusable in any CS or operations function.
**Trigger**: Periodic support pattern review; ticket volume anomaly or spike alert; request to identify systemic issues, documentation gaps, or process improvement opportunities across a batch of support tickets or conversations.
**Steps/contract**: (1) Intake — receive ticket batch or analysis question; clarify time window and scope if missing. (2) Categorise — tag each ticket by issue type, severity, product area, and customer segment; build a frequency table. (3) Pattern detection — rank issues by volume; flag sharp increases vs prior period; identify clusters by segment, area, or timeframe; escalate reproducible error patterns as candidate engineering bugs. (4) Root cause analysis — for the top 3–5 issues, determine whether each is a product bug, UX friction, missing/outdated doc, or process gap. (5) Synthesise — produce a Support Insights Report using the embedded template (Top Issues by Volume table / Emerging Trends / Engineering Escalations / Documentation Gaps / Process Improvement Recommendations / Appendix). (6) Recommend — attach a concrete next step to every finding; demote findings without an actionable owner to the Appendix.
**Strip**: Interface routing block ("Receives from: CS Orchestrator … Hands off to: Engineering Orchestrator / CPO / Technical Writer") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields — runtime config, not SOP content.
**Structure/format**: Role preamble + 5 hard rules + 6-step ordered workflow + fully-specified markdown report template + guidelines section. The hard rules are the most portable element and map cleanly to a checklist. The report template is the primary durable artifact and should be preserved verbatim. Guidelines section adds epistemic framing ("think like an epidemiologist") that strengthens rule compliance beyond mere procedure.
**Notes**: One of the stronger agent candidates in this repo. The five hard rules encode non-obvious analytical discipline that LLMs routinely skip (exact counts not vague "many"; confirmed/probable/suspected root-cause labelling; baseline comparison on every trend claim; explicit acknowledgement of insufficient data rather than fabricated precision). The "deflection potential" framing in the guidelines is high-value: it reframes documentation gaps as a measurable cost-reduction lever, not a secondary concern. The engineering escalation specificity rule ("'Login is broken' is useless; 'OAuth token refresh fails silently after password change — 23 tickets, reproducible on Chrome 120+, error code AUTH_REFRESH_EXPIRED' is actionable") is a concrete, preservable example of the precision standard. Strong promotion candidate; only the interface routing block requires stripping.

## agents/customer-service/escalation-manager.md
**Type**: Agent role / workflow SOP (Tier 3 escalation manager)
**Portable**: Yes — with minor stripping
**Reason**: The seven hard rules (never over-promise, quantify LTV-vs-cost, own the outcome, de-escalate first, document everything, reject routine work, flag systemic patterns) and the six-step workflow (triage → assess customer → de-escalate → resolution path → deliver → close/document) are universal escalation management principles applicable in any customer-facing org regardless of support tooling, product type, or team structure. The three output templates (Case Summary, Systemic Issue Report, Cross-Department Action Request) are immediately reusable artifacts.
**Trigger**: A customer issue cannot be resolved at lower support tiers; involves retention risk, policy exception, VIP/high-LTV account, compliance gray area, or multi-department coordination.
**Steps/contract**: (1) Triage — confirm the case genuinely requires Tier 3; return to lower tier with guidance if not. (2) Assess customer — account value (LTV, contract size, growth trajectory), churn risk level, prior escalation history. (3) De-escalate — acknowledge the specific failure by name; do not defend prior handling; make a concrete good-faith gesture before presenting the full solution. (4) Choose resolution path: standard remedy with exception / bespoke solution / cross-department coordination / engineering escalation / upward escalation. (5) Deliver — state what, why, timeline with checkpoints, direct contact for follow-up, and a proactive commitment for what happens if the timeline slips. (6) Close — write the Case Summary; if 3+ escalations share a root cause, produce a Systemic Issue Report.
**Strip**: The `## Interfaces` block (`Receives from` / `Escalates to` / `Flags to` routing labels) — these are org-specific agent-graph wiring with no portable value. The `model` / `model_alt` frontmatter fields — runtime config, not SOP content. Named role references (CS Orchestrator, Engineering Orchestrator, CS Analyst, CPO) — replace with role-type descriptions (support orchestrator, engineering lead, analytics team, product leadership) when promoting.
**Structure/format**: Frontmatter → role preamble → 7 Hard Rules → 6-step Workflow → Output Format section with three fully-templated output blocks (markdown with table and code fences) → Guidelines section. Exceptionally well-structured; hard rules double as a pre-action checklist; guidelines section adds epistemic depth beyond procedure. No restructuring needed.
**Notes**: Three elements are particularly high-value for preservation verbatim: (1) "De-escalate before resolving — an angry customer cannot evaluate a solution" as a hard rule with the no-skip directive; (2) the LTV-vs-cost math framing in Guidelines ("a $500 refund to retain a $50,000/year account is not generosity — it is basic math"); (3) the empathy-as-technique distinction ("'I understand your frustration' is empty; name the specific failure before offering the fix"). The Systemic Issue Report trigger (3+ escalations with a shared root cause → produce a report) is a strong operational discipline that prevents patch-only thinking. The precedent-awareness note (every exception may be cited by the next customer; decide whether it should become policy) is an unusually sophisticated portable governance insight. High-confidence promotion candidate; only the Interfaces block and named role references need substitution.

## agents/executive-board/ceo.md
**Type**: Agent role / workflow SOP (strategic decision synthesizer)
**Portable**: Partial — the decision framework, hard rules, and Strategic Brief format are fully portable; the specific executive roster and agent-graph wiring are org-specific.
**Reason**: The 6-step workflow (frame → gather → assess landscape → decide → write brief → assign next actions), the three-state disposition (GREENLIGHT / REJECT / NEED MORE DATA), and the hard rules (evidence over instinct, explicit trade-offs, time-bound decisions, escalate unknowns) are universally applicable to any multi-stakeholder strategic decision process. The specific 12-agent input roster (CTO, CFO, CPO, VP Engineering, VP Design, Head of QA, Contrarian, Moonshot, Compounder, Revenue, Customer Oracle, Market Strategist) and the "Hands off to: Executive Board Orchestrator" handoff assume a particular agent-graph topology that will not transfer as-is.
**Trigger**: A high-stakes strategic decision requires synthesizing multiple expert perspectives (technical, financial, product) into a single authoritative recommendation with explicit trade-offs and a time-bound mandate.
**Steps/contract**: (1) Frame — restate the decision as one precise, concrete question; sharpen vague prompts. (2) Gather — audit all available inputs; if a critical input is missing, list what is needed and who must provide it, then block. (3) Assess landscape — market timing (cost of waiting), competitive position (moat impact), opportunity cost (what we give up), risk profile (blast radius, reversibility). (4) Decide — choose GREENLIGHT / REJECT / NEED MORE DATA with explicit conditions and success metrics. (5) Write Strategic Brief using the defined template (Decision → Strategic Question → Context & Evidence → Trade-offs table → Risk Assessment → Timing → Success Metrics → Next Actions). (6) Assign each next action to a named role with deliverable and deadline.
**Strip**: (a) `Interfaces` section (Receives from / Hands off to) — org-specific agent-graph wiring naming 12 specific agents; replace with generic "synthesizes inputs from domain experts" and "delivers Strategic Brief to orchestrator". (b) `model` / `model_alt` frontmatter — runtime config, not SOP content. (c) `Next Actions` table in the Output Format shows CTO/CPO/CFO rows as concrete placeholders — replace with generic `[Role]` rows so the template adapts to any org's executive structure.
**Structure/format**: Role preamble (2 sentences) + Interfaces block + 6 Hard Rules + 6-step Workflow + Output Format (fenced markdown Strategic Brief template) + Guidelines section. Clean hierarchy; the Output Format template is the highest-value portable artifact. Hard Rules double as a decision-quality checklist. Guidelines section encodes decision theory (bets vs. certainties, reversibility preference, challenge constraints) that is unusually strong for an agent prompt.
**Notes**: The three-state GREENLIGHT / REJECT / NEED MORE DATA disposition is a standout portable pattern — it prevents the common LLM failure mode of producing narrative without a clear call. The Hard Rules "explicit trade-offs" (every yes names the no) and "time-bound decisions" (no open-ended mandates) are strong guardrails worth preserving verbatim. The "think in bets, not certainties" and "silence is a decision" guidelines encode sound decision theory that elevates this beyond a generic executive prompt. The reversibility heuristic ("prefer reversible decisions when options are close; reserve irreversible commitments for high-conviction bets") is a particularly portable and underspecified-elsewhere principle. Primary promotion work is stripping the Interfaces block and genericising the Next Actions table rows.

## agents/executive-board/orchestrator.md

**Type**: Agent role / orchestration SOP (multi-phase strategic decision workflow with parallel specialist delegation)
**Portable**: Partial — the six-phase protocol, six hard rules, adversarial trio pattern, and both output templates are fully portable; the 13-role specialist roster is a domain-specific configuration example, not the transferable mechanism
**Reason**: The classify → frame → parallel evaluate → adversarial stress-test → conflict synthesis → decision-maker close loop is a universal multi-stakeholder decision protocol. It applies to any situation where a decision requires coordinated input from multiple domain specialists before a final arbiter makes the call. The Contrarian + Moonshot + Compounder adversarial trio is a particularly distinctive and portable pattern: it systematically generates the dissent case, the upside case, and the compounding-effects case as three independent, parallel perspectives — a structured alternative to ad-hoc devil's-advocate prompting. The six hard rules encode universal facilitation ethics (never decide yourself, never fabricate missing input, surface all conflicts with equal weight, maintain an audit trail).
**Trigger**: A strategic proposal, major feature decision, significant technical initiative, or financial commitment requires evaluation across multiple perspectives before a final decision-maker makes the call; the scope is too broad or cross-functional for a single specialist.
**Steps/contract**: (0) **Classify** the proposal type (full strategic / product-feature / technical / financial / design) and announce the selected evaluation flow to the user — adjust if they disagree, then proceed. (1) **Frame** — delegate to the appropriate framing specialist (CPO for product/strategic, CTO for technical, CFO for financial, VP Design for design); if framing surfaces unresolved blockers, surface them to the user and hold before continuing. (2) **Parallel evaluate** — dispatch domain specialists in parallel groups (strategy + feasibility; planning + design; market + customer + monetization); non-standard proposals compose groups from relevant roles only. (3) **Stress test** — delegate to a quality/risk reviewer with the full context gathered so far; then dispatch the adversarial trio in parallel: Contrarian (strongest case against), Moonshot (10× upside alternative), Compounder (compounding-advantage analysis). (4) **Conflict synthesis** — identify disagreements across all inputs; produce a Conflict Brief naming each tension with both positions and the trade-off the decision-maker is resolving. (5) **Decision** — compile the full package (framing brief + all evaluations + stress-test results + Conflict Brief) and delegate to the final decision-maker; if verdict is NEED MORE DATA, re-engage the relevant specialists with the specific questions raised — maximum two re-engagement loops before escalating unresolved items directly to the user. (6) **Close** — produce the Board Meeting Record and present the decision and next actions to the user.
**Strip**: (a) The 13 specific role names (CEO, CTO, CFO, CPO, VP Eng, VP Design, Head of QA, Contrarian, Moonshot, Compounder, Revenue, Customer Oracle, Market Strategist) — these are an illustrative roster configuration; any target system substitutes its own specialist roles. (b) The example tension list in Phase 4 (CTO vs CFO, CPO vs VP Eng, etc.) — assumes the same 13-role lineup; replace with a generic instruction to name the two positions and their trade-off. (c) The Board Meeting Record table rows listing all 12 roles by name — replace with a generic "one row per consulted specialist" instruction. (d) Model/model_alt frontmatter fields — runtime infrastructure config.
**Structure/format**: Role preamble + 6 hard rules + 6-phase workflow (14 numbered steps) + two fully-templated output formats (Conflict Brief + Board Meeting Record) + guidelines section. Exceptionally well-structured. The two output templates are the primary portable artifacts — both are clean, complete, and immediately reusable with only role names genericised.
**Notes**: The "never decide" hard rule is the defining discipline of this SOP and must be preserved verbatim — it is the entire point of the orchestrator role, and its companion rationale ("if you catch yourself forming an opinion, reframe it as a question for the right executive") is unusually specific and valuable. The "never fabricate executive input" rule is equally critical: missing input is flagged as missing, not invented — this is a common LLM failure mode that this rule directly prevents. The adversarial trio (Contrarian + Moonshot + Compounder) is the standout portable pattern and should be retained as a named phase rather than folded into generic parallel evaluation — the three perspectives are meaningfully distinct and the parallelism is intentional. The Conflict Brief format (named tension + Position A + Position B + Stakes as a trade-off) is a reusable decision-framing template applicable far beyond board meetings. The "max two re-engagement loops then escalate" rule in Phase 5 is a clean runaway-guard. The guidelines — "compress, don't parrot" (summarise inter-agent handoffs, never re-read full prior output), "silence is signal" (note when a specialist has nothing to add — this is information), "name what's missing" (ask for minimum viable context before convening any process) — are high-value facilitation principles worth preserving verbatim. The proposal-type classification routing table (four named flows) is portable as a decision template even when role names change. Strong candidate for promotion as a coordinator-role template and adversarial-review pattern; primary modification is genericising the roster from named roles to a configurable specialist-set interface.

## agents/executive-board/cfo.md
**Type**: Agent role / Financial Analysis SOP
**Portable**: Yes — with minor stripping
**Reason**: The six hard rules (financial lens only, no false precision, surface hidden costs, "can we" vs "should we", never a blanket no, show your math) and the six-step evaluation workflow are universally applicable to any financial review context. The structured Financial Assessment output template — cost table with confidence tiers, hidden-cost enumeration, ROI analysis, budget-impact section, and a four-stance funding recommendation — has no dependency on any specific org, toolchain, or product domain.
**Trigger**: A proposal, initiative, or decision needs financial analysis, cost estimation, ROI projection, burn-rate impact assessment, or a budget recommendation.
**Steps/contract**: (1) Extract all cost components from the proposal (headcount, infrastructure, tools, opportunity cost, timeline). (2) Check affordability against current burn rate and runway; identify any required reallocation. (3) Model expected return: revenue impact, cost savings, or strategic value; compute payback period. (4) Stress-test assumptions at 2× timeline, 50% adoption, and backfill-hire scenarios. (5) Compare alternatives: cheaper path to 80% of value; cost of inaction. (6) Produce a Financial Assessment with cost table, hidden costs, ROI analysis, budget impact, and a FUND / FUND WITH CONDITIONS / DEFER / DECLINE recommendation with explicit reasoning.
**Strip**: Interface routing labels ("Receives from: CTO / VP Engineering / CEO", "Hands off to: CEO") — org-specific agent-graph wiring that carries no portable SOP value. Model/model_alt frontmatter fields are runtime config, not SOP content.
**Structure/format**: Role preamble + 6 hard rules + 6-step workflow + fully-templated Financial Assessment markdown block (cost table, hidden costs, ROI analysis, budget impact, funding stance) + guidelines section. The output template is the highest-value portable artifact and should be preserved verbatim. Guidelines ("think in unit economics", "time-value matters", "headcount is the biggest cost") are concise, actionable heuristics worth retaining.
**Notes**: Strong candidate. The "no false precision" rule — mandating cost tiers over fabricated point estimates and explicitly permitting "I don't have enough data" — is an important LLM-specific guardrail that combats hallucinated specificity. "Never a blanket no" (offer a phased/scoped/timed alternative) is a useful anti-obstruction principle for any advisory role. The "headcount is the biggest cost" guideline with its concrete example ($150K–$250K for two senior engineers over three months) is a portable calibration anchor worth preserving. The funding-stance vocabulary (FUND / FUND WITH CONDITIONS / DEFER / DECLINE) is a clean, reusable decision taxonomy.

## agents/executive-board/contrarian.md
**Type**: Agent role / adversarial reasoning SOP
**Portable**: Yes
**Reason**: The steel-man-then-attack protocol, named reasoning patterns (inversion, pre-mortem, incentive analysis, base rates, second-order effects), "silence is a signal" discipline, and the Dissent Report output format are universal decision-quality practices independent of any org, domain, or tech stack.
**Trigger**: A group or agent is converging on a decision and needs adversarial pressure-testing; user asks to stress-test a proposal, play devil's advocate, or run a pre-mortem.
**Steps/contract**: (1) Identify the majority/consensus position. (2) Steel-man it — restate in its strongest form. (3) Apply inversion and pre-mortem: ask what makes this fail 12 months from now. (4) Run incentive analysis and base-rate check on the proposal. (5) Follow second-order chains at least two steps. (6) Produce a Dissent Report: position challenged → strongest counter-case (2–3 paragraphs) → three concrete failure scenarios (trigger + mechanism + blast radius) → unstated assumptions → what would change my mind → residual risk if majority proceeds. (7) If no strong counter-case exists, say so explicitly — "I tried to break this and couldn't."
**Strip**: Interface routing block ("Receives from: CEO", "Hands off to: CEO") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields are runtime config, not SOP content. "Track your record" guideline references agent-persistent memory ("note it in your expertise") — replace with a session-scoped note or omit.
**Structure/format**: Role preamble + 6 hard rules + 5 named reasoning patterns with triggers + reasoning-pattern quick-reference table (5 "when X → do Y" prompts) + Dissent Report template + 5-item guidelines section. Well-structured; the Dissent Report template and the reasoning-pattern triggers are the highest-value portable elements.
**Notes**: The "silence is a signal" discipline (if you genuinely cannot break the proposal, say so explicitly) is an unusually valuable meta-rule — it prevents performative dissent and raises the credibility of real objections. Worth preserving verbatim. The "quality over quantity" guardrail (three devastating objections beat ten weak ones) encodes a common LLM failure mode fix. The Dissent Report's "What Would Change My Mind" section is distinctive: it forces the contrarian to commit to falsifiable conditions rather than holding an unfalsifiable position. Strong portable candidate; the entire SOP content can be lifted verbatim once the agent-graph routing and runtime-config fields are stripped.

## agents/executive-board/compounder.md
**Type**: Agent role / strategic-analysis SOP
**Portable**: Yes — with stripping
**Reason**: The core analytical protocol — require a named flywheel loop, state a time horizon, enumerate decay risks, score compounding potential — is a universal strategic-evaluation discipline applicable in any product, business, or engineering decision context. The structured Compounding Analysis output format carries no org-specific assumptions.
**Trigger**: A decision needs evaluation for long-term durable value vs. one-time gain; board or team is assessing whether a move builds a moat, flywheel, or knowledge accumulation.
**Steps/contract**: (1) Ask "does this compound?" — classify the decision as creating an appreciating asset or a consumable. (2) Name the flywheel with a specific A→B→C→A loop; reject any thesis that can't produce this diagram. (3) State when compounding begins vs. accelerates vs. breaks even with the quick-win alternative. (4) Enumerate at least one concrete decay risk (market shift, tech change, competitor move, team attrition). (5) Produce a Compounding Analysis with Thesis, Flywheel Mechanics, Time Horizon, What Breaks the Flywheel, and Compounding Score (1–10 with rationale).
**Strip**: Interface/routing metadata ("Receives from: CEO", "Hands off to: CEO") — org-specific agent-graph wiring. Model/model_alt frontmatter fields — runtime config, not SOP content. The "Natural tension with Revenue is healthy" rule is framing for a specific board persona; strip the board context but preserve the underlying principle (state the trade-off explicitly, don't paper over it).
**Structure/format**: Role preamble + 6 hard rules + 5 reasoning patterns + fully-templated output format. The output template (Compounding Thesis → Flywheel Mechanics step-list → Time Horizon table → What Breaks the Flywheel → Compounding Score) is the highest-value portable artifact and should be preserved verbatim. Reasoning-pattern bullets are also portable as decision heuristics.
**Notes**: The "name the flywheel or it isn't one" hard rule is unusually strong and combats a common failure mode (calling any optimistic narrative a flywheel). The Time Horizon section's three-date structure (begins / accelerates / break-even vs. quick-win) is a best-practice framing that forces honest comparison rather than vague future-tense optimism. The Compounding Score 1–10 anchoring (1=pure one-shot, 10=self-reinforcing with deep moat) gives the output a crisp summary signal. The "patience has a price" guideline (compounding is a luxury if runway is short) prevents dogmatic application and is worth preserving verbatim. Strong candidate for promotion as a portable strategic-evaluation SOP; only modifications needed are stripping board/agent-graph framing.

## agents/executive-board/cto.md
**Type**: Agent role / workflow SOP (technical feasibility assessment specialist)
**Portable**: Yes — with minor stripping
**Reason**: The hard rules, effort-tier T-shirt sizing table, tiered risk-flag system (🔴/🟠/🟡), build-vs-buy framework, and fully-templated Technical Assessment output format are entirely org-agnostic. The role boundary discipline ("stay in your lane; push market/priority questions to CEO/CPO") and the "distinguish hard from expensive" directive are universally applicable to any technical advisory role. The interface routing section is the only org-specific surface.
**Trigger**: A proposal, feature brief, or strategic question needs technical feasibility evaluation before stakeholders commit to building; architecture implications, effort sizing, and risk assessment are required.
**Steps/contract**: (1) Receive proposal from requesting party. (2) Assess current state — stack, infrastructure, patterns, relevant tech debt. (3) Evaluate feasibility: can it be built with what exists? If not, what's missing? Is the blocker hard (fundamental constraint) or expensive (time/money/hiring)? (4) Identify architecture implications — fit with current architecture, integration points, dependencies; flag one-way doors with ⚠️. (5) Estimate effort tier (S/M/L/XL/XXL with calendar ranges; no false-precision hour estimates). (6) Surface risks and trade-offs with severity tiers (🔴 High / 🟠 Medium / 🟡 Low). (7) Evaluate build-vs-buy where applicable. (8) Produce a Technical Assessment using the output template; recommend a concrete path (or ranked options with trade-offs) and flag if the proposed timeline is unrealistic.
**Strip**: The `## Interfaces` section ("Receives from / Hands off to" named roles) — org-specific agent-graph wiring with no portable value. The `model` / `model_alt` frontmatter fields — runtime config, not SOP content.
**Structure/format**: Role preamble + 6 hard rules + 8-step workflow + fully-templated output format (seven named sections with headers, tone guidance, and conditional sections) + guidelines block. Exceptionally well-structured; the Technical Assessment template is the primary durable artifact and should be preserved verbatim. Hard rules double as a pre-assessment checklist.
**Notes**: "Distinguish hard from expensive" (Hard Rule 3) is an unusually sharp epistemic directive that combats a pervasive failure mode of conflating "infeasible" with "costly" — worth preserving verbatim. The effort-tier table (S = days through XXL = 6+ months with calendar ranges and justification requirement) is an immediately adoptable sizing framework that is more honest than point estimates; portable as-is. The one-way-door flag (⚠️ on irreversible architectural decisions) is a strong, lightweight guardrail. "Think in systems, not features" and "Communicate in the board's language" (translate coupling/debt into business-impact sentences) are high-value reasoning prompts with no org dependency. The "protect the existing stack / every new technology must justify itself" default is a strong portable bias rule. High-confidence promotion candidate; only modification needed is removing the Interfaces routing section.

## agents/executive-board/market-strategist.md
**Type**: Agent role definition (executive board persona — Market Strategist)
**Portable**: Partially
**Reason**: The hard rules and reasoning patterns encode genuinely transferable competitive-analysis discipline (name names, market clocks, TAM ≠ strategy, positioning as choice, intelligence shelf life). The structured output format (Market Intelligence Brief with competitor table, timing, positioning, TAM/SAM/SOM, risks, go/no-go) is reusable in any strategic-planning context. The board-interface wiring (receives from CEO, hands off to CEO) is org-topology-specific and should be stripped.
**Trigger**: Agent or session needs to produce a competitive landscape analysis, market timing assessment, or positioning recommendation.
**Steps/contract**: (1) Gather live data via web search/fetch — competitor sites, Crunchbase, Product Hunt, HN, analyst reports, job postings. (2) Apply Porter's Five Forces (AI-updated), positioning maps, market-timing triangulation, and category dynamics. (3) Follow reasoning patterns for common board prompts (market entry, timing assumption, unclear differentiation, "no competitors" claim, big bet). (4) Produce a Market Intelligence Brief: competitive landscape table → market timing → positioning current/recommended/gap → TAM/SAM/SOM with confidence → market risks → strategic window verdict (MOVE NOW / MOVE SOON / WAIT / PASS).
**Strip**: Board interface section (Receives from / Hands off to CEO), model/model_alt frontmatter fields, "executive board" framing, references to "deliberation cycle."
**Structure/format**: Well-structured with named sections, a reasoning-patterns block, a competitor table template, and a verbose but precise output format. Could be condensed by merging the Guidelines into the Hard Rules, but the current verbosity is load-bearing — each rule has a "why" that prevents agent shortcuts.
**Notes**: The six Hard Rules are the highest-value extract — they encode the most common failure modes of competitive analysis (vagueness, fantasy TAM, stale data, empty positioning). The "Think like the competitor" heuristic and the three-signal market-timing triangulation are also strong portable primitives. The output format is detailed but self-consistent; preserving it intact is preferable to summarising.

## agents/executive-board/head-of-qa.md
**Type**: Agent role / workflow SOP (quality risk evaluation specialist)
**Portable**: Yes
**Reason**: The risk-first methodology, blast-radius scoping, risk-register format, and three-verdict release readiness system (READY / CONDITIONALLY READY / NOT READY) are universal QA practices with no dependency on any specific tech stack, org structure, or tooling. The six Hard Rules encode non-obvious QA discipline (no rubber stamps, evidence-based only, proportional response) that applies to any engineering organisation.
**Trigger**: A proposal, spec, PR, or release candidate needs a quality risk evaluation; someone asks "what could go wrong?", wants release readiness criteria, or needs a structured testing strategy review.
**Steps/contract**: (1) Receive input — proposal, spec, PR, or release candidate. (2) Map blast radius — systems, data, users affected; worst-case impact. (3) Identify risk surfaces across seven dimensions: data integrity, state management, backward compatibility, concurrency, edge cases, rollback safety, monitoring. (4) Assess each risk: Likelihood × Impact = Priority. (5) Recommend testing strategy per risk — name the specific test type and scenario, not just "add tests". (6) Define quality gates with verifiable criteria. (7) Produce release readiness verdict (READY / CONDITIONALLY READY / NOT READY) with confidence level and explicit conditions or blockers. (8) Escalate if blast radius exceeds authority (data loss at scale, compliance implications).
**Strip**: Interface routing labels ("Receives from: CTO", "Hands off to: CEO") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter fields are runtime config, not SOP content. The `tags: stress-test` frontmatter is a platform-specific classification tag; strip or remap.
**Structure/format**: Role preamble + Interfaces block + 6 Hard Rules + 8-step ordered workflow + fully-templated Quality Risk Assessment output format (Summary → Blast Radius → Risk Register table → Testing Strategy → Quality Gates checklist → Release Readiness → Monitoring & Rollback → Escalation) + Guidelines section. Exceptionally well-structured; the Risk Register table and output template are the highest-value portable artifacts and should be preserved verbatim.
**Notes**: One of the strongest candidates in this repo. The "no rubber stamps" rule (time pressure is not a reason to skip gates) and "concrete over vague" rule (with a worked payment-idempotency example) are non-obvious discipline worth preserving verbatim. The Likelihood × Impact = Priority triage grid embedded in step 4 is a compact, universally applicable risk-scoring protocol. The Guidelines section adds genuine epistemic value — especially the "insurance underwriter" framing (price the risk, don't block all risk) and the distinction between "we tested this and it works" vs "nobody tested this and it hasn't broken yet". The Monitoring & Rollback section (canary signals, rollback plan, rollback window) is a dimension often missing from generic QA guides and should be retained as a first-class output section. Natural counterpart to `agents/engineering/debugger.md` (post-incident root cause) — this skill is the pre-ship risk gate. High-confidence promotion candidate; only the interface routing block needs to be stripped.

## agents/executive-board/cpo.md
**Type**: Agent role / output-format SOP (product evaluation specialist)
**Portable**: Yes
**Reason**: The six Hard Rules (user problems first, outcomes over outputs, no architecture decisions, no financial projections, say no by default, evidence over intuition) and the Product Brief output format are domain-agnostic product-thinking contracts. They encode universal product management discipline with no dependency on any org, stack, or tool. The 7-step evaluation workflow is equally stack-agnostic.
**Trigger**: A proposal, feature request, or initiative needs product strategy evaluation — assess whether it solves a real user problem, for a clear segment, at the right scope.
**Steps/contract**: (1) Read the proposal and extract the core user pain point — if absent, block immediately. (2) Identify the target user segment (push back on "everyone"). (3) Evaluate market context — current workarounds, competitors, timing triggers. (4) Judge product-market fit signal — existing segment or new one? (5) Scope the recommendation: BUILD with MVP/cut list, VALIDATE FIRST with research steps, or KILL with rationale. (6) Produce a Product Brief with: Problem Statement, Target Users, Why Now, Competitive Landscape, Recommendation, Scope, Success Metrics, Prioritization Rationale, Open Questions.
**Strip**: Interface routing labels ("Receives from: Market/user signals, CEO", "Hands off to: CTO, VP Design…") — these are org-graph wiring specific to this executive-board multi-agent setup. Model/model_alt frontmatter fields are runtime config, not SOP content.
**Structure/format**: Role preamble → 6 Hard Rules → 7-step ordered workflow → Product Brief output template (8 named sections in fenced code block) → Guidelines prose section. Well-structured; the output template is the highest-value portable artifact and should be preserved verbatim. Guidelines section adds non-obvious nuance (stated vs observed preferences, force-rank competing proposals) worth keeping.
**Notes**: The "say no by default" rule and the BUILD / VALIDATE FIRST / KILL trichotomy are the most distinctive portable elements — they encode an explicit anti-scope-creep stance rarely seen in generic product-thinking prompts. The Product Brief template is immediately reusable as a standalone deliverable format for any feature-planning skill. The guideline distinguishing "users say they want X" from "users behave in ways indicating they need X" is a genuinely high-value epistemic principle worth preserving verbatim. Pairs naturally with `prd-create` (which drafts a PRD after product approval) and `prd-to-plan` (which executes the approved scope). Strong candidate for promotion as a `product-evaluation` SOP or to inform a `cpo` agent role in a portable agents library.

## agents/executive-board/customer-oracle.md
**Type**: Agent role / methodology SOP (customer intelligence specialist)
**Portable**: Partially — the six hard rules, five thinking patterns, and Customer Intelligence Report template are highly portable; the executive-board persona framing is org-specific context that wraps but does not contaminate the underlying methodology.
**Reason**: The intelligence-gathering workflow, churn analysis discipline, segmentation mandate, and report format are universal product-research practices applicable in any product-focused context regardless of company size, tech stack, or org structure. None of the core methodology depends on the simulated-board conceit.
**Trigger**: A product team is making a strategic decision (feature prioritisation, pricing change, experience redesign) and needs structured customer signal injected into the discussion before deciding.
**Steps/contract**: (1) Gather real customer data via web search and available data connectors — support ticket patterns, NPS verbatims, review excerpts (G2, App Store, Reddit, HN), public forum posts, churn cohort data; if data is unavailable, explicitly flag the absence as a risk. (2) Apply five analytical lenses: ticket-pattern recognition (clusters beat anecdotes), churn autopsy (segment-and-cause, not "churn is up"), journey friction mapping (full funnel from discovery to power user or churn), sentiment triangulation (cross-reference ≥2 signal sources before claiming insight), silent majority (absence of feedback is itself a signal). (3) Separate stated preferences from observed behavior — weight behavior more heavily. (4) Segment all findings — power vs new, enterprise vs self-serve, etc.; never present a single flattened "customers want X". (5) Produce a structured Customer Intelligence Report with five sections: What Customers Are Saying, Churn Risk Assessment (signals / risk level / vulnerable segment), Unmet Needs (evidence-backed), Customer Impact of This Decision (2–3 paragraphs by segment), and The Voice (a synthesized representative quote with segment label).
**Strip**: The executive-board interfaces block ("Receives from: CEO", "Hands off to: CEO") — org-specific agent-graph wiring with no portable value. The "You ARE the customer in this room" first-person persona framing — it is a simulation device for a board roleplay, not a transferable SOP instruction; replace with "Adopt a customer-advocate stance" or similar. Model/model_alt frontmatter fields are runtime config. The reasoning-patterns section is framed as board-room interruptions ("When Revenue pushes…") — the underlying heuristics are valuable but the framing should be rewritten as decision triggers rather than board-room stage directions.
**Structure/format**: Role preamble + 6 hard rules + 5 thinking patterns + 5 reasoning-pattern trigger→response pairs + Customer Intelligence Report template (fenced code block with five labelled sections) + guidelines section. Well-structured; the hard rules function as a compact checklist. The report template is the primary durable artifact and is immediately adoptable as-is.
**Notes**: The six hard rules are the highest-value portable elements — especially "real signals, not personas" (never argue from invented user archetypes; if no data exists, say so and treat the absence as a risk) and "segment, always segment" (a finding attributed to all customers is almost always wrong). The "churn is the emergency alarm" rule encodes a strong triage heuristic worth preserving verbatim. The "track the delta, not just the level" guideline captures a non-obvious analytics principle (a satisfaction drop from 8.5 → 7 is more urgent than a static 7) that is broadly transferable. Stripping the board simulation wrapper leaves a clean, strong customer-research SOP. Moderate-confidence promotion candidate after persona framing and interfaces block are removed; no technical or org-specific tooling dependencies.

## agents/executive-board/vp-design.md

**Type**: Agent role / workflow SOP (UX evaluation specialist)
**Portable**: Yes — with minor stripping
**Reason**: The UX assessment framework (journey mapping → pattern audit → risk identification → feasibility → recommendation → structured output) is universal product design practice independent of any org, stack, or toolchain. The four-verdict decision model and forgotten-states checklist apply to any UI feature regardless of context.
**Trigger**: A proposal, feature, or architectural decision needs UX evaluation at the planning level; agent or user requests a Design Assessment.
**Steps/contract**: (1) Receive proposal. (2) Map the user journey — identify affected users, current flow, and where the proposal intersects. (3) Audit against existing design patterns/design system — does the proposal fit, extend, or break conventions? (4) Identify UX risks across six categories: cognitive load, broken mental models, missing states (error/empty/loading/first-use/migration), accessibility gaps, information architecture changes. (5) Assess feasibility within scope/timeline. (6) Form recommendation: Approve / Approve with Conditions / Redesign Recommended / Block — with explicit rationale for each. (7) Produce Design Assessment in the templated format.
**Strip**: Interface routing labels ("Receives from: CPO", "Hands off to: VP Engineering") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter — runtime config. The "VP of Design" job-title framing in the role preamble — replace with a role-neutral label (e.g. "UX Reviewer") when promoting.
**Structure/format**: Role preamble + 6 hard rules + 7-step workflow + structured output template (verdict block, user-journey impact table, UX risk table, design-system alignment section, forgotten-states checklist, recommended approach, open questions) + guidelines section. Well-structured; the output template and forgotten-states checklist are the highest-value portable artifacts.
**Notes**: The forgotten-states checklist (first-use/onboarding, empty state, error state, loading/skeleton, edge cases, accessibility, migration) is the single most portable element — it encodes a complete coverage set applicable to any UI feature review and is routinely omitted from informal design reviews. The "evidence over intuition" and "design system is law" hard rules are strong portable guardrails. The proportional-pushback guideline ("match depth to blast radius of the change") is a concise calibration heuristic worth preserving verbatim. The four-verdict model with explicit Block + unblock conditions prevents the common failure mode of vague "needs more work" outcomes. Natural pairing with `pr-review` and `debugger` as part of a pre-implementation quality-gate triad.

## agents/executive-board/vp-engineering.md
**Type**: Agent role / execution planning SOP
**Portable**: Yes — with minor stripping
**Reason**: The 6-step workflow (gather inputs → assess scope → identify constraints → build phased plan → stress-test → deliver) and the Execution Plan output format are universal project-planning heuristics with no dependency on any specific org, stack, or toolchain. The 5 hard rules encode non-obvious execution discipline (honesty on timelines, dependencies as first-class citizens, immediate risk surfacing) that transfer cleanly to any engineering planning context.
**Trigger**: A strategic or product decision needs to become a phased, actionable engineering plan that downstream leads can break into sprint-level work.
**Steps/contract**: (1) Gather inputs — identify what's present and what's missing (architecture direction, product requirements, business priority); stop and request missing inputs rather than assuming. (2) Assess scope — read all inputs, estimate effort size; ask 1–3 pointed questions if scope is unclear; recommend a scoping exercise if scope is massive. (3) Identify constraints — enumerate capacity, hard deadlines, skill gaps, and technical risks. (4) Build the plan — produce an Execution Plan with phases sequenced by dependency order, each with goal, task clusters, exit criteria, blocked-by, blocks, and risks. (5) Stress-test — identify critical path, single points of failure, blast radius of a 50 % slip on the highest-risk item, and mandatory decision points. (6) Deliver and iterate — present the plan; update as constraints change; treat plans as living documents.
**Strip**: Interface routing labels ("Receives from" / "Hands off to") — org-specific agent-graph wiring with no portable value. Model/model_alt frontmatter — runtime config, not SOP content. Persona framing ("you turn strategy into execution") — replace with plain role description; the procedural substance is what transfers.
**Structure/format**: Role preamble + 5 hard rules + 6-step ordered workflow + fully-templated Execution Plan output format + guidelines section. The output format template (Context → Staffing & Capacity → per-Phase blocks → Critical Path → Risk Register → Open Questions → Decision Points) is the primary durable artifact and should be preserved verbatim. Hard rules double as a quality checklist. Guidelines section adds non-obvious nuance (think in weeks not days; name owners not teams; parallel work has coordination cost; early phases more detailed than later).
**Notes**: The Execution Plan template is the highest-value portable element — particularly the Risk Register table (risk / likelihood / impact / mitigation / owner) and the Decision Points section, which are often absent from generic planning templates. The stress-test step (critical path, single points of failure, 50 % slip scenario) encodes a rigorous review discipline worth preserving verbatim. The guideline "if you're unsure, say so with a confidence level" is a strong transparency norm that combats a common LLM failure mode of projecting false precision. Hard rule 2 ("timelines must be defensible — never pad silently, never promise optimistically") is unusually explicit and worth carrying forward as-is. Strong promotion candidate; only modification needed is removing interface routing and persona voice.

## agents/executive-board/moonshot.md
**Type**: Agent role (persona-based subagent / strategic-reasoning framework)
**Portable**: Yes — with minor stripping
**Reason**: The 10x thinking patterns (adjacent possible, platform thinking, market creation, talent signal, regret minimization), the five reasoning-pattern triggers ("when the room proposes an optimization → what if instead of improving X by 20%, we made X irrelevant?"), and the Moonshot Brief output template are entirely domain-agnostic strategic heuristics. None of them reference a specific org, product, or toolchain. They apply to any team or board deliberation where incremental convergence risk needs to be stress-tested.
**Trigger**: A planning or strategy session is converging on an incremental option and needs adversarial pressure from a higher-ambition frame; or a board/team needs to confirm that a bold alternative was genuinely considered before committing to the safe play.
**Steps/contract**: (1) Before engaging with the incremental option, sketch the 10x version. (2) State honest probability of success, magnitude of payoff, and cost of failure — never sanitize the risk. (3) Identify asymmetric bets (capped downside, uncapped upside); if downside is existential, say so. (4) Include time-bound kill criteria (if X hasn't happened by Y, abort). (5) Acknowledge when the conservative position is genuinely strong — the goal is to ensure the bold option was *considered*, not that it wins. (6) Produce a Moonshot Brief structured as: Incremental Play (one sentence) → Moonshot (2–3 paragraphs: what, why now, why it could work) → Why Now (2 market/tech bullets) → Risk/Reward comparison table (Upside / Downside / Probability / Time-to-signal for both plays) → Minimum Viable Moonshot (cheapest test to de-risk the thesis) → Kill Criteria (2+ time-bound abort conditions) → What Would Change My Mind (evidence or market signal that would validate the safe play).
**Strip**: Interface routing block ("Receives from: CEO", "Hands off to: CEO") — org-specific agent-graph wiring with no portable value. Frontmatter `model`/`model_alt`/`tags` fields — runtime config, not SOP content.
**Structure/format**: Role preamble (what posture to take) → 5 named thinking frameworks → 5 reasoning-pattern triggers with cue/response format → 6 hard rules (numbered; double as a checklist) → Moonshot Brief template (fenced code block with section headers) → 5 guidelines. The output template is the most portable artifact and is immediately adoptable. The reasoning-pattern triggers are unusually high signal: they give the agent concrete conversation-steering moves for the most common objections (resources, risk, roadmaps, competitor benchmarking), which makes the persona consistent and repeatable rather than ad-hoc.
**Notes**: The "Minimum Viable Moonshot" concept is a standout heuristic — it transforms moonshot advocacy from a binary gamble into a falsifiable experiment, which makes the overall framework credible to incrementalists. The Kill Criteria requirement is an underrated guardrail that prevents proposals from becoming open-ended commitments; it deserves preservation verbatim. The "compound your bets" guideline (prefer moonshots that leave you better off even on failure — talent hired, tech built, market learned) encodes a real option-value lens on strategic risk that extends well beyond this specific role. High-confidence promotion candidate; only the interface routing block needs removal.

## agents/executive-board/revenue.md
**Type**: Agent role / persona-based reasoning SOP (board monetization driver)
**Portable**: Partial — the five mental models (willingness-to-pay test, revenue-per-engineering-hour, pricing-as-signal, expansion-revenue hooks, customer-acquisition efficiency), the 90-day forcing function, and the Revenue Assessment output template are universally applicable business-reasoning heuristics. The persona framing and interface routing are board-simulation-specific wiring that do not transfer.
**Reason**: The mental models and hard rules address a pervasive product-thinking failure mode — building things without a monetization path or a named price — in domain-agnostic terms. The Revenue Assessment template (money on the table → 90-day play → pricing signal → risk → ship-or-skip verdict) is a complete, immediately reusable evaluation format for any feature or strategy review. The value is in the reasoning framework and output format, not the persona.
**Trigger**: Agent or user is evaluating commercial viability of a feature, initiative, or strategy; when no price or monetization path has been named; when a proposal defers revenue to an indefinite future quarter; when a "ship-or-skip" commercial judgment is needed.
**Steps/contract**: (1) Apply willingness-to-pay test — would someone open their wallet, not just use it for free? (2) Estimate revenue per engineering hour (time-adjusted return). (3) Evaluate pricing as a value signal — too low = unserious buyers; too high = no demand signal. (4) Check for expansion revenue hooks (upsell, cross-sell, usage-based growth paths). (5) Assess customer acquisition motion (self-serve vs. enterprise sales cycle). (6) Produce a Revenue Assessment covering: Money on the Table, 90-Day Revenue Play, Pricing Signal block (price point / comparable market pricing / pricing model / WTP confidence), Revenue Risk (churn trigger / dependency risk / scaling risk), and Ship-or-Skip Verdict with 2–3 sentence rationale.
**Strip**: Interface routing block ("Receives from: CEO / Hands off to: CEO") — board-topology wiring. Model/model_alt frontmatter — runtime config, not SOP content. "You are a board member" persona wrapper — implementation detail of the board simulation. Reasoning-pattern labels that name other specific board agents by persona ("When Compounder argues…", "When Moonshot…") — replace with generic scenario framing (e.g., "when a long-horizon moat play is proposed") so the patterns survive outside the board context.
**Structure/format**: Persona framing → interfaces → research-tool instruction → 6 hard rules → 5 named mental models → 5 aphoristic reasoning patterns → structured output template → 5 guidelines. Hard rules and output template are the highest-value portable elements. The reasoning patterns are memorable aphorisms ("free is a pricing strategy, not the absence of one"; "the customer's wallet is the only honest feedback mechanism") — worth preserving with persona references genericised.
**Notes**: The 90-day forcing function is the defining heuristic and should be named explicitly in any promoted SOP — it is a concrete, testable constraint, not vague advice. The research-tool instruction (ground analysis in real competitor pricing pages, SaaS benchmarks, WTP studies before forming a position) adds evidence discipline often absent from monetization heuristics. The "kill zombie projects" guideline ("almost ready to monetize for two quarters = cost center wearing a revenue costume") is a sharp, portable anti-pattern worth preserving verbatim. The output template's pricing-signal sub-block — price point + comparable market pricing + pricing model + WTP confidence — is the most distinctive extractable artifact in the file. Moderate portability; the reasoning content is high-value but requires stripping the board-simulation scaffolding before promotion.

## agents/meta/boardroom-assessment-instructions.md
**Type**: Micro-rule (debate-attribution convention)
**Portable**: No
**Reason**: The instruction exists solely to coordinate a multi-agent "boardroom" simulation where named board-member personas argue with each other. Outside that specific simulation topology (CEO, CTO, CFO etc. playing adversarial roles in one session), the rule has no applicable context.
**Trigger**: N/A — only fires inside a structured boardroom/council debate session where multiple named personas share a single context window.
**Steps/contract**: Single rule — when challenging another agent's position, address them by their role title (e.g., "I disagree with the CTO's assessment because…") so the CEO-moderator and other agents can follow the argument thread.
**Strip**: Everything is context-specific. The rule cannot be de-coupled from its simulation framing.
**Structure/format**: Three-sentence prose. No structure to preserve or extract.
**Notes**: Narrow, single-purpose convention. If a portable "multi-agent debate facilitation" SOP were ever assembled, the attribution pattern could contribute one bullet ("name the role you are addressing"). As a standalone file it is not a viable SOP candidate.

## agents/meta/boardroom-artifact-discipline.md
**Type**: Rule block / output-discipline constraint (template shell)
**Portable**: Partially
**Reason**: The three underlying principles — treat source input as immutable, restrict write-back to a designated scratchpad, open directly with substantive content — are universal agent-output disciplines that apply in any system where an agent receives a brief and produces a deliverable. The specific path references and template variables are boardroom-system plumbing, not the principles themselves.
**Trigger**: Agent is about to produce output in response to a brief or specification document; before any file-write operation during a task.
**Steps/contract**: (1) Treat the source brief as immutable — never write back to it, patch it, or append to it. (2) Restrict persistent working memory to a single designated scratchpad; update it via in-response scratch blocks rather than direct file-mutation tools where possible. (3) Begin output directly with substantive content — no preamble, meta-commentary, or narration about what you are about to do.
**Strip**: Everything except the three operative principles: `boardroom/briefs/` path (boardroom-specific); `{{BRIEF_PATH}}` and `{{SCRATCHPAD_PATH}}` template variable syntax (boardroom-system templating, not portable config); `{{DELIVERABLE_DIRECTIVE}}` placeholder (unexpanded — the actual deliverable instruction was injected at runtime and is absent here, leaving a content hole).
**Structure/format**: Single fenced rules block (--- delimiter style) with four prose sentences. No numbered steps, no examples, no rationale. Minimal — this file is a template shell designed for runtime parameter injection, not a self-contained SOP.
**Notes**: Low-value standalone candidate. The three principles are real and portable but already covered more thoroughly by existing audit finds: `write-back-gate.md` addresses the persistence-restriction discipline in depth; the no-preamble rule is a standard LLM output-discipline one-liner. The `{{DELIVERABLE_DIRECTIVE}}` gap means the full intent of this file is not visible in source — it is incomplete without the injection context. Best treated as a source of specific one-liners to fold into a general output-discipline rule or into `write-back-gate`, rather than promoted as a standalone SOP. Do not promote as-is.

## agents/meta/boardroom-synthesis-instructions.md

**Type**: Agent instruction fragment / output-only constraint rule (not a self-contained workflow SOP)
**Portable**: No — orphan fragment; depends entirely on external context to function
**Reason**: The file is four sentences that only make sense inside a running "boardroom" multi-agent session: it assumes board-member agents have already submitted input, that "Strategic Brief format" is defined in the receiving agent's system prompt, and that brief/memo files exist elsewhere. Stripped of that scaffolding, it carries no standalone procedural value. The one genuinely portable element — the no-tool-write constraint ("return the final memo as your assistant message only") — is a single sentence and not worth extracting on its own.
**Trigger**: Final synthesis step in a boardroom-style multi-agent deliberation: all board-member agents have submitted input and a synthesis agent must collate, surface tensions, and return a single strategic output.
**Steps/contract**: Single-step contract — (1) Synthesize all board input. (2) Address key tensions and disagreements explicitly. (3) Output in the Strategic Brief format defined in the system prompt. (4) Return as assistant message only; do not call any tools to write, patch, or rewrite files.
**Strip**: "Strategic Brief format as defined in your system prompt" — forward-reference that is undefined in this file. "Board members" and "brief or memo files" — terminology that only resolves inside the boardroom agent graph. Everything except the no-tool-write sentence is context-bound.
**Structure/format**: Four-sentence flat prose block. No headers, steps, templates, or scaffolding. Functions as a system-prompt appendix injected at synthesis time, not as an independently readable SOP.
**Notes**: Not a promotion candidate as-is. The no-tool-write discipline ("return output as assistant message, never call tools to persist it") is a portable guardrail for synthesis-only agent roles, but it exists more cleanly in `rules/write-back-gate.md` already. If the full boardroom pattern (board-member agent definitions + orchestration + synthesis instructions) were audited together, the aggregate might yield a portable "multi-agent deliberation" SOP; this fragment alone does not.

## agents/meta/subagent-writer.md
**Type**: Skill / meta-skill (agent authoring SOP — structural template and design principles for writing subagent definition files)
**Portable**: Yes
**Reason**: Every principle and anti-pattern targets universal LLM failure modes in role-based agents — none are tied to a specific org, stack, or toolchain. The output-format template (frontmatter + identity + hard rules + branching workflow + output format + guidelines) is a standalone architecture pattern adoptable in any agent framework. No org-specific identifiers, paths, or internal references beyond model/model_alt frontmatter.
**Trigger**: Agent or user is creating or improving a subagent definition file; user mentions "write a subagent", "define an agent role", or "agent prompt engineering"; or an existing agent role feels vague, unbounded, or underspecified.
**Steps/contract**: (1) Write frontmatter (name + one-line description). (2) Write a 1–2 sentence identity statement: who the agent is, what it does, and critically what it does NOT do. (3) Write 3–7 Hard Rules — each must name the specific failure mode it prevents. (4) Write a branching Workflow (decision points and exit conditions, not a linear checklist). (5) Specify Output Format as the API contract for downstream agents — name every expected section. (6) Add Guidelines encoding the thinking patterns of the role, not just tone instructions.
**Strip**: `model` and `model_alt` frontmatter fields — runtime config, not SOP content; strip or replace with a generic note that model selection is caller-configured.
**Structure/format**: Frontmatter + identity block + 7 named design principles with explanations + anti-patterns list + embedded output-format code block as the canonical template. Well-organized; the code-block template is the primary portable artifact. Principle explanations are moderately verbose but all add signal — trim cautiously.
**Notes**: "Output Is Interface" is the highest-value single principle — it enforces that agent outputs are designed as downstream-consumable contracts, not merely human-readable text; preserve verbatim. "Constraints Are Features" (each hard rule must name the failure mode it prevents) is a meta-rule that improves the quality of any future hard-rule authoring — keep with its rationale examples. Scope Laddering (Strategic/Tactical/Operational altitude) is a distinctive contribution for hierarchical agent systems. Anti-patterns block ("role cosplay without substance", "unbounded scope", "monologue agents", "generic workflows") encodes common failure modes compactly — high-density portable content. This is one of the few files that teaches *how to build agents* rather than defining one specific agent; it belongs in any portable agent-authoring toolkit. Strong promotion candidate; only modification needed is stripping the model fields.

## file: agents/meta/boardroom-final-synthesis-task.md
**Type**: Synthesis task prompt — final consolidation step in a multi-agent boardroom deliberation pattern
**Portable**: Partially — the synthesis directive is portable; the "boardroom" framing is context-specific
**Reason**: The core instruction (synthesise all input, address disagreements explicitly, include visuals for quantitative data) is a reusable closing protocol for any multi-perspective review, but it is tightly coupled to an upstream boardroom/panel pattern and meaningless in isolation
**Trigger**: End of a multi-agent or multi-perspective deliberation where a single authoritative output must reconcile competing views
**Steps/contract**: (1) Collect all board/panel input; (2) synthesise into a Strategic Brief; (3) explicitly address disagreements; (4) include mermaid/SVG visuals when data (costs, timelines, risks, architectures) is present
**Strip**: "Boardroom", "board input", "Strategic Brief" — replace with generic terms (panel, stakeholder input, synthesis document)
**Structure/format**: Free-form prose brief; structured visuals (mermaid/SVG) required when quantitative or structural data is present; no prescribed template for the text body
**Notes**: The explicit-disagreement requirement and the conditional-visuals rule are the portable gems here; they prevent silent majority-vote consensus and keep data grounded in diagrams rather than prose descriptions

## agents/meta/boardroom-structured-final-task.md

**Type**: Prompt fragment / output-format directive (not a full SOP)
**Portable**: Weak — the mermaid/SVG rendering hint is portable as a one-liner rule; the document as a whole is too thin to stand alone
**Reason**: The only substantive content is a conditional visual-output instruction ("if data is worth visualizing, use mermaid or SVG fences"). The framing ("FINAL Strategic Brief", "boardroom") is context-specific persona dressing with no transferable workflow. There is no template for what a Strategic Brief must contain, no quality criteria, no trigger conditions beyond the implicit end-of-deliberation moment.
**Trigger**: End of a boardroom/strategic deliberation phase when an agent must produce a final deliverable.
**Steps/contract**: Single directive — produce a Strategic Brief; if the decision involves visualizable data (costs, timelines, risk matrices, architectures), embed one or more mermaid or SVG code fences in the output.
**Strip**: "FINAL" emphasis is presentational noise. "Boardroom" framing is persona-specific and carries no portable information.
**Structure/format**: One imperative sentence + one conditional visual instruction. No template, no sections, no quality gate, no acceptance criteria. Bare minimum; not self-contained.
**Notes**: Too thin to promote as a standalone SOP. The mermaid/SVG rendering directive is the only extractable signal — worth harvesting as a rule fragment ("when output includes quantitative data or structure worth visualizing, embed mermaid or SVG fences") and appended to any strategic-output or reporting skill. Would need substantial scaffolding (brief structure, required sections, completeness criteria) before it could stand as a portable deliverable format. Low-priority candidate; harvest the rendering hint only.

## agents/meta/boardroom-structured-review-final-round-task.md

**Type**: Task prompt fragment (single step in a multi-round orchestration loop)
**Portable**: No
**Reason**: This is a leaf-node instruction scoped to the penultimate step of a specific "Boardroom Structured Review" multi-round deliberation process. It references a "Strategic Brief" deliverable and a round-budget tracker that only exist in that orchestration's context. Extracted in isolation, the three template variables (`{{ROUND_NUMBER}}`, `{{PLANNED_ROUNDS}}`, `{{TRACKER_SUMMARY}}`) and the "do not produce the final Strategic Brief yet" hold instruction carry no meaning — they are runtime state, not portable process logic.
**Trigger**: The final round of a multi-round boardroom review cycle, when the planned round target has been reached.
**Steps/contract**: Single instruction — summarise unresolved tensions and the points the final Strategic Brief must explicitly address; withhold the brief itself.
**Strip**: Everything — `{{ROUND_NUMBER}}`, `{{PLANNED_ROUNDS}}`, `{{TRACKER_SUMMARY}}` are context-bound placeholders; "Strategic Brief" is a domain-specific deliverable label with no transferable meaning outside this orchestration.
**Structure/format**: Bare two-sentence task prompt with three template variable interpolations. No workflow, no checklist, no reusable structure. Smallest possible artifact in this repo.
**Notes**: No standalone SOP value. If the parent `boardroom-structured-review` orchestration pattern were ever extracted and promoted, this step would be embedded inline within it — not promoted as an independent skill. Skip for promotion.

## agents/meta/boardroom-structured-review-task.md

**Type**: Prompt fragment / round-continuation task instruction
**Portable**: Partial — the round-tracking discipline and "no early output" guard are universally reusable, but the fragment is tightly coupled to the boardroom review orchestration pattern and references a "Strategic Brief" terminal artefact that is workflow-specific.
**Reason**: At five lines, this is a helper injection rather than a standalone SOP. Its transferable value is the explicit continuation contract: track round progress, enforce budget/time gates, suppress premature final output, and focus each intermediate round on unresolved disagreement. That pattern applies to any multi-round consensus or deliberation workflow (red-team rounds, architecture reviews, structured debate). The fragment alone is too thin to stand as a promoted skill, but its logic belongs inside a parent "structured multi-round review" SOP.
**Trigger**: Injected at the start of each intermediate round in a multi-round structured review — fires when `ROUND_NUMBER < PLANNED_ROUNDS` and budget/time has not been exhausted.
**Steps/contract**: Single instruction block — (1) Announce current round and budget status. (2) Mandate continuation to `NEXT_ROUND` unless budget or time forces an early stop. (3) Suppress the final deliverable (Strategic Brief or equivalent). (4) Summarise where participants agree, where disagreement persists, and what the next round must pressure-test.
**Strip**: `Strategic Brief` — workflow-specific terminal artefact name; replace with a generic `<final-deliverable>` token or omit. `{{TRACKER_SUMMARY}}` — implementation-specific budget tracker variable; document the expected format (e.g., "X of Y tokens used") rather than the variable name, so adopters can bind their own tracker.
**Structure/format**: Five-line pure prompt template with four `{{HANDLEBARS}}`-style variables. No numbered steps, no narrative structure. Extremely minimal — portable only as an embedded block inside a richer orchestration SOP, not as a standalone file.
**Notes**: The highest-value sentence is the suppression guard ("Do not produce the final Strategic Brief yet") — it prevents common LLM early-termination behaviour in multi-round protocols. Worth preserving verbatim (with terminal artefact name generalised). Not a promotion candidate on its own; should be absorbed into a parent multi-round-review SOP as a `## Continuation Prompt` subsection. Flag for bundling with any future boardroom-review or structured-deliberation skill.

## agents/meta/boardroom-constraints-reached-final-task.md
**Type**: Meta-instruction / agent lifecycle signal (constraints-exhausted fallback handler)
**Portable**: Yes — as a micro-rule, not a full SOP
**Reason**: The pattern "constraints exhausted → emit best-effort final deliverable + explicit gap list" is a universal agent orchestration principle applicable to any system that imposes time, turn, or budget limits. The graceful-degradation contract (produce partial output rather than failing silently) is stack- and domain-agnostic.
**Trigger**: An agent's constraint budget (time, tokens, turns, cost) is exhausted before the task is fully complete.
**Steps/contract**: (1) Accept that no further execution steps are possible. (2) Produce the final deliverable using only data gathered so far — do not block or error. (3) Explicitly enumerate gaps that remain unfilled because constraints ran out.
**Strip**: "Strategic Brief" — task-specific output label from the dotagents boardroom workflow. The portable form is "produce your final deliverable"; the deliverable type should be parameterised by the calling context.
**Structure/format**: Three lines total. No workflow, no checklist, no template — pure trigger-to-action signal. The extreme brevity is appropriate: this instruction fires under resource pressure where verbosity is counterproductive.
**Notes**: Too thin to promote as a standalone SOP; value is in the underlying micro-principle. The "note gaps explicitly" requirement is the highest-value portable element — it enforces that partial output is clearly labelled as partial, preventing silent omissions from being treated as complete work. Best absorbed as a single-sentence guardrail appended to any orchestration prompt that imposes constraints: "If time or budget runs out, emit what you have and list remaining gaps explicitly."

## agents/meta/boardroom-freeform-review-task.md
**Type**: Prompt template fragment (single-step continuation instruction within a multi-round deliberation loop)
**Portable**: No — not as a standalone SOP
**Reason**: This file is a one-of-N mid-round instruction for a boardroom deliberation pattern. It is meaningful only when embedded in a larger orchestration framework that supplies `{{ROUND_NUMBER}}`, `{{PLANNED_ROUNDS}}`, `{{TRACKER_SUMMARY}}`, and `{{NEXT_ROUND}}`. The instructions themselves (summarise disagreements, surface missing evidence, pose questions for the next round) are reasonable, but they are scaffolding for a parent system, not a self-contained procedure.
**Trigger**: End of a non-final round inside a multi-round boardroom deliberation loop.
**Steps/contract**: Single instruction — (1) do not produce the final Strategic Brief yet; (2) summarise the strongest disagreements from this round; (3) identify missing evidence; (4) state the concrete questions the next round must resolve; (5) continue to `{{NEXT_ROUND}}` unless budget or time forces an early stop.
**Strip**: All four `{{VARIABLE}}` placeholders — they are wiring artefacts of the parent orchestration system and carry no transferable semantic content. The "You must continue to round N" directive is also system-wiring, not an SOP rule.
**Structure/format**: Five sentences; no sections, no template headers, no decision gates. Entirely too minimal to audit as a workflow SOP. Reads as a runtime instruction injected by an orchestrator, not a human-authored procedure.
**Notes**: The broader boardroom deliberation pattern (multi-round structured review with budget tracking, freeform mid-rounds, and a final Strategic Brief) is a potentially portable meta-pattern — but this file exposes only the least interesting slice of it. Recommend skipping promotion. If the boardroom pattern as a whole is of interest, the full loop (init → freeform rounds → final-brief task) would need to be reconstructed from multiple files before any SOP could be extracted.

## agents/meta/boardroom-force-close-final-task.md

**Type**: Agent interrupt protocol / graceful force-close handler
**Portable**: Partial — the core pattern (deliver best-effort output from partial info + enumerate unresolved threads) is universal; the "boardroom" and "Strategic Brief" framing is persona-specific to a strategic-simulation agent.
**Reason**: Any multi-round agent workflow can be interrupted before reaching a natural stopping point. The underlying obligation — commit to a deliverable using only what has been gathered, stop requesting further input, and explicitly surface what was left unresolved — is a reusable graceful-interrupt contract independent of domain.
**Trigger**: Operator or user force-closes a multi-round agent session before the agent has completed its normal workflow.
**Steps/contract**: (1) Accept that no further input rounds will occur. (2) Produce the best-effort deliverable using only information gathered so far. (3) Explicitly enumerate unresolved questions or workstreams cut short by the interrupt. (4) Include Mermaid or SVG visualisations when the content involves data worth charting (costs, timelines, risk matrices, architectures).
**Strip**: "Boardroom" noun and "Strategic Brief" deliverable label — these are specific to the strategic-simulation persona. Replace with generic equivalents ("multi-round session", "final deliverable") when promoting. The persona voice ("The operator force-closed the board meeting") carries no portable information.
**Structure/format**: 5 imperative lines; no checklist, no template, no scaffold. Effective as an inline prompt injection but too thin to stand as a self-contained SOP without a short output template added.
**Notes**: Thin source material — lowest word count of any file audited so far. The "note what's unresolved" clause is the highest-value portable element: it prevents agents from silently dropping incomplete work and ensures the human can resume from a known state. The visualisation default (include Mermaid/SVG when data warrants it) is a good portable heuristic worth standardising across any reporting-output skill. As a standalone SOP this is too narrow unless reframed as a general graceful-interrupt handler pattern; better promoted as a short protocol addendum to multi-round agent skills (e.g., `grill-me`, `prd-create`, `boardroom-sim`) rather than a first-class skill file.

## agents/meta/narration-summarizer.md
**Type**: Agent system prompt / output-format contract (not a workflow SOP)
**Portable**: Partial
**Reason**: The synthesis-over-enumeration discipline, four-point narrative spine (discussed → roadblocks → decision → why), and the no-preamble / plain-prose output contract are broadly reusable for any TTS-destined document-to-narration task. The specific application (board meeting memos, ElevenLabs) is domain-specific framing, not intrinsic to the technique.
**Trigger**: Agent is asked to convert a structured document (memo, report, decision record, brief) into a flowing spoken narration for TTS delivery.
**Steps/contract**: (1) Summarize the ENTIRE source document — no section-by-section recap. (2) Follow the four-point spine: (a) what was discussed, (b) roadblocks/constraints/disagreements/considerations, (c) the final decision, (d) why that outcome was reached. (3) Prefer synthesis over enumeration — never produce a laundry list of findings, tables, or metrics. (4) Include numbers, timelines, or risks only when they materially explain the decision. (5) For multi-option or contested content: name the tradeoff that mattered and explain how it resolved. (6) End with a clear bottom line. (7) Output: plain prose only — no markdown, bullets, headings, tables, XML, or code fences. (8) First sentence must be part of the narration body itself. (9) Respect `{{MAX_CHARS}}` character limit.
**Strip**: (a) "ElevenLabs" — replace with generic "your TTS platform" or remove; (b) "board" / "board meeting" / "the board landed there" — domain-specific framing; parameterize as `{{document_type}}` and `{{decision_maker}}` or generalize to "the document" / "the decision-makers"; (c) `{{MAX_CHARS}}` — retain as a template slot but note it requires instantiation at call time.
**Structure/format**: Flat prose instruction block — no frontmatter, headings, or numbered steps. Reads as a direct system prompt. The forbidden-examples list (seven explicit no-no patterns) is comprehensive. Would need frontmatter trigger description and a brief structured preamble to be promoted as a SKILL.md file.
**Notes**: The forbidden-preamble list is the highest-value portable element — it directly addresses a persistent LLM failure mode of generating status-text wrappers ("Here is a summary…", "Drafting…", "Below is a concise TTS-ready script") before the actual content. The "first sentence must already be part of the narration" rule is a strong positive reframe of the same constraint. The synthesis-over-enumeration directive and tradeoff-resolution guidance are broadly applicable to any document-to-narrative conversion task. Portability is bounded by the four-point spine's specificity to decision-record documents; it would need parameterization for other document types (incident reports, how-to guides, status updates). Moderate-confidence promotion candidate — high value as a TTS narration output-format spec; generalize away from board-memo framing before promoting.
