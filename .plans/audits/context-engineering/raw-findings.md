## skills/multi-agent-patterns/SKILL.md
**Type**: Architecture decision guide + pattern reference
**Portable**: Yes
**Reason**: All core content — pattern selection criteria, context isolation principle, token economics data, failure modes with mitigations, and gotchas — is framework-agnostic and applies across any multi-agent implementation. Framework-specific code examples (LangGraph, AutoGen, CrewAI) are illustrative only and clearly scoped.
**Trigger**: Designing or implementing a multi-agent system; choosing between orchestration patterns; coordinating sub-agents or parallel agent execution; diagnosing coordination overhead or supervisor bottlenecks.
**Steps/contract**: Not a procedural SOP — structured as a decision guide: (1) when to use multi-agent at all, (2) pattern selection by coordination need (supervisor / swarm / hierarchical), (3) context isolation mechanisms (full delegation / instruction passing / filesystem), (4) consensus and anti-sycophancy protocols, (5) failure modes + mitigations, (6) gotchas. Includes concrete token-cost benchmarks (15× multiplier).
**Strip**: Framework-specific code blocks (LangGraph/AutoGen/CrewAI sections and the `forward_message` Python snippet — replace with pseudocode); internal cross-skill references (`context-fundamentals`, `memory-systems`, `context-optimization`); skill metadata block; "Skill Metadata" footer.
**Structure/format**: Excellent — tiered headers, decision tables with trade-off columns, named failure modes with mitigations, numbered gotchas. Preserves well as-is after stripping repo-specific cross-references.
**Notes**: Very high-value portable candidate. Three standout extractions: (a) "context isolation as primary purpose, not role anthropomorphism" design principle; (b) the 15× token cost benchmark with variance breakdown; (c) the 8-item gotchas list (supervisor scaling cap, sycophantic consensus, over-decomposition, telephone game) — dense tribal knowledge usable in any orchestration context.

## skills/context-fundamentals/SKILL.md
**Type**: Foundational reference / educational — principles, thresholds, gotchas, and patterns for context engineering in LLM agent systems; not a step-by-step workflow SOP.
**Portable**: Yes
**Reason**: Entirely model/framework-agnostic. Covers universally applicable mechanics (attention budget, U-shaped recall curve, position encoding limits, progressive disclosure, compaction triggers) with specific quantitative thresholds that hold across any LLM deployment. No project-specific tooling or codebase assumptions.
**Trigger**: Designing agent architectures, debugging unexpected agent behaviour related to context, optimising token costs, onboarding to context engineering concepts, reviewing context-related design decisions.
**Steps/contract**: Not a procedural SOP — structured as a reference document: core principles → detailed topics (anatomy, attention mechanics, quality vs quantity) → practical guidance → guidelines checklist (8 items) → gotchas (7 items). Actionable thresholds embedded throughout: effective capacity = 60–70 % of nominal window; compaction trigger at 70–80 % utilisation; sub-agent return budget 1 K–2 K tokens; tool schemas inflate 2–3× after JSON serialisation; middle-of-context recall drops to 76–82 % vs 85–95 % at ends.
**Strip**: Author/version metadata block; `Integration` section cross-links to sibling skills in the source repo (context-degradation, context-optimization, multi-agent-patterns, tool-design); internal `./references/context-components.md` relative path reference; vague external resource attributions (titles only, no URLs).
**Structure/format**: Well-organised with H2/H3 hierarchy, numbered guidelines, fenced-code examples, and a dedicated gotchas list. Dense but scannable. Approximately 1 800 tokens — suitable as a loaded reference rather than always-on context.
**Notes**: Highest-signal foundational reference in the repo. The specific quantitative benchmarks (83.9 % tool-output token share in agent trajectories, 15 % retrieval accuracy at extreme context lengths, 2–3× serialisation inflation) are the primary portable value and should be preserved verbatim in any derivative SOP. Consider splitting into two portable artefacts: (1) a concise "Context Budget Cheatsheet" with thresholds/gotchas and (2) a fuller "Context Engineering Primer" retaining the conceptual depth.

## skills/context-degradation/SKILL.md
**Type**: Reference/diagnostic — taxonomy of five degradation patterns (lost-in-middle, poisoning, distraction, confusion, clash) plus mitigation frameworks; primarily educational rather than procedural.
**Portable**: Partially — the five-pattern taxonomy, four-bucket mitigation framework (Write/Select/Compress/Isolate), and seven-item gotchas list are all portable. The benchmark thresholds (70% window onset trigger, U-curve placement rule) are portable heuristics. The integration/references sections are repo-internal only.
**Reason**: Core frameworks are universally applicable to any agent or RAG system; the gotchas in particular encode hard-won counter-intuitive findings. However, the skill lacks a clear trigger→action→output loop — it reads as a reference document rather than a SOP with defined inputs and deliverables.
**Trigger**: User asks to "diagnose context problems", "debug agent failures", "fix lost-in-middle", or mentions context poisoning / context degradation / attention patterns / agent performance degradation.
**Steps/contract**: No explicit procedural sequence. Closest to a decision procedure is the four-bucket framework: detect active degradation pattern → apply Write / Select / Compress / Isolate. Guidelines section gives 8 numbered rules but they are design principles, not task steps.
**Strip**: (1) "Core Concepts" prose (duplicates detailed-topics sections); (2) YAML/markdown illustrative examples (low signal); (3) "When Larger Contexts Hurt" section (covered better in benchmarks); (4) entire Integration and References sections (all internal); (5) verbose "Counterintuitive Findings" intro — keep findings, cut framing.
**Structure/format**: Well-organised — named H3 sections, numbered guidelines, gotchas list, code blocks. Four-bucket framework clearly formatted. Gotchas 1–7 are individually crisp and actionable. Would gain further value with an explicit pattern→mitigation decision table.
**Notes**: Highest-value extracts for a portable SOP: (a) five degradation pattern definitions with detection signals, (b) four-bucket mitigation framework with onset-threshold trigger (70% of window), (c) gotchas list verbatim. The U-curve placement rule ("critical info at start and end, never middle") is a single-sentence portable heuristic worth keeping standalone.
## skills/context-compression/SKILL.md
**Type**: Knowledge-base skill with decision frameworks — covers when and how to compress context in long-running agent sessions; includes three named compression methods, trigger strategies, structured summary templates, probe-based evaluation methodology, and calibrated benchmarks.

**Portable**: ✅ Yes — fully framework- and language-agnostic. Core principles (optimize tokens-per-task, anchored iterative summarization, structured mandatory-section summaries, probe-based eval, artifact trail preservation) apply to any agent or LLM platform with no proprietary dependencies.

**Reason**: Research-backed with quantitative benchmarks from a 36,000-message evaluation (compression ratios, quality scores across 6 dimensions). The three-method taxonomy, the "tokens-per-task not tokens-per-request" reframe, the artifact trail warning (universal weak spot at 2.2–2.5/5.0), and the structured summary template are immediately transplantable to any agent SOP. Gotchas section is exceptionally specific — all 7 items are non-obvious and actionable.

**Trigger**: Agent session approaching context window limit; designing or auditing conversation summarization strategy; debugging agent "forgetting" behavior (re-reading files it already processed); building evaluation frameworks for compression quality; long-running coding/debugging/migration sessions.

**Steps/contract**:
1. Choose compression method by session profile — anchored iterative (file tracking, 100+ messages), opaque (max savings, low re-fetch cost), regenerative (human-readable, clear phase boundaries).
2. Trigger at 70–80% context utilization; default to sliding window + structured summaries for coding agents.
3. Build structured summary with mandatory sections: Session Intent / Files Modified / Decisions Made / Current State / Next Steps (adapt sections to domain).
4. On first compression: summarize truncated span into sections. On subsequent: summarize *only* newly-truncated content and merge into existing sections — do not regenerate wholesale.
5. Preserve identifiers verbatim in dedicated sections (full file paths, function names, error codes, commit SHAs) — never embed in prose.
6. Evaluate with probe-based testing across 6 dimensions: Accuracy, Context Awareness, Artifact Trail, Completeness, Continuity, Instruction Following.
7. Monitor re-fetching frequency as the primary quality signal; if agent re-reads already-processed files, compression is too aggressive.

**Strip**:
- `## Integration` section (cross-links to sibling skills in source repo: context-degradation, context-optimization, evaluation, memory-systems — will be dead links elsewhere).
- `## References` internal sub-section (links to `./references/evaluation-framework.md` and sibling skill files).
- `## Skill Metadata` block (version, author, dates — internal provenance).
- External resource citations can be retained as footnotes but should not be framed as required reading.

**Structure/format**: Excellent. Well-layered: concept overview → detailed subsections → tables (trigger strategies, method comparison, probe types) → worked examples (compression before/after, probe response quality) → guidelines checklist → gotchas. The structured summary markdown template is ready-to-copy. Retains this structure verbatim.

**Notes**:
- Among the strongest skills in the collection. Density is high but justified — each section carries distinct, non-redundant guidance.
- The "tokens-per-task" reframe is the single most portable insight; worth surfacing as a headline principle in any derived SOP.
- Artifact trail problem is called out with concrete scoring data (2.2–2.5/5.0) — promotes it from vague advice to a design constraint.
- Three-phase compression workflow (Research → Planning → Implementation) for large codebases is a high-value portable pattern applicable beyond coding agents.
- Gotcha #4 (early turns contain irreplaceable constraints) and #5 (compression ratios compound across cycles) are underappreciated in practice — worth preserving verbatim.
- Probe-based evaluation methodology is portable to any LLM quality-testing context, not just compression.
- Recommend promotion to a top-tier SOP; strip sibling-skill cross-links and keep as self-contained reference.

## skills/advanced-evaluation/SKILL.md
**Type**: LLM-as-judge evaluation methodology — direct scoring, pairwise comparison, bias mitigation, rubric design, pipeline architecture
**Portable**: Yes
**Reason**: Entirely domain-agnostic. Covers universal evaluation patterns applicable to any team running automated quality assessment of LLM outputs — no repo-specific context required.
**Trigger**: "implement LLM-as-judge", "compare model outputs", "create evaluation rubrics", "mitigate evaluation bias", pairwise comparison, position bias, automated quality assessment, evaluation pipeline
**Steps/contract**: (1) Taxonomy decision — direct scoring when objective ground truth exists, pairwise when subjective preference; (2) Bias mitigation protocol — position swap (2-pass + consistency check), length-neutrality instructions, separate generator/evaluator models; (3) Metric selection table keyed to task structure (binary/ordinal/pairwise/multi-label); (4) Rubric generation with five required components (level descriptions, characteristics, examples, edge cases, scoring guidelines); (5) Pipeline layers: Criteria Loader → Primary Scorer → Bias Mitigation → Confidence Scoring → Output; (6) Scaling strategies (PoLL, hierarchical, human-in-the-loop)
**Strip**: Internal `./references/*.md` links (implementation-patterns, bias-mitigation, metrics-guide, pipeline diagram), Integration section listing sibling skills by name, Skill Metadata block (author/version/dates)
**Structure/format**: Excellent — taxonomy table, metric selection table, decision tree (ASCII), two prompt templates (direct + pairwise), JSON output examples × 3, numbered guidelines (10), numbered gotchas (8). Highly reusable as-is.
**Notes**: Strong concrete grounding — cites Zheng 2023, Liu 2023, Wang 2023 with quantified claims (CoT justification-first improves reliability 15–25%; rubrics reduce variance 40–60%). The 2-pass position-swap protocol and confidence calibration mapping are particularly portable primitives. One of the highest-signal skills in the collection.

## skills/filesystem-context/SKILL.md
**Type**: Technical pattern guide (6 named patterns + gotchas + file org guidance)
**Portable**: Yes
**Reason**: Entirely framework-agnostic; all patterns depend only on the agent having file-read/write/search tools, which is true of any capable agent environment. No vendor-specific APIs or platform assumptions.
**Trigger**: "offload context to files", "dynamic context discovery", "filesystem for agent memory", "reduce context window bloat", file-based context management, tool output persistence, agent scratch pads, just-in-time context loading
**Steps/contract**: Core 4-mode failure taxonomy (missing / under-retrieved / over-retrieved / buried context) → 6 patterns: (1) scratch pad for large tool outputs, (2) plan persistence in YAML, (3) sub-agent comms via per-agent workspace dirs, (4) dynamic skill loading, (5) terminal/log persistence + grep, (6) self-modification of instruction files. Practical guidance section adds use/avoid criteria and token accounting advice. 10-item guidelines checklist. 8-item gotchas list.
**Strip**: Python pseudocode snippets (implementation hints, not rules); Integration section cross-refs to sibling skills in this collection; external resource links (LangChain, Cursor, Anthropic spec); internal `./references/implementation-patterns.md` reference
**Structure/format**: Clear H2 sections — When to Activate → Core Concepts → Detailed Topics (patterns) → Practical Guidance → Examples → Guidelines → Gotchas → Integration. Highly scannable; guidelines and gotchas are numbered lists. Promote as-is with strip items removed.
**Notes**: Strong candidate — one of the most actionable skills in the collection. The 4-mode failure taxonomy is a high-value conceptual frame that should survive verbatim. Gotchas section is unusually thorough (8 failure modes with causes and fixes). The dynamic skill loading pattern is self-referential (describes how to use skills like this one) — worth preserving as it applies to any skills-based agent system.

## skills/evaluation/SKILL.md
**Type**: Evaluation methodology reference + framework-building guide — covers rubric design, LLM-as-judge, test-set design, continuous evaluation pipelines, and production monitoring; includes an 8-step sequenced framework-building procedure alongside reference material.
**Portable**: Yes — entirely framework- and language-agnostic. Principles, thresholds, and methodologies apply to any LLM agent regardless of stack.
**Reason**: All core content (multi-dimensional rubrics, outcome-over-path evaluation, complexity stratification, LLM-as-judge bias mitigation, continuous eval cadence) is universally applicable. The 95% variance finding (80% tokens / ~10% tool calls / ~5% model) is a calibrated benchmark with direct design implications — portable as a standalone heuristic. Quantitative thresholds (0.7 general pass / 0.9 high-stakes; 50-case floor; 0.85 warning / 0.70 critical production alerts) are actionable without modification in any agent project.
**Trigger**: Building an evaluation framework for agents; measuring agent quality or context engineering changes; setting up LLM-as-judge pipelines; establishing quality gates for agent deployment; diagnosing agent regressions; continuous production monitoring.
**Steps/contract**: Dual-mode — contains both a procedural 8-step sequence (define dimensions → create rubrics → build test sets → implement pipeline → establish baseline → run on changes → track over time → add human review) and reference-style coverage of methodologies. The 8-step sequence is clear and order-dependent; the rest is reference material. Enough procedural structure to be adapted as a SOP with defined inputs (agent system, use case quality requirements) and outputs (baseline metrics, automated pipeline, pass/fail thresholds).
**Strip**: `## Integration` section (cross-links to sibling skills: context-fundamentals, context-degradation, context-optimization, multi-agent-patterns, tool-design, memory-systems — dead links outside source repo); `## References` internal sub-section (links to `./references/metrics.md` and sibling skills); `## Skill Metadata` block; Python code examples can be retained as pseudocode but `evaluate_agent_response` snippet is trivially obvious — low signal.
**Structure/format**: Well-structured — H2/H3 hierarchy, a data table for the 95% variance finding, numbered guidelines (8 items), numbered gotchas (8 items), stratified test-set code example. Gotchas section is individually crisp and non-obvious. Density is appropriate; each section carries distinct guidance without redundancy.
**Notes**: High-value portable candidate. Three standout extractions: (a) **95% variance finding** — the three-factor breakdown (token budget 80%, tool calls ~10%, model ~5%) reframes evaluation budget design and multi-agent justification with empirical grounding; (b) **"outcomes not paths" principle** — single-sentence portable design rule with a full mitigation rationale; (c) **8-item gotchas list** — covers LLM self-enhancement bias, test-set contamination, metric gaming, single-dimension obsession, and "eval as one-time" anti-pattern, all underappreciated in practice. The multi-dimensional rubric design with per-dimension thresholds (fail if any single dimension drops below minimum) is a concrete, portable quality gate pattern. Recommend promotion; strip internal cross-links and keep as self-contained reference. Slight overlap with `context-compression/SKILL.md`'s probe-based evaluation methodology — consider a cross-note rather than merging.

## skills/tool-design/SKILL.md
**Type**: Reference/design guide — comprehensive framework for designing agent-facing tools. Covers the tool-agent contract, consolidation principle, architectural reduction, description engineering, response-format optimisation, error-message design, MCP naming conventions, and a description-optimisation feedback loop with production benchmarks.
**Portable**: Yes — fully portable. All core principles (tool as contract, description as prompt, consolidation, architectural reduction, 4-question description template, error recovery signals, feedback-loop pattern) are framework- and language-agnostic. MCP-specific naming section is protocol-specific but widely applicable given current MCP adoption across agent ecosystems.
**Reason**: No proprietary or project-specific dependencies anywhere in the core content. Applies equally to OpenAI function-calling, Anthropic tool use, MCP servers, LangChain tools, or any custom tool layer. Vercel case study (17→2 tools, better performance) and description-optimisation benchmark (40% task-completion-time reduction) add credibility without being proprietary. MCP guidance encodes a known pain-point (`tool not found` collisions) that recurs across any multi-server deployment.
**Trigger**: Designing new tools for agent systems; debugging tool-selection failures or misuse; reducing tool-count or complexity; optimising existing tool descriptions; implementing or auditing MCP tools; standardising naming conventions across a tool collection.
**Steps/contract**: Not a step-by-step SOP — structured as a design reference. Closest procedural content: (1) 5-step Tool Selection Framework (identify workflows → group into comprehensive tools → ensure unambiguous purpose → document errors → test); (2) 4-question description template (what does it do / when to use / what inputs / what returns); (3) description-optimisation feedback loop with pseudocode (collect failures → agent diagnoses → improve description → re-test). Guidelines checklist has 12 items; gotchas list has 9 items.
**Strip**: `## Integration` section (cross-links to sibling skills: context-fundamentals, multi-agent-patterns, evaluation — will be dead links elsewhere); `## References` internal sub-section (links to `./references/best_practices.md` and `./references/architectural_reduction.md` — relative paths, not portable); `## Skill Metadata` block (version/author/dates); external resource citation block (keep as footnotes if desired, not required reading). The `optimize_tool_description` Python snippet is illustrative pseudocode — retain as a pattern, not as runnable code.
**Structure/format**: Excellent. H2/H3 hierarchy throughout; 12-item numbered guidelines; 9-item gotchas (each crisp, named, and distinct); clear named subsections per topic; good-vs-bad code pair (`get_customer` vs `search`) ready to copy as a teaching pattern. Dense but scannable — approximately 2,200 tokens. Best loaded as a reference rather than always-on context.
**Notes**: High-value portable candidate. Standout extracts worth preserving verbatim in any derived SOP: (a) "description as prompt, not documentation" framing — the most important reframe in the skill; (b) consolidation principle with Vercel 17→2 benchmark and explanation of *why* it works (attention budget, context token cost, selection ambiguity); (c) 4-question description template (what/when/inputs/returns) — immediately actionable; (d) fully-qualified MCP naming convention (`ServerName:tool_name`) with failure mode; (e) description-optimisation feedback loop with 40% benchmark; (f) 5 evaluation criteria (unambiguity, completeness, recoverability, efficiency, consistency); (g) tool count limit of 10–20 with research backing; (h) gotchas #6 (description rot — treat descriptions as code), #7 (over-consolidation threshold at 8–10 params or fundamentally different use cases), and #8 (parameter explosion / cognitive-load framing) — all non-obvious and underappreciated in practice. The "build for future models, not current limitations" architectural principle is a standalone portable heuristic worth surfacing in any tool-design SOP.

## skills/context-optimization/SKILL.md
**Type**: Technique reference / operational guide — four prioritised context optimisation strategies (KV-cache, observation masking, compaction, context partitioning), each with triggers, targets, and code sketches.
**Portable**: Yes — fully model-agnostic and infrastructure-agnostic; no dependency on repo tooling, specific frameworks, or vendor APIs.
**Reason**: Concrete, measurable guidance with specific numeric thresholds (70 % utilisation trigger for compaction, 60–80 % masking reduction target, 70 %+ cache-hit-rate goal) and a decision-framework table mapping context composition to first/second actions. Gotchas section is unusually high quality — covers whitespace KV-cache invalidation, timestamp pitfalls, late-compaction degradation, masking suspension during active debugging, and partitioning overhead math.
**Trigger**: Context limits constrain task complexity; cost/latency reduction is required; building long-running or production-scale agent systems; context utilisation exceeds 70 % threshold.
**Steps/contract**: Priority-ordered four-step framework — (1) KV-cache optimisation first (zero quality risk), (2) observation masking (largest capacity gain), (3) compaction at ≥ 70 % utilisation (lossy; apply after masking), (4) context partitioning when estimated context exceeds 60 % of window limit. Each step has explicit performance targets and failure modes.
**Strip**: Integration section (cross-links to sister skills context-fundamentals, context-degradation, evaluation, multi-agent-patterns, memory-systems); internal `./references/optimization_techniques.md` link; skill-metadata block.
**Structure/format**: Layered — Core Concepts (priority order) → Detailed Topics per technique → Decision Framework table → Performance Targets table → Code Examples → Guidelines checklist → Gotchas (7 items) → Integration → References. Decision table and Gotchas are the highest-value portable elements.
**Notes**: Strongest context-management SOP in this collection. The ordered strategy hierarchy (cache → mask → compact → partition) is a direct, reusable mental model. Performance targets (50 %+ cost reduction from caching, <5 % quality loss from compaction, <2 % from masking) are specific enough to set measurable acceptance criteria. The gotcha on "compaction under pressure" (trigger at 70–80 %, not 90 %+, and use a separate clean-context model call if forced late) is a production-hardened detail not found in most references.

## skills/hosted-agents/SKILL.md
**Type**: Architecture reference + implementation guide for hosted/background agent infrastructure — covers sandbox lifecycle, warm pools, agent framework selection, multiplayer support, auth, client patterns, metrics, and gotchas. Not a step-by-step procedural SOP.
**Portable**: Mostly yes — with selective stripping.
**Reason**: Core infrastructure principles (pre-built environment images, warm-pool + predictive warm-up, parallel reads before git sync, per-session state isolation, server-first framework architecture, multiplayer-from-day-one, merged PRs as primary metric) are platform-agnostic and apply to any hosted agent system. Specific tech references (Modal, Cloudflare Durable Objects, GitHub app tokens) appear as illustrative examples and can be replaced with generics without losing the substance.
**Trigger**: Building background or hosted coding agents; designing sandboxed execution environments; scaling agent infrastructure beyond local machines; implementing multiplayer agent sessions; building self-spawning or sub-agent systems.
**Steps/contract**: Not strictly procedural — structured as a layered architecture guide: core concepts → sandbox infrastructure (image registry, warm pools, snapshot/restore, git config) → agent framework selection (server-first, plugin system) → speed optimizations (predictive warm-up, parallel reads, build-time maximization) → self-spawning agents (3 primitives: start / status / continue) → API layer (per-session isolation, real-time streaming, sync) → multiplayer → auth (user-based commits, sandbox-to-API flow) → client implementations → practical guidance → 8-item guidelines checklist → 8-item gotchas list.
**Strip**: Client implementations section (Slack/Web/Chrome — too product-specific; reduce to a one-sentence note on multi-surface distribution); external resource links with vendor URLs; internal cross-skill references (`multi-agent-patterns`, `tool-design`, `context-optimization`, `filesystem-context`); GitHub-specific auth detail (keep the principle "commits attributed to prompting user, not app identity", drop the PR webhook sequence); skill metadata footer.
**Structure/format**: Good — tiered H2/H3 hierarchy, named gotchas with bold titles, numbered guidelines, concrete defaults (30-min image cadence, 4-hour timeout, SQLite per session). Dense at ~2 800 tokens; trimming client implementations and vendor specifics brings it to a loadable ~1 800-token reference.
**Notes**: High-value portable candidate. Four standout extractions: (1) Warm-pool + predictive warm-up pattern — start sandbox on first keystroke, not on submit, using the 5–30 s typing interval as free setup time; (2) "reads before writes during git sync" optimization — safe because 30-min staleness rarely affects research tasks; (3) Per-session state isolation principle — SQLite per session prevents cross-session interference and scales to hundreds of concurrent sessions; (4) 8-item gotchas list — cold start latency, image staleness, cost runaway, auth token expiration mid-session, missing git config, state loss on recycle, warm pool oversubscription, missing output extraction — dense tribal knowledge directly applicable to any hosted agent build.

## skills/project-development/SKILL.md
**Type**: Methodology reference + project planning guide — covers task-model fit evaluation, pipeline architecture, file-system state management, cost estimation, single-vs-multi-agent selection, and agent-assisted development; includes both decision frameworks and a procedural 5-phase project planning template.
**Portable**: Yes — fully framework- and language-agnostic. The pipeline pattern, file-system state machine, cost formula, task-model fit tables, and project planning template apply to any LLM project regardless of stack.
**Reason**: No proprietary or project-specific dependencies. All benchmarks (Karpathy HN: $58 / 930 items / 1 hour; Vercel d0: 17→2 tools, 80%→100% success, 274s→77s) are from public case studies and add credibility without being repo-internal. The canonical pipeline structure and file-system idempotency pattern are universally reusable primitives.
**Trigger**: Starting a new LLM-powered project; evaluating whether a task suits an LLM or traditional code; designing a batch pipeline architecture; choosing single-agent vs multi-agent; estimating token costs before development; structuring agent-assisted development workflow.
**Steps/contract**: Dual-mode — contains a clear procedural 5-phase project planning template (Task Analysis → Manual Validation → Architecture Selection → Cost Estimation → Development Plan, each with numbered sub-steps) plus reference-style content (task-model fit tables, canonical pipeline stages, file-system state machine pattern, architectural reduction guidance). The manual prototype step and canonical pipeline (acquire → prepare → process → parse → render, with explicit deterministic/non-deterministic labeling) are independently portable procedures. 10-item guidelines checklist; 8-item gotchas list.
**Strip**: `## Integration` section (cross-links to sibling skills: context-fundamentals, tool-design, multi-agent-patterns, evaluation, context-compression — dead links outside source repo); `## References` sub-section (relative paths `./references/case-studies.md`, `./references/pipeline-patterns.md` — not portable); `## Skill Metadata` block (version/author/dates); inline "See `<skill-name>` skill for…" cross-references embedded in body text (2 occurrences: multi-agent-patterns, tool-design). External URLs (GitHub, Vercel blog) can be retained as footnotes.
**Structure/format**: Excellent — two decision tables (proceed/stop) with rationale columns; fenced code blocks for canonical pipeline and directory structure; explicit stage-by-stage pipeline spec with determinism labeling; 5-phase project template with numbered sub-steps per phase; two worked examples (Karpathy HN, Vercel d0); 10-item guidelines; 8-item gotchas. Highly scannable and ready-to-copy.
**Notes**: High-value portable candidate. Standout extracts worth preserving verbatim in any derived SOP: (a) **task-model fit tables** (proceed/stop with rationale column) — the most actionable decision gate in the skill; (b) **canonical 5-stage pipeline** (acquire → prepare → process → parse → render) with explicit deterministic/non-deterministic labeling — elegant separation of concerns; (c) **file-system state machine pattern** with per-item directory structure — natural idempotency without a database, debuggable by inspection; (d) **cost formula** (items × tokens_per_item × price + 20–30% buffer) — simple and immediately deployable; (e) **manual prototype step** as a mandatory validation gate before any automation — high-signal standalone heuristic; (f) **architectural reduction principle** with Vercel d0 benchmark — overlaps with `tool-design` skill's consolidation section, cross-note rather than merge; (g) **8-item gotchas** (skipping manual validation, monolithic pipelines, over-constraining the model, premature optimisation, model version lock-in, evaluation-less deployment) — non-obvious and actionable. The "Architectural Reduction" and "Iteration and Refactoring" sections overlap modestly with `tool-design` and `multi-agent-patterns` findings — recommend a cross-reference note in the final SOP rather than deduplication at extraction time.

## skills/memory-systems/SKILL.md
**Type**: Framework comparison + decision guide + layered architecture reference — covers the full spectrum from volatile context to persistent temporal knowledge graphs, with a framework selection table, benchmark data, retrieval strategy matrix, consolidation policy, and error-recovery hierarchy. Not a step-by-step procedural SOP, but contains a concrete escalation path that functions as a decision ladder.
**Portable**: Yes — fully portable at the principles level. All core content (memory layer taxonomy, framework selection criteria, retrieval strategy decision table, consolidation rules, error-recovery patterns, gotchas) is universally applicable to any agent system. Framework-specific code examples (Mem0, Cognee, Zep) use public open-source APIs and are illustrative only.
**Reason**: No project-specific tooling or codebase assumptions anywhere in the skill. The escalation path (file-system → vector store → graph → full control), the "invalidate but don't discard" consolidation principle, and the just-in-time retrieval + attention-favored-placement guideline are framework-agnostic design rules. Benchmark figures are sourced from published papers and open benchmarks (LoCoMo, LongMemEval, DMR, HotPotQA) — they are time-sensitive signals but not proprietary. Gotchas section encodes hard-won counter-intuitive findings applicable to any memory architecture.
**Trigger**: Building agents that must persist knowledge across sessions; choosing between memory frameworks; implementing knowledge graphs or temporal KGs; designing retrieval strategies; evaluating memory quality with benchmarks (LoCoMo, LongMemEval, DMR); agents needing multi-hop reasoning, entity tracking, or time-travel queries.
**Steps/contract**: Not a procedural SOP — structured as a decision guide with an escalation ladder as the closest procedural sequence: (1) prototype with file-system memory; (2) scale to Mem0/vector store for semantic search + multi-tenant isolation; (3) add Zep/Graphiti/Cognee for relationship traversal and temporal validity; (4) use Letta/Cognee for full agent self-management. Supporting framework: framework selection table → memory layer taxonomy (working/short-term/long-term/entity/temporal KG) → retrieval strategy matrix → consolidation policy → error-recovery hierarchy (empty retrieval → stale results → conflicting facts → storage failure) → 8-item guidelines checklist → 8-item gotchas list.
**Strip**: `## Integration` section (cross-links to sibling skills: multi-agent-patterns, context-optimization, evaluation — dead links outside source repo); `## References` internal sub-section (`./references/implementation.md` relative path); `## Skill Metadata` block (version/author/dates); note benchmark figures (specific percentages) as version-dated signals that may not reflect current framework state — recommend adding a "benchmarks current as of [date]" caveat rather than deleting.
**Structure/format**: Excellent — H2/H3 hierarchy; four decision tables (framework landscape, benchmark comparison, memory layers with when-to-use, retrieval strategies with limitations); three code examples in distinct languages/APIs; 8 numbered guidelines; 8 numbered gotchas. Dense but well-layered and scannable. Approximately 2,400 tokens — suitable as a loaded reference rather than always-on context.
**Notes**: Strong portable candidate. Standout extracts worth preserving verbatim in any derived SOP: (a) **"reliability beats tool complexity" principle** — Letta's filesystem agent scored 74% on LoCoMo vs Mem0's 68.5% — the single most important counter-intuitive benchmark in the skill; (b) **memory escalation ladder** (file-system → vector store → graph → full control) as a concrete, order-dependent decision path; (c) **"invalidate but do not discard" consolidation rule** — prevents temporal queries from losing historical state; (d) **just-in-time retrieval + attention-favored position placement** (beginning or end of context) — directly connects to context-fundamentals U-curve; (e) **8-item gotchas** — all non-obvious: stuffing context (expensive + degrades attention), ignoring temporal validity (stale facts poison silently), over-engineering early, no consolidation threshold, embedding model mismatch (re-embed on model change), graph schema rigidity (prefer flexible property bags), stale memory poisoning (confidence decay / surface contradictions), memory-context mismatch (domain metadata filtering); (f) **error-recovery hierarchy** (4 failure modes with ordered mitigations) — portable pattern for any memory-backed agent. The temporal validity pattern (valid_from/valid_until with "prefer most recent valid_from on conflict") is a portable data-model decision applicable regardless of framework. Slight overlap with `context-compression` on consolidation trigger strategy (both recommend 70–80% threshold) — worth a cross-note rather than merging. Note benchmark table is the most time-sensitive section — frame as directional signals, not definitive rankings.

## docs/skills-improvement-analysis.md
**Type**: Internal meta-analysis / skills audit report — retrospective evaluation of this specific repo's 13 skills against lessons from an Anthropic article on building Claude Code skills. Identifies gaps (knowledge-first vs action-first, missing gotchas, no on-demand hooks, no config pattern, no measurement infrastructure) and ranks them by impact across three tiers of recommendations.
**Portable**: No
**Reason**: Nearly every sentence references repo-specific internals: named skills (`context-compression`, `multi-agent-patterns`, etc.), commit hashes (`c847b20`), specific directory structures (`scripts/`, `references/`, `sandbox/`), score statistics computed over this repo's 13 files, and recommendations phrased as "update template/SKILL.md" or "add a `sandbox/` directory." The document is a point-in-time audit of this repo's compliance, not a reusable methodology. The underlying principles it surfaces (skills should be action-first, gotchas are highest-signal, hooks convert knowledge to guardrails) are all secondhand from the cited Anthropic article, not original to this document.
**Trigger**: N/A — not a procedural SOP; no trigger condition applies.
**Steps/contract**: None — structured as gap analysis → ranked recommendations → summary compliance table. No input/output contract or sequenced steps.
**Strip**: Everything — the entire document is repo-internal commentary. No section survives repo context removal.
**Structure/format**: Well-organised (gap list ordered by impact, tiered recommendation table, audit summary table with scores) but the structure is scaffolding for repo-specific findings, not a reusable format.
**Notes**: Not a portable SOP candidate. If the source Anthropic article ("Lessons from Building Claude Code: How We Use Skills") is accessible, that would be the higher-value primary source for any "what makes a good skill" SOP. The one meta-principle worth noting for cross-SOP framing: the knowledge-skill vs action-skill distinction (skills that give Claude things to *do* outperform skills that give Claude things to *know*) is a transferable design heuristic — but it should be sourced from the Anthropic article, not from this derivative analysis.

## .references/context-engineering/SKILL.md
**Type**: Collection-level index / skill map  
**Portable**: No — as a standalone SOP  
**Reason**: This is a navigation manifest for a 13-skill sub-collection, not a procedural document. It summarises the *topics* covered (context fundamentals, multi-agent patterns, memory systems, tool design, etc.) but contains no step-by-step procedure. The summaries of each skill area are high-quality concept prose but they are condensed survey text, not executable guidance.  
**Trigger**: N/A — acts as a router to individual sub-skills  
**Steps/contract**: None defined; section headings describe concept areas, not ordered actions  
**Strip**: Metadata footer (version/date/author), internal relative links (all point to sub-skills not present in this audit batch), "Integration" boilerplate  
**Structure/format**: YAML frontmatter → titled sections per skill area → references list  
**Notes**: Individual sub-skills (`skills/context-fundamentals/SKILL.md`, `skills/multi-agent-patterns/SKILL.md`, etc.) are the real candidates for SOP extraction. The concept summaries here (lost-in-middle, tokens-per-task, supervisor vs. swarm patterns, filesystem-as-memory) are genuinely portable mental models that could seed an "agent-context-engineering" SOP if written as guidance rather than survey prose.

## .references/context-engineering/CLAUDE.md
**Type**: Repo-specific contributor guide  
**Portable**: Partially — the skill-authoring rules section is portable; the rest is not  
**Reason**: Build commands, file paths, plugin manifest locations, and example project tooling are all repo-internal. The "Skill Authoring Rules" block (8 numbered rules) and "Key Design Principles" block are genuinely portable — they encode craft knowledge about writing LLM skill files that applies to any skill-authoring context.  
**Trigger**: Creating or editing a `SKILL.md` file; adding skills to any collection  
**Steps/contract**: Skill authoring rules are an unordered checklist (not a sequential procedure), covering: 500-line cap, required YAML frontmatter, lowercase-hyphen folder names, third-person prose, platform-agnosticism, token-consciousness, mandatory Gotchas section, manifest update obligation  
**Strip**: Everything under "Project Overview", "Repository Structure", "Build & Test Commands", "Plugin Architecture"; retain only "Skill Authoring Rules" and "Key Design Principles"  
**Structure/format**: H2 sections, numbered lists for rules, bullet lists for principles  
**Notes**: The "write in third person" and "challenge each paragraph for token cost" rules are high-signal and non-obvious. The "Include a Gotchas section" rule with its rationale ("highest-signal content in any skill") is directly portable as a skill-quality standard. These 8 rules + 4 design principles would make a compact portable SOP for skill authoring.

## .references/context-engineering/template/SKILL.md
**Type**: Canonical skill-authoring template  
**Portable**: Yes — highly portable as an SOP artifact  
**Reason**: This is a complete, opinionated template that encodes the *procedure* for writing a skill file. It defines required sections, quality criteria per section, freedom-level guidance for specificity, and a mandatory Gotchas section. It is self-contained, platform-agnostic, and requires no context about the source repo to use.  
**Trigger**: Creating a new skill from scratch; restructuring an existing skill; reviewing a skill for quality  
**Steps/contract**: Implied sequential structure — YAML frontmatter → description paragraph → When to Activate → Core Concepts → Detailed Topics → Practical Guidance → Examples → Guidelines → Gotchas → Integration → References → Skill Metadata. Each section has inline authoring criteria (e.g., "third person", "<500 lines", "challenge each paragraph").  
**Strip**: Placeholder text ("Skill Name", "[Date]", "[Author or Attribution]") and the opening meta-instruction about the template itself; the structural sections and inline guidance should be preserved verbatim  
**Structure/format**: YAML frontmatter, H1 title, H2 canonical sections, H3 subsections for topics, numbered Gotchas, flat bullet Integration list, metadata footer  
**Notes**: This is the strongest portable artifact in the batch. It could be adopted directly as the canonical `SKILL.md` template for this repo with minimal adaptation — the only change needed is updating the Integration section convention (this repo uses relative links; the template recommends plain-text names). The inline authoring guidance embedded in each section (especially the freedom-level tiering and the Gotchas rationale) is unusually high-signal.

## skills/bdi-mental-states/SKILL.md
**Type**: Highly domain-specific reference skill for BDI (Belief-Desire-Intention) cognitive architecture — covers ontology modeling, RDF/Turtle serialization, SPARQL competency queries, T2B2T bidirectional pipeline, temporal mental state management, neuro-symbolic LAG integration, and deployment to formal BDI frameworks (SEMAS, JADE, JADEX).
**Portable**: Partially — the conceptual BDI framework (beliefs ground world states, desires link to beliefs, intentions fulfill desires and specify plans), the T2B2T pipeline principle, justification-as-first-class-design-constraint, temporal bounding of all mental states, and the gotchas list are portable abstractions. Everything touching RDF/Turtle serialization, SPARQL, SEMAS Prolog rules, specific ontology libraries, and framework-specific APIs is implementation-narrow and not portable outside formal knowledge-representation toolchains.
**Reason**: Core BDI design principles (separate persistent mental states from causal processes; ground every belief in a world state; attach justifications for traceability; assign validity intervals for conflict detection; cap belief chains at 3 levels) apply to any agent that must reason about context through a cognitive lens. The T2B2T pipeline is a generic design pattern (consume external context → internalize as beliefs → project conclusions back). But the bulk of the skill — Turtle code blocks, SPARQL queries, SEMAS Prolog rules, C4 notation table, ArchiMate/UML guidance — is specific to the RDF/Linked Data toolchain and irrelevant to the vast majority of agent projects.
**Trigger**: Accurately narrow — "model agent mental states", "implement BDI architecture", "create belief-desire-intention models", "transform RDF to beliefs", "build cognitive agent", mentions BDI ontology, mental state modeling, rational agency, or neuro-symbolic AI integration.
**Steps/contract**: Not a procedural SOP. Closest to a procedure is the T2B2T paradigm: Phase 1 — translate external RDF/context into belief instances via BeliefProcess triggers; Phase 2 — after BDI deliberation, project intentions/plan results back into output assertions via bringsAbout. The cognitive chain (belief → desire → intention via motivates/fulfils properties) is a modeling schema, not a task flow. Guidelines section provides 10 ontological design constraints rather than ordered task steps.
**Strip**: All RDF/Turtle code blocks (illustrative of one toolchain only); SPARQL competency queries section; SEMAS Prolog rule syntax; C4 notation selection table (ArchiMate/UML); Python LAG integration snippet (replace with pseudocode if retained at all); internal `./references/*.md` relative links (bdi-ontology-core, rdf-examples, sparql-competency, framework-integration); Skill Metadata footer; primary source academic citations (retain as footnotes, not required reading).
**Structure/format**: Well-organized — clear H2 sections (Core Concepts → Mental Reality Architecture → Cognitive Chain Pattern → World State Grounding → Goal-Directed Planning → T2B2T → Notation → Justification → Temporal Dimensions → Compositional Mental Entities → Integration → Guidelines → Competency Questions → Gotchas). Gotchas list (7 items) is the highest-signal section. Guidelines checklist (10 items) is design-constraint style. Code blocks are illustrative but toolchain-specific and voluminous relative to the conceptual content they support.
**Notes**: Very low breadth-portability — suitable only for teams using formal BDI cognitive architectures with RDF/Linked Data tooling. Portable residue is modest but usable: (a) BDI taxonomy as a conceptual frame for belief/desire/intention separation in any reasoning agent; (b) T2B2T principle as a generic bidirectional pipeline metaphor (consume context → internalize → project outputs); (c) "every belief needs a world-state reference, a justification, and a temporal validity interval" as a three-part memory-design checklist; (d) gotchas 2 (missing temporal bounds allow stale beliefs to persist indefinitely), 4 (implicit justifications make mental states unauditable), 5 (skipping the plan layer removes strategy reuse), 6 (start with ≤10 core classes, expand later), 7 (cap belief chains at 3 levels — deeper chains explode LLM inference cost without quality gain). Overlaps with memory-systems skill on temporal validity and belief invalidation — a cross-note is preferred over duplication. Not a top-tier SOP promotion candidate — narrow audience and too much domain-specific implementation detail to distill cleanly. Best treated as a specialist reference for BDI/neuro-symbolic projects, not a general-purpose agent engineering SOP.

## docs/agentskills.md
**Type**: Multi-section normative reference — concatenated docs-site pages covering the Agent Skills format spec, client integration guide, and authoring best practices. Meta-documentation: describes the very format used by all skills in this collection.
**Portable**: Partially. The authoring best practices section is fully portable — conciseness principle, degrees-of-freedom framework, progressive disclosure patterns, feedback loops, eval-first methodology, anti-patterns list, and 24-item authoring checklist apply to writing any LLM instruction document, not just SKILL.md files. The formal specification (field constraints, naming rules) is portable as a normative reference. The client-integration section is only relevant for agent platform implementers.
**Reason**: Authoring best practices carry universally applicable principles: "only add context Claude doesn't already have"; degrees-of-freedom as a narrow-bridge/open-field decision (fragility and variability determine specificity); progressive disclosure with one-level-deep reference chains; validate-before-execute patterns; evaluation-first development (build evals *before* writing docs); iterative authoring with a generator/tester split (Claude A writes, Claude B tests). These apply to any skill, prompt, or SOP authoring context regardless of platform.
**Trigger**: Writing or auditing a SKILL.md or equivalent instruction document; establishing quality standards for a skills library; onboarding skill authors; reviewing existing skills for over-explanation, time-sensitive content, or excessive nesting; building an agent that discovers and activates skills at runtime.
**Steps/contract**: No single procedural SOP — document is structured as reference + best practices. Closest procedural content: (1) 24-item authoring checklist (Core quality / Code and scripts / Testing categories); (2) eval-driven development sequence (run without skill → document failures → create 3 evals → establish baseline → write minimal instructions → iterate); (3) iterative Claude A/B methodology (complete task normally → identify reusable pattern → ask Claude A to create skill → test with Claude B → refine based on observed failures). The degrees-of-freedom decision framework (high / medium / low freedom with explicit criteria) is independently portable.
**Strip**: Overview / adoption section (marketing; lists adopting products — adds no authoring signal); "Integrate skills into your agent" section (client-side plumbing, irrelevant to skill authors); evaluation JSON structure note ("we do not currently provide a built-in way to run these evaluations" — implementation caveat, not guidance); all `github.com/anthropics/skills` external links; inline product-comparison mentions (Haiku/Sonnet/Opus testing note references specific tiers — keep the principle "test with all models you plan to use", strip the tier names); "Open development" and "Adoption" lists.
**Structure/format**: Poor as a unit — multiple docs-site pages concatenated with no top-level H1 page breaks; header levels are inconsistent between sections (H1 used for page titles mid-document). High-value content is fragmented across pages. For extraction, should be restructured as: (1) Spec reference (field constraints, directory layout), (2) Authoring principles (conciseness, degrees-of-freedom, progressive disclosure), (3) Pattern catalogue (template / examples / conditional workflow / feedback loop), (4) Anti-patterns, (5) Authoring checklist. Checklist at document end is well-formatted and ready to copy.
**Notes**: This is a meta-SOP — a SOP for writing SOPs. Its primary extraction value is the authoring best practices content, not the format spec (which is already embedded in every SKILL.md frontmatter via the fields themselves). Three standout portable extractions: (a) **degrees-of-freedom decision framework** — the narrow-bridge/open-field analogy maps fragility + variability to specificity level; provides three concrete tiers (text instructions / pseudocode-with-params / exact-script-no-flags) with explicit "use when" criteria — immediately applicable to any instruction-writing context; (b) **eval-first methodology** — "create evaluations BEFORE writing extensive documentation" with the specific rationale (solve real gaps, not imagined ones); the Claude A/B iterative pattern (one instance writes, a fresh instance tests) is a practical workflow primitive; (c) **progressive disclosure authoring rules** — keep references one level deep, add table of contents for files >100 lines, main SKILL.md under 500 lines, push detailed content to separate reference files loaded on demand — directly applicable to any long-form SOP. The "concise is key / default assumption is Claude is already smart" principle is the single most important reframe and should survive verbatim in any derived skill-authoring SOP. Slight overlap with `filesystem-context` on the dynamic skill loading pattern — that skill describes the *runtime* behaviour that these authoring guidelines are designed to support.

## skills/filesystem-context/references/implementation-patterns.md
**Type**: Implementation reference — six Python class patterns (ScratchPadManager, AgentPlan, AgentWorkspace, SkillLoader, TerminalCapture, PreferenceStore) plus file-layout diagram and token-accounting benchmarks. **Portable**: Partially — the *concepts* (offload large outputs to files, persist plans as YAML, isolate per-agent workspaces, load skills on demand, guard self-modification) are framework-agnostic SOPs; the Python code is illustrative scaffolding. **Reason**: The patterns encode real agent-loop discipline (token budgeting, scratch-pad hygiene, workspace isolation) that applies regardless of language or harness. **Strip**: All Python class implementations — keep the conceptual pattern names, the file-organisation tree, and the four token-accounting metrics with target benchmarks. **Notes**: The `PreferenceStore` self-modification guard (MAX_ENTRIES, MAX_VALUE_LENGTH) is a rare concrete safety pattern worth preserving verbatim in prose form.

## skills/hosted-agents/references/infrastructure-patterns.md
**Type**: Platform integration reference — Modal sandbox lifecycle, Cloudflare Durable Objects session state, warm-pool management, Slack bot classification, Chrome extension DOM extraction, multiplayer authorship, metrics/monitoring, sandbox security config. **Portable**: Low — almost every section is bound to a specific vendor (Modal, Cloudflare DO, Slack Bolt, GitHub Apps OAuth). **Reason**: The infrastructure topology (image build → warm pool → session DO → event stream) is architecturally instructive but not extractable as a vendor-neutral SOP without heavy rewriting. **Strip**: All vendor-specific code (Modal, CF DO, Slack Bolt, Chrome API); retain the warm-pool concept, multiplayer authorship-tracking pattern, and the `SessionMetrics` fields as an adoption-metrics SOP. **Notes**: The `SandboxSecurityConfig` allowed-hosts / resource-limits block is a useful security-policy template; worth porting as a prose checklist.

## skills/evaluation/references/metrics.md
**Type**: Evaluation rubric reference — five scored dimensions (factual accuracy 30 %, completeness 25 %, citation accuracy 15 %, source quality 10 %, tool efficiency 20 %) each with five levels (0 / 0.3 / 0.6 / 0.8 / 1.0), plus weighted scorer, test-set manager, evaluation runner, and 1 %-sample production monitor. **Portable**: High — the dimension taxonomy, five-level scale, and pass threshold (≥ 0.7) are framework-agnostic and directly adoptable. **Reason**: Rubric design and weighting rationale encode durable evaluation hygiene applicable to any LLM-output quality process. **Strip**: Python `TestSet`, `EvaluationRunner`, and `ProductionMonitor` class bodies — keep the dimension table with weights and level descriptors, and the sampling + alerting thresholds (warn 0.85, critical 0.70) as prose. **Notes**: Tool-efficiency dimension (20 % weight) is underrepresented in most public rubrics — highlight as a distinguishing SOP element.

## skills/advanced-evaluation/references/bias-mitigation.md
**Type**: Bias-mitigation reference — five named LLM-as-a-Judge biases (position, length, self-enhancement, verbosity, authority) each with problem statement, primary mitigation, secondary mitigation, and code example; plus aggregate `BiasMonitor` and summary table. **Portable**: High — the bias taxonomy and mitigation strategies are model-agnostic and apply to any pairwise or rubric-based evaluation setup. **Reason**: The summary table (bias → primary mitigation → secondary mitigation → detection method) is a ready-made SOP checklist. Position-swap protocol and cross-model evaluator selection are concrete, actionable rules. **Strip**: Python implementation bodies (`position_swap_comparison`, `multi_shuffle_comparison`, `BiasMonitor`); retain the summary table verbatim, the explicit anti-length-bias prompt text, and the cross-model evaluator selection rule as prose. **Notes**: Self-enhancement bias section references specific model names (gpt-5.2, claude-4-5-sonnet) that will date quickly — abstract to "different model family" in any SOP extraction.

## skills/context-fundamentals/references/context-components.md
**Type**: Technical reference — foundational patterns for all context components (system prompt structure, tool schemas, document chunking, message history, token budget allocation, progressive disclosure).
**Portable**: Yes — all patterns are model-agnostic and framework-agnostic; nothing repo-specific.
**Reason**: Covers the full stack of "what goes in context and how to shape each piece" with concrete before/after examples (altitude calibration, weak vs. strong tool descriptions). Directly maps to SOP rules.
**Strip**: Pseudocode implementations (chunking loop, skill-activation loop) — illustrative only; the underlying principles are what matters.
**Notes**: Token-budget table (system prompt 500-2000 / tool defs 100-500 each / docs variable) is directly reusable as a budgeting heuristic. Overlaps with optimization_techniques.md budget table — merge on synthesis pass.

## skills/context-degradation/references/patterns.md
**Type**: Technical reference — diagnostics and mitigations for context degradation (lost-in-middle, context poisoning, distraction scoring, health monitoring, recovery truncation).
**Portable**: Partial — the conceptual patterns (U-shaped attention curve, truncation priority order, alert thresholds) are portable; the class-based monitoring code assumes access to raw attention weights which most hosted APIs don't expose.
**Reason**: Lost-in-middle detection logic and the 5-category truncation priority (system → recent turns → critical docs → summarise → truncate oldest) are strong portable SOPs. Health-monitor implementation is illustrative, not deployable as-is.
**Strip**: `HallucinationTracker`, `ContextHealthMonitor`, and `analyze_error_propagation` Python classes — useful as concept illustrations but not extractable as runnable code.
**Notes**: Alert thresholds (70% warning / 90% critical, 30% relevance floor) are directly actionable as config constants. Recovery truncation ordering is the most distillable item here.

## skills/context-compression/references/evaluation-framework.md
**Type**: Evaluation framework — complete rubric for measuring context compression quality across 6 dimensions using 4 probe types and an LLM judge.
**Portable**: Yes — probe types (recall / artifact / continuation / decision), 0-5 scoring rubrics, and LLM judge config are fully model- and framework-agnostic.
**Reason**: The rubric dimensions map directly to what any compression SOP needs to optimise for. Benchmark data (36k+ messages) gives empirical backing to the "anchored iterative > regenerative > opaque" ranking.
**Strip**: Benchmark result table numbers may age; strip specific scores from SOP prose but keep the ranking and the "artifact trail is universally weak" finding.
**Notes**: Key actionable insight — artifact trail (files created/modified) scores 2.19–2.45 across all methods, far below other dimensions. Any compression SOP must include explicit artifact-state preservation. LLM judge system prompt is ready-to-use.

## skills/context-optimization/references/optimization_techniques.md
**Type**: Technical reference — full optimization lifecycle: compaction strategies, observation masking, KV-cache prefix stability, sub-agent partitioning, decision framework, common pitfalls, monitoring, performance benchmarks.
**Portable**: Yes — decision framework (when / what / evaluate), common pitfalls, KV-cache design rules, and compaction thresholds are all model-agnostic.
**Reason**: The "when to optimize" decision tree and "what to apply based on which component dominates" table are directly distillable as SOP decision rules. KV-cache prefix-stability guidance is concrete and widely applicable.
**Strip**: `OptimizingAgent` and `MemoryAwareOptimizer` integration-code classes — framework-specific scaffolding, not portable.
**Notes**: Performance benchmark targets (50-70% token reduction, <5% quality degradation, 70%+ cache hit rate) are useful as acceptance criteria in a compression SOP. "Common Pitfalls" section (over-aggressive compaction, masking critical observations, ignoring attention distribution, premature optimization) maps 1:1 to SOP guardrails.

## skills/advanced-evaluation/references/evaluation-pipeline.md
**Type**: Reference diagram — ASCII visual of a 4-stage eval pipeline (Criteria Loader → Primary Scorer → Bias Mitigation → Confidence Scoring) with brief stage descriptions.
**Portable**: Partially — the stage sequence and names are a solid portable pattern; the diagram itself is decorative.
**Reason**: The four-stage decomposition (load rubrics, score, debias, calibrate confidence) is a clean reusable mental model applicable to any LLM-as-judge setup. The ASCII art adds no instructional value beyond the prose.
**Strip**: The ASCII diagram block; keep the stage list as a numbered sequence.
**Notes**: Best absorbed as the "pipeline skeleton" underpinning the fuller patterns in `implementation-patterns.md`. Very thin on its own — only useful paired with that file.

## skills/advanced-evaluation/references/implementation-patterns.md
**Type**: Reference implementation — five Python patterns for production LLM evaluation: structured pipeline, hierarchical eval, Panel-of-LLM-Judges (PoLL), confidence calibration, output formatting; plus error-handling and testing code.
**Portable**: Mostly — the architectural patterns (PoLL, position-swap bias mitigation, hierarchical cheap→expensive routing, structured output dataclasses) are language-agnostic SOPs. The Python code is illustrative scaffolding.
**Reason**: Each named pattern encodes a distinct, transferable decision rule (e.g., "always swap positions for pairwise comparison; treat inconsistency as a tie"). These translate directly to skill guidance regardless of language.
**Strip**: Model name literals (`gpt-5.2` appears twice as both "cheap" and "capable" — clearly placeholder/typo); boilerplate retry/backoff code duplicated in `pipeline-patterns.md`; raw pytest stubs. Keep pattern prose and the bias-mitigation logic.
**Notes**: `gpt-5.2` used for both cheap and expensive model in Pattern 2 is a copy-paste error in the source — flag for correction if promoting to a skill.

## skills/advanced-evaluation/references/metrics-guide.md
**Type**: Reference guide — metric selection catalogue covering classification (Precision/Recall/F1), agreement (Cohen's κ, weighted κ), correlation (Spearman, Kendall, Pearson), pairwise metrics, a decision tree, three use-case recipes, a threshold table, and a reporting template.
**Portable**: High — metric definitions, interpretation thresholds, and the decision tree are fully language-agnostic. The reporting template is directly reusable as a skill output format.
**Reason**: The decision tree (`binary → P/R/F1/κ; ordinal → Spearman/weighted-κ; pairwise → agreement rate + position consistency`) is a precise, actionable SOP. The threshold table (Good/Acceptable/Concerning) gives concrete pass/fail criteria for eval system validation.
**Strip**: Python `sklearn`/`scipy` code snippets (language-specific plumbing); the `QualityMonitor` class (too implementation-specific). Keep decision tree, threshold table, warning signs, and reporting template verbatim.
**Notes**: The reporting template Markdown block is the single most portable artifact in the whole advanced-evaluation reference set — strong candidate for direct inclusion in a skill's output spec.

## skills/project-development/references/pipeline-patterns.md
**Type**: Reference implementation — comprehensive 5-stage LLM pipeline (acquire → prepare → process → parse → render) covering file-system state management, parallelism, structured output/parsing, error handling, cost estimation, CLI scaffolding, rendering, and checkpoint/resume.
**Portable**: High for architecture; Low for code verbatim — the stage model, idempotency table, state-checking/clean-from-stage pattern, and checkpoint/resume logic are all broadly applicable SOPs. Python code is illustrative.
**Reason**: The canonical stage table (which stages are deterministic, expensive, parallelisable, idempotent) is a high-signal mental model for any LLM batch pipeline. The "only Process involves LLM calls; all other stages are deterministic" principle is a strong portable design rule.
**Strip**: `tiktoken`-specific cost estimation code (tool-specific); the full CLI `argparse` block (boilerplate); HTML rendering helpers. Keep the stage characteristics table, directory structure pattern, state-check/clean logic, checkpoint class interface, and batch-size guidance prose.
**Notes**: The `needs_processing` / `clean_from_stage` pair is a clean SOP pattern worth preserving as pseudocode. Overlaps significantly with retry/backoff content in `implementation-patterns.md` — one canonical version should be kept if both are promoted.

## skills/multi-agent-patterns/references/frameworks.md
**Type**: Framework-coupled implementation reference — LangGraph, AutoGen, CrewAI code examples for supervisor, swarm, and hierarchical patterns, plus context isolation, consensus (weighted voting, debate), circuit breaker, and checkpoint/resume.
**Portable**: Partially — the *patterns* (supervisor routing, full-vs-minimal context delegation, file-system coordination, debate protocol, circuit breaker) are framework-agnostic; the code snippets are not.
**Reason**: Conceptual patterns survive framework churn; Python/LangGraph syntax does not. A portable SOP should extract the pattern logic and strip the library-specific wiring.
**Strip**: All import statements, class bodies tied to LangGraph `StateGraph` / AutoGen `GroupChat` / CrewAI APIs; boilerplate `if "research" in task.lower()` routing examples.
**Notes**: The file-system coordination pattern (shared JSON + lock files) and the checkpoint/resume pattern are directly actionable in any language — worth promoting verbatim as portable primitives. Consensus mechanisms (weighted vote, structured debate with convergence check) are also self-contained and portable.

## skills/memory-systems/references/implementation.md
**Type**: Python implementation reference — VectorStore (cosine similarity, metadata filtering), PropertyGraph (entity registry, Cypher-like query), TemporalKnowledgeGraph, MemoryConsolidator (dedup + merge), MemoryContextIntegrator (entity extraction → retrieval → token budget), and library quick-starts (Mem0, Graphiti, Cognee).
**Portable**: Partially — the *architecture decisions* (vector + graph as complementary tiers, temporal validity on edges, entity-scoped retrieval, consolidation trigger threshold, token-budget truncation) are portable; the NumPy/uuid/Graphiti SDK code is not.
**Reason**: The concepts (episodic vs. semantic memory, temporal scoping, consolidation before context overflow) are broadly applicable SOP content. The library quick-starts are version-sensitive and will rot.
**Strip**: All `import numpy`, `import uuid`, Mem0/Graphiti/Cognee SDK calls, the deterministic pseudo-embedding (`np.random.seed(hash(text))`), and the simplified Cypher parser stub.
**Notes**: The `MemoryContextIntegrator.build_context()` flow (extract entities → retrieve → format → enforce token limit) is a clean, reusable SOP step sequence worth extracting verbatim as prose. The `_time_range_key` / `valid_until=None → "infinity"` convention for open-ended temporal facts is a concrete portable convention.

## skills/tool-design/references/best_practices.md
**Type**: Design principles document — tool description engineering (4-question framework), naming conventions, dual-audience error messages, token/accuracy response format trade-off, tool collection consolidation, agent testing pattern, anti-patterns checklist.
**Portable**: High — almost entirely framework-agnostic. Applies to any tool/function exposed to a language model.
**Reason**: The guidance is grounded in LLM behavior (models infer intent from descriptions, ambiguity becomes failure mode) rather than any specific library. The 4-question framework, dual-audience error design, and consolidation heuristic are directly transferable to skill-writing and MCP server design.
**Strip**: The Python `get_customer_response(format)` code snippet (illustrative only; the prose already captures the principle). The JSON error-structure example can stay — it is a concrete format spec, not library code.
**Notes**: The "if a human engineer cannot definitively say which tool to use, an agent cannot be expected to" rule-of-thumb is a high-signal heuristic worth quoting in any tool-design SOP. The pre-deployment checklist (7 items) is ready to promote as-is.

## skills/tool-design/references/architectural_reduction.md
**Type**: Evidence-based design philosophy document — production case study (text-to-SQL, 17 tools → 2 tools), benchmark table (3.5× faster, +20% success, −37% tokens, −42% steps), rationale (file systems as stable abstractions, tools constraining reasoning, documentation > tooling), prerequisites, failure modes, and evaluation framework.
**Portable**: High — the principle (prefer primitive + well-documented data layer over many specialised tools) applies to any agent architecture regardless of stack.
**Reason**: Backed by concrete numbers from a real production system. The "addition by subtraction" and "trust model reasoning" principles directly inform skill and agent design decisions in this repo.
**Strip**: Vercel-SDK-specific code (`from vercel_sandbox import Sandbox`, `ToolLoopAgent` import). The sandbox pattern is illustrative; the principle is the portable artifact.
**Notes**: The four prerequisites (high doc quality, sufficient model capability, safety constraints permit, navigable domain) and four failure conditions (messy data layer, required specialist knowledge, compliance restrictions, genuinely complex workflows) together form a ready-made decision matrix for a "should I reduce?" SOP. The evaluation framework (5 questions: maintenance overhead, failure analysis, doc quality, constraint necessity, model capability) can be lifted directly into a checklist skill.

## skills/bdi-mental-states/references/bdi-ontology-core.md
**Type**: OWL/RDF ontology reference — class hierarchy, object properties, OWL restrictions, and DOLCE alignment for Belief-Desire-Intention mental state modeling.
**Portable**: No.
**Reason**: Entirely tied to the `bdi:` namespace and FOSSR academic ontology project. The OWL restrictions and DOLCE alignment are implementation details for a specific formal-ontology research codebase, not transferable agent-skill patterns.
**Strip**: Yes — no SOP or policy extractable; pure schema reference for a niche academic framework.
**Notes**: Could inform a theoretical "model agent cognition as BDI" skill, but the content itself is a namespace spec, not a skill. Nothing to promote.

## skills/bdi-mental-states/references/framework-integration.md
**Type**: Integration guide — maps BDI ontology constructs to SEMAS production rules, JADE/JADEX agent platforms, RDF triple stores, and FIPA ACL messages. Includes Python/Java code samples.
**Portable**: No.
**Reason**: All patterns are tightly coupled to the `bdi:` ontology IRI, SEMAS rule syntax, JADE agent infrastructure, and FIPA ACL — none of which are in scope for general-purpose Claude skills. The LAG (Logic Augmented Generation) pipeline is the only potentially reusable idea, but even that is written against this specific ontology.
**Strip**: Yes — no SOP extractable. The LAG retry-with-validation loop is a generic pattern already covered by self-validation and cot-gate skills.
**Notes**: The ontology-injection + LLM validation loop (LAG section) is a reasonable pattern, but nothing here adds to what existing skills already provide.

## skills/bdi-mental-states/references/sparql-competency.md
**Type**: SPARQL query library — 27 competency questions and 5 validation queries for the BDI ontology, organized by category (motivational chains, mental processes, temporal, justification, world state, multi-agent).
**Portable**: No.
**Reason**: Every query is written against the `bdi:` namespace. The query patterns (temporal filtering, cognitive chain tracing, validation ASK queries) are generically useful SPARQL techniques, but there is no skill or SOP here — just a test harness for one ontology.
**Strip**: Yes — domain-specific test fixtures; no portable policy or workflow pattern.
**Notes**: If a "write SPARQL validation queries for an OWL ontology" skill were ever needed, this is a decent example set, but that skill does not exist and is out of scope.

## researcher/llm-as-a-judge.md
**Type**: Researcher system prompt + evaluation rubric — defines a Principal Research Curator agent persona with a 4-gate / 4-dimension scoring framework (weighted totals, override rules, JSON output schema, worked examples) for triaging context-engineering content.
**Portable**: Yes — moderate.
**Reason**: The rubric structure (gatekeeper binary gates → dimensional scoring → weighted decision → override rules) is a reusable evaluation pattern applicable to any curation or triage task, not just context engineering. The JSON output schema and worked examples (APPROVE / HUMAN_REVIEW / REJECT) are immediately adaptable. The specific domain criteria (CE taxonomy) would need to be swapped for other domains.
**Strip**: No — keep as a reference for the `issue-triage` or `skill-create` skill patterns; the gatekeeper + dimensional scoring template is worth extracting.
**Notes**: Best candidate in the BDI batch for cross-domain reuse. The 4-gate hard-stop + weighted scoring model is a clean, self-contained evaluation SOP. Consider extracting a generic `evaluate-content` or `triage-with-rubric` skill template from this.

## examples/llm-as-judge-skills/prompts/evaluation/pairwise-comparison-prompt.md
**Type**: Prompt template (structured evaluation)
**Portable**: Yes — high
**Reason**: The three-step evaluation scaffold (independent analysis → criterion-by-criterion head-to-head → final determination with confidence score) is domain-agnostic and can apply to any pairwise quality judgment. The position-bias mitigation pattern (swap positions, average, flag inconsistency) is a concrete, reusable operational technique with no repo-specific dependencies.
**Strip**: TypeScript `compareWithPositionSwap` implementation and the "explain the benefits of regular exercise" worked example — both are illustrative scaffolding, not portable SOP content.
**Notes**: Strong candidate. The bias-mitigation protocol alone is worth extracting as a standalone policy. JSON output schema is a useful reference for any LLM-judge integration.

## examples/llm-as-judge-skills/prompts/agent-system/orchestrator-prompt.md
**Type**: System prompt / orchestration protocol
**Portable**: Yes — medium-high
**Reason**: The five-phase workflow (Task Analysis → Agent Assignment → Execution Planning → Execution & Monitoring → Synthesis), the task-delegation template (Agent / Task / Context / Expected Output / Success Criteria), and the three-step error-handling protocol (Assess → Decide → Document) are all portable orchestration SOPs. The dependency-graph notation is a useful communication pattern.
**Strip**: The specific agent roster (Evaluator, Researcher, Writer, Analyst with their exact capability descriptions) and the "LLM evaluation report" worked example — these are project-specific. The JSON status output schema can be genericised.
**Notes**: The task-delegation template and error-handling protocol are the highest-value extractions. The parallelism principle ("Parallel When Possible") and quality-gate principle are worth surfacing explicitly in any multi-agent SOP.

## examples/llm-as-judge-skills/prompts/research/research-synthesis-prompt.md
**Type**: Prompt template (synthesis structure)
**Portable**: Yes — medium
**Reason**: The seven-section synthesis framework (Executive Summary, Key Themes, Findings by Topic, Consensus, Disagreement, Gaps/Limitations, Actionable Insights + Source Quality Assessment) is a strong portable document structure for any multi-source synthesis task. The citation style variants (inline, footnote, endnote) add useful flexibility.
**Strip**: The LLM-evaluation-specific worked example (MT-Bench, Eugene Yan references) and the Handlebars template syntax — these are implementation details, not SOP content.
**Notes**: The "Gaps and Limitations" and "Source Quality Assessment" sections are the differentiating elements vs. a generic summary prompt; they should be preserved in any extraction. The five best-practice rules at the bottom are concise and portable.

## examples/llm-as-judge-skills/skills/llm-evaluator/llm-evaluator.md
**Type**: Reference / knowledge card
**Portable**: Yes — medium (as reference, not procedure)
**Reason**: The scoring-approach taxonomy (Direct Scoring / Pairwise Comparison / Reference-Based) with use-case and reliability guidance is a portable decision aid. The bias taxonomy (position, verbosity, self-enhancement) and mitigation checklist (swap positions, normalise length, PoLL, CoT + n-shot) are directly actionable policies for any LLM-judge setup.
**Strip**: The TypeScript interface definitions (`EvaluatorConfig`, `EvaluationCriteria`, `RubricLevel`) — these are language-specific scaffolding. The "References" section lists papers but adds no extractable SOP content.
**Notes**: Best treated as supporting reference material for a pairwise-comparison or LLM-judge SOP, not as a standalone procedure. The metric selection guidance (classification metrics for binary tasks, correlation metrics for Likert scale) is a useful decision rule worth preserving.

## examples/interleaved-thinking/SKILL.md
**Type**: Agent-debugging tool skill (MiniMax M2.1 reasoning-trace optimizer)
**Portable**: Low
**Reason**: Entire skill is coupled to MiniMax M2.1's interleaved-thinking API, a proprietary Python library, and a CLI tool (`rto`). Nothing runs without that vendor stack.
**Strip**: Yes — vendor machinery; but the six-pattern failure taxonomy (`context_degradation`, `tool_confusion`, `instruction_drift`, `goal_abandonment`, `circular_reasoning`, `premature_conclusion`) is portable as a conceptual reference.
**Notes**: The failure taxonomy is the only extract worth generalising. The optimization-loop concept (run → trace → analyse → patch prompt → re-run) is sound but implemented entirely in proprietary tooling.

## examples/interleaved-thinking/generated_skills/comprehensive-research-agent/SKILL.md
**Type**: Agent behaviour / research-quality best-practices skill (auto-generated by trace optimiser)
**Portable**: High
**Reason**: All guidance is model- and platform-agnostic: error acknowledgment after tool failures, pre-read source ranking, substantive thinking blocks, source-tracking tables, pre-completion checklists, `read_file` over `list_directory` for write verification.
**Strip**: Minimal — remove the "Score Expectations" section (M2.1-specific benchmarks) and the generated-metadata footer. Body stands alone.
**Notes**: Excellent anti-pattern / recommended-practice paired format. Seven named anti-patterns each have a concrete before/after example. Strong candidate for direct promotion or merging into a research-agent SOP.

## examples/digital-brain-skill/SKILL.md
**Type**: Personal productivity / PKM personal-operating-system skill
**Portable**: Low
**Reason**: Requires a pre-populated user-specific file tree (`identity/voice.md`, `network/contacts.jsonl`, `operations/goals.yaml`, etc.). Without that infrastructure the skill has nothing to act on.
**Strip**: Yes — the full skill is user-infrastructure-dependent; the **progressive-disclosure loading pattern** (L1 metadata always, L2 module instructions on-demand, L3 data files as-needed) is a reusable architectural idea worth extracting.
**Notes**: Progressive disclosure architecture is the one portable SOP fragment. Content-creation workflow and pre-meeting-prep checklists are useful templates but only if the user adopts the matching file layout.

## examples/book-sft-pipeline/SKILL.md
**Type**: ML pipeline skill (ePub → segmentation → instruction generation → LoRA SFT)
**Portable**: Low-medium
**Reason**: Extraction/segmentation heuristics (150-400 word paragraph-boundary chunks with overlap) and instruction-diversity principles (15+ templates, 5+ system prompts, base-not-instruct model) are broadly applicable; training phase is Tinker-platform-specific.
**Strip**: Partially — strip Tinker API code and the Tinker-specific config block; keep segmentation algorithm, prompt-diversity rationale, known-issues section, and modern-scenario validation test.
**Notes**: Unusually strong "why" explanations (base vs instruct, chunk size tradeoffs, template memorisation risk). Known-issues section (character-name leakage, parroting, fragmentation) is high-signal. The modern-scenario out-of-distribution validation test is a portable evaluation pattern.

## examples/llm-as-judge-skills/agents/orchestrator-agent/orchestrator-agent.md
**Type**: Agent definition (TypeScript) + embedded system-prompt + orchestration pattern diagrams
**Portable**: High (principles and patterns); Low (code)
**Reason**: The orchestration principles baked into the `instructions` string are framework-agnostic and directly reusable as an SOP: decompose by capability, parallelise independent ops, validate intermediate outputs, maintain context continuity across handoffs. The three Mermaid pattern diagrams (sequential pipeline, parallel-with-aggregation, iterative refinement) are the best portable artefact — they describe universal multi-agent topologies.
**Strip**: `ToolLoopAgent` / `@ai-sdk/anthropic` scaffolding, specific tool function names (`delegateToAgent`, `waitForCompletion`, etc.), TypeScript interface blocks, SDK config objects.
**Notes**: `OrchestratorConfig` retry-policy defaults (maxRetries=3, backoffMultiplier=2) are worth keeping as opinionated defaults in a skill. The "Available Agents" list is example-specific and should be replaced with a placeholder instruction to enumerate real agents.

## examples/llm-as-judge-skills/agents/evaluator-agent/evaluator-agent.md
**Type**: Agent definition (TypeScript) + embedded evaluation system-prompt
**Portable**: High (guidelines and bias-mitigation rules); Low (code)
**Reason**: The evaluator `instructions` block contains strong, concise bias-mitigation rules that transfer directly to any LLM-as-judge skill: ground scores in explicit evidence, avoid position bias in pairwise comparisons, do not reward verbosity unless it adds value. The four capabilities (direct scoring, pairwise comparison, criteria extraction, rubric generation) map cleanly to a reusable evaluation SOP.
**Strip**: TypeScript SDK wiring, `EvaluatorConfig` interface, `ToolLoopAgent` boilerplate, SDK imports.
**Notes**: The `swapPositionsForPairwise: true` default is a subtle but important bias-mitigation practice worth surfacing explicitly in a skill. The config flag `useChainOfThought: true` aligns with the workspace's `cot-gate` skill — worth cross-referencing.

## examples/llm-as-judge-skills/agents/research-agent/research-agent.md
**Type**: Agent definition (TypeScript) + embedded research methodology prompt
**Portable**: High (methodology and quality standards); Medium (workflow diagram); Low (code)
**Reason**: The "Research Methodology" and "Quality Standards" sections in the `instructions` string are the most transferable content: broad-to-narrow query progression, multi-source cross-verification, explicit distinction between facts/claims/opinions, and a hard rule against fabrication. The Mermaid workflow diagram (decompose → search → read → extract claims → verify → synthesise) is a clean reusable SOP backbone.
**Strip**: TypeScript/OpenAI SDK code, `ResearchConfig` interface, tool-function names, example usage block.
**Notes**: Agent uses `openai("gpt-4o")` (not Anthropic) — model reference is irrelevant to the SOP. `ResearchConfig.minSourcesForVerification: 2` is a practical default worth keeping. The integration-points list (knowledge-base building, fact-checking, market research) is useful as a "when to use" trigger list for a skill description.

## examples/llm-as-judge-skills/prompts/evaluation/direct-scoring-prompt.md
**Type**: Prompt template (Handlebars / Markdown)
**Portable**: Very High
**Reason**: This is the most immediately portable artefact in the batch. It is a complete, self-contained LLM-as-judge scoring prompt with: (1) an evidence-first evaluation chain (`evidence → score → justification → improvement`), (2) explicit anti-bias guidelines (no length bias, explain uncertainty before scoring), (3) a well-structured JSON output schema, and (4) fully documented template variables. The "Best Practices" section (Evidence First, Rubric Alignment, Constructive Feedback, Consistency, Calibration) is high-signal and portable verbatim.
**Strip**: Handlebars `{{#each}}` / `{{#if}}` syntax — replace with a neutral `<placeholder>` or Jinja2 convention. Remove the JSON code-fence wrapping the template to avoid nesting confusion.
**Notes**: The variable table is production-quality documentation and should be preserved in the skill. The example input (quantum entanglement) is a good calibration reference; keep as a worked example. This prompt pairs directly with the evaluator-agent findings above and should be co-located in any resulting `llm-as-judge` skill.

## examples/digital-brain-skill/AGENT.md
**Type**: Project-scoped operating instructions (personal PKM system)
**Portable**: No
**Reason**: Entirely tied to a specific "Digital Brain" folder layout (`identity/voice.md`, `network/contacts.jsonl`, `operations/todos.md`, etc.) and bespoke Python scripts. The routing table and file conventions only make sense within that exact repo structure.
**Trigger**: N/A — not a triggerable skill, just ambient project rules loaded whenever working in that repo
**Steps/contract**: Read voice.md before writing; append-only to JSONL; update timestamps; cross-reference modules; dispatch table maps user requests to file/script actions
**Strip**: All file paths, script references, and JSONL/YAML/template conventions — they are instance-specific
**Notes**: The *pattern* of "read a voice/persona file before drafting content" is worth noting as a lightweight portable heuristic; the dispatch table format (request → action) is a clean authoring pattern but the content is not reusable

## examples/digital-brain-skill/agents/AGENTS.md
**Type**: Script-runner manifest for a personal PKM automation layer
**Portable**: No
**Reason**: Defines five Python scripts (`weekly_review.py`, `content_ideas.py`, etc.) that are hard-wired to the Digital Brain file layout. The workflow automations (Sunday review, pre-meeting prep, content ideation) are useful mental models but reference specific JSONL files and paths throughout.
**Trigger**: N/A — automation catalogue, not a triggerable SOP
**Steps/contract**: Sunday workflow: metrics → stale contacts → weekly review → present; content ideation: bookmarks → ideas → script → calendar; pre-meeting: lookup contact → interactions → todos → brief
**Strip**: All script names, file paths, Python invocation details, and custom-script-development instructions
**Notes**: The pre-meeting prep sequence (look up contact → pull recent interactions → check pending todos → generate brief) is a genuinely portable "relationship context retrieval" pattern worth extracting as a sub-step in a contacts/CRM SOP

## examples/digital-brain-skill/content/templates/linkedin-post.md
**Type**: Content scaffold template (LinkedIn post formats: story, lesson/how-to, hot-take)
**Portable**: No
**Reason**: Platform-specific (LinkedIn character limits, "see more" fold, hashtag conventions) and persona-specific (brand voice calibrated to a specific creator's professional tone). Sections assume a personal narrative voice and engagement hooks suited to LinkedIn's social graph.
**Strip**: Platform name references, hashtag section, "voice matches brand" checklist item, all [PLACEHOLDER] labels
**Notes**: The underlying multi-format scaffold pattern (hook → body-by-format → closing → CTA) is a reusable structural idea; the formats themselves (story/lesson/hot-take/how-to) map cleanly to generic persuasive writing archetypes worth extracting as a portable content-structure SOP.

## examples/digital-brain-skill/content/templates/newsletter.md
**Type**: Content scaffold template (email newsletter issue structure)
**Portable**: No
**Reason**: Email-channel-specific (subject line A/B variants, preview text, mobile formatting checklist) and persona-specific (sign-off style, "voice.md" reference). Structure assumes a recurring numbered issue format tied to a personal newsletter brand.
**Strip**: `issue_number`, `publish_date`, "voice matches brand (checked against voice.md)" checklist item, URL placeholder rows, all [PLACEHOLDER] labels
**Notes**: The layered hook → insight sections → key takeaway → single action item → closing pattern is a strong, portable long-form content structure. The pre-publish checklist items around value clarity, CTA specificity, and mobile readability are portable quality gates worth preserving in an abstracted form.

## examples/digital-brain-skill/content/templates/thread.md
**Type**: Content scaffold template (Twitter/X thread structure)
**Portable**: No
**Reason**: Hard-wired to Twitter/X (280-character limit, thread numbering convention `1/`, `2/`, scroll-stopping hook language). The `target_platform: "twitter"` metadata makes this explicitly single-platform.
**Strip**: Platform name, tweet-count constraint, 280-character checklist item, thread-number prefixes, "voice.md" reference
**Notes**: The numbered-beat structure (hook → context → N evidence points → takeaway → CTA) is a strong portable argument scaffold that generalises to any short-form sequential format. The scroll-stopping hook types (controversial take, surprising stat, curiosity gap) are a useful taxonomy worth surfacing in a portable writing SOP.

## examples/digital-brain-skill/operations/OPERATIONS.md
**Type**: Skill module — personal productivity OS (todos, goals, meetings, metrics, reviews). **Portable**: Partial. **Reason**: Priority taxonomy (P0–P3) and daily/weekly/quarterly review cadences are broadly portable; file-format choices (JSONL, YAML) and script references (`weekly_review.py`) are implementation-specific. **Strip**: Script paths, specific file names (`todos.md`, `goals.yaml`, etc.), the `reviews/` folder convention. **Notes**: The P0–P3 priority ladder and "generate insights, not just summaries" directive are strong portable SOPs. Weekly review as a non-negotiable cadence is worth preserving as a standing rule.

## examples/digital-brain-skill/content/CONTENT.md
**Type**: Skill module — content creation pipeline (ideation → draft → voice review → publish → archive). **Portable**: High. **Reason**: The five-stage pipeline (capture → develop → voice-check → publish → log) and the "always append, never overwrite" append-only log discipline are tool-agnostic SOPs applicable to any content workflow. **Strip**: Specific file names (`ideas.jsonl`, `posts.jsonl`, `calendar.md`), engagement metric field names, and the `engagement.jsonl` convention. **Notes**: "MUST read voice.md before writing content" is a strong portable guardrail. The append-only capture rule and priority scoring rubric (High/Medium/Low with rationale) are reusable across knowledge-work contexts.

## examples/digital-brain-skill/knowledge/KNOWLEDGE.md
**Type**: Skill module — personal knowledge base (bookmarks, learning tracking, research, notes). **Portable**: High. **Reason**: The save→read→summarize→review lifecycle for resources, the knowledge-graph cross-linking hints (bookmarks → content ideas → authority pieces), and the structured bookmark schema are broadly portable patterns for any second-brain or research workflow. **Strip**: Specific category taxonomy (ai_agents, building, growth, etc.), file names, and JSONL schema field names. **Notes**: "Check research/ folder first before synthesising" is a portable retrieval-before-generation rule. The learning.yaml quarterly review loop is a reusable SOP for skills tracking. Cross-module linking pattern (knowledge → content → brand) is a notable architectural signal.

## examples/digital-brain-skill/network/NETWORK.md
**Type**: Skill module — personal CRM (contacts, interactions, introductions, relationship tiers). **Portable**: Partial. **Reason**: Pre/post-meeting protocols (look up → review interactions → note follow-ups; log → update → add todos → update notes) and the intro-matching via mutual-value cross-reference are portable SOPs. The four-tier circle model (inner/active/network/dormant) is a reusable relationship classification. **Strip**: JSON schema field names, specific file names (`contacts.jsonl`, `interactions.jsonl`, `circles.yaml`, `intros.md`), platform-specific fields (`twitter`, `linkedin`). **Notes**: "Follow up is everything" and "give before you ask" are philosophy-level signals, not SOPs. The cross-module integration pattern (interaction follow-ups → operations/todos.md) is the strongest portable takeaway — relationship actions should feed back into the task system.

## examples/digital-brain-skill/identity/IDENTITY.md
**Type**: Module index / agent-instruction hub — lists sibling files and declares pre-flight loading rules for any content task.
**Portable**: Yes
**Reason**: The pattern of a `SKILL.md`-style index that tells the agent *which* context files to load *before* acting (voice → brand → values) is fully portable. The `<instructions>` block (read voice.md first, match energy/vocabulary, honour never-use list, check brand) is a reusable pre-content-creation gate.
**Strip**: Specific filenames tied to this persona (`brand.md`, `values.yaml`, `bio-variants.md`) and the "When to Use" table rows that name those files — replace with generic slot names in the SOP.
**Notes**: The "Quick Reference → see voice.md" pointer pattern is a clean way to keep an index short while delegating detail; worth preserving in a portable identity-skill template.

## examples/digital-brain-skill/identity/voice.md
**Type**: Voice & tone template — structured scaffold with every field left as `[PLACEHOLDER]`.
**Portable**: Yes — the *structure* is the value; the blanks are intentional.
**Reason**: The five-axis attribute table (Formal↔Casual, Serious↔Playful, Technical↔Simple, Reserved↔Expressive, Humble↔Confident), the vocabulary split (signature phrases / power words / never-use), the platform-adaptation sections, and the Anti-Patterns block are all reusable across any persona skill. This is the canonical shape of a voice profile.
**Strip**: The `[PLACEHOLDER: e.g., …]` filler text — promote the example content into proper inline guidance so agents filling the template aren't seeded with bad defaults. The `*Last updated: [DATE]*` footer is noise.
**Notes**: The "Never Use" section is particularly strong — an explicit deny-list is more actionable than a tone description. Promote it as a required section in the SOP. The platform-adaptation section (Twitter, LinkedIn, long-form) is optional but valuable for multi-channel skills.

## examples/digital-brain-skill/identity/prompts/content-generation.xml
**Type**: Parameterised XML prompt template — master content-generation prompt with `{{VARIABLE}}` slots.
**Portable**: Yes — the structure is portable; the XML container format is not required.
**Reason**: The three-step pre-flight sequence (review voice profile → check brand positioning → reference recent examples), the `<quality_checks>` block (authentic voice? content-pillar aligned? audience value? platform tone?), and the `<output_requirements>` slot set (format / platform / length / CTA flag) are all reusable patterns. Quality checks as a closing gate is especially strong.
**Strip**: XML wrapper — convert to Markdown sections in the SOP. The `<!-- Populated from voice.md -->` comments imply dynamic injection that won't generalise; replace with explicit "copy relevant fields from your voice profile here" instructions.
**Notes**: The pre-flight sequence mirrors the `cot-gate` skill pattern (load context before acting). The `<examples>` stub pointing to `content/posts.jsonl` is a good signal that few-shot examples should live alongside the prompt — capture this as a pattern: *prompt templates should declare where their examples live*.

## examples/digital-brain-skill/identity/prompts/reply-generator.xml
**Type**: Parameterised XML prompt template — contextual reply generation with channel-aware tone calibration.
**Portable**: Yes
**Reason**: The `<reply_principles>` list (match energy, add value not just acknowledgement, be concise on social / detailed in DMs, respectful disagreement, shared-context references) is universally applicable to any reply-generation task. The `<tone_calibration>` split across public reply / DM / comment is a clean, reusable three-channel model. The multi-option output format (standard / casual-playful / conversation-extending) is portable best practice.
**Strip**: XML wrapper and `{{VARIABLE}}` slots — convert to Markdown. The `<relationship>{{RELATIONSHIP_CONTEXT}}</relationship>` slot is useful in concept but needs clearer guidance on what to put there (follower / collaborator / first contact / etc.) or it will be ignored.
**Notes**: "Add value — don't just acknowledge" is one of the sharpest single-line reply rules seen in this audit; extract it verbatim into the SOP. The three-variant output pattern (options 1/2/3) is a strong default for any generative prompt — gives the user agency without requiring re-prompting.

## examples/llm-as-judge-skills/skills/context-fundamentals/context-fundamentals.md
**Type**: Reference/educational — context window management theory, information hierarchy, context-type taxonomy (static/dynamic/ephemeral), RAG + multi-turn patterns, and utilisation metrics. **Portable**: Yes — principles (explicit over implicit, structured formatting, redundancy removal, source attribution, freshness signals) and the tiered information-hierarchy model are framework-agnostic. **Reason**: Provides a clean mental model and vocabulary for context construction that applies across any agent stack. **Strip**: SDK-specific RAG template placeholders (`{user_input}`, `{summary}`) are illustrative only and carry no runtime logic — fine to keep as examples; nothing needs stripping. **Notes**: Metrics section (utilisation rate, relevance score, compression ratio, information retention) is worth extracting as an evaluation checklist; context-type taxonomy (static/dynamic/ephemeral) is reusable as a classification scheme in SOP drafts.

## examples/llm-as-judge-skills/skills/tool-design/tool-design.md
**Type**: Reference/educational — agent tool design principles, tool-category taxonomy (read-only / state-modifying / dangerous), and Vercel AI SDK v6-specific features (`needsApproval`, `strict`, `inputExamples`, `toModelOutput`). **Portable**: Partially — the design principles (single responsibility, clear input schemas, predictable output structure, graceful error handling) and the approval-tier taxonomy are framework-agnostic; the AI SDK 6 code snippets are Vercel-specific. **Reason**: Single-responsibility, typed output contract, and the three-tier danger classification (read-only → state-modifying → dangerous) are strong portable SOPs for any tool-calling agent. **Strip**: AI SDK 6 TypeScript examples (`tool({...})`, `z.object`, `toModelOutput`) before promoting to a generic SOP — retain as illustrative appendix or replace with pseudocode. **Notes**: `needsApproval` pattern maps directly to a human-in-the-loop gate SOP; idempotency and audit-logging best practices belong in a standing safety rule rather than a skill.

## examples/llm-as-judge-skills/agents/index.md
**Type**: Registry / pattern catalogue — three agent archetypes (Evaluator, Research, Orchestrator) with capability lists, tool assignments, and three interaction patterns (sequential pipeline, parallel fan-out, iterative refinement with evaluator feedback loop).
**Portable**: Yes
**Reason**: The three patterns (sequential, fan-out, iterative-refinement) and the agent-addition checklist are workflow conventions that apply to any multi-agent system, not llm-as-judge-specific.
**Strip**: Specific tool names (`directScore`, `webSearch`, `delegateToAgent`, etc.) — they are implementation details for this example stack.
**Notes**: "Iterative Refinement" (Agent → Evaluator → Agent-if-fail) is the strongest portable SOP candidate; it encodes a quality-gate loop with structured feedback that maps cleanly to a skill or rule.

## examples/llm-as-judge-skills/prompts/index.md
**Type**: Prompt-engineering meta-guide — covers template format, Handlebars variable syntax, four design principles (role definition, explicit instructions, structured output, guard rails), and a prompt testing checklist.
**Portable**: Yes
**Reason**: The four principles and the testing checklist are LLM-agnostic prompt hygiene that applies to any skill or agent prompt, not just evaluation prompts.
**Strip**: The category table (evaluation / research / agent-system) and file paths — those are specific to this example project's directory layout.
**Notes**: The guard-rails principle ("do NOT prefer responses simply because they are longer / based on position") is especially portable as a concrete anti-bias instruction pattern. The six-item prompt testing checklist is a ready-to-lift self-validation gate.

## examples/llm-as-judge-skills/tools/index.md
**Type**: Tool-design reference — standard TypeScript tool structure with Zod schema, `needsApproval` / `strict` / `toModelOutput` fields, a typed `ToolResult<T>` error pattern, and a tool-selection decision table.
**Portable**: Yes — the structural conventions
**Reason**: The `ToolResult<T>` error shape (success, data, error.code/message/retryable, metadata.executionTimeMs) and the `toModelOutput` truncation pattern are reusable implementation contracts for any tool, independent of the llm-as-judge domain.
**Strip**: All domain-specific tool names and the category directories (`evaluation/`, `research/`, `orchestration/`); the TypeScript snippet should be treated as a pattern template, not copied verbatim.
**Notes**: `retryable: boolean` on error responses is a particularly portable design signal — it lets orchestrators decide whether to retry without parsing error messages. The tool-selection decision table (action → category → suggested tools) is a useful documentation pattern worth adopting in skill docs.

## .references/context-engineering/examples/llm-as-judge-skills/tools/research/web-search.md
**Type**: Tool definition doc (TypeScript/Zod schema + usage example for a web-search tool)
**Portable**: No
**Reason**: Implementation is framework-specific (Vercel AI SDK `tool()` + Zod). The schema fields (query, maxResults, filters) and output shape are useful as a reference design, but the code cannot be lifted as-is into a skill.
**Strip**: All TypeScript/Zod code blocks and the inline `// Result:` JSON literals — retain only the parameter table, output schema prose, query optimisation tips, and implementation-notes checklist.
**Notes**: The query-optimisation tips (specific terms, quotes, operators, recency) and implementation checklist (rate-limiting, caching, error handling, robots.txt respect) are the portable signal; they map cleanly to a tool-design SOP checklist.

## .references/context-engineering/examples/llm-as-judge-skills/tools/research/read-url.md
**Type**: Tool definition doc (TypeScript/Zod schema + usage example for a URL-content-extraction tool)
**Portable**: No
**Reason**: Same framework dependency as web-search.md. Concrete I/O schema and content-type handling table are reference-quality but not drop-in portable.
**Strip**: TypeScript interface blocks, Zod parameter definitions, and inline result JSON. Retain the content-type handling table, error-code catalogue, input/output field tables, and implementation-notes checklist.
**Notes**: The error-code taxonomy (URL_NOT_FOUND, ACCESS_DENIED, TIMEOUT, BLOCKED, INVALID_CONTENT, UNSUPPORTED_TYPE) is reusable as a standard error-handling vocabulary in a tool-design SOP. The content-type optimisation table (article / documentation / paper / code / auto) is a clean portable pattern.

## .references/context-engineering/.claude-plugin/marketplace.json
**Type**: Plugin registry / manifest (Claude marketplace metadata)
**Portable**: No
**Reason**: Pure configuration — lists skill folder paths and a plugin descriptor. No behavioural guidance, patterns, or principles to extract.
**Strip**: Everything. The skill-name list is only useful as an index for navigating the repo, not as SOP content.
**Notes**: Confirms the full skill surface of this package: context-fundamentals, context-degradation, context-compression, context-optimization, multi-agent-patterns, memory-systems, tool-design, filesystem-context, hosted-agents, evaluation, advanced-evaluation, project-development, bdi-mental-states. Use as a checklist to ensure all 13 skills are audited.

## .references/context-engineering/.plugin/plugin.json
**Type**: Package metadata / plugin descriptor
**Portable**: No
**Reason**: Name, version, author, and one-line description only — no actionable content.
**Strip**: Everything.
**Notes**: Duplicate of the descriptor embedded in marketplace.json. The description string ("covering fundamentals, degradation patterns, compression, optimization, multi-agent coordination, memory systems, tool design, evaluation, and more") is useful as a scope summary for the audit header.

## examples/llm-as-judge-skills/tools/evaluation/direct-score.md
**Type**: Tool definition (TypeScript/Zod) — LLM-as-judge single-response scorer with weighted multi-criteria rubric, CoT justification, and structured output (scores + strengths/weaknesses/suggestions). **Portable**: Yes — framework-agnostic pattern; Zod schema and execute stub are illustrative, the eval logic is the portable core. **Reason**: Encodes a best-practice scoring protocol (justification-before-score, calibrated rubric levels, per-criterion weighting, length normalisation note) that is reusable across any LLM eval pipeline. **Strip**: Vercel AI SDK import (`import { tool } from "ai"`), `execute` stub calling `evaluateWithLLM` — both are harness-specific; also strip the TypeScript type-signature boilerplate and the literal Zod schema wiring. **Notes**: Key portable ideas — (1) weighted criteria array, (2) configurable 1-3/1-5/1-10 rubric scale with level descriptions, (3) justification-first to reduce anchoring bias, (4) few-shot calibration recommendation, (5) structured output shape (per-criterion score + examples + overall weighted score).

## examples/llm-as-judge-skills/tools/evaluation/generate-rubric.md
**Type**: Tool definition (TypeScript/Zod) — rubric generator that produces per-level descriptions, characteristics, examples, scoring guidelines, and edge-case guidance for a named criterion. **Portable**: Yes — the output schema and rubric templates are harness-independent and directly reusable. **Reason**: Codifies the practice of generating calibrated rubrics before scoring, with domain adaptation, strictness tuning, and edge-case coverage — a reusable SOP step for any eval workflow that needs consistent scoring standards. **Strip**: Vercel AI SDK import, `execute` stub calling `generateRubricWithLLM`, Zod schema wiring. **Notes**: Key portable ideas — (1) generate rubric as a distinct pre-step before direct scoring, (2) `strictness` enum (lenient/balanced/strict) to tune score-boundary sharpness, (3) `domain` field for context-specific calibration, (4) built-in edge-case guidance section, (5) three ready-made templates (Factual Accuracy, Clarity, Completeness) are directly liftable as SOP defaults.

## examples/llm-as-judge-skills/tools/evaluation/pairwise-compare.md
**Type**: Tool definition (TypeScript/Zod) — LLM-as-judge pairwise comparator with position-swap bias mitigation, per-criterion winner breakdown, confidence score, and consistency check. **Portable**: Yes — the position-swap algorithm and output schema are fully harness-agnostic. **Reason**: Implements the research-backed pairwise preference pattern with explicit position-bias mitigation (double-pass swap + consistency reconciliation), making it a strong portable SOP for subjective quality evaluation. **Strip**: Vercel AI SDK import, `execute` stub calling `evaluateWithPositionSwap`/`evaluatePairwise`, Zod schema wiring. **Notes**: Key portable ideas — (1) always swap positions in production to detect position bias, (2) return `positionConsistency` flag — downgrade to TIE when passes disagree, (3) confidence score (0–1) separate from winner verdict, (4) per-criterion `aStrength`/`bStrength` for transparent reasoning, (5) explicit `allowTie` flag — domain-tunable.

## examples/llm-as-judge-skills/tools/orchestration/delegate-to-agent.md
**Type**: Tool definition (TypeScript/Zod) — orchestration router that delegates tasks to named specialist agents (evaluator, researcher, writer, analyst) with structured context passing, output format constraints, timeout, and error taxonomy. **Portable**: Yes — the delegation protocol (context shape, expectedOutput contract, error codes, audit-trail note) is harness-independent. **Reason**: Establishes a reusable agent-handoff SOP: complete-context passing, explicit output format/schema contract, timeout discipline, and structured error taxonomy with `retryable` flag — applicable to any multi-agent pipeline. **Strip**: Vercel AI SDK import, `execute` stub calling `executeAgentDelegation`, Zod schema wiring, hardcoded `agentName` enum (agent roster is project-specific). **Notes**: Key portable ideas — (1) pass `previousOutputs` + `documents` + `constraints` as distinct context fields, (2) declare `expectedOutput.format` and optional JSON schema upfront, (3) five named error codes with `retryable` flag, (4) token-usage tracking per delegation for cost auditing, (5) context-compression recommendation before handoff.
