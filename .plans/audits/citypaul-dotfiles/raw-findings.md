## claude/.claude/agents/tdd-guardian.md
**Type**: Sub-agent (Claude agent definition with frontmatter: name, description, tools, model, color)
**Portable**: Yes — fully portable; contains no project-specific business logic beyond a small "Project-Specific Guidelines" section that mirrors generic TypeScript best practices
**Reason**: Red-green-refactor loop, violation detection heuristics, behavior-vs-implementation test quality checks, and factory-over-beforeEach conventions are universally applicable TDD patterns independent of any codebase
**Trigger**: Proactively when user plans to write code; reactively when code is already written or tests are green — dual-mode invocation
**Steps/contract**: (1) Identify simplest behavior to test first → (2) Write failing test (RED) → (3) Write minimal implementation (GREEN) → (4) Assess refactoring necessity (REFACTOR) → (5) Commit; reactive path uses git diff/log to audit test-first order and generates a structured violation report
**Strip**: "Project-Specific Guidelines" section (TypeScript `type` vs `interface` rules, Zod schema-first note, no-comments rule) — these are repo-local conventions, not TDD SOP; also strip hardcoded model/color frontmatter fields
**Structure/format**: Well-structured — frontmatter metadata, dual-role sections with explicit response-pattern templates, phase-by-phase coaching guidance, quality-gate checklist, violation taxonomy with example report, and concrete git commands; highly copy-paste-ready
**Notes**: One of the strongest TDD skill candidates in this audit. The factory-function-over-let/beforeEach heuristic and the behavior-vs-implementation test naming heuristic are portable gems worth preserving verbatim. The structured violation report format (with file, issue, git evidence, recommendation fields) is a concrete deliverable contract that elevates this above generic TDD advice.

## claude/.claude/agents/ts-enforcer.md
**Type**: Agent definition — dual-mode TypeScript strict-mode enforcer (proactive coaching + reactive audit)
**Portable**: Yes — core quality gates and report structure are stack-portable; Zod-specific examples need minor generalisation to "schema validation library"
**Reason**: The severity-tiered violation taxonomy (🔴 CRITICAL / ⚠️ HIGH PRIORITY / 💡 STYLE), compliance-score summary, and dual proactive/reactive framing are patterns any TS project can adopt regardless of validation library
**Trigger**: Proactively when defining types/schemas or writing TypeScript; reactively when auditing files for type-safety compliance
**Steps/contract**: (1) Scan TS files (glob + git diff); (2) verify tsconfig strict flags; (3) grep for critical violations (`any`, `as` assertions, `@ts-ignore`, `interface`, mutations); (4) validate schema-first at trust boundaries; (5) emit structured severity-tiered report with file:line citations, issue, impact, and fix
**Strip**: `model: sonnet`, `color: red`, `tools:` frontmatter; internal cross-references to `typescript-strict` and `functional` skills; Zod-specific syntax in examples (generalise to schema-lib placeholder)
**Structure/format**: Excellent — severity tiers with emoji anchors, per-violation blocks (File / Code / Issue / Impact / Fix), compliant-files list, summary table, compliance %, next-steps checklist
**Notes**: The proactive "response pattern" template (What you're doing → Issue → Correct approach → Why it matters) is a portable teaching scaffold worth extracting independently. Quality-gate checklist at the bottom is concise and commit-gate ready.
## claude/.claude/agents/pr-reviewer.md
**Type**: Sub-agent definition — PR review SOP with dual proactive/reactive mode
**Portable**: Partially
**Reason**: The structural scaffold is highly portable — dual-mode invocation (guide reviewer vs. analyse and post), 5-category review framework, severity tiering (🔴/⚠️/💡), detection-command patterns, and GitHub posting methods all transfer cleanly. The *content* of each category is opinionated to one coding philosophy (strict TDD-first, zero `any`, functional-only, no comments), which limits portability as-is.
**Trigger**: Manual invocation during a Claude Code session when user asks to review a PR, check merge-readiness, or understand a specific issue
**Steps/contract**: (1) Gather PR info via `gh pr view`/`gh pr diff`; (2) Categorise changed files; (3) Evaluate 5 categories with `grep`-based detection commands; (4) Generate structured report with summary table + critical/high/suggestion tiers; (5) Post review to GitHub via MCP tools, `gh pr review`, or `gh pr comment`
**Strip**: Repo-specific coding mandates — TDD-first as hard gate, no-`any`-ever rule, functional-only patterns (no loops, no mutation, no comments), TypeScript `type` vs `interface` preference; these embed one team's philosophy, not universal standards
**Structure/format**: Excellent — concise frontmatter, clear dual-mode separation, per-category sections each with principle/check-for/detection-commands/report-format, a final summary table template, severity legend, quality-gate checklists, and a posting-methods reference table. Very copy-paste ready.
**Notes**: The strongest reusable primitive here is the dual-mode (proactive guide / reactive analyse-and-post) pattern plus the category-table + severity-tier report template. A portable SOP could lift those and treat category content as a slot to be filled per-project. The GitHub posting section (MCP tools vs `gh` CLI, when to use each) is also a clean, standalone utility block worth extracting.

## claude/.claude/CLAUDE.md
**Type**: Lean core-philosophy CLAUDE.md (v3.0.0 architecture: ~100-line hub delegating detail to on-demand skills)
**Portable**: YES — multiple distinct portable SOPs embedded; architecture pattern itself is also portable
**Reason**: Contains four independently extractable SOPs that are repo/toolchain-agnostic: (1) TDD mandate + RED-GREEN-REFACTOR cycle, (2) output guardrails (write to files, plan-only mode, incremental output), (3) TypeScript strict-mode principles, (4) testing principles (behavior not implementation, factory functions, no 1:1 file mapping). The lean-hub + on-demand-skills architecture is a strong meta-pattern for CLAUDE.md design.
**Trigger**: (1) Any code-generation task → TDD cycle; (2) Any request for a plan/doc/artifact → output guardrails; (3) TypeScript file touched → TS strict principles; (4) Writing or reviewing tests → testing principles
**Steps/contract**:
  - TDD cycle: RED (failing test first, NO production code without it) → GREEN (minimum code to pass) → REFACTOR (only if adds value) → commit approval gate
  - Output guardrails: persist artifacts to files (not chat); plan-only mode produces zero implementation code; first draft within 3–4 tool calls then iterate
  - TS strict: no `any`, no type assertions without justification, `type` over `interface` for data, schemas at trust boundaries, plain types internally
  - Testing: test public API only, factory functions for test data (no `let`/`beforeEach`), tests document business behavior, no 1:1 file mapping
**Strip**: `agent-browser` workflow (tool-specific); `/setup` command reference; named skill load triggers (`load the tdd skill`, etc.); external resource links; "Update CLAUDE.md" self-referential instruction; project-level PostToolUse hook specifics; version history header
**Structure/format**: Quick-reference bullets under `## <Section>` headings with a one-line "Core principle" lead; detailed content deferred to skills — clean pattern for a portable CLAUDE.md SOP hub
**Notes**: The **output guardrails** section is the highest-signal portable SOP — "write to files not chat", "plan-only mode produces ONLY the artifact", and "first draft in 3–4 tool calls" are crisp, enforceable rules rarely stated this cleanly in other dotfiles. The TDD mandate language ("NON-NEGOTIABLE. No exceptions.") is stronger than average; worth preserving that tone in extraction. The lean-hub architecture (v1 monolith → v2 imports → v3 hub+skills) is a useful prior art reference for the meta-SOP on CLAUDE.md design.

## claude/.claude/agents/adr.md
**Type**: Agent definition (subagent role) — ADR author and decision-capture specialist  
**Portable**: Yes — the ADR format, decision framework, and authoring guidance are fully project-agnostic  
**Reason**: No repo-specific content; the format, criteria, anti-patterns, and index maintenance protocol apply to any engineering project  
**Trigger**: Proactively when evaluating technology options / making foundational decisions; reactively to document choices already made; invocable by other agents (progress-guardian, docs-guardian, learn)  
**Steps/contract**: (1) Apply 5-question "should I create an ADR?" decision framework (3+ YES → create); (2) determine next ADR number from `docs/adr/`; (3) gather context, alternatives, trade-offs, consequences; (4) write ADR using standard template (Status · Date · Decision Makers · Tags · Context · Decision · Alternatives Considered · Consequences · Implementation Notes · Related Decisions · References); (5) update `docs/adr/README.md` index  
**Strip**: References to companion agents (progress-guardian, docs-guardian, learn) and `color: purple` / `model: sonnet` frontmatter — these are harness-specific  
**Structure/format**: Includes a fully-specified, copy-ready Markdown ADR template with clear section headings; decision framework is a clean 5-question checklist; DO/DO NOT lists with 5 categories each are well-structured and scannable  
**Notes**: Exceptionally strong portable candidate. The decision framework (5 yes/no questions, 3+ threshold) is the most valuable extract — it prevents ADR inflation without needing custom rationale. The anti-patterns section is unusually explicit and actionable. The retroactive ADR convention (`Status: Accepted (Retroactive)`) is a practical detail worth preserving. Template is production-ready as-is.

## claude/.claude/agents/docs-guardian.md
**Type**: Sub-agent definition — dual-mode documentation quality guardian (proactive creation + reactive improvement)
**Portable**: Yes — entirely generic; no project-specific tech stack, library, or toolchain references; pure documentation philosophy and process
**Reason**: The 7 Pillars framework, severity-tiered assessment report (🔴/⚠️/💡), progressive-disclosure layer model (10 s hook → 5 min quick-start → 30 min concepts → deep dive), and dual proactive/reactive invocation pattern are all stack-agnostic best practices applicable to any documentation task
**Trigger**: Proactively when user is creating documentation (READMEs, guides, API docs, tutorials); reactively when existing documentation needs review or improvement
**Steps/contract**:
  - Proactive path: (1) Clarify audience, purpose, doc type (who/what/when/why); (2) Recommend structure by type (README, Guide, Reference, API); (3) Apply progressive-disclosure layering; (4) Create navigation aids (problem-oriented table, ToC anchors, cross-references); (5) Demonstrate value with before/after example patterns
  - Reactive path: (1) Read target file(s) + discover related docs via glob/grep; (2) Assess against 7-pillar checklist; (3) Classify issues by severity (🔴 Critical / ⚠️ High / 💡 Enhancement / ✅ Good); (4) Generate structured assessment report (exec summary → strengths → critical → high-priority → enhancements → proposed restructuring → priority actions); (5) Propose specific edits
**Strip**: Frontmatter (`model: sonnet`, `color: purple`, `tools:` list); the three full document templates (README, Concept Guide, API Reference) are verbose and formulaic — a portable SOP can replace them with concise structure-slot summaries; the "Response Patterns" and "Commands to Use" sections have significant overlap with the main body and can be condensed
**Structure/format**: Excellent — clear dual-mode separation, 7-pillar framework with per-pillar sub-checklists, severity-tiered issue blocks (Location / Problem / Fix / Proposed Addition), progressive-disclosure layer model with time targets, three doc-type structural templates, quality-gate checklists at end, and response-pattern templates; highly scannable and copy-paste ready
**Notes**: The 7 Pillars + progressive-disclosure layer model is the standout portable primitive — concise, memorable, and immediately actionable. The reactive assessment report structure (strengths → critical → high-priority → enhancements → restructuring → actions) is a clean deliverable contract worth preserving verbatim. The proactive clarifying-questions scaffold (who/what/when/why) is a useful forcing function rarely made explicit in other agents. Minor weakness: the three full document templates add bulk without adding new principles — they illustrate the pillars but can be summarised in extraction. Overall one of the cleanest, most self-contained portable SOPs in this audit.

## claude/.claude/agents/learn.md
**Type**: Sub-agent definition (Claude agent with frontmatter: name, description, tools, model, color) — dual-mode "Learning Integrator" that captures institutional knowledge into CLAUDE.md
**Portable**: Yes, with section-reference stripping — the dual-mode invocation pattern, discovery question framework, significance-assessment rubric, quality gates, and documentation proposal format are all project-agnostic; only the "Classify the Learning" section hardcodes local CLAUDE.md section names
**Reason**: Knowledge-capture workflows (proactive during development, reactive after completion), significance-assessment heuristics, and structured proposal format apply to any project that maintains a persistent context file, regardless of stack or domain
**Trigger**: Proactively when learning moments occur mid-development (gotchas, architectural decisions, breakthroughs, anti-patterns); reactively after completing features, fixing complex bugs, or making significant architectural decisions
**Steps/contract**: (1) Identify learning moment (proactive) or be invoked post-completion (reactive) → (2) Ask discovery questions (problem / solution / context axes) → (3) Read current CLAUDE.md to check for duplication → (4) Classify into appropriate section → (5) Format to match doc voice/style → (6) Emit documentation proposal (Summary / Proposed Location / Proposed Addition / Rationale / Verification Checklist)
**Strip**: All hardcoded section names from local CLAUDE.md ("Core Philosophy", "Schema-First Development", etc.) in "Classify the Learning"; the worked Zod-schema-export example (repo-specific); `model: sonnet`, `color: blue` frontmatter fields; internal `grep` commands referencing CLAUDE.md by literal filename (generalise to "persistent context file")
**Structure/format**: Excellent — frontmatter metadata, dual-role sections with named response-pattern templates for 5 invocation scenarios, discovery-question framework (3 axes), significance-assessment rubric (document-if-ANY / skip-if-ALL), quality-gate checklist, integration guidelines (voice, duplication check), one fully worked example with verification checklist; highly copy-paste-ready
**Notes**: Three portable gems worth preserving verbatim: (1) the 5-scenario response-pattern library (gotcha mid-work, feature complete, architectural decision, tricky bug, "I wish I'd known") — concrete scripted responses that remove friction from knowledge capture; (2) the significance-assessment rubric (document if ANY of 7 conditions; skip only if ALL of 4 conditions) — a portable decision heuristic that prevents both under- and over-documentation; (3) the documentation proposal format (Summary / Proposed Location / Proposed Addition / Rationale / Verification Checklist) — a clean deliverable contract. Slight weakness: the quality-gate checklist partially overlaps with the significance-assessment rubric; a portable version should merge these into one gate.

## claude/.claude/agents/refactor-scan.md
**Type**: Sub-agent definition (Claude agent with frontmatter: name, description, tools, model, color)
**Portable**: Yes — no project-specific logic; all principles are universal refactoring heuristics applicable to any codebase
**Reason**: The semantic-vs-structural duplication distinction, TDD third-step positioning, dual proactive/reactive mode, and severity-tiered findings format are stack- and domain-agnostic. TypeScript-flavoured examples are incidental; the underlying decision logic works for any language.
**Trigger**: Proactively during refactoring decisions (user asks "should I abstract this?", "is this duplication worth fixing?"); reactively after tests turn green as TDD's third step
**Steps/contract**:
  - Proactive path: (1) understand the proposed refactoring; (2) apply semantic test (same concept or just same structure?); (3) assess value; (4) provide recommendation with rationale; (5) guide implementation pattern if proceeding
  - Reactive path: (1) `git diff`/`git log` to identify recently changed files; (2) assess 5 dimensions — Naming Clarity, Structural Simplicity, Knowledge Duplication, Abstraction Opportunities, Immutability/Functional Patterns; (3) classify findings into 🔴 Critical / ⚠️ High Value / 💡 Nice-to-Have / ✅ Skip; (4) emit structured report with file:line citations; (5) produce action plan with commit-sequencing (commit green state first, then refactor commits)
**Strip**: `model: sonnet`, `color: yellow`, `tools:` frontmatter; "Per CLAUDE.md: …" sacred-rule citation (repo-local cross-reference); TypeScript-specific syntax in examples (`readonly`, `const` keyword rules); domain-specific example details (free shipping £50 threshold — keep the pattern, drop the domain specifics)
**Structure/format**: Excellent — dual-role sections each with an explicit response-pattern template; five-dimension analysis framework; full structured report template (📁 Files Analyzed → 🎯 Assessment tiers → 📊 Summary → 🎯 Action Plan → ⚠️ Checklist); four scenario-specific response patterns; code before/after examples for Extract Constant, Early Returns, and Extract Function; decision-making checklist; quality gates. Highly copy-paste ready.
**Notes**: The central insight — "DRY is about knowledge duplication, not code duplication" — is precisely and memorably stated, with concrete counter-examples (validatePaymentAmount vs validateTransferAmount). The semantic decision heuristic ("if business rules change for one, should the other change?") is a crisp, portable test worth preserving verbatim. The explicit **✅ Skip** classification tier is the standout differentiator vs. generic refactoring checklists — explicitly telling agents when *not* to refactor is rare and high-value. This is a strong portable SOP candidate; it functions as the deep REFACTOR-step companion to tdd-guardian.md (which covers RED/GREEN), and the two together form a complete TDD cycle coverage.

## claude/.claude/agents/twelve-factor-audit.md
**Type**: Agent definition — specialized auditor that executes a full 12-Factor App compliance review against a service codebase, writing a structured report to a file.
**Portable**: Yes — the 12-Factor methodology is universal; the 3-step audit process, factor-by-factor search patterns, report template, and scoring rubric are all reusable.
**Reason**: The audit structure (discover → audit each factor with specific grep patterns → generate prioritised compliance report) is a clean, self-contained SOP that transfers to any repo audit context. The false-positive callouts embedded per factor are especially valuable tribal knowledge.
**Trigger**: Onboarding to a service project; assessing deployment readiness; reviewing infrastructure patterns; user asks for "12-factor" or "compliance audit".
**Steps/contract**: (1) Discover project structure (ls, git log, glob for Dockerfile/.env.example/k8s/platform configs, detect monorepo). (2) Audit each of the 12 factors using Grep/Glob with specified patterns, reading context before flagging; each factor has explicit false-positive guidance. (3) Generate compliance report file (`twelve-factor-audit.md`) with factor summary table (✅/⚠️/❌), per-factor detail with file paths + line numbers, code-snippet suggestions, and a prioritised action plan. Supports three response modes: full audit, quick health check (factors III/VI/IX only), or single-factor deep-dive.
**Strip**: Frontmatter agent config (`name`, `tools`, `model`, `color`) is harness-specific. Grep/Glob/Read tool names are pi-specific — would normalise to generic "search"/"read" verbs. Node.js/TypeScript-specific patterns (e.g., `PrismaClient`, `pino`, `pnpm-lock.yaml`) could be generalised or moved to a language-specific appendix.
**Structure/format**: Excellent — step-numbered audit process; per-factor sub-sections with "Check for" lists, concrete grep patterns, and false-positive notes; full report template with markdown table; scoring rubric; three response-mode variants. Very copy-paste-ready as a skill.
**Notes**: One of the highest-quality agent definitions in the repo. The embedded false-positive guidance (e.g., don't flag `localhost` in `.env.example`, don't flag `exec` in test files) is unusually mature and prevents noisy reports. Strong SOP candidate; would port cleanly as a `twelve-factor-audit` skill with a thin language-adapter section.

## claude/.claude/agents/progress-guardian.md
**Type**: Sub-agent definition (Claude agent with frontmatter: name, description, tools, model, color) — macro feature-lifecycle manager using plan files in a `plans/` directory
**Portable**: Yes — no hardcoded business logic, domain, or tech-stack dependencies; all lifecycle phases and approval gates are project-agnostic
**Reason**: The plan-file-as-source-of-truth pattern, dual approval gates (plan changes + commits), three-phase lifecycle (create → track → complete+delete), and end-of-feature cleanup protocol are universal feature-development workflow primitives applicable to any engineering project
**Trigger**: (1) Starting significant work → draft and approve plan file; (2) During work at each milestone → update step status and request commit approval; (3) Feature declared complete → verify acceptance criteria, invite learning capture, delete plan file
**Steps/contract**: (1) Draft plan (Goal + Acceptance Criteria + Steps each with Test and Done-when fields) → present to user for approval before writing; (2) During work: after each RED-GREEN-REFACTOR cycle, request explicit commit approval — never commit autonomously; (3) Plan change needed: propose diff showing old vs. new steps → require explicit approval before modifying; (4) Feature complete: verify all acceptance criteria met + all tests passing → offer to invoke learn/adr agents → delete plan file + directory if empty
**Strip**: `model: sonnet`, `color: green`, `tools:` frontmatter; internal named-agent cross-references in the integration table (tdd-guardian, ts-enforcer, refactor-scan, learn, adr, docs-guardian) — generalise to role descriptions; "Pre-PR Quality Gate" block in the plan template references specific skills by name (mutation-testing, refactoring) — replace with generic placeholder slots
**Structure/format**: Excellent — clear three-phase lifecycle (start/during/end) each with invocation-example dialogue, a copy-ready Markdown plan file template (Goal → Acceptance Criteria → Steps with Test + Done-when per step + Pre-PR Gate), Key Behaviors section with approval-contract prose, Anti-Patterns list with ❌ anchors, integration table, and a fully worked example session; highly copy-paste-ready
**Notes**: Two standout portable primitives: (1) the dual approval gates — plan changes and commits both require explicit user approval, stated as non-negotiable rules, which prevents autonomous drift without adding friction to the happy path; (2) the per-step "Done when" field in the plan template — makes completion criteria concrete at the step level, not just the feature level, which eliminates ambiguity about when to advance. The end-of-feature closure loop (verify → merge learnings → delete plan) is a clean, enforceable ritual. This agent is the macro counterpart to tdd-guardian.md (RED/GREEN/REFACTOR micro-cycle): together they provide complete coverage from feature kick-off to final commit. One of the stronger lifecycle-management SOP candidates in this audit.

## claude/.claude/agents/use-case-data-patterns.md
**Type**: Sub-agent definition (Claude agent with frontmatter: name, description, tools, model, color) — pure read-only analytical agent that maps user-facing use cases to underlying data access patterns and architectural implementation
**Portable**: Yes — the 5-phase analysis process, structured report template, and read-only mandate are fully stack- and domain-agnostic; the agent's analytical value scales to any layered architecture (MVC, clean arch, hexagonal, etc.)
**Reason**: The use-case-to-data-pattern tracing protocol (parse → trace layers → map access → identify patterns → surface gaps) applies universally whenever a developer needs to understand how a business requirement maps to existing implementation; the structured report template (Architecture Flow, Data Access Patterns, Code Locations, Gaps, Recommendations) is a clean, reusable investigative deliverable
**Trigger**: Proactively when implementing a new feature that touches data access or requires understanding existing patterns; proactively when reviewing an API endpoint or checkout/processing flow; proactively before planning refactoring work on data-bearing systems
**Steps/contract**: (1) Parse use case — extract core user action, expected behavior, business requirements; (2) Trace architecture layers — endpoints/routes → middleware/guards → business logic (services, domain models) → data access (repos, ORM, queries) → caching → external integrations; (3) Map data access patterns — data flow, DB queries, transformations/DTOs, caching layers, external API calls; (4) Analyze architectural patterns — design patterns in use, adherence to project conventions, version-specific or conditional implementations; (5) Identify gaps — missing patterns, incomplete implementations, scalability concerns; (6) Emit structured report with 9 sections: Use Case Summary, Architecture Flow (5-step numbered), Database Patterns, Caching Patterns, External Integration Patterns, Code Locations (6 categories), Current Implementation Status, Gaps and Missing Patterns, Recommendations
**Strip**: `model: sonnet`, `color: orange`, `tools:` frontmatter; attribution block ("adapted from Kieran O'Hara's dotfiles") — harness-specific provenance; the three extended `<example>` blocks in the description frontmatter are verbose for a portable SOP and can be condensed to a single-line trigger summary; "What You Should NOT Do" section partially restates the read-only mandate already embedded in "Primary Function"
**Structure/format**: Good — numbered analysis phases in the body, a fully specified 9-section Markdown report template with per-section slot descriptions, a 7-item "Key Principles" list, an explicit "How to Handle Uncertainty" fallback protocol, and a "Context Awareness" section covering both documented and undocumented codebases; report template is copy-paste ready; overall structure is clear but slightly verbose — the principles and "what not to do" sections could be merged into a single constraints block
**Notes**: Narrower utility than top-tier agents (tdd-guardian, docs-guardian, learn) — value is high when onboarding to an unfamiliar codebase or before architectural changes, but the agent is passive-only (no output artifact beyond the analysis report). The standout portable extracts are: (1) the 5-layer Architecture Flow trace scaffold (Entry Point → Middleware → Business Logic → Data Access → External Integrations) as an investigation checklist; (2) the 6-category Code Locations index (Entry Points / Business Logic / Abstractions / Implementations / Data Layer / Transformations) — a useful forcing function for complete codebase navigation; (3) the explicit uncertainty protocol ("state what you searched for, what you expected to find, note as a gap, suggest where it would logically belong") — rare to see this made explicit. Attribution note confirms this agent was itself adapted from Kieran O'Hara's dotfiles, suggesting it has already been refined through at least one prior extraction cycle.

## claude/.claude/commands/plan.md
**Type**: Slash command / workflow definition — a planning-mode command that creates a feature plan document on a branch with a PR; no production or test code allowed during execution
**Portable**: Yes — highly portable; the plan file template, BDD acceptance criteria pattern, per-step TDD loop (RED/GREEN/REFACTOR/MUTATE/Done-when), and Pre-PR Quality Gate are all stack-agnostic
**Reason**: Nothing in the core workflow (branch → explore → write plan → PR) or the plan file template is project-specific; the `plans/<feature-name>.md` convention, BDD observable-outcomes framing, and the enforcement block (plan-only mode, prefer small PRs, test behaviour not implementation) apply to any engineering project
**Trigger**: Invoked as a `/plan` slash command in Claude Code when user wants to plan a feature before writing any code; also functions as a standalone SOP for structured pre-implementation planning sessions
**Steps/contract**: (1) If on main, create a feature branch first; (2) explore relevant codebase areas (read-only); (3) write plan to `plans/<feature-name>.md` (create directory if needed); (4) open a PR for plan review — zero implementation files touched. Plan file structure: **Goal** (one sentence) → **Acceptance Criteria** (BDD-style observable outcomes, checkbox list) → **Steps** (each: RED failing test → GREEN minimum code → REFACTOR if it adds value → MUTATE run mutation testing → Done-when) → **Pre-PR Quality Gate** (mutation testing, refactoring assessment, typecheck + lint, DDD glossary check)
**Strip**: `allowed-tools` and dynamic `!` context-injection frontmatter (Claude Code harness-specific); named skill cross-references (`mutation-testing`, `refactoring` skills — replace with verb descriptions); hardcoded `plans/` path (parameterise as a slot); "Delete this file when complete" cleanup instruction can stay but is cosmetic
**Structure/format**: Compact and well-layered — frontmatter tool allowlist, three `!`-injected live-context blocks (git log, current branch, active plans), numbered 4-step workflow, then a full fenced Markdown plan-file template, followed by a Constraints enforcement block; clean separation between "how to run the command" and "what the plan file looks like"
**Notes**: The standout portable primitive is the **plan file template** — Goal + BDD Acceptance Criteria + per-step RED/GREEN/REFACTOR/MUTATE + Pre-PR Quality Gate forms a complete pre-implementation planning contract rarely expressed this concisely elsewhere. The MUTATE step (mutation testing as a mandatory fourth step in the TDD loop) is an unusual and high-value extension — signals a mature testing culture and is worth preserving verbatim as a distinguishing element. The Constraints section is unusually strong enforcement language: "No production code without a failing test. No exceptions." and "plan-only mode — the only file you should create/modify is in plans/" — both worth lifting verbatim into a portable skill. Pairs with `tdd-guardian.md` (step execution) and `CLAUDE.md`'s TDD mandate (philosophy hub): this command handles the pre-execution planning layer. The "prefer multiple small PRs — each reviewable in isolation" constraint complements the plan template and is a portable PR hygiene SOP in miniature.

## claude/.claude/commands/continue.md
**Type**: Slash command (Claude Code `/continue`)
**Portable**: Yes — the pattern is repo-agnostic; only the `plans/` directory convention is repo-specific and easily parameterised
**Reason**: Encodes a clean post-merge handoff ritual: pull main, read plan, mark done items, cut next branch, summarise — all without starting implementation. Prevents the common failure mode of jumping straight into code before orientation.
**Trigger**: After a PR is merged and the developer is ready to start the next slice
**Steps/contract**: (1) `git checkout main && git pull`, (2) read plan file, (3) update plan (mark completed items), (4) create new branch, (5) summarise what was just completed and what is next
**Strip**: Inline dynamic context injections (`!` backtick blocks for `git branch` and `git log`) — those are Claude Code-specific slash-command syntax and won't transfer to a plain skill/SOP
**Structure/format**: YAML frontmatter (description + allowed-tools) → dynamic shell context → prose instruction list → constraints block; concise (~130 words)
**Notes**: The hard constraint "Do NOT start implementing anything yet" is a strong portable principle worth preserving verbatim. The `plans/` fallback ("if no plan file exists, ask") is a good defensive step. Overlaps with `next-slice` skill already in this repo — check for delta before promoting.

## claude/.claude/commands/setup.md
**Type**: Slash command — one-shot project onboarding workflow (detect stack → generate Claude Code config files).
**Portable**: Partially — the detection phase logic is highly portable; the generation phase targets Claude Code-specific infrastructure (`.claude/` dir, hooks JSON schema, slash command format).
**Reason**: The two-phase structure (detect → generate) and the detection checklist (TS strict flags, DDD signals, hexagonal arch layout, 12-factor patterns, CI system, existing config) are strong reusable SOP material. The artifact schemas (`.claude/settings.json`, agent frontmatter) are platform-specific and should be stripped.
**Trigger**: User wants to bootstrap a new project for AI-assisted development — run once per repo.
**Steps/contract**: (1) Collect live context via shell probes (pwd, ls, cat manifests). (2) Detect: tech stack, TS strict config, CI pipeline, existing config, DDD glossary, hexagonal arch, 12-factor patterns. (3) Generate four artifacts: project CLAUDE.md, PostToolUse typecheck hook, `/pr` command, `pr-reviewer` agent — skipping anything that already exists. (4) Present summary and ask approval before writing any file.
**Strip**: `allowed-tools` frontmatter, the specific `.claude/settings.json` hook JSON schema, the Claude Code slash-command and agent file formats, the `twelve-factor` skill cross-reference, and `/generate-pr-review` command reference.
**Structure/format**: Markdown slash command with YAML frontmatter; two labelled phases (Analysis / Generation) with numbered sub-steps; explicit constraints block at end. Well-organised and scannable.
**Notes**: The "ask before overwriting" constraint is worth preserving verbatim in any extracted SOP. The detection checklist (DDD, hexagonal, 12-factor, TS strict) is the most reusable section and could become a standalone "stack-fingerprint" SOP step. Strong candidate for a `project-bootstrap` SOP covering the detection phase; generation phase should reference tool-specific skill variants rather than being baked in.

## claude/.claude/commands/pr.md
**Type**: Slash command / workflow definition — a PR creation command that enforces a pre-PR quality gate before running `gh pr create`
**Portable**: Partially — the gate checklist structure, PR body format spec, and "prefer small PRs" constraint are portable; the gate items reference repo-local skill names and the "no test plan needed" note presupposes a TDD context
**Reason**: The Pre-PR Quality Gate pattern (enumerate gates, enforce each, "run it now" instruction) transfers to any project; the PR body format (1–3 bullets, WHAT+WHY, no test plan section when TDD coverage is guaranteed) is a clean, opinionated PR hygiene contract. Gate *content* (mutation-testing skill, refactoring skill, DDD glossary) is repo-local and must be treated as a fillable slot in a portable extraction.
**Trigger**: Invoked as `/pr` slash command when user wants to create a pull request after completing work on a branch
**Steps/contract**: (1) Inject live context (`git log main..HEAD --oneline` + `git diff main...HEAD --stat`); (2) Evaluate 4 pre-PR gates — mutation testing, refactoring assessment, typecheck+lint zero-errors, DDD glossary conformance (conditional); (3) Run any incomplete gates before proceeding; (4) Create PR with `gh pr create`: title + body of 1–3 bullets (WHAT + WHY), no test plan section
**Strip**: `allowed-tools` frontmatter and `!`-injected context blocks (Claude Code harness-specific); named skill cross-references (`mutation-testing`, `refactoring` skills — replace with verb descriptions or leave as slots); DDD glossary gate (project-conditional — mark as optional slot); "No test plan needed" note should become conditional ("when tests are guaranteed by TDD workflow") rather than absolute
**Structure/format**: Minimal — frontmatter tool allowlist, two live-context injection lines, numbered 4-item gate checklist with inline `(if project uses DDD)` qualifier, and a compact PR Creation section with a Summary sub-spec and a tool invocation line; lean but sufficient for its scope
**Notes**: The narrowest of the three `commands/` files by portable surface area — most content is the gate checklist, which is largely skill-reference-heavy. The standout portable primitive is the **gate enforcement pattern** itself: enumerate required gates, check each, and block PR creation until all are satisfied (or explicitly waived). The "prefer small PRs — if independently mergeable, consider splitting" constraint is a clean PR hygiene micro-SOP worth preserving verbatim. The "no test plan section" rationale ("TDD means tests are already written and passing") is a useful philosophy statement to carry into a portable PR SOP as a conditional. Lowest standalone value of the three commands files; highest value as part of a feature-lifecycle trilogy with `plan.md` (pre-implementation planning) and `progress-guardian.md` (in-flight lifecycle management): plan → execute → PR.

## claude/.claude/commands/generate-pr-review.md
**Type**: Slash command / meta-command — a bootstrap generator that analyzes a project and produces three PR review artifacts: `.claude/agents/pr-reviewer.md`, `.claude/settings.json` (PostToolUse typecheck hook), and `.claude/commands/pr.md` (pre-flight PR command)
**Portable**: Partially — the project-discovery protocol (AI/LLM config file locations, ADR paths, CI config patterns, tech-stack detection) and the three-artifact generation scaffold are highly portable; the embedded "Global Rules (Non-Negotiable)" content encodes one team's specific philosophy (TDD-first hard gate, no `any` ever, functional-only, no comments, `type` over `interface`) and limits direct lift
**Reason**: The discovery checklist is unusually comprehensive and categorized — it removes the blank-page problem when onboarding a new project. The three-artifact output pattern (reviewer agent + settings hook + PR command) is a clean opinionated setup contract. The embedded global rules carry strong stylistic opinions that are appropriate as slots, not hard-coded mandates, in a portable version.
**Trigger**: One-time bootstrap slash command invoked when setting up PR review automation for a new project; generates the artifacts that `pr-reviewer.md` (already audited) describes executing
**Steps/contract**: (1) Analyze project across 6 source categories — AI/LLM config files (`.cursorrules`, `CLAUDE.md`, `copilot-instructions.md`, etc.), documentation (ADRs, `CONTRIBUTING.md`, `README.md`), CI pipeline (GitHub Actions, Forgejo, Woodpecker, CircleCI), tech stack (`package.json`, `tsconfig.json`, ESLint/Biome, test config), framework/library detection (React/Vue/etc., backend, state management, schema libs), existing code conventions; (2) Create `.claude/agents/pr-reviewer.md` with global rules + project-discovered conventions populated into template slots; (3) Generate `.claude/settings.json` with PostToolUse typecheck hook triggered on `.ts`/`.tsx` edits; (4) Create `.claude/commands/pr.md` with detected test/lint/typecheck commands as pre-flight gates; (5) Optionally create `.claude/skills/pr-review/SKILL.md` for complex patterns; (6) Emit summary report (files created → sources analyzed → key conventions found → usage instructions → customization guide)
**Strip**: Embedded "Global Rules (Non-Negotiable)" block (hardcodes one team's coding philosophy as non-negotiable — replace with a generic slot labeled `[PROJECT_CODING_STANDARDS]`); `allowed-tools` frontmatter and `!`-injected context blocks (Claude Code harness-specific); the PostToolUse hook command literal (harness-specific JSON); Step 5 optional skill creation (adds complexity without a clear trigger condition — fold into a note rather than a numbered step); the lengthy fenced example templates in Steps 2–4 (illustrative only — replace with slot-annotated skeletons in a portable skill)
**Structure/format**: Six numbered steps with extensive sub-sections; discovery sources organized into six named categories with concrete file paths per category; output artifact templates using `[DETECTED_*]` and `[PROJECT_*]` placeholder annotations; a final summary output format block. Structure is clear but verbose — the artifact templates dominate and could be collapsed to annotated skeletons with a reference to the companion `pr-reviewer.md` agent.
**Notes**: The standout portable primitive is the **project-discovery protocol** — the categorized, path-explicit inventory of where AI/LLM configs, ADRs, CI configs, and tech-stack signals live is unusually complete and rarely assembled in one place. Worth extracting as a standalone "project onboarding discovery checklist" SOP independent of the PR review use case. The second extract is the **three-artifact bootstrap pattern** (agent + settings hook + command) as a minimal opinionated PR review setup — this is a clean, self-contained workflow. Significant overlap with the already-audited `pr-reviewer.md`: this command is the meta-layer that *generates* the artifacts that `pr-reviewer.md` *executes*; the two are designed to work together. Weaker as a standalone portable SOP than `pr-reviewer.md` because it is a template generator rather than a review executor — its portability lives in its discovery protocol, not its output content.

## claude/.claude/skills/test-design-reviewer/SKILL.md
**Type**: Skill (agent-mode skill with frontmatter: name, description, context, agent, model)
**Portable**: Yes — fully portable; the entire evaluation framework references a public external standard (Dave Farley's 8 properties) with no project-specific coupling; attribution credits an upstream OSS repo
**Reason**: Scoring rubric (UMRANGT+F), weighted Farley Score formula, 1–10 band anchors per property, structured output template, and review process steps are universal test-quality heuristics applicable to any codebase in any language
**Trigger**: When reviewing tests, assessing test suite quality, or analysing test effectiveness against TDD best practices
**Steps/contract**: (1) Read tests before implementation → (2) Score each of 8 properties 1–10 with evidence → (3) Apply weighted Farley Score formula → (4) Output structured table + score + detailed analysis + ranked recommendations + reference link
**Strip**: Hardcoded `model: sonnet`, `agent: Explore`, `context: fork` frontmatter fields (runtime concerns, not SOP); attribution block is informational but can be retained as a comment
**Structure/format**: Excellent — self-contained rubric with per-property 1/3/5/7/10 band anchors, explicit weighted formula with rationale, score-band interpretations, a copy-paste output template, and a step-by-step review process; highly operationalisable
**Notes**: Complements `tdd-guardian` well — guardian enforces TDD discipline during development; this reviewer audits finished test suites against quality criteria. The Farley Score formula (with weights and justifications) is a rare concrete artifact worth preserving verbatim. The output template is production-ready. Consider pairing or cross-referencing with `tdd` skill in a merged SOP.

## claude/.claude/skills/testing/SKILL.md
**Type**: Skill definition — comprehensive behavior-driven testing SOP covering test philosophy, factory patterns, coverage analysis, and structural conventions
**Portable**: Yes — fully portable; no repo-specific business logic, toolchain, or tech-stack dependencies beyond TypeScript/Jest syntax in examples (easily generalised)
**Reason**: The core principles (test behavior not implementation, public-API-only testing, no 1:1 file mapping, factory-over-let/beforeEach) and the coverage theater detection taxonomy are universal testing best practices applicable to any language or framework with minor example substitution
**Trigger**: Writing tests, creating test factories, structuring test files, deciding what to test, or reviewing tests for quality; explicitly excludes UI-specific testing (separate skills for front-end/react testing)
**Steps/contract**: No procedural steps — this is a reference skill with four independent modules: (1) Test Through Public API: never test implementation, private methods, or internal state — always test via public API inputs and observable outputs; (2) Coverage Through Behavior: ask "what business behavior am I not testing?" not "what line am I missing?"; (3) Test Factory Pattern: `getMockX(overrides?: Partial<T>): T` — factory validates with real schema, returns complete object, no `let`/`beforeEach`, compose factories for nested objects; (4) Coverage Theater Detection: four named anti-patterns (mock-the-function-being-tested, test-only-that-function-was-called, test-trivial-getters/setters, 100%-line-coverage-zero%-branch-coverage) each with ❌/✅ code examples; (5) No 1:1 Mapping: tests organised by behavior/use-case not mirroring implementation file tree; (6) Summary Checklist: 10-item pre-commit gate
**Strip**: Nothing material — the skill is clean of repo-specific content; the `Partial<T>` and `UserSchema.parse()` TypeScript specifics in examples should be noted as "TypeScript idioms — generalise to target language" in a portable extraction, but they add clarity and can stay in a TS-targeted version; frontmatter `description` field references two companion skills (`mutation-testing`, `test-design-reviewer`) by name — make these generic cross-references in a portable version
**Structure/format**: Well-structured — frontmatter with name/description trigger, cross-skill load instructions at top, then six sections each anchored with a bolded principle statement, followed by ❌/✅ code examples; summary checklist at end serves as a standalone commit-gate artifact; highly scannable and copy-paste ready
**Notes**: Strong portable candidate but largely overlaps with content already captured in the `tdd-guardian.md` agent audit and the testing section of `CLAUDE.md` — both cover public-API-only testing and factory-over-beforeEach. The **incremental value** here is the Coverage Theater Detection section (four named anti-pattern patterns with ❌/✅ pairs) and the **No 1:1 Mapping** section — these are the most explicit and concrete treatments of these concepts in the citypaul-dotfiles audit and worth preserving verbatim. The factory composition pattern (nested `getMockOrder` composing `getMockItem`/`getMockCustomer`/`getMockPayment`) and the `Pick<T, 'field1'>` tip for constraining overrides are practical details not present in companion files. The 10-item summary checklist is a clean, self-contained pre-commit gate worth extracting independently. Overall: high-quality reference material with moderate standalone value given audit overlap — best role is as the "deep reference" companion to `tdd-guardian.md` (procedure) and `CLAUDE.md` (mandate hub).

## claude/.claude/skills/tdd/SKILL.md
**Type**: Skill definition (frontmatter: name, description) — standalone TDD workflow/process reference; self-described as complementing a separate `testing` skill (test-writing guidance) and `refactoring` skill (refactor method detail)
**Portable**: Yes — entirely stack-agnostic; no codebase references, only generic placeholder examples
**Reason**: RED-GREEN-REFACTOR cycle, 100% coverage gate with formal exception process, TDD evidence-in-commits guidance, and anti-patterns list are universally applicable regardless of language or framework
**Trigger**: Any code-generation task — features, bug fixes, refactoring; "TDD is non-negotiable" framing means it is always active
**Steps/contract**: RED (write failing test first, NO production code without it) → GREEN (minimum code to pass, commit immediately after green) → REFACTOR (assess after every green, commit before refactoring, all tests must pass after); supplementary gates: 100% coverage verified by running it yourself (never trust a claim), PR checklist (tests + lint + type checks pass, coverage verified, single feature/fix scope, behavior description)
**Strip**: `pnpm test:coverage` / `pnpm exec vitest run --coverage` toolchain-specific commands (generalise to "run your coverage tool"); vitest-format coverage output example (illustrative only — replace with a generic table skeleton); conventional commit format cheat-sheet (duplicated more concisely elsewhere; keep the type list, drop the repetitive prose intro); "load the `testing` skill" and "load the `refactoring` skill" cross-references (harness-local)
**Structure/format**: Well-structured — frontmatter → RED/GREEN/REFACTOR three-subsection phase guide → TDD Evidence in Commit History (default expectation + three named exception categories + PR documentation template) → Coverage Verification with verification steps, output-reading guide, and red-flag ❌ list → 100% Coverage Exception Process (three-step approval workflow) → Development Workflow with bash walkthrough → Commit Messages format → PR Requirements checklist → Refactoring Priority table (Critical/High/Nice/Skip) → Anti-Patterns ❌ list → Summary Checklist; highly copy-paste-ready
**Notes**: Complements `tdd-guardian.md` (sub-agent) and `CLAUDE.md` TDD mandate already audited; unique value-adds over those files are three distinct portables worth preserving verbatim: (1) the **TDD evidence-in-commits section** with three named exception categories (Multi-Session Work, Context Continuation, Refactoring Commits) each with an "Evidence:" fallback and a PR template to document exceptions — this is unusually mature guidance rarely stated explicitly; (2) the **100% coverage exception process** (document in package README → get explicit approval → document in CLAUDE.md/context file) — a three-step governance protocol that prevents coverage erosion without banning exceptions; (3) the **Refactoring Priority table** (Critical: fix now | High: this session | Nice: later | Skip: already clean) — a concise decision matrix that pairs cleanly with `refactor-scan.md`'s Skip classification. The "NEVER Trust Coverage Claims Without Verification" emphasis with the self-run verification protocol is strong and portable. Slight weakness: the anti-patterns list substantially overlaps with `tdd-guardian.md`; a merged skill should deduplicate. Overall, lower standalone value than `tdd-guardian.md` but the exception-process and evidence-in-commits sections are the strongest such guidance in the entire citypaul-dotfiles audit.

## claude/.claude/skills/mutation-testing/SKILL.md
**Type**: Skill definition (SKILL.md with frontmatter: name, description) — a comprehensive mutation testing SOP covering the full verify-test-effectiveness workflow
**Portable**: Yes — no project-specific business logic; TypeScript-flavoured examples are illustrative, not prescriptive; the operator taxonomy, execution process, and decision frameworks apply to any language and test suite
**Reason**: The mutation operator taxonomy, 4-step execution loop (identify changed code → mutate+run → report → fix), surviving-mutant triage heuristic ("fix immediately" vs "ask the human"), mutation score benchmarks, and equivalent-mutant detection guidance are universal testing quality concepts independent of stack, framework, or domain
**Trigger**: After the TDD GREEN phase to verify tests would catch realistic bugs; when reviewing code changes on a branch; when a test suite has high coverage but low confidence; when identifying weak tests; referenced directly by the MUTATE step in `commands/plan.md`'s per-step TDD loop
**Steps/contract**: (1) Identify changed code via `git diff main...HEAD` filtered to source files; (2) For each changed function, select applicable operators from taxonomy, mutate production code, run tests, evaluate killed/survived, always revert before next mutation; (3) Produce structured Markdown report with Killed list, Survived list (each with action), and summary score; (4) Fix surviving mutants using TDD — write failing test against mutated code first, verify passes against original; triage: fix immediately for realistic bugs in critical paths, ask the human when uncertain, complex, or likely equivalent
**Strip**: `Integration with Stryker` section (optional tooling appendix — not core SOP; can be a footnote); the ASCII TDD-integration diagram (decorative); the `Method Expression Mutations` table is TypeScript/JS-specific — move to a language-specific appendix or note as illustrative rather than normative
**Structure/format**: Excellent — Core Concept section with process summary, Execution Process section with 4 numbered steps, full Markdown report template with Killed/Survived/Summary slots, operator tables (Original | Mutated | Test Should Verify) with ❌/✅ before/after code examples, mutant-state table, mutation-score benchmark table (< 60% / 60–80% / 80–90% / > 90%), equivalent-mutant pattern catalogue, Branch Analysis Checklist with checkboxes, Strengthening Weak Tests section with 4 named patterns, and Quick Reference section. Highly copy-paste ready.
**Notes**: Three standout portable primitives worth preserving verbatim: (1) The **"fix immediately vs. ask the human" triage framework** — explicit criteria for when an agent should act vs. defer on surviving mutants (realistic bug? critical path? simple boundary test? vs. uncertain risk? complex to name? covered by integration tests?) — this nuanced human-in-the-loop contract is rarely stated this cleanly; (2) The **Quick Reference table** ("Operators Most Likely to Have Surviving Mutants" and "Test Values That Kill Mutants / Avoid") — a high-signal distillation that functions as a standalone checklist; (3) The **report template** (Killed / Survived / Summary with action field per survived mutant) — a concrete deliverable contract. The mutation score benchmark table provides enforceable targets. This skill is the MUTATE-step complement to tdd-guardian.md (RED/GREEN/REFACTOR) and progress-guardian.md (lifecycle management) — together with plan.md's per-step loop they form a complete four-step TDD discipline. The explicit "Do not just reason about whether tests would catch mutations — prove it" instruction is unusually strong and worth carrying verbatim to prevent agent reasoning-shortcuts.

## claude/.claude/skills/refactoring/SKILL.md
**Type**: Refactoring SOP — post-GREEN phase assessment, commit discipline, and speculative-code avoidance
**Portable**: YES
**Reason**: Entirely language/project-agnostic principles; priority classification table, DRY-as-knowledge framing, and commit-before-refactor rule are universally applicable to any TDD workflow
**Trigger**: After tests pass (GREEN phase); assess improvement opportunities before committing refactored code
**Steps/contract**: (1) GREEN — tests pass; (2) COMMIT working code; (3) REFACTOR — improve structure; (4) COMMIT refactored code; (5) Verify checklist (tests still pass, no new public APIs, behavior unchanged, no speculative code)
**Strip**: TypeScript code examples (illustrative only; portable version should use pseudocode or language-neutral snippets); `refactor:` commit message format (conventional-commits convention, may clash with other repos' policies)
**Structure/format**: Well-structured — priority table (Critical/High/Nice/Skip), DRY distinction (knowledge vs. structural similarity), "When NOT to Refactor" negative list including the testability-extraction anti-pattern; checklist at end; good reference density
**Notes**: The "Don't extract purely for testability" rule is an unusually strong and differentiated insight — worth preserving verbatim. Overlaps with the `tdd` skill's GREEN phase but focuses exclusively on the refactor step; complement, not duplicate.

## claude/.claude/skills/expectations/SKILL.md
**Type**: Composite rules skill — bundles TDD mandate, documentation-capture framework, and communication norms into one file.
**Portable**: Partially. The documentation framework (gotcha/pattern/decision taxonomy + format template) and the "what do I wish I'd known?" end-of-task prompt are highly portable. The TDD mandate and TypeScript-specific lines are project-flavoured.
**Reason**: The documentation framework is a genuinely reusable SOP — it gives a clear trigger ("at the end of every significant change"), a taxonomy of learning types, and a concrete Markdown format. The TDD section largely duplicates common TDD skills and adds little that isn't already in a dedicated TDD SOP.
**Trigger**: "document this", "remember this pattern", "what should I know about", after completing significant features, or any gotcha discovery.
**Steps/contract**: (1) At end of significant change, ask "What do I wish I'd known?"; (2) Evaluate against seven capture criteria; (3) Classify as Gotcha / Pattern / Anti-pattern / Decision / Edge case / Tool knowledge; (4) Write entry using the structured Markdown template (Context / Issue / Solution + code examples); (5) Update CLAUDE.md if meaningful architectural change.
**Strip**: TDD mandate (belongs in a TDD skill), TypeScript strict-mode line, "ask clarifying questions / think from first principles" generic guidance, Communication section (generic agent etiquette not documentation-specific).
**Structure/format**: Provides a concrete Markdown template for gotcha entries — reusable as-is.
**Notes**: The documentation-capture SOP is the keeper here. Worth extracting as a standalone `doc-capture-learnings` or `knowledge-capture` skill with the gotcha/pattern/decision taxonomy, the seven-criteria checklist, and the template. The bundling with TDD and generic working principles dilutes the skill's focus.

## claude/.claude/skills/functional/SKILL.md
**Type**: Coding-style / language-agnostic patterns skill — TypeScript-flavoured but principles are broadly portable.
**Portable**: YES — the six core patterns (pure functions, immutability, array methods over loops, options objects, composition, early returns + guard clauses) are language-agnostic conventions. The `readonly` / `ReadonlyArray<T>` TypeScript specifics are TS-only but the underlying concept strips cleanly.
**Reason**: Immutability, pure functions, guard clauses, and options-objects are universal best practices that appear across many codebases and are worth carrying forward as a standalone SOP. The "functional light" framing (no monads/fp-ts) is a valuable, opinionated constraint that prevents over-engineering.
**Trigger**: "writing logic, data transformations, mutation bugs, functional patterns" — description is accurate and well-scoped. Would fire correctly when FP guidance is needed and not over-fire for unrelated tasks.
**Steps/contract**: Comprehensive reference catalog with before/after examples for every pattern: immutable array ops, object spread, nested updates, early returns, Result<T> error type, pure function isolation, options objects, composition, readonly properties. Ends with a summary checklist. No sequential steps — it's a reference SOP, not a workflow.
**Strip**: TypeScript-specific syntax (`ReadonlyArray<T>`, `readonly` modifier, `type Result<T, E>`, generics) should be noted as TS-only when adapting to a language-agnostic SOP. The "no comments / self-documenting code" section and the JSDoc exception are good portable hygiene rules.
**Structure/format**: Well-structured with H2 sections, consistent ❌/✅ before/after code blocks, inline rationale, and a summary checklist. Very scannable. Could be condensed ~40% by collapsing repetitive before/after examples into a single reference table per topic, but the current verbosity is intentional as teaching material.
**Notes**: High-quality, opinionated skill. The "Functional Light" positioning (practical, not academic) is its distinguishing value vs. generic FP rules — worth preserving verbatim. The Result<T> error-handling pattern is a bonus that many FP skills omit. Strong candidate for a portable "functional-patterns" SOP with a TS variant and a language-agnostic abstract.

## claude/.claude/skills/typescript-strict/SKILL.md
**Type**: Skill definition (frontmatter: name, description) — TypeScript strict mode reference covering type vs interface guidance, tsconfig strict flag catalogue, schema-first boundary rule, and branded types pattern
**Portable**: Yes — largely portable; Zod-specific syntax in schema examples needs minor generalisation to "schema validation library"; no model/color/tools frontmatter to strip; cross-references to `functional` skill by name are harness-local but easily resolved
**Reason**: The type-vs-interface distinction, annotated tsconfig strict-flag block, schema-first-at-trust-boundaries decision matrix, and branded types pattern are applicable to any TypeScript project regardless of framework or toolchain
**Trigger**: Writing TypeScript code, defining types or schemas, or reviewing type safety in any TypeScript file
**Steps/contract**: Reference skill with five independent modules — (1) Core rules: no `any` (use `unknown`), no type assertions without justification; (2) type vs interface decision: `type` for data structures with `readonly`, `interface` for behavior contracts only; (3) tsconfig strict flags: full annotated block covering both the core `strict: true` umbrella and four "CRITICAL additional safety flags" (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noPropertyAccessFromIndexSignature`, `forceConsistentCasingInFileNames`), each with a one-line explanation; (4) schema-first at trust boundaries: explicit ARE-required vs AREN'T-required decision matrix with ❌/✅ code examples; (5) branded types: `& { readonly brand: unique symbol }` pattern with usage example; summary checklist (7 items) at end
**Strip**: Zod-specific syntax in schema examples (`z.object`, `z.string().email()`, `z.infer<>`) — generalise to `[schema-lib].object(...)` or add a note that Zod is illustrative; `// ✅ Define once` / `// ✅ CORRECT` inline comments in examples can stay; cross-references to `functional` skill by name — replace with "see your immutability/functional-patterns reference"
**Structure/format**: Frontmatter → Core Rules (4 items) → Type vs Interface (two subsections with code blocks + "Why?" rationale + Schema Duplication note) → Strict Mode Configuration (tsconfig block + per-flag explanation table) → Immutability note (deferred to companion skill) → Schema-First at Trust Boundaries (ARE/AREN'T sections with ❌/✅ pairs) → Branded Types (pattern + usage example) → Summary Checklist; clean, consistently structured, copy-paste ready
**Notes**: Significant content overlap with the already-audited `ts-enforcer.md` and the TypeScript section of `CLAUDE.md` — both cover no-`any`, no-assertions, and type-vs-interface preference. The **incremental value** here is threefold: (1) the **annotated tsconfig block** is the most complete TypeScript compiler config reference in the audit — includes the four additional safety flags that `strict: true` alone does not enable, with per-flag explanations that explain *why* each flag matters (e.g., `noUncheckedIndexedAccess` prevents runtime errors from assuming array elements exist); this block alone is a high-value portable artifact; (2) the **schema-first decision matrix** (when schemas ARE vs AREN'T required) is more nuanced than the bare mandate in CLAUDE.md — the AREN'T-required category (pure internal types, Result/Option types, utility types, interfaces, component props) prevents schema inflation; (3) the **branded types pattern** is not present in companion files and provides a concise, copy-paste-ready implementation. Best portable role: a "TypeScript strict config reference" skill that pairs with `ts-enforcer.md` (runtime enforcer) and CLAUDE.md (mandate hub). The `noUnusedParameters`-as-architecture-signal note is a compact, memorable insight worth preserving verbatim.

## claude/.claude/skills/ci-debugging/SKILL.md
**Type**: Skill (SKILL.md with frontmatter: name, description; plain Markdown body)
**Portable**: Yes — fully portable; no project-specific references, tooling, or business logic
**Reason**: The hypothesis-first diagnosis method, environment delta analysis matrix, full-error-reading discipline, fix-verification checklist, and anti-pattern taxonomy are universal CI/CD debugging practices applicable across any stack, language, or CI platform
**Trigger**: Load when debugging CI failures, build issues, or test pipeline problems (as stated in frontmatter)
**Steps/contract**: (1) Generate ≥3 hypotheses before investigating → (2) Reproduce locally using the exact failing command → (3) Run environment delta analysis across 8 factors (runtime version, OS, deps, env vars, parallelism, resources, network, filesystem) → (4) Read full error output from top (not just last line) → (5) Explain why fix addresses root cause, verify locally, confirm it doesn't mask a deeper issue
**Strip**: Nothing — the skill is entirely generic; the example hypotheses for a test timeout are illustrative rather than project-specific and should be kept
**Structure/format**: Well-structured — short principle statement, five named sections each with focused guidance, two comparison tables (environment delta matrix and anti-pattern taxonomy), and an evidence-based flakiness definition; concise and copy-paste-ready
**Notes**: The "Every CI failure is real until proven otherwise" axiom and the formal flakiness standard (identical environment + identified non-deterministic source = evidence required) are standout portable principles. The environment delta table is a practical diagnostic checklist that fills a gap not covered by other skills in this audit. Strong candidate for direct promotion.

## claude/.claude/skills/planning/SKILL.md
**Type**: Workflow/process skill — incremental planning, TDD integration, commit discipline, plan file lifecycle
**Portable**: Partially — the planning philosophy (small increments, known-good states, one-commit steps) and plan-file template are highly portable; the `/plan` and `/continue` command references and `plans/` directory convention are repo-specific conventions that would need adaptation
**Reason**: Strong, opinionated SOP for breaking features into small independently-mergeable PRs with full TDD loop (RED-GREEN-REFACTOR-MUTATE-FIX), explicit human-approval gates before each commit, and a structured plan-file format; mutation testing integration is distinctive and rarely seen in planning skills
**Trigger**: "Use when starting significant work or breaking down complex tasks" — broad but clear; fires on feature planning, task decomposition, or incremental delivery requests
**Steps/contract**: Explicit per-step contract: CONFIRM acceptance criteria → RED → GREEN → REFACTOR → MUTATE → FIX → STOP for commit approval; plan lifecycle: create → execute → delete on completion; plan changes require explicit approval before proceeding
**Strip**: `/plan` and `/continue` slash-command references (tool-specific); `plans/` directory path convention (project-specific); references to sibling skills (`tdd`, `testing`, `refactoring`, `mutation-testing`, `learn`, `adr` agents) — these should become standalone or link to equivalents; DDD glossary check in pre-PR gate (project-specific)
**Structure/format**: Excellent — prose rationale, ASCII-art flowcharts, anti-patterns section, quick-reference summary, full plan-file template in fenced markdown; very readable and self-contained modulo slash-command refs
**Notes**: The MUTATE phase (mutation testing as a mandatory gate) is an unusually high bar; worth preserving as an optional or recommended step rather than dropping entirely. The "delete plan file when done" lifecycle is clean and avoids stale plans accumulating. Human-approval-before-commit is a strong safety primitive worth extracting as a standalone rule. Overall one of the more complete planning SOPs seen — good candidate for promotion with minor genericisation.

## claude/.claude/skills/hexagonal-architecture/SKILL.md
**Type**: Skill (SKILL.md with name/description/trigger frontmatter)
**Portable**: Yes — the ports-and-adapters model, composition-root DI, testing-with-fakes strategy, CQRS-lite guidance, anti-pattern catalogue, and checklist are framework- and language-agnostic structural patterns; TypeScript code samples are illustrative, not binding
**Reason**: Hexagonal architecture applies to any backend project regardless of framework or database; the core invariants (domain has zero infra deps, adapters implement ports, driving adapters are thin glue, dependencies injected via parameters) are universally valid
**Trigger**: When implementing ports, adapters, dependency inversion, or domain isolation in a project that has opted into hexagonal architecture; also when introducing hex arch incrementally into an existing codebase
**Steps/contract**: (1) Define port interfaces in domain (named by business purpose, never technology); (2) Implement driven adapters per technology — translate domain types ↔ infra types, no business logic; (3) Wire at composition root (entry point): create adapters from config, inject into use cases; (4) Use cases named after business operation, take ports as params; (5) Reads that JOIN across aggregates use query functions (CQRS-lite), bypass repos intentionally; (6) Test at use-case boundary with in-memory fakes (not mocks); (7) Checklist: domain has no infra imports, swapping any adapter requires zero domain changes
**Strip**: All `resources/` relative-link references (worked-example.md, testing-hex-arch.md, cqrs-lite.md, cross-cutting-concerns.md, incremental-adoption.md) — these point to sibling files that will not exist in a new context; also strip `../REFERENCES.md` cross-reference; resource table can be collapsed to a note that extended guidance should be authored per-project
**Structure/format**: Excellent — ASCII port/adapter diagram, concept prose, wrong/right code examples with inline annotations, file-organisation table (layer/location/contains/tests), testing-priority table, cross-cutting-concerns table, anti-patterns section (five named patterns each with ❌/✅ code blocks), and an 11-point completion checklist; highly copy-paste-ready
**Notes**: Among the strongest architecture skills in this audit. The ASCII driving/driven diagram, the "if swapping an adapter requires changing domain code, the boundary is wrong" heuristic, and the explicit DI composition-root pattern are portable gems worth preserving verbatim. The skill correctly delineates hex arch from DDD (structural isolation vs domain model) — that framing distinction is worth keeping. Main gap without the resource files is depth on incremental adoption and detailed test patterns; a portable version should note these as "extend with project-specific resources."

## claude/.claude/skills/twelve-factor/SKILL.md
**Type**: Skill definition — comprehensive reference skill covering all 12 factors of the 12-Factor App methodology with TypeScript/Node.js code examples, brownfield adoption priority order, anti-pattern callouts per factor, and a 17-item compliance checklist
**Portable**: Yes — fully portable; the 12-Factor methodology is a universal standard applicable to any deployed application regardless of language or framework; TypeScript/Node.js code examples are illustrative and incidental
**Reason**: The greenfield/brownfield split, brownfield adoption priority order, anti-pattern rationale blocks, graceful-shutdown contract, structured-logging semantic requirements, and compliance checklist are all language-agnostic best practices applicable to any service, API, worker, or frontend deployment
**Trigger**: Configuring environment variables; connecting to backing services; structuring application startup/shutdown; handling graceful shutdown and process signals; setting up a new deployed service or reviewing deployment readiness of an existing one
**Steps/contract**: Reference skill — no procedural steps; organized by factor with: (1) cross-applicability scoping (all deployed apps vs. backend-only factors); (2) brownfield adoption priority order (Config → Logs → Disposability → Backing services → Stateless processes); (3) per-factor prose + code examples + anti-pattern block with "why these are wrong" rationale; (4) graceful shutdown template (SIGTERM/SIGINT handlers, 30s drain timeout, force-exit fallback, drain in-flight connections, non-zero exit on failure); (5) 7-property structured logging semantic contract (structured output, stdout/stderr only, standard levels, contextual data, ISO 8601 timestamp, request correlation — library-agnostic); (6) 17-item compliance checklist
**Strip**: TypeScript/Node.js-specific syntax in code examples (Zod `ConfigSchema`, `z.coerce`, `z.infer`, Prisma/Redis client imports) — generalise to pseudo-code or mark as "TypeScript idioms" in a portable extraction; cross-skill references (`typescript-strict`, `testing`, `hexagonal-architecture`, `functional` skills) — harness-local, replace with generic cross-reference slots; `.env.example` sample (illustrative — can condense to a note)
**Structure/format**: Well-structured — frontmatter trigger description; "When to Apply" section with greenfield/brownfield split; 12 factor sections each with prose + code examples + anti-pattern block; compliance checklist at end; ~400 lines due to code-heavy examples but all sections are independently extractable; companion to `twelve-factor-audit.md` (audit executor already audited) — together they form a reference/guide + compliance-checker pair
**Notes**: Three standout portable extracts: (1) **Brownfield adoption priority order** (Config → Logs → Disposability → Backing services → Stateless processes with rationale per tier) — a practical sequencing contract rarely stated this explicitly anywhere; (2) **Graceful shutdown template** with 30s drain timeout, force-exit fallback, and non-zero exit on failure — production-ready and language-adaptable; (3) **Structured logging semantic contract** (7 required properties, library-agnostic) — fills a gap that most logging guidance leaves implicit. The cross-applicability distinction (all-app factors vs. backend-only factors) is a useful scoping clarification absent from the companion audit agent. Overlaps significantly with `twelve-factor-audit.md` — the two are designed as a pair; this skill is the implementation guide, that agent is the compliance checker. Neither fully replaces the other. The compliance checklist at the end is a clean standalone pre-deploy gate worth extracting independently.

## claude/.claude/skills/frontend-design/SKILL.md
**Type**: Skill definition — aesthetic design philosophy and implementation guidance for distinctive frontend interfaces; no procedural steps, pure reference/philosophy doc
**Portable**: Yes — entirely project-agnostic; pure design philosophy with no codebase, domain, or toolchain dependencies
**Reason**: All content (typography opinionation, color commitment, motion hierarchy, spatial composition, background atmosphere) addresses universal frontend aesthetics and applies to any framework (HTML/CSS, React, Vue, Svelte, etc.)
**Trigger**: User asks to build web components, pages, applications, dashboards, posters, or any UI; explicitly framed as "use when … styling/beautifying any web UI"
**Steps/contract**: No procedural steps — reference doc with four modules: (1) Design Thinking pre-code checklist (Purpose / Tone / Constraints / Differentiation); (2) Typography — use distinctive non-generic fonts, pair display + body; (3) Color/Theme — commit to cohesive palette with dominant + sharp accent; (4) Motion — prioritise CSS-only, one well-orchestrated page load with staggered reveals over scattered micro-interactions; (5) Spatial Composition — asymmetry, overlap, diagonal flow, grid-breaking; (6) Background & Visual Details — gradient meshes, noise textures, layered transparencies, shadows; explicit NEVER list: Inter/Roboto/Arial, purple gradients on white, predictable layouts, Space Grotesk convergence
**Strip**: Nothing to strip — no project-specific content at all; the "CRITICAL" and "IMPORTANT" bold callouts are useful signal-to-noise markers worth preserving; no frontmatter to remove beyond standard skill header
**Structure/format**: Frontmatter (name, description, license) → prose intro → Design Thinking section (bullet checklist) → implementation requirements → Frontend Aesthetics Guidelines (five named sub-sections each with bolded heading and prose) → NEVER block; compact and scannable; ~500 words; no redundancy
**Notes**: Near-identical in scope and content to the `web-frontend-design` skill already in this workspace (`/Users/mia/.agents/skills/web-frontend-design/SKILL.md`). Incremental value over the existing skill: the explicit **one well-orchestrated page-load animation > scattered micro-interactions** heuristic and the **"Differentiation" forcing question** ("What's the one thing someone will remember?") are more crisply stated here than in most comparable skills. The NEVER list (including the explicit ban on Space Grotesk convergence across generations) is unusually specific and practical. Low standalone SOP value — not a workflow or process; functions as a quality-bar reference injected into any frontend generation task. Recommend comparing verbatim against the existing `web-frontend-design` skill to check whether those two incremental refinements warrant a merge rather than a new extraction.

## claude/.claude/skills/domain-driven-design/SKILL.md
**Type**: Skill (frontmatter-triggered Claude skill; TypeScript-focused DDD reference with companion `resources/` directory)
**Portable**: Conditionally yes — core DDD patterns (ubiquitous language, value objects, entities, aggregates, domain services, repository pattern, bounded contexts, error modeling) are universally applicable to any TypeScript DDD project; code examples are idiomatic and transferable
**Reason**: Comprehensive, deeply structured DDD skill with decision tables, code examples for every building block, anti-patterns, and a thorough checklist; the only coupling is to companion skills (`hexagonal-architecture`, `typescript-strict`) and `resources/` sibling files that are not self-contained within this file
**Trigger**: Implementing ubiquitous language, value objects, entities, aggregates, domain events, domain services, or bounded contexts in TypeScript; explicitly NOT for simple CRUD or projects without domain modeling
**Steps/contract**: (1) Assess DDD fitness (complex domain rules present?) → (2) Establish glossary/ubiquitous language file → (3) Model value objects with factory functions enforcing invariants → (4) Define entities with branded IDs and always-valid principle → (5) Apply "make illegal states unrepresentable" with discriminated unions → (6) Design aggregates (root owns all invariants; cross-aggregate refs by ID only) → (7) Extract domain services for cross-aggregate logic → (8) Define repository interfaces in domain layer → (9) Model expected outcomes as discriminated union result types → (10) Draw bounded context boundaries; run end-of-task checklist
**Strip**: All `resources/` directory `Load when...` references (aggregate-design.md, domain-services.md, domain-events.md, bounded-contexts.md, error-modeling.md, testing-by-layer.md) — sibling files not present; `../REFERENCES.md` pointer; cross-skill `Load the hexagonal-architecture skill` / `typescript-strict skill` references — replace with inline note or drop; domain-specific example names (OccasionId, GiftIdeaId, Gifting Context) are fine to keep as illustrations
**Structure/format**: Excellent — frontmatter, when-to-use decision tree, "where does code belong" decision table, TypeScript code blocks for every building block (value objects, branded types, entities, discriminated unions, aggregates, specifications, domain services, repository pattern, error modeling, bounded contexts), anti-patterns section, and a 17-point actionable checklist; highly copy-paste-ready
**Notes**: One of the strongest DDD skills encountered in this audit. Stand-out portable gems: "Where Does This Code Belong?" decision table (pure ≠ domain; business rule test is the filter), the purity-is-necessary-but-not-sufficient caveat, "make illegal states unrepresentable" with exhaustive switch + never guard, domain-service-vs-use-case comparison table, always-valid principle articulation, and the Specifications (predicate function) pattern. The checklist alone is worth extracting. Would promote with resource references stripped and key resource content folded inline or noted as future companion files.

## claude/.claude/skills/react-testing/SKILL.md
**Type**: Skill definition — React-specific testing SOP covering vitest-browser-react (preferred, real-browser) and @testing-library/react (legacy fallback), with hooks, context, forms, anti-patterns, and special cases (loading states, error boundaries, portals, Suspense)
**Portable**: Yes — highly portable within the React ecosystem; no project-specific business logic; all patterns reference standard, widely-adopted React tooling
**Reason**: React + Vitest/Testing Library are de facto standard choices across projects; the vitest-browser-react guidance adds modern real-browser testing advice that supersedes jsdom-based patterns and is rarely documented this clearly; the anti-patterns section is precise and actionable; no repo-specific content anywhere
**Trigger**: Any React component, hook, context, or form testing task; skill explicitly defers to `front-end-testing` for query/event/async/a11y patterns and to `tdd` for workflow — making it a focused rendering-primitives complement in a multi-skill test suite
**Steps/contract**: Reference skill (not procedural); eight independent modules: (1) vitest-browser-react setup (config + async render + expect.element auto-retry + no act() needed); (2) component testing — props, callbacks, conditional rendering; (3) hooks with renderHook (async in browser mode, sync in RTL); (4) context with wrapper option and multi-provider AllProviders pattern; (5) forms — controlled inputs, submission, validation; (6) five anti-patterns with ❌/✅ pairs (unnecessary act(), manual cleanup(), beforeEach render, testing internals, shallow rendering); (7) special cases — loading/error-boundary/portal/Suspense; (8) summary checklist (12 items)
**Strip**: Frontmatter cross-references to `front-end-testing` and `tdd` skill names (harness-local — replace with role descriptions); `npm install -D` command with exact packages (move to setup appendix); vitest.config.ts full config block (illustrative only — condense to a prose description with a link slot); `renderHook` + `act()` caveats for browser mode vs. jsdom are slightly inconsistent (browser mode uses `await act()`, RTL does not — clarify with a comparison table in portable extraction
**Structure/format**: Excellent — frontmatter with name/description/trigger, two clearly demarcated sections (vitest-browser-react preferred vs. legacy RTL), each with component/hook/context/form subsections, ❌/✅ code examples throughout, horizontal-rule separators, and a 12-item summary checklist at end; highly scannable and copy-paste ready
**Notes**: The vitest-browser-react section is the highest-value new material in this file — async render(), expect.element() auto-retrying assertions, no-act() rationale, and the pre-test auto-cleanup behaviour (before, not after, enabling component visibility during debugging) are modern details not present in companion testing skills. The anti-patterns section is very clean: the "factory function per test vs. beforeEach shared state" and "no shallow rendering" patterns are stated more concisely here than in the `testing` skill. Overlaps with `testing.md` (factory patterns, public-API principle) and `front-end-testing` (query/event patterns) — this file focuses specifically on React rendering primitives. The 12-item summary checklist is a clean standalone pre-commit gate for React test authors. Together with `testing.md`, `front-end-testing`, and `tdd`, this forms a complete four-skill React testing SOP suite.

## claude/.claude/skills/front-end-testing/SKILL.md
**Type**: Skill definition — comprehensive behavior-driven UI testing reference covering Vitest Browser Mode, DOM Testing Library, async patterns, MSW integration, accessibility-first testing, and a 14-item anti-pattern catalog
**Portable**: Yes — fully portable; no project-specific business logic, domain, or codebase references; all toolchain examples (Vitest, Playwright, MSW, Testing Library) are widely-used open-source tools
**Reason**: The behavior-driven testing philosophy (test what users see, not implementation details), 8-tier query priority hierarchy, userEvent-over-fireEvent mandate, async testing patterns (findBy / waitFor / waitForElementToBeRemoved), MSW network-level interception pattern, accessibility-first query reasoning, and 14-item anti-pattern catalog are universally applicable to any web frontend regardless of framework
**Trigger**: Writing UI tests for any front-end application; querying DOM elements; simulating user interactions; setting up browser-based testing infrastructure; reviewing tests for quality against accessibility and behavior-first standards
**Steps/contract**: Reference skill with nine independent modules: (1) Vitest Browser Mode setup and why-over-jsdom (6-dimension comparison table, config, built-in locators, auto-retrying `expect.element()` assertions, CDP-based userEvent, multi-project setup, gotchas, idempotency rules); (2) DOM Testing Library legacy patterns; (3) Query selection priority — 8-tier accessibility-first hierarchy (role → labelText → placeholderText → text → displayValue → altText → title → testId) with rationale and ❌/✅ examples; (4) User event simulation — userEvent.setup() per-test pattern vs fireEvent; (5) Async testing — findBy, waitFor (single-assertion rule, no side effects in callback), waitForElementToBeRemoved; (6) MSW integration — setupServer, default handlers, per-test overrides; (7) Accessibility-first testing — when to add ARIA, semantic HTML priority; (8) 14-item anti-pattern catalog each with ❌/✅ pair and explanation; (9) Summary checklist (12 checkboxes)
**Strip**: Companion skill cross-references (`react-testing`, `tdd`, `testing` — harness-local; generalise to verb descriptions); "2025" annotation in userEvent.setup() comment (cosmetic); frontmatter `description` field reference to `react-testing` skill
**Structure/format**: Excellent — frontmatter trigger description; clear `##`/`###` heading hierarchy; 6-column decision table (jsdom vs Browser Mode); fenced code blocks throughout with `// ✅ CORRECT` / `// ❌ WRONG` inline annotations; numbered priority list with rationale per tier; 14 mini anti-pattern sections each structurally consistent; 12-item summary checklist; highly scannable and copy-paste ready
**Notes**: One of the strongest portable testing SOP candidates in this audit. Four standout extracts worth preserving verbatim: (1) the Vitest Browser Mode vs jsdom comparison table — concise, convincing, and rarely assembled this clearly in one place; (2) the 8-tier query priority hierarchy with embedded accessibility rationale — simultaneously a testing SOP and an a11y forcing function; (3) the idempotency rules for browser-mode tests (self-contained state, unique identifiers, clean DOM per test) with ❌/✅ example — addresses the hardest failure mode in parallel browser test execution; (4) the 14-item anti-pattern catalog — unusually complete and actionable. Moderate overlap with `testing/SKILL.md` (behavior-not-implementation, factory pattern) and `tdd/SKILL.md` (workflow) — this skill's unique territory is browser-mode setup, async patterns, MSW, and the accessibility-first query hierarchy. Together with `testing/SKILL.md` and `tdd/SKILL.md` this forms a complete testing trilogy: philosophy+factories (testing) → workflow (tdd) → UI-specific patterns+async+a11y (front-end-testing).

## claude/.claude/skills/domain-driven-design/resources/domain-services.md
**Type**: Resource/reference document — a conceptual DDD reference explaining the domain service pattern; a supporting file loaded by a parent DDD skill, not a standalone skill definition
**Portable**: Yes — principles are language-agnostic at the conceptual level; TypeScript examples are illustrative and transfer with minor syntax substitution
**Reason**: The when-to-use / when-NOT-to-use decision taxonomy, naming convention rule, and testing guidance are universal DDD principles independent of any framework, stack, or project domain
**Trigger**: When deciding where business logic belongs in a layered/DDD architecture — specifically when distinguishing domain service vs entity vs use case vs infrastructure adapter; useful before implementing any new business operation
**Steps/contract**: Not procedural — four reference modules: (1) **when-to-use**: logic spans multiple aggregates / doesn't fit a single entity / is a named business concept domain experts use; (2) **when-NOT-to-use**: single-entity logic → put it on the entity as a pure function; orchestration (load/call/save) → use case; display/formatting → lib or view layer; infrastructure (email, external API) → adapter; (3) **naming**: name after the business operation (`pledgeContribution`, `placeOrder`), never after the pattern (`ContributionService`, `PlaceOrderUseCase`); distinguish use case from domain service by signature (use case takes ports/infrastructure interfaces; domain service takes only domain types); (4) **testing**: domain services are pure functions — unit test with no mocking, pass domain objects in, assert result
**Strip**: Project-specific domain identifiers (`walletBalance`, `isFundingClosed`, `occasionId`, `contributorId`) in examples — these are illustrative and should stay as-is (they concretise the pattern effectively); no harness-specific frontmatter or tool references to remove
**Structure/format**: Compact (~100 lines), four H2 sections, TypeScript code fence contrasting domain service and use case side-by-side with inline comments labelling each; second fence shows signature-based identification; third fence shows unit test pattern; bullet lists for when-to/when-not-to use; clean and scannable
**Notes**: Supporting resource rather than a top-level SOP — highest value as a companion reference card within a broader DDD skill. The standout portable extract is the **when-NOT-to-use taxonomy**: four crisp single-sentence boundary rules (entity / use case / presentation / infrastructure) prevent the most common DDD placement errors. The **signature-based identification heuristic** ("you can tell a use case from a domain function by its signature, not its name — use cases take ports as parameters") is a precise, memorable rule rarely stated this cleanly. Narrow scope (domain services only) gives it moderate standalone portability; pairs naturally with entity, aggregate, value-object, and use-case reference files for a complete DDD resource library. Lower priority for promotion than procedural skills but solid material for a `domain-driven-design` skill's resources directory.

## claude/.claude/skills/domain-driven-design/resources/domain-events.md
**Type**: Reference/educational resource document — deep DDD sub-resource (not a skill or agent definition); pure Markdown with no frontmatter or scaffolding
**Portable**: Partially — the decision frameworks, dispatch-mechanism selection table, and Decider pattern are fully portable; TypeScript code examples are language-specific but serve as clear illustrations rather than harness dependencies
**Reason**: Contains multiple independently extractable decision frameworks that apply to any DDD project regardless of language: when to use vs. avoid domain events (with Khorikov citation on same-transaction consumers), escalating dispatch mechanism guidance (explicit returns → in-process → outbox → event sourcing), and process manager use/avoid criteria. No repo-specific business logic, toolchain, or infrastructure references.
**Trigger**: When implementing cross-aggregate coordination, choosing event dispatch strategies, designing long-running workflows, or testing domain logic that produces events
**Steps/contract**: Not procedural — reference material with four key frameworks: (1) Use/avoid decision criteria for domain events (cross-aggregate side-effects and open/closed extension = use; same-aggregate or same-transaction = avoid); (2) Decider pattern — three pure functions: `decide(command, state) → events`, `evolve(state, event) → newState`, `initialState`; (3) Dispatch mechanism escalation table — explicit returns (lowest complexity) → in-process dispatch (at-most-once) → outbox pattern (at-least-once) → full event sourcing (complete history); (4) Process manager pattern — pure function `(state, event) → {newState, commands}` for multi-aggregate workflows with compensation
**Strip**: Nothing harness-specific; TypeScript syntax in examples is illustrative and can stay in a TS-targeted extraction — generalise to pseudocode for a language-agnostic version; the Domain Events vs Integration Events table is self-contained and clean as-is
**Structure/format**: Pure Markdown reference doc — H2 sections (When to Use / When to Avoid / Decider Pattern / Explicit Returns / Domain vs Integration Events / Naming / Dispatching / Process Managers / Testing); each section mixes prose rationale, code examples, and comparison tables; no frontmatter, no skill scaffolding; not copy-paste-ready as a skill without wrapping in skill frontmatter
**Notes**: Lower priority as a direct standalone SOP candidate — this is a deep reference document, not an actionable workflow. Three portable gems worth extracting verbatim: (1) The **"when to avoid" section** — the Khorikov quote ("If all the consumers of an event reside within the same database transaction, domain events add very little value") and the "start with explicit returns, add events when explicit returns can't express the coordination" escalation principle are rarely stated this cleanly; (2) The **dispatch mechanism escalation table** (4 rows: mechanism / reliability / complexity / use-when) — a concrete decision aid that prevents over-engineering; (3) The **testing principle** ("no mocks, no event bus setup, no subscriber verification — test the decision, not the plumbing") — a crisp, portable testing heuristic. Best role in a portable SOP set: supporting reference material for a DDD or domain-events skill, paired with the Decider pattern as its architectural anchor. The process manager section is the most advanced and most underrepresented concept in typical DDD guidance — worth preserving as a standalone "when to use a process manager" decision block.

## claude/.claude/skills/domain-driven-design/resources/bounded-contexts.md
**Type**: Reference resource / educational document — DDD bounded contexts guide covering context mapping patterns, Anti-Corruption Layer, Shared Kernel, code structure options, enforcement mechanisms, and context discovery heuristics; no procedural workflow or agent definition
**Portable**: Yes — fully portable; no project-specific business logic; the "gifting/budgeting" and Stripe/PaymentResult examples are clearly illustrative placeholders; pure DDD concepts applicable to any domain-driven project
**Reason**: Bounded-context theory, ACL implementation contract, Shared Kernel constraints, 3-tier split-signal taxonomy, and 4 discovery techniques are universal DDD guidance independent of language, framework, or domain
**Trigger**: Designing or refactoring a system with DDD; adding a service/feature that touches multiple domains; language friction appears ("billing user" vs "gifting user") suggesting a boundary; team asks "should we split this?"
**Steps/contract**: Not procedural — 6 independent reference modules: (1) Context Mapping Patterns table (6 patterns: ACL, Shared Kernel, Published Language, Open Host Service, Conformist, Separate Ways — each with relationship description and when-to-use); (2) ACL implementation rule: translate at the boundary, domain code never sees the external model; (3) Shared Kernel constraints: value objects only, no entity types, no business logic beyond construction/validation, warning signs list; (4) Code structure options: directory-based, monorepo packages, separate services — deployment model is independent of context boundary; (5) Enforcement mechanisms: ESLint boundary rules, barrel exports via `index.ts`, code review against glossary, separate packages; (6) Discovery heuristics: Language Test table, 3-tier signal taxonomy (Strong/Moderate/Weak), 4 numbered discovery techniques, Common Mistakes list
**Strip**: Nothing material — no harness-specific content; TypeScript syntax in ACL example is illustrative and can be kept or generalised; "gifting/budgeting" directory names are placeholders already visually obvious as such; Stripe type names in ACL example can be noted as illustrative
**Structure/format**: Well-organised — opening definition sentence, then 6 `##` sections; Context Mapping Patterns uses a 3-column table (Pattern / Relationship / When to use); signal taxonomy uses 3-tier bold headers (Strong/Moderate/Weak) with bullet sub-lists; discovery techniques are numbered with embedded worked example showing "Occasion" divergence across gifting vs notifications workflows; Common Mistakes are bulleted with bold anti-pattern names; highly scannable
**Notes**: Four standout portable primitives worth preserving verbatim: (1) the 6-pattern Context Mapping table — the most concise decision guide for inter-context integration I've seen, with clear when-to-use framing; (2) the 3-tier split-signal taxonomy (Strong: split now / Moderate: consider splitting / Weak: probably don't split yet) — unusually structured and directly combats premature boundary creation, a failure mode rarely addressed explicitly; (3) the Language Test table showing same-word divergence across contexts — vivid, concrete, immediately usable in a team conversation; (4) the "deployment model is independent of the context boundary" statement — a common misconception addressed in one sentence. The worked boundary-discovery example (Occasion appearing in both gifting and notifications workflows but using different fields and rules) is the best illustration of the discovery method. This file is a natural companion to the `ubiquitous-language` skill already in this repo and would pair with it in a DDD skill bundle. Weaker standalone SOP candidate than the agent definitions (no procedure, no deliverable contract) but high reference value as a DDD resource skill.

## claude/.claude/skills/domain-driven-design/resources/aggregate-design.md
**Type**: Reference/resource document — deep-dive companion within the domain-driven-design skill suite; not a skill definition or agent, no frontmatter
**Portable**: Yes — all content is domain-agnostic; TypeScript examples are illustrative only; the principles transfer to any OO or functional codebase
**Reason**: The always-valid principle, aggregate-sizing heuristic, one-aggregate-per-transaction rule, aggregate-root rules, split-vs-combine decision framework, and optimistic-locking guidance are canonical DDD patterns independent of language, ORM, or business domain
**Trigger**: When designing or reviewing aggregates in a DDD context; when hitting consistency issues between entities; when deciding whether to split or merge aggregates; when adding concurrency control to domain models
**Steps/contract**: Not a procedural SOP — five independent decision frameworks: (1) Always-Valid: enforce invariants in factory and every state-transition, never allow temporary invalid states; (2) Sizing test: "Does modifying X require checking Y's state to maintain an invariant?" → yes = same aggregate, no = reference by ID only; (3) One-aggregate-per-transaction: domain service for cross-aggregate computation → sequential saves in use case → eventual consistency via domain events if strong consistency unnecessary; (4) Root rules: external access only through root, root enforces all invariants, delete cascades from root, root IDs globally unique; (5) Split-vs-combine: split on independent change reasons / performance / concurrency; combine when invariant spans both; default to combined, split when pain is felt; (6) Optimistic locking: version field on aggregate, rowsAffected === 0 = conflict; add when multi-user + long-lived + concurrent modifications violate invariants; skip for single-user, append-only, or short-lived aggregates
**Strip**: TypeScript-specific syntax and `OccasionSchema.parse()` / Drizzle ORM `.set()`/`.where()` idioms in examples (toolchain-local); Occasion/GiftIdea domain details (retain structural patterns, substitute generic names in a portable extraction)
**Structure/format**: Six principle-anchored sections, each leading with a bold statement followed by ❌/✅ code examples and decision heuristics; no checklist or deliverable-contract structure; readable as a reference but not immediately commit-gate ready as a standalone SOP — best paired with a higher-level DDD skill that calls it as a resource
**Notes**: Content largely mirrors canonical DDD literature (Evans, Vernon) — solid and correct, but not novel. Three standout portable primitives worth preserving verbatim: (1) the sizing test question — "Does modifying X require checking Y's state to maintain an invariant?" — crisp, memorable, immediately applicable; (2) the split-vs-combine heuristic with explicit anti-premature-splitting advice ("Start combined, split when you feel the pain") and the acknowledgement that aggregate boundaries are expected to evolve; (3) the optimistic-locking when-to-add / when-unnecessary checklist — practical and rarely stated this clearly. Moderate-priority portable candidate; weaker standalone value than top-tier agents (tdd-guardian, docs-guardian) because it is a reference resource rather than a workflow driver. Highest value as a linked companion to a `domain-driven-design` aggregate-design skill.

## claude/.claude/skills/hexagonal-architecture/resources/cross-cutting-concerns.md
**Type**: Reference/resource document (companion to hexagonal-architecture skill — not a standalone skill, no frontmatter)
**Portable**: Yes — fully portable; purely architectural principle, zero project-specific logic or business domain references
**Reason**: Provides sharp, actionable placement rules for every major cross-cutting concern (auth vs authz, logging, transactions, error formatting, input vs business validation) in hexagonal / ports-and-adapters architecture. The authn=adapter / authz=domain distinction is a high-value, commonly-confused rule. Includes a summary table that works as a standalone quick-reference card.
**Trigger**: When designing or reviewing hexagonal architecture and deciding where auth, logging, transactions, or error-formatting code should live; when a reviewer sees logging or HTTP status codes leaking into domain code
**Steps/contract**: Not imperative steps — organised as one section per concern (Auth, Logging, Transactions, Error Formatting, Validation) each with principle statement, before/after TypeScript examples, and a closing summary table. Contract: domain stays pure (returns business reasons), adapters handle all protocol/infrastructure side effects.
**Strip**: TypeScript-specific syntax in code blocks can be replaced with pseudocode for a language-agnostic version; no names or project identifiers to strip
**Structure/format**: H2 sections per concern → principle statement → illustrative code pair → callout rule → H2 Summary table. Clean and skimmable.
**Notes**: Strong candidate for inclusion in a hexagonal-architecture skill as a sub-resource. The summary table alone (auth/authz/logging/transactions/error-formatting/validation × where/why) is worth preserving verbatim. Could also surface as a standalone "cross-cutting-concerns" quick-reference SOP independent of any architecture skill.

## claude/.claude/skills/domain-driven-design/resources/testing-by-layer.md
**Type**: Reference resource (testing strategy guide) — part of a DDD skill suite, not a standalone workflow SOP
**Portable**: Yes — moderate-to-high priority; the testing pyramid and fake-over-mock reasoning are broadly applicable; the DDD framing and TypeScript examples are incidental scaffolding around portable principles
**Reason**: Defines a clear, opinionated four-tier testing strategy (use case → domain pure functions → driven adapters → E2E) grounded in Valentina Jemuović's UCDD, with strong rationale for each tier's scope. The fake-vs-mock table and the compile-time safety argument ("if the interface changes, the fake breaks at compile time") are concise, portable gems rarely stated this precisely. The property-based testing section with `fast-check` is a useful companion, though niche.
**Trigger**: When designing tests for a hexagonal/DDD architecture; when debating mock vs fake vs stub; when deciding which layer to test at; when reviewing a test suite for over-mocking or under-integration; when adding property-based tests for domain invariants
**Steps/contract**: Not a step-by-step procedure — four-tier strategy with decision rules per tier: (1) Primary: test at the use case boundary with faked driven ports — exercises orchestration + domain together, proves feature end-to-end without infrastructure; (2) Complement: test pure domain functions directly when logic is complex with many edge cases, boundary conditions, or state transitions — not as a substitute for use case tests; (3) Secondary: narrow integration tests for driven adapters against real infrastructure (fresh DB per test via `createTestDb`); (4) Verification: E2E (Playwright) for full-stack smoke — slow, not a substitute for use case tests. Decision rule for (1) vs (2): test through the use case when correctness depends on orchestration; test domain function directly when business rule has exhaustive edge cases. Fake-over-mock rule: fakes implement real interface → type-safe drift detection; mocks verify call sequences → test implementation, not behavior
**Strip**: TypeScript-specific syntax (async/await shapes, `Map` idioms, `vi.mock()`); `fast-check` import and library-specific property API; `handlePledge`, `createFakeOccasionRepo` domain names (retain structural pattern, swap for generic placeholders); cross-reference to `hexagonal-architecture` skill (retain conceptual pointer, drop file path)
**Structure/format**: Five sections — primary boundary rationale with code example → fake/stub/spy/mock comparison table → domain unit tests as complement (with when-to/when-not guidance + property-based testing) → adapter integration tests → E2E tests. Closes with a priority/boundary/proof/speed summary table. High signal-to-noise; each section leads with principle before example. Not a checklist-gate SOP — a strategy reference. Best consumed as a linked resource from a higher-level testing or DDD skill.
**Notes**: The use-case-as-primary-test-boundary is the most portable and valuable insight — it resolves the perennial "unit vs integration" debate with a clear structural answer rather than a vague "test the right things." The fake-vs-mock rationale (compile-time drift detection via type system) is a crisp, defensible argument that adds real value beyond "mocks are bad." The four-tier table is immediately lift-and-use in a new skill. The property-based testing section is solid but niche — portable as an optional appendix, not core. Cross-references `hexagonal-architecture` for `createTestDb` and fakes implementation; those two resources would need to be co-extracted for full portable value. Overall: higher standalone value than the aggregate-design resource because it gives structural decision rules, not just principles. Strong candidate for a `testing-ddd` or `testing-hexagonal` SOP that could merge cleanly with the existing `tdd` skill.

## claude/.claude/skills/hexagonal-architecture/resources/cqrs-lite.md
**Type**: Reference/concept document — architectural pattern explanation (CQRS-lite: writes go through repositories, reads use free-join query functions, domain-layer pure functions transform raw results into display-ready types)
**Portable**: Yes — the core insight and decision framework are architecture-agnostic beyond requiring a layered/hexagonal context; ORM syntax in examples is incidental
**Reason**: The N+1-vs-boundary-violation-vs-denormalisation triad is the universal forcing function that motivates CQRS-lite in any hexagonal/clean-arch codebase; the read/write split and domain-transform placement rules apply regardless of language or ORM
**Trigger**: Implementing read-heavy features in a hexagonal or clean-arch codebase; encountering N+1 queries when routing reads through repositories; deciding whether to adopt full CQRS
**Steps/contract**: (1) Writes: route through repositories to maintain aggregate consistency; (2) Reads: standalone query functions in adapter layer (`db/queries/`) JOIN freely across tables without aggregate boundary constraints; (3) Domain transforms: pure functions in the domain layer convert raw query rows into display-ready types — business rules (urgency thresholds, derived calculations) belong here, not in the query; (4) Testing: query functions → integration tests with real DB; domain transforms → unit tests (pure, no mocks); full read path → E2E; (5) Upgrade gate: stay with CQRS-lite until separate read/write DBs, event sourcing, cross-bounded-context aggregation, or independent read scaling are required
**Strip**: TypeScript/Drizzle ORM syntax in code examples (keep concept, strip library specifics); `createTestDb()` test-utility reference (project-specific); concrete import paths (`db/queries/`, `domain/dashboard/`) — replace with layer-label placeholders
**Structure/format**: Good — problem statement → rationale (N+1/boundary/denormalisation triad) → adapter-layer query function pattern → domain-layer transform pattern → testing strategy per layer → upgrade decision criteria; ~50 lines, concise and self-contained; no frontmatter
**Notes**: This is a conceptual reference, not a procedural SOP. Highest-value portable extracts: (1) the three-option forcing function (N+1 / break aggregate boundary / denormalise → CQRS-lite as the pragmatic middle path) — rarely stated this crisply; (2) the pure-function domain-transform rule with its "isUrgent is a business rule, the query is not" example — a strong, memorable heuristic; (3) the upgrade decision criteria list — a clean decision gate that prevents premature full-CQRS adoption. Best role is as a supporting resource within a hexagonal-architecture or architecture-decision skill rather than a standalone SOP. Lower priority than the agent/skill definitions audited earlier, but worth extracting as a reference block.

## claude/.claude/skills/domain-driven-design/resources/error-modeling.md
**Type**: Reference resource — DDD sub-resource on error modeling patterns; companion doc to the domain-driven-design skill (not a standalone skill with frontmatter)
**Portable**: Yes — core principles (discriminated unions for business outcomes, exceptions for bugs/infrastructure failures, layered error propagation, specific reason literals) are architectural patterns applicable to any typed language; TypeScript examples are illustrative
**Reason**: The discriminated union / exception decision heuristic ("Could a user's action legitimately cause this outcome? If yes → result type; if no, it means a bug → exception"), the layer-by-layer propagation model, and the reason-literal-union pattern for exhaustive handling are universal design principles independent of any framework or codebase
**Trigger**: When designing error handling in a layered application; when implementing domain functions with expected failure modes; when building use cases or route/controller handlers that must translate domain errors into protocol-level responses (HTTP, gRPC, etc.)
**Steps/contract**: (1) Classify each error — apply the "user-action legitimacy" test to decide result-type vs. exception; (2) Model expected business outcomes as discriminated unions with specific reason string literals (not generic `error: string`); (3) Propagate through layers: domain returns result → use case inspects result and controls side effects (save only on success) → transport layer (route handler) translates `result.reason` to protocol status/body; (4) Each layer works at its own abstraction — domain never knows about HTTP, route handler never knows about business rules; (5) Test error paths through the use case boundary — assert both the rejection value AND the absence of side effects in the same test
**Strip**: `NextResponse.json` / Next.js-specific route handler code (replace with generic "transport layer" pseudocode slot); TypeScript-specific compiler-enforcement language ("the compiler enforces exhaustive handling") — keep the pattern, generalise the rationale to "static analysis"; `type PledgeResult`/pledge domain specifics in examples (illustrative only)
**Structure/format**: Six sections with prose rationale followed by code examples — The Default (result type), When Exceptions Are Appropriate, Layer Propagation (with ASCII flow diagram + layered code block), Modeling Multiple Errors, What NOT To Do (❌ anti-patterns), Testing Errors; clean and scannable; no frontmatter
**Notes**: Two standout portable primitives worth preserving verbatim: (1) the **user-action legitimacy heuristic** — a single binary test that resolves most result-vs-exception ambiguity without lengthy case analysis; (2) the **layered propagation contract** — domain returns result, use case gates side effects on it, transport layer maps reasons to protocol codes — each layer handles errors at its own abstraction level. The testing principle (assert rejection value AND zero saved entities in the same test) is a clean behavioral test contract rarely stated this explicitly. Moderate standalone value as a sub-resource; highest value when combined into a DDD error-handling SOP alongside the parent domain-driven-design skill. The anti-patterns section (exceptions for business rules, generic error string, catch-and-rethrow for context) is concise and actionable; the "don't wrap exceptions in PledgeError" rationale (use cases don't need to know why the DB is down — let it propagate to the top-level handler) is a portable architectural principle worth lifting verbatim.

## claude/.claude/skills/hexagonal-architecture/resources/testing-hex-arch.md
**Type**: Reference doc (testing strategy guide within a hex-arch skill suite)
**Portable**: Yes — the four-tier priority model, fakes-over-mocks rationale, and swappability-test boundary validator are universally applicable to any ports-and-adapters project regardless of language or framework
**Reason**: Core principles (use case as primary test boundary, typed in-memory fakes over mocks, narrow integration tests for driven adapters only, E2E as final verification, swappability as boundary correctness proof) stand independently of the TypeScript/Drizzle/MSW/Playwright tooling used in examples
**Trigger**: When defining or reviewing test strategy for a hexagonal/ports-and-adapters architecture project; when a team is over-mocking or testing at the wrong layer
**Steps/contract**: (1) Test use cases first — call driving port with in-memory fakes replacing driven ports; (2) complement with direct unit tests for complex pure domain functions; (3) add narrow integration tests for each driven adapter (real DB or MSW); (4) add E2E tests as final full-stack verification; (5) apply the swappability test — swap any adapter without changing domain code or use case tests; if that fails, the boundary is wrong
**Strip**: Internal `../../REFERENCES.md` cross-reference; Valentina Jemuović / UCDD attribution (or demote to a footnote credit); TypeScript-specific tool names in examples (Drizzle, MSW, Playwright, fast-check) should become illustrative placeholders, not requirements; `fast-check` cross-reference to sibling DDD skill
**Structure/format**: Excellent — narrative rationale per section, inline code examples, explicit "why fakes over mocks" comparison list, and a concise priority/speed summary table; highly copy-paste-ready as a team testing standard
**Notes**: Two standout portable gems: (a) the **swappability test** — operationalises correct boundary placement as a binary pass/fail check that any team can run; (b) the **fakes-over-mocks rationale** — compile-time drift detection via typed interface implementation is a concrete, memorable argument that elevates this above boilerplate TDD advice. The explicit note that fakes use mutable internal state as a deliberate testing-only exception (while domain types remain immutable) is a nuanced and useful callout worth preserving verbatim.

## claude/.claude/skills/hexagonal-architecture/resources/incremental-adoption.md
**Type**: Resource/reference document — supplementary how-to guide within the hexagonal-architecture skill; not an agent or slash command but a step-by-step adoption walkthrough
**Portable**: Yes — the Strangler Fig approach, 6-step extraction sequence, migrate-first priority matrix, and anti-patterns list are language- and framework-agnostic; TypeScript/Next.js/Drizzle examples are illustrative only
**Reason**: The 6-step sequence (identify boundary → extract pure domain function → extract port → create adapter → create use case → thin route handler) is a universal incremental migration SOP that applies to any codebase regardless of language or ORM; the priority matrix gives explicit guidance on when hex arch adds value vs. overhead
**Trigger**: Adopting hexagonal architecture in an existing codebase; refactoring a "big ball of mud" with business logic tangled in route handlers or DB access; user asks about strangler fig pattern, hex arch migration, extracting ports/adapters from existing code
**Steps/contract**: (1) Identify boundary — find a route handler mixing business rules + DB + response; (2) Extract pure domain function — no infrastructure, no async; (3) Extract port interface — define what the domain needs from infrastructure; (4) Create adapter — wrap existing DB access behind the port; (5) Create use case — wire domain function to port; (6) Thin route handler to pure glue. Priority matrix: migrate-first signals (business logic in routes, direct DB in multiple places, untestable code) vs. leave-alone signals (simple CRUD with no business rules, stable rarely-changing code). Anti-patterns: big-bang migration, ports for everything, hex arch on CRUD, abstract base classes.
**Strip**: Next.js (`NextResponse`), Drizzle ORM (`db.select().from(users)`), and TypeScript-specific syntax in examples — retain as illustrative but flag as "TypeScript-flavoured"; no harness-specific frontmatter to remove
**Structure/format**: Good — named approach (Strangler Fig), 6 numbered steps each with before/after TypeScript code blocks, a 2-column priority matrix table (Signal / Priority), a "What NOT to Do" section with bolded anti-patterns, and a closing "test is the proof" principle; scannable and copy-paste ready
**Notes**: Strongest portable primitive is the **migrate-first priority matrix** — it proactively prevents hex arch over-engineering by explicitly labelling simple CRUD and stable code as Low priority, a rare and valuable counter-signal. The "test is the proof" closing principle ("if you can write a use case test with fakes without touching the DB, the boundary is correct") is an elegant, enforceable correctness criterion worth preserving verbatim. The 6-step sequence is clean and well-ordered. Limited standalone value as an SOP — best role is as the deep-dive adoption annex to the parent `hexagonal-architecture/SKILL.md`; together they form a complete hex arch onboarding resource.

## claude/.claude/skills/hexagonal-architecture/resources/worked-example.md
**Type**: Reference/educational document — a single feature ("pledge a contribution to a gift fund") traced end-to-end through every layer of a hexagonal + DDD architecture; companion to a hexagonal-architecture skill, not a standalone procedural SOP
**Portable**: Partially — the 9-section layer-tracing *structure* and all prose annotations are fully portable; the gift-fund business domain (OccasionId, ContributorId, PledgeResult) is illustrative scaffolding; the Cloudflare/Next.js route and Drizzle ORM adapter code are tech-stack-specific examples of a portable pattern
**Reason**: The layer-by-layer walkthrough format teaches both *where* code lives and *why* — each code block is followed by a precision annotation ("The use case is identifiable by its signature — it takes ports as parameters. It contains zero business logic. It loads, delegates to the domain service, and saves."); these annotations are the portable payload, the domain is just a carrier
**Trigger**: When implementing any new feature in a hexagonal architecture; when onboarding to a hexagonal codebase; when deciding where a new function, type, or class belongs in a layered system
**Steps/contract**: 9 ordered sections — (1) Glossary (bounded context terms); (2) Domain Types (branded IDs, value objects, discriminated union result type); (3) Pure Domain Function (no ports, no async, types in → types out); (4) Port Interfaces (defined in domain layer, not infra); (5) Use Case (orchestration-only, takes ports as params, zero business logic, load → delegate → save); (6) Driven Adapter (repo impl, translates domain ↔ DB rows); (7) Driving Adapter (thin route handler: parse → wire → delegate → translate to HTTP); (8) Fakes for Testing (implement real interface, fail at compile time if interface changes); (9) Tests (use case tests via fakes — no mocks; domain unit tests for complex rules); capped with a File Map diagram and a "What Lives Where" decision table
**Strip**: Gift-fund domain specifics (Occasion, Contributor, PledgeResult types) — illustrative wrapper only; Cloudflare/Next.js route syntax and Drizzle ORM method calls (replace with generic adapter pseudocode in a portable extraction); cross-reference to `resources/cross-cutting-concerns.md` (repo-local); `getCloudflareContext()` and `env.DB` wiring (infra-specific)
**Structure/format**: Excellent — numbered sections with TypeScript code blocks each followed by a single-sentence or short-paragraph annotation isolating the key architectural principle; File Map section uses a labelled directory tree; "What Lives Where" closes with a 3-column Markdown table (What / Where / Why) serving as a portable placement decision guide; highly scannable, educational, and copy-paste ready as a template for documenting any hexagonal feature
**Notes**: Not a procedural SOP — it is a worked example that *instantiates* the hexagonal architecture principles defined in the parent skill. The highest-value portable extracts are: (1) the **"What Lives Where" table** — a concise, memorable decision guide for code placement covering all six layer types; (2) the **annotated section headers** — each section title paired with its prose annotation forms a self-contained definition (e.g., "The driven adapter translates between domain types and infrastructure types. No business logic."); (3) the **fake repository pattern with compile-time enforcement** — "If the interface changes, the fake breaks at compile time" is a precise, actionable design principle worth preserving verbatim; (4) the **use case signature rule** — "identifiable by its signature — it takes ports as parameters" is a crisp recognition heuristic. The 9-section structure itself is a reusable template for documenting any hexagonal feature from scratch. Standalone portable value is moderate (requires the parent skill's conceptual definitions to be fully actionable), but as a companion reference within a hexagonal-architecture SOP bundle it is the strongest concrete artifact in this skill directory.
