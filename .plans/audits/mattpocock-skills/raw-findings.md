## .references/mattpocock-skills/edit-article/SKILL.md
**Type**: Workflow SOP (interactive multi-step editing protocol)
**Portable**: Yes
**Reason**: Describes a reusable, domain-agnostic article-editing loop. The DAG-ordering heuristic and 240-char paragraph constraint are concrete, transferable rules with no repo-specific assumptions.
**Trigger**: User wants to edit, revise, or improve an article draft.
**Steps/contract**: (1) Divide article into sections based on headings; model the section order as a DAG so dependencies flow correctly; confirm sections with user. (2) For each section: rewrite for clarity, coherence, and flow; enforce ≤240 characters per paragraph.
**Strip**: Nothing — the skill is already minimal. The DAG framing is novel enough to keep verbatim.
**Structure/format**: Numbered steps with a sub-step (2a); YAML frontmatter name/description; prose body only (~120 words total).
**Notes**: Unusually terse but high signal-to-noise. The DAG metaphor for section ordering is the standout insight. Paragraph-length cap (240 chars) is an opinionated but portable constraint. Lacks an explicit review/sign-off step and no mention of voice/tone preservation — worth adding if promoted to a shared SOP.
## .references/mattpocock-skills/design-an-interface/SKILL.md
**Type**: Workflow skill — parallel multi-agent API/module design exploration ("Design It Twice" pattern).
**Portable**: Yes.
**Reason**: The five-step SOP (gather requirements → spawn 3+ parallel agents each under a different design constraint → present designs sequentially → compare → synthesize) is entirely framework- and language-agnostic. The operative value is the enforced divergence: giving each sub-agent a distinct constraint (minimize methods / maximize flexibility / optimize common case / draw from a named paradigm) structurally prevents convergence on the first idea. Works for any interface design task regardless of stack.
**Trigger**: User wants to design an API, explore interface options, compare module shapes, or says "design it twice".
**Steps/contract**: (1) Gather requirements via five targeted questions (problem, callers, key operations, constraints, public vs private surface). (2) Spawn ≥3 sub-agents in parallel, each assigned one divergent constraint; each agent returns: interface signature, usage example, what it hides, trade-offs. (3) Present designs sequentially so user absorbs each before comparison. (4) Compare on interface simplicity, generality, implementation efficiency, and depth (small interface / significant hidden complexity = good). (5) Synthesize — invite the user to cherry-pick elements across designs.
**Strip**: Book citation in evaluation criteria (cosmetic; keep or drop without loss). Specific sub-agent prompt template wording is illustrative, not prescriptive — can be condensed.
**Structure/format**: Numbered workflow with per-step checklists and a reusable sub-agent prompt template. Evaluation criteria named and defined. Anti-patterns list at the end (no implementation, enforce radical difference, comparison is mandatory).
**Notes**: The explicit "don't implement — this is purely about interface shape" guardrail is high-value and should be preserved verbatim. The four preset constraint archetypes are well-calibrated starting points. Strong candidate for promotion; minimal adaptation needed.

## .references/mattpocock-skills/git-guardrails-claude-code/scripts/block-dangerous-git.sh
**Type**: Claude Code PreToolUse hook script (bash)
**Portable**: Yes
**Reason**: Self-contained bash script that enforces a fixed blocklist of destructive git commands via Claude Code's hook exit-code protocol. No repo-specific logic; the patterns and mechanism are universally applicable to any project.
**Trigger**: Runs as a Claude Code `PreToolUse` hook on every `Bash` tool call. Fires whenever Claude attempts to execute a shell command, reads stdin JSON, extracts `.tool_input.command`, and pattern-matches against the dangerous-command list.
**Steps/contract**: (1) Read full tool-call JSON from stdin. (2) Extract `.tool_input.command` via `jq`. (3) Iterate over `DANGEROUS_PATTERNS` array using `grep -qE`. (4) On first match: print a blocking message to stderr and `exit 2` (Claude Code convention for "block + show message"). (5) On no match: `exit 0` (allow).
**Strip**: Nothing — the script is already minimal. The patterns array is the only knob; callers may add or remove entries but the control flow is boilerplate.
**Structure/format**: Single bash script, ~25 lines. Depends on `jq` for JSON parsing; uses only POSIX-compatible constructs otherwise. Designed to be wired into `.claude/settings.json` as a hook entry.
**Notes**: The blocked-command message deliberately includes the phrase "The user has prevented you from doing this." — this is intentional UX to attribute the block to the user, not Claude, reducing confusion. Patterns include both full command forms (`git push`) and suffix forms (`push --force`, `reset --hard`) to catch aliased or compound invocations. No allowlist/override path exists; this is a hard block.
## .references/mattpocock-skills/git-guardrails-claude-code/SKILL.md
**Type**: Setup/installation — installs a Claude Code `PreToolUse` hook + shell script to intercept and block destructive git commands before execution.
**Portable**: Yes — the pattern (hook config + shell script) applies to any Claude Code project or global install; no repo-specific logic, no external service dependencies.
**Reason**: Pure safety guardrail that works identically regardless of stack or domain. The two-scope option (project vs global) and the settings-merge instruction are universally useful.
**Trigger**: User wants to prevent destructive git operations (push, reset --hard, clean, branch -D, checkout .), add git safety hooks, or block dangerous git commands in Claude Code.
**Steps/contract**: Five steps — (1) ask scope (project `.claude/settings.json` vs global `~/.claude/settings.json`); (2) copy bundled shell script to target path + `chmod +x`; (3) merge `PreToolUse` hook entry into settings JSON without overwriting other keys; (4) offer customisation of blocked-pattern list; (5) verify via `echo '{"tool_input":{"command":"git push …"}}' | <script>` and confirm exit-code 2 + BLOCKED output.
**Strip**: Reference to `scripts/block-dangerous-git.sh` as a bundled companion file — a portable SOP must inline the script body (or provide a self-contained canonical copy) since consumers won't have the source repo on hand.
**Structure/format**: Well-structured — clear numbered steps, dual-scope JSON snippets for settings wiring, and a concrete bash verification command. Ready to adapt with minimal reformatting.
**Notes**: The "merge, don't overwrite" instruction for existing `settings.json` is good defensive practice worth preserving verbatim. The blocked-command list (`push`, `reset --hard`, `clean -f/-fd`, `branch -D`, `checkout .`, `restore .`) is a sensible baseline; the customisation step makes it extensible. Main gap for portability is the missing script body — inlining it is the only prerequisite before promoting to a standalone SOP.

## .references/mattpocock-skills/grill-me/SKILL.md
**Type**: Workflow SOP (interactive Socratic interview protocol)
**Portable**: Yes
**Reason**: The core pattern — ask one question at a time, provide a recommended answer, walk each branch of the decision tree until dependencies are resolved — is entirely domain-agnostic. It applies to any plan or design stress-test regardless of stack or project.
**Trigger**: User wants to stress-test a plan, get grilled on their design, or says "grill me".
**Steps/contract**: (1) Interview the user relentlessly, covering every aspect of the plan. (2) Walk the decision tree branch-by-branch, resolving dependencies between decisions in order. (3) For each question, provide a recommended answer. (4) Ask questions one at a time (never batch). (5) If a question can be answered by exploring the codebase, do that instead of asking.
**Strip**: Nothing — the skill is already at minimum viable size (~60 words body). The codebase-exploration clause is contextually specific to coding agents but is broadly useful and should be kept.
**Structure/format**: YAML frontmatter (name, description); single-paragraph prose body (~60 words). No numbered steps, no sub-agents. Extremely lean.
**Notes**: Exceptional signal-to-noise ratio. The "provide your recommended answer" instruction is the key differentiator — it prevents the interview from becoming an interrogation and models the desired outcome for the user. The one-question-at-a-time constraint prevents cognitive overload. Nearly identical to the grill-me skill already in `/Users/mia/.agents/skills/grill-me/SKILL.md`; check for delta before promoting to avoid duplication.

## .references/mattpocock-skills/migrate-to-shoehorn/SKILL.md
**Type**: Migration workflow SOP — replaces `as` type assertions in test files with type-safe `@total-typescript/shoehorn` equivalents.
**Portable**: Conditionally yes — portable to any TypeScript project that uses `as` casts in tests, but the skill is narrow by design: it targets one specific library (`@total-typescript/shoehorn`) and one specific problem (`as` overuse in tests). Not useful outside TypeScript test contexts.
**Reason**: The three-pattern mapping (`as Type` → `fromPartial()`, `as unknown as Type` → `fromAny()`, full-object stub → `fromPartial()`) is a precise, executable checklist with no repo-specific assumptions. The `grep` command for discovery and the type-check verification step make this independently reproducible in any TS project.
**Trigger**: User wants to migrate `as` assertions out of test files, mentions shoehorn, or asks for partial test data patterns in TypeScript.
**Steps/contract**: (1) Gather requirements — identify affected test files, whether partial data or intentionally wrong data is needed. (2) Install: `npm i @total-typescript/shoehorn`. (3) Discover: `grep -r " as [A-Z]" --include="*.test.ts" --include="*.spec.ts"`. (4) Replace `as Type` with `fromPartial()`. (5) Replace `as unknown as Type` with `fromAny()`. (6) Add imports. (7) Run type check to verify.
**Strip**: "Why shoehorn?" motivation section — already covered in the trigger description; trim on promotion. The `fromExact()` row in the table is an edge case; can be a footnote rather than a first-class step.
**Structure/format**: YAML frontmatter + prose rationale + before/after code samples per pattern + function-selection table + numbered checklist workflow. Well-structured and ready to adapt. Code samples are the most valuable part and should be preserved verbatim.
**Notes**: The "test code only, never production" guardrail is high-signal and must be preserved. The skill is a strong candidate for the existing `test-migrate-shoehorn` slot — it is essentially the same SOP already living in the destination repo. Main delta worth checking: the source skill's `grep` discovery command and `fromExact()` note may not be in the destination; worth a diff before promoting to confirm what (if anything) is new.

## .references/mattpocock-skills/improve-codebase-architecture/SKILL.md
**Type**: Workflow SOP — 7-step codebase architecture improvement process: organic exploration → candidate list → user picks → problem framing → parallel divergent interface design → user picks → GitHub RFC issue.
**Portable**: Yes.
**Reason**: The process is entirely framework- and language-agnostic. The core insight — use the friction an AI explorer naturally experiences as the signal for architectural debt — is a strong, transferable heuristic. The "deep module" framing (Ousterhout: small interface hiding large implementation) is a canonical concept applicable to any codebase. Steps 1–3 are a reusable discovery pattern; Steps 4–7 are a reusable delivery pattern. No repo-specific assumptions.
**Trigger**: User wants to improve architecture, find refactoring opportunities, make a codebase more testable or AI-navigable, or consolidate tightly-coupled modules.
**Steps/contract**: (1) Organic codebase exploration — note friction (concepts that require bouncing many files, shallow modules, pure functions extracted just for testability, tightly-coupled seams, untested areas); "the friction you encounter IS the signal." (2) Present numbered deepening candidates, each showing: cluster (modules involved), why they're coupled, dependency category, test impact (which existing tests boundary tests would replace). Do NOT propose interfaces yet — ask user to pick. (3) User picks candidate. (4) Frame the problem space for the user: constraints any new interface must satisfy, dependencies it must rely on, an illustrative code sketch (not a proposal — grounds the constraints). Immediately proceed to step 5 in parallel. (5) Spawn ≥3 sub-agents in parallel, each with a distinct design constraint: Agent 1 "minimize interface (1-3 entry points max)"; Agent 2 "maximize flexibility/extension"; Agent 3 "optimize for most common caller"; Agent 4 "ports & adapters pattern." Each agent returns: interface signature, usage example, what complexity it hides, dependency strategy, trade-offs. Present sequentially, compare in prose, give an opinionated recommendation (and hybrid if warranted). (6) User picks interface or accepts recommendation. (7) Create GitHub issue RFC via `gh issue create` — do not ask user to review before creating; share URL.
**Strip**: References to external REFERENCE.md (four dependency categories, issue template) — must be inlined for portability. Reference to `Agent tool with subagent_type=Explore` is Claude-internal phrasing; replace with generic subagent/parallel-execution framing. `gh issue create` assumes GitHub + gh CLI — note as a prerequisite or make the output format agnostic.
**Structure/format**: Numbered 7-step workflow with embedded sub-agent prompt templates and per-agent design constraints. Relies on external REFERENCE.md for dependency taxonomy and RFC template — a gap that must be resolved before promotion.
**Notes**: This skill is a superset of `design-an-interface` (already assessed as portable) — it wraps the same divergent parallel design pattern with an upstream discovery phase and a downstream RFC-creation phase. The two overlap significantly at Step 5; promoting both risks duplication. Options: (a) promote this as the "full architecture SOP" and have it call `design-an-interface` as a sub-skill; (b) merge them. The "friction as signal" exploration philosophy and the candidate-framing (cluster / coupling reason / dependency category / test impact) are the unique additions worth preserving. Strong candidate for promotion once REFERENCE.md content is inlined.

## .references/mattpocock-skills/improve-codebase-architecture/REFERENCE.md
**Type**: Reference/template — dependency classification taxonomy, testing strategy principle, and a structured GitHub issue template for codebase architecture improvement work.
**Portable**: Yes
**Reason**: The four-category dependency taxonomy (in-process → local-substitutable → remote-but-owned via ports & adapters → true external/mock) is a stack-agnostic mental model for deciding how to test any module-deepening task. The "replace, don't layer" testing principle and the five-section issue template are universally applicable regardless of language or framework.
**Trigger**: User wants to improve codebase architecture, deepen shallow modules, reduce coupling, plan a refactor, or file an architectural improvement issue.
**Steps/contract**: This is reference material rather than a procedural SOP. It supplies three building blocks: (1) Dependency taxonomy — classify the dependency being crossed to select the correct test strategy: in-process (merge + test directly), local-substitutable (use PGLite / in-memory FS stand-in), remote-but-owned (ports & adapters: port interface + HTTP adapter for prod + in-memory adapter for tests), true external (mock at the boundary via injected port). (2) Testing principle — "replace, don't layer": once boundary tests exist, delete old shallow-module unit tests; all new tests assert observable outcomes through the public interface, not internal state. (3) Issue template (five sections): Problem (shallow/coupled modules, integration risk), Proposed Interface (signature, usage example, hidden complexity), Dependency Strategy (which category + how dependencies are handled), Testing Strategy (new boundary tests to write, old tests to delete, test environment needs), Implementation Recommendations (responsibilities, hidden details, exposed contract, migration path).
**Strip**: The "Recommendation shape" inline example under "Remote but owned" is illustrative prose — useful as a worked example but not load-bearing. The XML `<issue-template>` wrapper is a formatting convention that can be dropped when inlining into a skill.
**Structure/format**: Three-section markdown (~60 lines): taxonomy (4 named categories, each with a paragraph of guidance), testing strategy (3 bulleted principles), issue template (XML-wrapped markdown with 5 H2 sections). Clean and minimal.
**Notes**: Companion reference to the `design-an-interface` skill — that skill handles *what* interface to build; this handles *how* to safely restructure toward it. The dependency taxonomy is the standout element: it operationalises the test-strategy decision as a lookup table. "Replace, don't layer" is a high-signal rule worth preserving verbatim. Main gap for promotion: the file is pure reference with no workflow steps — a promoted SOP would need a wrapping procedural shell that calls into this taxonomy at the right moment (e.g., after interface design, before filing the issue).

## .references/mattpocock-skills/prd-to-plan/SKILL.md
**Type**: Process/workflow skill — converts a PRD into a phased vertical-slice implementation plan saved as a local Markdown file.
**Portable**: Yes — no repo-specific tooling, languages, or infra; applies to any software project with a PRD.
**Reason**: The core practice (tracer-bullet vertical slices, durable architectural decisions upfront, quiz-before-write loop, structured plan template) is universally sound and independent of stack or domain.
**Trigger**: User wants to break down a PRD into phases / implementation plan, mentions "tracer bullets", or asks for a plan file from a PRD.
**Steps/contract**: (1) Confirm PRD in context. (2) Explore codebase for existing patterns. (3) Identify durable architectural decisions (routes, schema, models, auth, 3P boundaries). (4) Draft tracer-bullet phases — thin, complete, end-to-end, demoable. (5) Quiz user on granularity and iterate. (6) Write plan as `./plans/<feature>.md` using the provided template (header decisions → phase sections with title, user stories, what to build, acceptance criteria).
**Strip**: Hard-coded output path `./plans/` (should be configurable); the "explore codebase" step is implementation-specific and may not apply in non-code contexts.
**Structure/format**: Well-structured — `<vertical-slice-rules>` block + `<plan-template>` XML fence with clear placeholder sections; process numbered 1–6; easy to follow.
**Notes**: Strong vertical-slice discipline is the standout portable value. The explicit rule to avoid ephemeral file/function names (keep only durable decisions) is a high-signal SOP principle worth preserving verbatim. Closest counterpart in local skills: `skills/prd-to-plan/SKILL.md` — compare for delta before promoting.
## .references/mattpocock-skills/qa/SKILL.md
**Type**: Workflow SOP (interactive multi-turn bug-reporting and issue-filing session)
**Portable**: Yes
**Reason**: Fully domain-agnostic; the five-step loop (listen → background explore → scope → file → continue) applies to any GitHub-backed project. Issue templates and the thin-issues / parallelism principles are strong universal practices with no repo-specific coupling.
**Trigger**: User wants to report bugs, run a QA pass, file issues conversationally, or says "QA session".
**Steps/contract**: (1) Listen — let user describe the problem; ask ≤3 short clarifying questions (expected vs actual, repro steps, consistency). (2) Background codebase explore — learn domain language (UBIQUITOUS_LANGUAGE.md) and feature boundary; do NOT hunt for a fix. (3) Scope decision — single issue or breakdown? Break down when fix spans independent areas; keep single when one root behavior. (4) File via `gh issue create` without asking user to review first; share URL immediately. Single-issue template: What happened / What I expected / Steps to reproduce / Additional context. Breakdown template adds Parent issue + Blocked by fields; create in dependency order; maximize parallelism. Issue rules: no file paths or line numbers, use domain language, describe behaviors not code, mandatory repro steps, ≤30-second read. (5) Print all URLs with blocking relationships, ask "Next issue or done?"; repeat.
**Strip**: Breakdown sub-issue template `Parent issue` field ("Reported during QA session" fallback is noise); minor prose padding around the "don't over-interview" advice.
**Structure/format**: YAML frontmatter + H1 + per-issue H2/H3 numbered steps; two fenced-code issue templates; bullet rule lists. Well-structured and skimmable at ~450 words.
**Notes**: Background exploration kept separate from issue body is a strong architectural principle — it improves issue durability without polluting it with implementation details. The "no file paths in issues" rule is the standout portable constraint. Slightly overlaps with `issue-triage` (which also files GitHub issues) — the differentiator is the multi-turn conversational session model vs single-issue deep triage. If promoted, consider a brief note distinguishing it from `issue-triage`.

## .references/mattpocock-skills/prd-to-issues/SKILL.md
**Type**: Workflow SOP — interactive 5-step process to decompose a PRD into independently-grabbable GitHub issues as tracer-bullet vertical slices.
**Portable**: Yes
**Reason**: The core decomposition strategy (tracer bullets cutting end-to-end through all integration layers, HITL vs AFK classification, dependency ordering) is entirely stack- and domain-agnostic. The issue template and quiz loop apply to any project with a GitHub-hosted PRD. No repo-specific logic.
**Trigger**: User wants to convert a PRD to GitHub issues, create implementation tickets, break down a PRD into work items, or mentions "tracer bullets" / "vertical slices".
**Steps/contract**: (1) Locate PRD — ask for issue number/URL; fetch with `gh issue view <number> --comments` if not in context. (2) Optional codebase exploration to understand current state. (3) Draft vertical slices — each is a thin end-to-end path through every layer (schema → API → UI → tests); must be demoable or verifiable independently; classify each as HITL (requires human decision/review) or AFK (can merge autonomously). (4) Quiz user — present numbered list showing Title / Type / Blocked-by / User-stories-covered; iterate on granularity, dependencies, merge/split decisions, and HITL/AFK labels until approved. (5) Create issues in dependency order via `gh issue create` using the five-section template (Parent PRD, What to build, Acceptance criteria, Blocked by, User stories addressed).
**Strip**: Nothing structural. The `gh issue create` call assumes GitHub + gh CLI — note as a prerequisite. The HITL/AFK distinction is contextually specific to agentic workflows; condense or footnote if promoting to a non-agentic context.
**Structure/format**: YAML frontmatter + numbered 5-step workflow with inline `<vertical-slice-rules>` callout and `<issue-template>` XML block. Clean, self-contained (~250 words). Template is complete and immediately reusable without modification.
**Notes**: Very close to the `prd-to-issues` skill already at `/Users/mia/.agents/skills/prd-to-issues/SKILL.md` — diff before promoting to confirm what (if anything) is genuinely novel here. The HITL/AFK classification step and the "create in dependency order so you can reference real issue numbers" instruction are the standout additions worth checking against the destination. The "do NOT close or modify the parent PRD issue" guardrail is high-signal and should be preserved verbatim. Strong portability candidate; minimal adaptation needed.

## .references/mattpocock-skills/obsidian-vault/SKILL.md
**Type**: Tool-specific environment configuration — Obsidian note-taking workflow.
**Portable**: No.
**Reason**: The skill is bound to a hardcoded Windows/WSL absolute path (`/mnt/d/Obsidian Vault/AI Research/`) and to Obsidian-specific affordances (wikilinks `[[...]]`, flat-folder-with-index-notes architecture). The bash commands embed the vault path directly, so the skill only works on the original author's machine without manual adaptation. Even the structural conventions (Title Case filenames, no folders, index notes as pure link aggregators) presuppose an Obsidian install with that specific vault layout.
**Trigger**: User wants to find, create, or organize notes in Obsidian.
**Steps/contract**: (1) Search notes by filename or content via `find`/`grep` against the hardcoded vault path. (2) Create a note: Title Case filename, content as a self-contained unit of learning, wikilinks to related notes at the bottom, hierarchical numbering if in a sequence. (3) Find backlinks by grepping for `[[Note Title]]` across the vault. (4) Find index notes via `find … -name "*Index*"`.
**Strip**: Hardcoded vault path (`/mnt/d/…`) — must become a configurable variable or user-supplied context before the skill can transfer. Machine-specific bash commands would need path substitution. The flat-vault philosophy is a personal convention, not a universal SOP.
**Structure/format**: YAML frontmatter (name, description); prose body with H2 sections (Vault location, Naming conventions, Linking, Workflows); bash code blocks for each workflow variant. Compact (~350 words).
**Notes**: The underlying patterns — index notes as pure wikilink aggregates, Title Case filenames, backlink-first navigation — are a coherent personal knowledge-management philosophy, but they are Obsidian conventions rather than portable process steps. Not a candidate for promotion to a shared SOP unless reframed as "how to set up a personal vault skill" with the vault path as a required input parameter. Low generalizability outside Obsidian users on Windows/WSL.

## .references/mattpocock-skills/request-refactor-plan/SKILL.md
**Type**: Workflow SOP — 8-step interactive refactor-planning session that produces a structured GitHub issue.
**Portable**: Yes
**Reason**: The interview-then-document pattern (problem → codebase verify → alternatives → scope → test coverage check → tiny-commit breakdown → GitHub issue) is entirely language-, framework-, and repo-agnostic. The Martin Fowler "smallest possible step" principle and the explicit test-coverage gate are universally sound practices. The GitHub issue template (Problem Statement / Solution / Commits / Decision Document / Testing Decisions / Out of Scope / Further Notes) is a reusable deliverable format that works for any refactor on any project.
**Trigger**: User wants to plan a refactor, create a refactoring RFC, or break a refactor into safe incremental steps.
**Steps/contract**: (1) Gather long-form problem description and candidate solutions from user. (2) Explore codebase to verify assertions and understand current state. (3) Surface and discuss alternative approaches; ask user whether they have considered them. (4) Deep implementation interview — extremely detailed and thorough. (5) Nail down exact scope: what changes, what does not. (6) Check test coverage in the affected area; if insufficient, ask user's testing plan. (7) Break implementation into the tiniest possible commits (Fowler: each step must leave the codebase working). (8) File a GitHub issue using the seven-section template.
**Strip**: Step permissiveness ("You may skip steps if you don't consider them necessary") — this is an anti-pattern for a portable SOP; a promoted version should state conditions for skipping each step explicitly rather than leaving it to agent discretion. The XML `<refactor-plan-template>` wrapper is a formatting artifact; inline the template sections directly. The "DO NOT include specific file paths or code snippets" rule in the Decision Document section is high-signal and should be preserved verbatim.
**Structure/format**: YAML frontmatter + prose preamble + numbered 8-step list + fenced `<refactor-plan-template>` with seven H2 sections. Well-structured and self-contained (~300 words total). Template is complete and immediately reusable.
**Notes**: Very close to the `refactor-plan` skill already at `/Users/mia/.agents/skills/refactor-plan/SKILL.md` — diff before promoting to identify any genuine delta. The standout elements to check for in the destination: the explicit test-coverage gate (step 6) and the "no file paths / no code snippets in the Decision Document" guardrail. The tiny-commits philosophy with the Fowler attribution adds rhetorical weight and is worth preserving. Strong portability candidate; minimal adaptation needed.

## .references/mattpocock-skills/scaffold-exercises/SKILL.md
**Type**: Workflow SOP (filesystem scaffolding + lint-gate loop).
**Portable**: No — repo-specific.
**Reason**: Every concrete detail (directory schema `XX-section-name/` / `XX.YY-exercise-name/`, the `pnpm ai-hero-cli internal lint` command, the `ai-hero` CLI itself, and the `exercises/` root path) is tied to a single proprietary course repo. The *pattern* of "create stubs → run project linter → fix → commit" is universal, but the skill body encodes only the repo-specific realisation of that pattern.
**Trigger**: Would trigger when user wants to scaffold course exercises, create exercise stubs, or set up a new section — only meaningful inside the ai-hero-cli monorepo.
**Steps/contract**: (1) Parse the section/exercise plan. (2) `mkdir -p` all variant subdirectories (`problem/`, `solution/`, `explainer/`). (3) Write minimal stub `readme.md` per subfolder (title + one-line description). (4) Run `pnpm ai-hero-cli internal lint`. (5) Fix any lint errors iteratively until clean. (6) `git commit`.
**Strip**: Everything domain-specific: all naming conventions, the CLI command, the lint-rule list, the `git mv` renumbering guidance, and the worked example. What remains is a generic three-sentence "scaffold-stub-lint-commit" loop — too thin to stand alone as a portable SOP.
**Structure/format**: YAML frontmatter + prose body with headed sections (Directory naming, Exercise variants, Required files, Workflow, Lint rules summary, Moving/renaming exercises, Worked example). Total ~120 lines; example bash blocks and a worked plan-to-scaffold mapping.
**Notes**: The underlying discipline (lint gate before commit, readme required in every generated folder, `git mv` instead of `mv` for renames) are portable hygiene rules, but they are not novel enough to warrant a standalone SOP — they belong as bullet-points inside a general scaffolding checklist or a `tdd`/`setup-ci` skill addendum. Do not promote.

## .references/mattpocock-skills/setup-pre-commit/SKILL.md
**Type**: Setup/installation SOP — bootstraps Husky v9 pre-commit hooks with lint-staged (Prettier on all staged files), optional typecheck, and optional test runner in a Node.js project.
**Portable**: Yes — no repo-specific logic; works with any Node.js project using npm, pnpm, yarn, or bun. The package-manager detection step makes it explicitly multi-ecosystem.
**Reason**: The 8-step procedure (detect PM → install deps → husky init → write hook file → write .lintstagedrc → write .prettierrc if absent → verify → commit) is a complete, reproducible recipe that transfers verbatim to any frontend or Node backend project. The only variables are the package manager and the presence of `typecheck`/`test` scripts, both of which are handled by explicit conditional logic in the skill.
**Trigger**: User wants to add pre-commit hooks, set up Husky, configure lint-staged, or add commit-time formatting/typechecking/testing.
**Steps/contract**: (1) Detect package manager from lockfile (npm/pnpm/yarn/bun; default npm). (2) Install `husky lint-staged prettier` as devDependencies. (3) Run `npx husky init` — creates `.husky/` and adds `prepare: "husky"` to package.json. (4) Write `.husky/pre-commit` with `npx lint-staged`, `<pm> run typecheck`, `<pm> run test` — omit typecheck/test lines if scripts are absent. (5) Write `.lintstagedrc` with `"*": "prettier --ignore-unknown --write"`. (6) Write `.prettierrc` (opinionated defaults) only if no Prettier config exists. (7) Verify: check file existence, executability, and run `npx lint-staged` manually. (8) Commit all created files with message `Add pre-commit hooks (husky + lint-staged + prettier)` — this also smoke-tests the hook.
**Strip**: Nothing — the skill is already lean and well-scoped. The commit-as-smoke-test trick (Step 8) is a clever self-verifying closure worth preserving verbatim.
**Structure/format**: YAML frontmatter + H1 + `## What This Sets Up` bullet summary + `## Steps` numbered 1–8 with fenced bash/json code blocks per step + checklist under Verify + `## Notes` callouts. Well-structured, immediately actionable, ~300 words.
**Notes**: Nearly identical to the skill already at `/Users/mia/.agents/skills/setup-pre-commit/SKILL.md` — diff before promoting to confirm whether there are meaningful deltas. Likely differences to check: Husky v9 shebang-free hook detail, `prettier --ignore-unknown` flag, the `--write` flag placement, and the commit-as-smoke-test step. All four are precise, portable improvements worth incorporating into the destination if absent. Strong promotion candidate; minimal adaptation needed.

## .references/mattpocock-skills/tdd/SKILL.md
**Type**: Workflow SOP — red-green-refactor TDD loop with enforced vertical-slice discipline and behavior-first test philosophy.
**Portable**: Yes.
**Reason**: The entire skill is framework- and language-agnostic. The philosophy (test behavior through public interfaces, not implementation), the anti-pattern identification (horizontal slicing), the four-phase workflow (plan → tracer bullet → incremental loop → refactor), and the per-cycle checklist are universally applicable to any codebase or language.
**Trigger**: User wants to build features or fix bugs using TDD, mentions "red-green-refactor", wants integration tests, or asks for test-first development.
**Steps/contract**: (1) Planning — confirm interface changes and prioritize behaviors to test (not implementation steps); identify deep-module opportunities; get user approval before writing code. (2) Tracer bullet — write ONE test for ONE behavior → RED; write minimal code to pass → GREEN; proves the path works end-to-end. (3) Incremental loop — repeat RED→GREEN one behavior at a time; rules: one test at a time, only enough code to pass current test, never anticipate future tests, focus on observable behavior. (4) Refactor — after all tests GREEN: extract duplication, deepen modules, apply SOLID where natural, run tests after each refactor step; rule: never refactor while RED. Per-cycle checklist: test describes behavior not implementation; uses public interface only; would survive an internal refactor; code is minimal for this test; no speculative features added.
**Strip**: All companion-file cross-references (`tests.md`, `mocking.md`, `deep-modules.md`, `interface-design.md`, `refactoring.md`) — these are dead links outside the source repo; either inline their essential content or drop the pointers entirely before promoting.
**Structure/format**: YAML frontmatter + H1 + four H2 sections (Philosophy, Anti-Pattern, Workflow, Checklist); ASCII code block comparing horizontal vs vertical slicing; numbered workflow sub-steps each with checklist items. Well-organized, ~450 words. Easy to adapt with minimal reformatting.
**Notes**: The explicit anti-pattern section ("DO NOT write all tests first — this is horizontal slicing") is the standout high-signal content: it names the failure mode, explains why it produces poor tests, and backs it up with a concrete ASCII diagram. This is meaningfully stronger than generic TDD descriptions. The per-cycle checklist is concise and immediately actionable. Very close to `/Users/mia/.agents/skills/tdd/SKILL.md` — diff before promoting to confirm what (if anything) is genuinely novel; the horizontal-slicing anti-pattern block and the "you outrun your headlights" framing are the elements most worth checking against the destination. Strong portable candidate; only gap is the companion-file references.

## .references/mattpocock-skills/tdd/tests.md
**Type**: Reference guide — good-test vs bad-test pattern catalogue (testing philosophy).
**Portable**: Yes.
**Reason**: The two contrasting patterns — integration-style tests through public interfaces vs implementation-detail tests coupled to internals — are language- and framework-agnostic. The "WHAT not HOW" heuristic and the rule to verify through the same interface the caller uses apply equally in TypeScript, Python, Go, or any OOP/FP context.
**Trigger**: User wants to write, review, or improve tests; mentions test quality, brittle tests, mocking strategy, or TDD.
**Steps/contract**: Not a procedural SOP — it is a decision rule: (1) Assert only on observable behavior accessible through the public API. (2) Avoid mocking internal collaborators; mock only external boundaries (network, DB driver, clock). (3) Name tests as "subject does X" (WHAT), not "subject calls Y" (HOW). (4) Prefer one logical assertion per test. (5) Treat a test that breaks on internal refactor without behavior change as a red flag requiring rewrite.
**Strip**: TypeScript-specific syntax in examples — keep the patterns, make examples language-neutral or parameterise by context. The `jest.mock` reference is illustrative; the principle is what matters.
**Structure/format**: Two top-level sections (Good / Bad) each with a code example followed by a bullet characteristics/red-flags list. Compact (~60 lines). No frontmatter.
**Notes**: Strongest insight is the "verify through interface, not external means" contrast (the `createUser` / `getUser` vs raw DB query pair) — this is non-obvious and worth preserving verbatim in any promoted SOP. The file is a pattern reference rather than a workflow; it pairs naturally with a TDD loop SOP as the "what a valid test looks like" companion. Lacks coverage guidance (how many tests, edge cases) and has no mention of test naming conventions beyond WHAT-not-HOW.

## .references/mattpocock-skills/tdd/interface-design.md
**Type**: Reference cheatsheet — three design-for-testability principles (not a workflow SOP).
**Portable**: Yes — the principles (inject dependencies, return results over side effects, minimise surface area) are language- and framework-agnostic; TypeScript examples are illustrative only.
**Reason**: Classic testability heuristics that apply universally. No repo-specific assumptions; the rules transfer verbatim to any OO or FP codebase.
**Trigger**: Agent is designing a module interface, reviewing code for testability, or advising on refactoring to make code easier to test.
**Steps/contract**: No sequential workflow. Three standalone rules: (1) Accept dependencies as parameters — don't instantiate them internally. (2) Return computed results — avoid mutating state as a side effect. (3) Minimise surface area: fewer public methods and parameters → simpler test setup and fewer test cases required.
**Strip**: TypeScript code examples can be replaced with language-neutral pseudocode if promoting to a cross-stack SOP. No other overhead.
**Structure/format**: Numbered list with paired "testable / hard-to-test" TypeScript snippets; ~30 lines total. No frontmatter, no trigger description, no worked examples beyond the code pairs.
**Notes**: Too thin to stand alone as a promoted skill — it is a supplementary reference rather than an actionable SOP. Best absorbed as a sub-section of a broader TDD or interface-design skill. The content is already partially covered by `design-an-interface/SKILL.md` (which handles divergent interface exploration); this file adds the orthogonal "testability" lens. If promoted, pair with the dependency-injection and pure-function rules from the TDD skill to avoid duplication. Low priority for standalone promotion.

## .references/mattpocock-skills/tdd/deep-modules.md
**Type**: Reference concept note — defines "deep module" (small interface + large hidden implementation) vs "shallow module" (large interface + thin pass-through) with ASCII diagrams and three heuristic design questions. Not a procedural workflow.
**Portable**: Conditionally — the concept is universally applicable to any codebase or language, but the file is too thin to stand alone as a portable SOP. It functions as a vocabulary anchor and a decision checklist fragment.
**Reason**: The Ousterhout "deep module" principle is canonical and stack-agnostic. The three design questions (reduce methods? simplify params? hide more complexity?) are a concise, actionable restatement of the principle. However, the file contains no workflow steps, no trigger conditions, and no procedural contract — it is a reference excerpt, not a skill.
**Trigger**: Not independently triggerable. Relevant when a user wants to evaluate interface depth, improve architecture, review module design, or during any codebase refactor involving API surface decisions.
**Steps/contract**: Three heuristic self-check questions only: (1) Can I reduce the number of methods? (2) Can I simplify the parameters? (3) Can I hide more complexity inside? No procedural steps beyond these.
**Strip**: ASCII diagrams are orientation aids — useful but not load-bearing; can be condensed to a one-line contrast if space is constrained. Book attribution ("A Philosophy of Software Design") is credibility context worth keeping. The shallow-module definition and contrast are essential context; nothing else to trim.
**Structure/format**: H1 + attribution line + definition + two ASCII block diagrams + contrast definition + three bulleted questions. ~30 lines total. Extremely lean.
**Notes**: Companion fragment to `improve-codebase-architecture/SKILL.md` and `tdd/SKILL.md` (both already assessed as strong portable candidates). The `improve-codebase-architecture` skill already invokes the deep-module concept by name in its exploration phase. Best disposition: inline verbatim as a callout/sidebar inside the architecture SOP or TDD skill rather than promoting as a standalone skill. Do not promote independently — too thin; add no standalone value beyond what the parent skills already convey. If inlined, preserve the three questions and the small-interface/large-implementation contrast formula verbatim.

## .references/mattpocock-skills/tdd/mocking.md
**Type**: Reference doc — two concrete mocking principles (boundary-only mocking, designing for mockability) with TypeScript code examples. Not a procedural SOP; no workflow steps.
**Portable**: Yes — both principles are language-agnostic; TypeScript examples are illustrative only.
**Reason**: "Mock only at system boundaries, never what you control" and the DI + SDK-style interface rules carry no repo-specific assumptions. The before/after code samples make the advice executable in any language or framework.
**Trigger**: User wants guidance on what to mock in tests, asks about test isolation, designing for testability, or is setting up dependency injection for test boundaries.
**Steps/contract**: Two rules, not steps. (1) Boundary rule — mock only: external APIs, databases (prefer test DB), time/randomness, filesystem. Never mock your own classes, internal collaborators, or anything you control. (2) Design-for-mockability rule — at boundaries: prefer dependency injection (pass dependencies in, don't construct internally); prefer SDK-style interfaces (one function per operation) over generic fetchers (one function with conditional routing) so each mock returns exactly one shape with no conditional test-setup logic.
**Strip**: Nothing — the file is already minimal (~100 words prose + two before/after code pairs). Code samples are the load-bearing content; preserve verbatim.
**Structure/format**: Two H2 sections; bullet lists for the boundary taxonomy; TypeScript fenced code blocks per pattern (before/bad vs after/good). No frontmatter, no numbered workflow, no YAML header. ~100 words prose, ~30 lines of code.
**Notes**: Companion reference to `tdd/SKILL.md` (already assessed — strong portable candidate). The "don't mock what you control" rule is the standout portable constraint and is often absent from generic mocking guides. The SDK-style vs generic-fetcher distinction is the more distinctive insight: it operationalises mockability as an interface-design decision, not just a test-setup decision. Standalone promotion value is low — this functions best as an inlined appendix or sidebar within the `tdd` SOP or the `codebase-architecture` SOP rather than as an independent skill. Main action: check whether the destination `skills/tdd/SKILL.md` already contains either rule; if not, inline both as a "Mocking" sub-section.

## .references/mattpocock-skills/triage-issue/SKILL.md
**Type**: Workflow SOP (automated bug investigation + GitHub issue creation with TDD fix plan).
**Portable**: Yes.
**Reason**: The end-to-end triage loop — capture problem → explore codebase → identify fix approach → design TDD RED/GREEN cycles → create GitHub issue — is entirely language- and framework-agnostic. The durability constraint ("describe behaviors and contracts, not internal structure; tests assert on observable outcomes") is a standalone best-practice rule transferable to any codebase.
**Trigger**: User reports a bug, wants to file an issue, mentions "triage", or wants to investigate and plan a fix for a problem.
**Steps/contract**: (1) Get one-sentence problem description — ask at most one question, start investigating immediately. (2) Deep codebase exploration: locate manifestation, trace code path, identify root cause, review related tests and recent git history. (3) Determine fix approach: minimal change, affected modules, regression vs missing feature vs design flaw. (4) Design ordered RED-GREEN TDD cycles — each is a vertical slice with a behaviorally-defined test and a minimal code change; include a final REFACTOR step. (5) Run `gh issue create` with the canonical template (Problem / Root Cause Analysis / TDD Fix Plan / Acceptance Criteria) without asking user to review first; print URL and one-line root cause summary.
**Strip**: Sub-agent call (`Agent tool with subagent_type=Explore`) — implementation detail of one harness. The canonical issue template and durability rules should be preserved verbatim.
**Structure/format**: Numbered five-step process; each step has a goal statement and bulleted sub-tasks; inline issue template with section headings and a checklist; explicit rules block for TDD cycles. Moderate length (~450 words).
**Notes**: The durability constraint ("A good suggestion reads like a spec; a bad one reads like a diff") is the standout insight and should survive verbatim into any promoted SOP. The "create immediately, no review gate" posture is an opinionated but coherent choice — worth preserving as a named decision. The issue template is concrete and reusable. Minor gap: no guidance on what to do when root cause is ambiguous or spans multiple components.

## .references/mattpocock-skills/tdd/refactoring.md
**Type**: Reference checklist — post-TDD refactor-candidate aide-mémoire.
**Portable**: Yes (with caveats — see Notes).
**Reason**: The six code-smell patterns (duplication, long methods, shallow modules, feature envy, primitive obsession, newly revealed problems) are language- and framework-agnostic; the remedies are standard OO/FP refactor moves applicable to any codebase.
**Trigger**: End of a TDD red-green cycle, when the implementation passes and the agent is deciding whether to refactor before committing.
**Steps/contract**: Single-phase scan — after green tests, check for each smell in order: (1) Duplication → extract function/class. (2) Long methods → break into private helpers, preserve tests on the public surface only. (3) Shallow modules → combine or deepen. (4) Feature envy → move logic to where its data lives. (5) Primitive obsession → introduce value objects. (6) Existing code made visibly worse by the new code → flag and plan a follow-on fix.
**Strip**: Nothing — the file is already 6 bullet points with no prose noise. It is a pure checklist.
**Structure/format**: Flat bullet list with inline `→` remedy notation. No frontmatter, no headings beyond the title. ~60 words total.
**Notes**: This is a *supporting fragment*, not a standalone SOP — it covers only the refactor phase of a TDD loop. Portable value is highest when embedded as the "Refactor" section inside a larger TDD skill rather than promoted as an independent skill. The one standout constraint worth preserving verbatim: "keep tests on public interface" (for long-method decomposition) — this prevents the common mistake of adding tests to private helpers during refactor.

## .references/mattpocock-skills/ubiquitous-language/SKILL.md
**Type**: Workflow SOP — interactive DDD-style glossary extraction and formalization protocol.
**Portable**: Yes.
**Reason**: The five-step process (scan conversation → identify ambiguity/synonym problems → propose canonical glossary → write UBIQUITOUS_LANGUAGE.md → output inline summary) is entirely domain- and stack-agnostic. The output format (grouped tables with "aliases to avoid" column, relationships prose, example dialogue, flagged ambiguities section) and the eight opinionated rules are transferable to any project that needs to harden shared terminology.
**Trigger**: User wants to define domain terms, build a glossary, harden terminology, create a ubiquitous language, or mentions "domain model" or "DDD".
**Steps/contract**: (1) Scan conversation for domain-relevant nouns, verbs, and concepts. (2) Identify problems: same word used for different concepts (ambiguity), different words for the same concept (synonyms), vague/overloaded terms. (3) Propose a canonical glossary with opinionated term choices — pick one winner per concept, list others as aliases to avoid. (4) Write to `UBIQUITOUS_LANGUAGE.md` in the working directory using the prescribed format: grouped tables (term / definition / aliases to avoid), relationships prose with cardinality, a 3–5 exchange example dialogue, and a flagged ambiguities section. (5) Output an inline summary in the conversation. Re-run protocol: read existing file, incorporate new terms, update evolved definitions, re-flag new ambiguities, rewrite example dialogue.
**Strip**: The inline `<example>` block (filesystem/Docker/sandbox dialogue) is illustrative scaffolding — replaceable with a generic placeholder without loss. The domain-specific table example (Order/Invoice/Customer/User) similarly serves as illustration only.
**Structure/format**: YAML frontmatter (name/description); Process section (5 numbered steps); Output Format section with a full markdown template; Rules section (8 opinionated bullets); illustrative example block; Re-running section. Moderately long (~120 lines) but well-structured with clear section headers.
**Notes**: Three standout elements worth preserving verbatim: (1) the "Flagged ambiguities" section with explicit conflicting-usage callout and a canonical recommendation — rare and high-value in glossary tooling; (2) the "aliases to avoid" table column as an enforced convention that actively discourages synonym drift; (3) the required example dialogue, which forces boundary-clarification between related concepts in a way tables alone cannot. The rules are notably concrete ("one sentence max", "show cardinality", "group only when natural clusters emerge — don't force groupings"). Strong promotion candidate; minimal adaptation needed beyond swapping illustrative examples.

## .references/mattpocock-skills/write-a-prd/SKILL.md
**Type**: Workflow SOP — 5-step interactive PRD creation session: long-form problem gather → codebase explore → relentless multi-branch interview → deep-module sketch + user sign-off → write and file as GitHub issue.
**Portable**: Yes — no repo-specific commands, hardcoded paths, or proprietary tooling. The PRD template and the five-step workflow apply to any software project regardless of stack or domain.
**Reason**: The interview-driven approach (walk every branch of the decision tree, resolve dependencies one-by-one) and the module-sketching step (explicitly looking for deep-module opportunities) are universally sound practices. The seven-section PRD template is a durable, reusable document structure with no project-specific coupling.
**Trigger**: User wants to write a PRD, create a product requirements document, or plan a new feature.
**Steps/contract**: (1) Ask user for a long, detailed problem description and candidate solution ideas. (2) Explore the repo to verify assertions and understand current codebase state. (3) Relentless multi-branch interview — cover every aspect of the plan; walk the decision tree branch-by-branch resolving dependencies in order; ask one question at a time. (4) Sketch the major modules to build or modify; actively seek "deep module" opportunities (small testable interface hiding large implementation); confirm module list and testing scope with user before proceeding. (5) Write the PRD using the seven-section template and submit as a GitHub issue. PRD template sections: Problem Statement, Solution, User Stories (extensive numbered "As a / I want / so that" list), Implementation Decisions (modules, interfaces, architecture, schema, API contracts — no file paths or code snippets), Testing Decisions (good-test definition, which modules, prior art in codebase), Out of Scope, Further Notes.
**Strip**: Step permissiveness preamble ("You may skip steps if you don't consider them necessary") is an anti-pattern for a portable SOP — replace with explicit per-step skip conditions on promotion. The `<prd-template>` XML wrapper is a formatting artifact; inline the sections directly. The "DO NOT include specific file paths or code snippets" rule in Implementation Decisions is high-signal and must be preserved verbatim.
**Structure/format**: YAML frontmatter (name, description) + prose preamble + numbered 5-step list + fenced `<prd-template>` with seven H2 sections. Clean, self-contained, ~250 words. Template is complete and immediately reusable without modification.
**Notes**: Very close to `/Users/mia/.agents/skills/prd-create/SKILL.md` — diff before promoting to identify genuine delta. Standout elements to check against the destination: (a) "relentless interview walking every decision-tree branch" framing (step 3) is stronger than a generic requirements-gather instruction; (b) the deep-module module-sketching step (step 4) explicitly bridges requirements to architecture — often absent from PRD workflows; (c) the "extremely extensive" user-story requirement signals ambition beyond a minimal list; (d) Testing Decisions as a first-class PRD section (not a post-implementation concern) is a high-value structural choice worth preserving. "No file paths or code snippets in Implementation Decisions" is the standout portable guardrail. Strong promotion candidate; only friction is the overlap with the existing prd-create skill.

## .references/mattpocock-skills/write-a-skill/SKILL.md
**Type**: Meta-skill / process SOP — guides authoring of new agent skills with proper structure, progressive disclosure, and bundled resources.
**Portable**: Yes.
**Reason**: The entire skill describes a generic skill-authoring process with no repo-specific assumptions. The output format (SKILL.md + optional REFERENCE.md / EXAMPLES.md / scripts/), the 100-line split threshold, the description-first authoring philosophy, and the review checklist are universally applicable to any agent skill system that uses SKILL.md files — which is exactly the format used throughout this library.
**Trigger**: User wants to create, write, or build a new agent skill.
**Steps/contract**: (1) Gather requirements — four questions: domain/task covered, specific use cases, need for executable scripts, reference materials available. (2) Draft — write SKILL.md (required); create REFERENCE.md / EXAMPLES.md if body would exceed ~100 lines or covers distinct domains; add utility scripts for deterministic/repeated operations to save tokens and improve reliability. (3) Review with user — present draft; ask three targeted questions (coverage, gaps, detail calibration). Structural rules embedded in the skill: description ≤1024 chars, written in third person, first sentence = capability, second sentence = "Use when [specific triggers]"; SKILL.md body under 100 lines; split files when content has distinct domains or advanced features are rarely needed; add scripts when the same code would be generated repeatedly or needs explicit error handling.
**Strip**: The "Bad example" annotation and surrounding explanation are illustrative — condensable to a one-line anti-pattern note without loss. The template frontmatter/body scaffold could be tightened; the structure is clear but the prose framing around it is padded. The "Max 1024 chars" limit is itself the operative rule — the paragraph justifying it can be dropped.
**Structure/format**: YAML frontmatter (name, description) + numbered 3-step process + directory tree scaffold (code block) + SKILL.md template (fenced markdown) + Description Requirements section (format rules + good/bad examples) + When to Add Scripts section + When to Split Files section + Review Checklist (6 items). Total ~120 lines. Well-organized; each section is independently skimmable.
**Notes**: This is a strong candidate — it is effectively the SOP for authoring the very format used throughout this skills library, making it intrinsically portable and high-leverage. The standout elements: (a) "the description is the only thing your agent sees" framing — this insight correctly surfaces the description as the load-bearing trigger mechanism, not the body; (b) the 100-line SKILL.md body limit as a concrete maintainability gate; (c) scripts-for-deterministic-ops as a token-economy heuristic, not merely a style preference. Very close counterpart exists at `/Users/mia/.agents/skills/skill-create/SKILL.md` — diff before promoting to identify genuine delta. Key additions to check against the destination: the description-as-only-signal framing, the 100-line split threshold (vs content-based judgement), the three-question review loop, and the scripts-reliability rationale. If the destination already covers these, promotion value is low; if any are missing, they are worth incorporating verbatim.
